# Claude Code task — connectivity proof

This application change must be implemented by Claude Code, not by ChatGPT.

## Goal
Temporarily display `19 AOÛT 2026` in very large red text at the top of the PASSIO feed (`#screen-feed`).

## Constraints
- Read `AGENTS.md` and `CLAUDE.md` first.
- Keep the change minimal and easily reversible.
- Do not modify Supabase, authentication, user data, or business logic.
- Do not edit this task file.
- Run relevant checks for the touched file(s).
- Commit and push the implementation to this PR branch.
- Never push to `main` and never merge the PR.

## Proof required
The implementation commit must be produced by the Claude Code GitHub Action on this PR branch.
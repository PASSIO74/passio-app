# Tâche Claude Code — preuve E2E finale

Lis `AGENTS.md` et `CLAUDE.md` avant toute modification.

Objectif : afficher temporairement `19 AOÛT 2026` en très grand texte rouge en haut du fil `#screen-feed`.

Contraintes :
- modification minimale, claire et réversible ;
- aucune modification de Supabase, des données, de l'authentification ou de la logique métier ;
- ne modifie pas ce fichier de tâche ;
- travaille uniquement sur la branche actuelle de cette Pull Request ;
- ne pousse jamais directement sur `main` ;
- exécute les vérifications pertinentes ;
- laisse le changement applicatif non committé si le workflow te le demande : le workflow persistera uniquement les fichiers que tu as réellement modifiés.

Cette modification applicative doit être réellement produite par Claude Code, pas par ChatGPT.

<!-- CI retrigger uniquement : ajouté par ChatGPT après le commit applicatif Claude vérifié. -->

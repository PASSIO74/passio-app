# Claude Code task — PASSIO visual connectivity test

This task must be implemented by Claude Code, not ChatGPT.

## Goal
Temporarily display the date `19 AOÛT 2026` in very large red text at the top of the PASSIO feed (`#screen-feed`) so the owner can visually confirm the remote Claude Code workflow works from a phone.

## Constraints
- Read `AGENTS.md` and `CLAUDE.md` first.
- Make the smallest safe and reversible change.
- Do not change Supabase, authentication, user data, migrations, or business logic.
- Do not edit this task file.
- Run relevant checks for the touched file(s).
- Commit and push the implementation to the current PR branch.
- Never push to `main` and never merge the PR yourself.

# PASSIO — Continuous Work Handoff — 2026-08-18 19:00 Europe/Paris

## Sentinel stack — verified parents

- PR #3 `1ded1272c4c0ede0ad962771792dcd862d13cfd8` — mergeable, CI #1748 SUCCESS.
- PR #5 `c319d5e95c8e7d46efee24e41d79c77f5eff692e` — mergeable, CI #1750 SUCCESS.
- PR #6 `958c149861074592a8f04d0d54c088aabdf226e4` — mergeable, CI #1751 SUCCESS.
- PR #7 `2c4501b9e0aeba7ede062b520ef77b8b431907aa` — mergeable, CI #1779 SUCCESS.

PR #7 includes deterministic learning, separate recurrence evidence, quarantine after repeated failures/recurrences, transactional local promotion, exact rollback after failed verification, production mutation prohibition, startup bootstrap through `server/start.js`, and bounded post-promotion recurrence monitoring.

Known unresolved design issue: full Release Guardian GO is currently required for local Autopilot promotion, but the causal high/critical incident can keep that Guardian NO_GO. Do not weaken production Guardian. A separate local pre-promotion gate is acceptable only after exact causal incident traceability can exclude that one incident while every unrelated high/critical incident remains blocking.

## PR #8 — Mobile Control Center + public release evidence

- Branch: `feat/mobile-control-center-pwa`
- Base: exact PR #7 head `2c4501b9e0aeba7ede062b520ef77b8b431907aa`
- Current head: `43b1080d803aea58fc3195bd39e01b7cd6575ca3`
- PR mergeable on latest verification.
- Exact-head CI: #1790 — IN PROGRESS at this update.

Implemented/strengthened:

- same-backend five-tab PWA; Sentinel read-only; whitelisted tests only;
- `Promise.allSettled` partial-domain resilience;
- `/api/*` never intercepted/cached by mobile SW;
- static mobile assets now network-first with cache only as offline fallback, eliminating indefinite stale `mobile.js` risk;
- public `PASSIO_PUBLIC_URL/release.json` no-store/no-cache evidence;
- production release health fail-closed unless public evidence is LIVE;
- expected commit recalculated each probe;
- missing expected public commit => MISMATCH;
- public commit proof must be hexadecimal and have at least 12 common characters; short prefixes are rejected by test;
- expected buildId only used when local release manifest is tied to the same expected commit.

Clean resync provenance remains recorded: reconstruction started from exact #7 head and contained only the 10 #8-specific files before the later PWA/release hardening commits.

Do not call #8 green until CI #1790 is COMPLETED SUCCESS on `43b1080d...`.

## PR #4 — Application Integrity Wave 2

- head `1942f55e8c4f7e3602552852cb1431f3668826d3`
- mergeable
- CI #1749 SUCCESS.

## PR #9 — Product Passion Intelligence + release-guard concurrency fix

- Base: exact #4 head `1942f55e8c4f7e3602552852cb1431f3668826d3`
- Current head: `fee5a8fc4ee53ea7ac709a29e706f1d0e3b4cf37`
- PR mergeable after GitHub recalculation.
- Exact-head CI: #1791 — IN PROGRESS at this update; static audits green and AUTHZ-CRITICAL green, full E2E running, artifact gate pending.

Privacy state:

- telemetry emits only `passion_ctx`;
- `profileLocalId` stays internal to helper closure for persona-change deduplication;
- `window.PassioPassionContext.current()` returns `{ passionId }` only;
- production artifact tests assert no local profile ID exposure;
- no historical backfill/provenance is invented.

CI #1785 on prior head `f833f475...` is intentionally NOT treated as a green proof. It showed:

- AUTHZ-CRITICAL SUCCESS;
- application E2E: 170 passed, 18 skipped;
- production artifact failure only in RELEASE-INTEGRITY N→N+1 detection.

Root cause found and fixed on current head: overlapping `PassioReleaseGuard.check()` callers could receive a stale immediate snapshot when another check was already running. The guard now shares one in-flight Promise so every concurrent/manual/visibility/online caller gets the final current verdict. No forced reload was added.

Do not call #9 green until CI #1791 is COMPLETED SUCCESS including artifact gates.

## Critical release governance

Fresh GitHub metadata shows `main` is protected but required status checks are NOT enforced (`enforcement_level=off`, no required contexts/checks). This is a release blocker: PR mergeability is not proof that CI is mandatory. The available connector can inspect this state but cannot mutate branch protection/rulesets.

See `.passio/RELEASE_BLOCKERS_2026-08-18.md` for the fail-closed blocker register and `.passio/FRIDAY_REVIEW_PROMPTS.md` for independent Claude Code / Codex review briefs.

Node 20 deprecation/engine warnings were also observed in CI. Keep the Node 22+ migration as a separate hardening track after the current exact-head release proofs stabilize; do not mix it into #8/#9 now.

## Expected Friday merge order

Sentinel stack, re-querying exact head/base/CI immediately before every merge:

1. #3
2. #5
3. #6
4. #7
5. #8

Parallel integrity/product stack:

1. #4
2. #9

Before merge/deploy:

- exact-head CI green;
- AUTHZ-CRITICAL fresh/pass;
- independent Claude Code + Codex review where capability is available;
- branch required status checks/ruleset enforced and re-verified;
- Observation / Release Guardian evidence fresh; UNKNOWN/STALE never PASS;
- stacked PR base verified after each parent merge;
- production deployment decision separate from merge decision;
- Sentinel Autopilot never used as a production deployment mechanism.

# PASSIO — Continuous Work Handoff — 2026-08-18 19:00 Europe/Paris

## Sentinel stack — verified parents

- PR #3 `1ded1272c4c0ede0ad962771792dcd862d13cfd8` — mergeable, CI #1748 SUCCESS.
- PR #5 `c319d5e95c8e7d46efee24e41d79c77f5eff692e` — mergeable, CI #1750 SUCCESS.
- PR #6 `958c149861074592a8f04d0d54c088aabdf226e4` — mergeable, CI #1751 SUCCESS.
- PR #7 `2c4501b9e0aeba7ede062b520ef77b8b431907aa` — mergeable, CI #1779 SUCCESS.

PR #7 includes deterministic learning, separate recurrence evidence, quarantine after repeated failures/recurrences, transactional local promotion, exact rollback after failed verification, production mutation prohibition, startup bootstrap through `server/start.js`, and bounded post-promotion recurrence monitoring.

Current runtime still requires full Release Guardian GO for local Autopilot promotion. Do not weaken production Guardian. PRs #11/#12 below prepare causal evidence and a pure local policy but do not activate runtime behavior.

## PR #8 — Mobile Control Center + public release evidence — TECHNICALLY GREEN

- Branch: `feat/mobile-control-center-pwa`
- Base: exact PR #7 head `2c4501b9e0aeba7ede062b520ef77b8b431907aa`
- Head: `4506c9951ce3fbdc7fef1faa7488c0eb5983564a`
- PR mergeable.
- Exact-head CI #1799: COMPLETED SUCCESS.
- Fresh compare: ahead of #7, behind 0, exactly 10 changed files in intended surface.

Validated hardening:

- same-backend five-tab PWA; Sentinel read-only; whitelisted tests only;
- `Promise.allSettled` partial-domain resilience;
- `/api/*` never intercepted/cached by mobile SW;
- root-scoped SW intercepts only four explicit mobile pilot assets;
- mobile assets network-first with cache only as offline fallback;
- public `PASSIO_PUBLIC_URL/release.json` no-store/no-cache evidence;
- production fail-closed unless public evidence is LIVE and aligned to CURRENT expectations;
- commit proof hexadecimal only;
- 12 common characters is a non-weakenable proof floor;
- `public-release-evidence` and `release-recorder` share the same proof constant;
- cache/in-flight evidence keyed to exact expected commit/build pair;
- changed expectations during in-flight probe force a fresh probe;
- `releaseHealth()` rejects stale cached LIVE proof as `STALE_EXPECTATION`;
- expected buildId only derives from a local manifest tied to the same commit with the same proof strength;
- dashboard/Sentinel tests + AUTHZ + E2E/workflow all SUCCESS on #1799.

Merge remains blocked by governance/review/production-evidence gates, not by CI.

## PR #10 — Desktop entry to Mobile Pilot — TECHNICALLY GREEN

- Branch: `feat/control-center-mobile-entry`
- Base: exact #8 head `4506c9951ce3fbdc7fef1faa7488c0eb5983564a`
- Head: `75acace7dc476ff627b846c6a092591a33625cad`
- PR mergeable.
- Exact-head CI #1801: COMPLETED SUCCESS.
- Changed files: exactly 2, 0 deletions.
- `index.html` final patch: one topbar navigation link `Pilot` -> `/mobile.html`.
- test proves navigation-only behavior with no inline API/mutation action.

Merge only after #8 and revalidate base after parent merge.

## PR #11 — Sentinel causal incident trace

- Branch: `feat/sentinel-causal-incident-trace`
- Base: exact #10 head `75acace7dc476ff627b846c6a092591a33625cad`
- Head: `6092ee22dc0a883728f51d50c2f112fd43ed1f43`
- PR mergeable on latest verification.
- Diff: exactly `dashboard/server/alerts.js` + `dashboard/test/sentinel-causal-trace.test.js`.
- CI #1802: IN PROGRESS; static audits, dashboard/Sentinel tests and AUTHZ are SUCCESS; E2E running.

Purpose/proof:

- `alerts.emit()` attaches exact `incidentId` and `incidentClusterKey` to alert meta at Incident Packet creation;
- Sentinel already persists alert meta in diagnosis and passes diagnosis to repairer;
- integration tests prove Incident Packet -> alert/meta identity and unchanged persistence into repairer;
- no Guardian/Autopilot decision/git mutation/production behavior is changed.

## PR #12 — Pure fail-closed local promotion gate — NOT RUNTIME ACTIVATED

- Branch: `feat/sentinel-local-promotion-gate`
- Base: exact #11 head `6092ee22dc0a883728f51d50c2f112fd43ed1f43`
- Current head: `95929e64a2c5d4d830435daa508ead10e3cecb0d`
- PR mergeable.
- Diff: two new files only — pure policy + tests.
- Exact-head CI #1805: IN PROGRESS; static audits and dashboard/Sentinel tests SUCCESS.

`evaluateLocalPromotionGate()` requires explicit PASS authorization, observation, critical journeys and anomalies; an exact causalIncidentId matching an open high/critical incident; zero unrelated high/critical incidents; AND `incidentsComplete:true` proving the supplied incident set is exhaustive. Missing/partial incident evidence, missing gates, UNKNOWN, STALE, FAIL, missing/mismatched causal id all HOLD.

For LOCAL evaluation only, release_chain and the exact causal incident may be ignored. Full production Guardian remains unchanged. Module reports `productionDeploy:false`, `runtimeActivated:false`; `sentinel-autopilot.js` is unchanged.

IMPORTANT: #12 is policy definition only. Runtime activation requires a separate PR and independent review after #11/#12 exact-head CI are green.

## PR #4 — Application Integrity Wave 2

- head `1942f55e8c4f7e3602552852cb1431f3668826d3`
- mergeable
- CI #1749 SUCCESS.

## PR #9 — Product Passion Intelligence + release-guard concurrency fix — TECHNICALLY GREEN

- Base: exact #4 head `1942f55e8c4f7e3602552852cb1431f3668826d3`
- Head: `fee5a8fc4ee53ea7ac709a29e706f1d0e3b4cf37`
- PR mergeable.
- Exact-head CI #1791: COMPLETED SUCCESS.

Validated: static audits, AUTHZ, full application E2E, production artifact gates and preview all SUCCESS. `profileLocalId` remains closure-private and absent from public passion API/telemetry. Overlapping `PassioReleaseGuard.check()` callers share a single in-flight Promise and receive the final release verdict; no forced reload.

## Critical release governance

Fresh GitHub metadata: `main` is protected but required status checks are NOT enforced (`enforcement_level=off`, no required contexts/checks). This remains a release blocker. Current connector can inspect but cannot mutate branch protection/rulesets.

Hourly Passio Continuous Optimization automation is active. Friday `Fusion Sentinel 2` automation will NOT merge #3 unless required checks/ruleset on `main` are enforced. Friday Claude/Codex handoff automation is active and separates merge GO from production deployment GO.

See `.passio/RELEASE_BLOCKERS_2026-08-18.md` and `.passio/FRIDAY_REVIEW_PROMPTS.md`.

Node 20 deprecation/engine warnings remain a separate hardening track after current release stacks stabilize. One high-severity npm warning was observed during Playwright install, but no package-level audit proof is available in current connector.

## Expected Friday merge order

Sentinel/mobile stack, subject to fresh exact-head/base/CI/review evidence before each merge:

1. #3
2. #5
3. #6
4. #7
5. #8
6. #10
7. #11
8. #12

Parallel integrity/product stack:

1. #4
2. #9

Before merge/deploy:

- exact-head CI green;
- AUTHZ fresh/pass;
- independent Claude Code + Codex review where capability is available;
- branch required status checks/ruleset enforced;
- Observation / Release Guardian evidence fresh; UNKNOWN/STALE never PASS;
- stacked PR base verified after each parent merge;
- production deployment decision separate from merge decision;
- Sentinel Autopilot never used as production deployment mechanism;
- #12 remains inactive until a separate activation PR is reviewed and validated.

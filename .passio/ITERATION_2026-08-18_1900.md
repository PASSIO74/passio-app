# PASSIO — Continuous Work Handoff — 2026-08-18 19:00 Europe/Paris

## Sentinel stack — verified parents

- PR #3 `1ded1272c4c0ede0ad962771792dcd862d13cfd8` — mergeable, CI #1748 SUCCESS.
- PR #5 `c319d5e95c8e7d46efee24e41d79c77f5eff692e` — mergeable, CI #1750 SUCCESS.
- PR #6 `958c149861074592a8f04d0d54c088aabdf226e4` — mergeable, CI #1751 SUCCESS.
- PR #7 `2c4501b9e0aeba7ede062b520ef77b8b431907aa` — mergeable, CI #1779 SUCCESS.

PR #7 includes deterministic learning, separate recurrence evidence, quarantine after repeated failures/recurrences, transactional local promotion, exact rollback after failed verification, production mutation prohibition, startup bootstrap through `server/start.js`, and bounded post-promotion recurrence monitoring.

Known unresolved design issue: full Release Guardian GO is currently required for local Autopilot promotion, but the causal high/critical incident can keep that Guardian NO_GO. Do not weaken production Guardian. A separate local pre-promotion gate is acceptable only after exact causal incident traceability can exclude that one incident while every unrelated high/critical incident remains blocking.

## PR #8 — Mobile Control Center + public release evidence

- Branch: `feat/mobile-control-center-pwa`
- Base: exact PR #7 head `2c4501b9e0aeba7ede062b520ef77b8b431907aa`
- Current head: `4506c9951ce3fbdc7fef1faa7488c0eb5983564a`
- PR mergeable on latest verification.
- Exact-head CI: #1799 — IN PROGRESS at this update.
- Fresh compare: ahead of #7, behind 0, exactly 10 changed files in intended surface.

Implemented/strengthened:

- same-backend five-tab PWA; Sentinel read-only; whitelisted tests only;
- `Promise.allSettled` partial-domain resilience;
- `/api/*` never intercepted/cached by mobile SW;
- root-scoped SW intercepts only the four explicit mobile pilot assets, never the general Control Center surface;
- static mobile assets network-first with cache only as offline fallback;
- public `PASSIO_PUBLIC_URL/release.json` no-store/no-cache evidence;
- production release health fail-closed unless public evidence is LIVE and aligned to CURRENT expectations;
- expected commit recalculated each probe;
- missing expected public commit => MISMATCH;
- public commit proof must be hexadecimal;
- 12 common characters is a non-weakenable proof floor; environment may request more but never less;
- `public-release-evidence` and `release-recorder` share the same proof constant, preventing a weaker local buildId provenance rule than the public release rule;
- cache/in-flight probe keyed to exact expected commit/build pair;
- if expectations change during an in-flight probe, old evidence is not reused and a fresh probe is required;
- `releaseHealth()` degrades stale cached LIVE proof as `STALE_EXPECTATION`;
- expected buildId only used when local release manifest is tied to the same expected commit with the same proof strength.

CI #1799 dashboard/Sentinel tests are already SUCCESS. AUTHZ/E2E/workflow completion remain required. Do not call #8 green until #1799 is COMPLETED SUCCESS on `4506c995...`.

## PR #10 — Desktop entry to Mobile Pilot

- Branch: `feat/control-center-mobile-entry`
- Base: #8 current head at creation `4506c9951ce3fbdc7fef1faa7488c0eb5983564a`
- Current head: `75acace7dc476ff627b846c6a092591a33625cad`
- PR mergeable on latest verification.
- CI #1801: IN PROGRESS.
- Changed files: exactly 2, 0 deletions.
- `index.html` net patch is exactly one topbar navigation link `Pilot` -> `/mobile.html`.
- separate test proves presence and navigation-only behavior, with no inline API/mutation action.

#10 must remain stacked after #8. Revalidate its base after any #8 movement/merge.

## PR #4 — Application Integrity Wave 2

- head `1942f55e8c4f7e3602552852cb1431f3668826d3`
- mergeable
- CI #1749 SUCCESS.

## PR #9 — Product Passion Intelligence + release-guard concurrency fix

- Base: exact #4 head `1942f55e8c4f7e3602552852cb1431f3668826d3`
- Current head: `fee5a8fc4ee53ea7ac709a29e706f1d0e3b4cf37`
- PR mergeable.
- Exact-head CI #1791: COMPLETED SUCCESS.

Validated together on #1791:

- static audits SUCCESS;
- AUTHZ-CRITICAL SUCCESS;
- full application E2E SUCCESS;
- production artifact gates SUCCESS;
- preview workflow SUCCESS;
- telemetry emits only `passion_ctx`;
- `profileLocalId` remains internal to the helper closure and absent from the public helper API;
- overlapping `PassioReleaseGuard.check()` callers share a single in-flight Promise and receive the final current release verdict;
- no forced reload was introduced.

PR #9 is technically green on its exact head. Merge still requires current base verification, branch-protection remediation, and independent Friday review.

## Critical release governance

Fresh GitHub metadata shows `main` is protected but required status checks are NOT enforced (`enforcement_level=off`, no required contexts/checks). This is a release blocker: PR mergeability is not proof that CI is mandatory. The available connector can inspect this state but cannot mutate branch protection/rulesets.

The hourly Passio Continuous Optimization automation is active. The Friday `Fusion Sentinel 2` automation will NOT merge #3 unless required checks/ruleset on `main` are actually enforced. The Friday Claude/Codex handoff automation is active and explicitly separates merge GO from production deployment GO.

See `.passio/RELEASE_BLOCKERS_2026-08-18.md` for the fail-closed blocker register and `.passio/FRIDAY_REVIEW_PROMPTS.md` for independent Claude Code / Codex review briefs.

Node 20 deprecation/engine warnings were observed in CI; keep the Node 22+ migration as a separate hardening track after current release stacks stabilize. An npm install warning also reported one high-severity dependency vulnerability, but no package-level proof is available in the current connector, so no unsupported blocker classification is made.

## Expected Friday merge order

Sentinel/mobile stack, re-querying exact head/base/CI immediately before every merge:

1. #3
2. #5
3. #6
4. #7
5. #8
6. #10

Parallel integrity/product stack:

1. #4
2. #9

Before merge/deploy:

- exact-head CI green;
- AUTHZ-CRITICAL fresh/pass;
- independent Claude Code + Codex review where capability is available;
- branch required status checks/ruleset enforced and re-verified;
- Observation / Release Guardian evidence fresh; UNKNOWN/STALE never PASS;
- stacked PR base verified after each parent merge;
- production deployment decision separate from merge decision;
- Sentinel Autopilot never used as a production deployment mechanism.

# Relecture complémentaire — les 81 problèmes jamais relus et les contrôles débloqués (2026-09-08)

> Étape 1 de la contre-revue (`15-CONTRE-REVUE-ASTRA-PROMPT.md` §0), exécutée par la session « Avancer sur le projet BILAN » (Claude Fable 5.1, `session_014R2zEeBRiyNk1STv73GrrA`) pendant l'indisponibilité de GPT-6 Astra. **SHA inchangé** : `c8cb8e995b88159a1e9d4c2f7dc196ad93a133bf` (`main` n'a reçu aucun commit depuis le 2026-09-04). Aucun code applicatif modifié, rien déployé, base en lecture seule, aucune donnée réelle touchée.
>
> Méthode : les 81 problèmes marqués « NON VÉRIFIÉ (pas de relecture) » dans le rapport 11 ont été confiés, domaine par domaine, à huit relecteurs adversariaux indépendants de l'audit initial (même modèle, contexte vierge, consigne de RÉFUTER), avec accès au code au SHA, aux preuves déposées, au connecteur Supabase en lecture seule pour trois domaines, et à un navigateur local. Chaque verdict cite `fichier:ligne`, une requête ou une commande. Les sorties brutes sont dans `donnees/relecture-complement-2026-09-08/*.json`. Ce fichier est GÉNÉRÉ par `outillage/gen-relecture-complement.py`.
>
> Réserve : c'est une relecture par Claude d'un audit fait par Claude. Elle ferme le trou de méthode (81 problèmes sans second regard), elle ne vaut pas la revue d'un modèle tiers, toujours attendue le 2026-09-14.

Domaines relus : **auth-rgpd, exploitation-continuite, irl, profils-passions, robustesse-pannes, tests-ci, appareils-a11y** — manquants : perf-capacite-couts.

## 1. Compteurs après relecture

| Problèmes relus le 08 | CONFIRMÉ | RÉFUTÉ | INCERTAIN | Priorités changées | Oublis nouveaux |
|---|---|---|---|---|---|
| 75 | 73 | 2 | 0 | 1 | 27 |

| | P0 | P1 | P2 | P3 |
|---|---|---|---|---|
| Registre du 04 (retenus) | 8 | 57 | 66 | 58 |
| Après relecture du 08 (réfutés retirés, priorités amendées) | 8 | 58 | 65 | 56 |
| + oublis trouvés le 08 | 8 | 60 | 78 | 68 |

Relecture globale des 192 problèmes : CONFIRMÉ 177, INCERTAIN 4, Non relu 6, RÉFUTÉ 5.

Priorités amendées : IRL-07 → P1 (était P2).

## 2. Verdicts, domaine par domaine

### auth-rgpd

| Id | Priorité 04 → 08 | Verdict | Preuve | Commentaire du relecteur |
|---|---|---|---|---|
| AUTH-01 | P1 → P1 | **CONFIRMÉ** | js/access-gate.js:25-26 (sel + hash publics), :32 (sessionStorage === GATE_HASH suffit), :109-113 (aucun compteur d'essais) ; test exécuté relecture/auth-rgpd/verif-auth01-auth06.js → A_gate_present_sans_token=true, A_gate_absent_avec_token=true ; node sha256 0000-9999 → trouvé au 2126e essai en 7 ms ; api.github.com/repos/PASSIO74/passio-app → "private": false ; code en clair dans 31 fichiers du dépôt (grep -rlw 2125, hors node_modules) ; docs/SECURITE_CODE_ACCES.md:44 (code + hash en clair) et :47 (« le hash empêche seulement de retrouver le code » — faux) ; politique §7 app-02-state-utils.js:3146 | Tout est vérifié au SHA : le gate se contourne par un seul setItem, le code se retrouve hors ligne en quelques ms et figure en clair dans un dépôt public. La doc elle-même annonce la limite mais en surestime la portée. P1 juste : ce n'est pas une frontière de sûreté (la RLS l'est), mais la promesse « bêta privée » faite aux testeurs et dans la politique est fausse. |
| AUTH-02 | P1 → P1 | **CONFIRMÉ** | app-02-state-utils.js:3179-3194 (onbValidateAge, seul écrivain de birthYear/isMinor) ; seul appelant index.html:318 (étape data-onb-step="age") ; onbNext() n'est atteint qu'à app-02:3504, APRÈS le return de :3461-3467 (signup sans session → onglet connexion) ; connexion : app-02:3496-3501 onboarded=true + reload ; session au boot : app-08-ui-modals-tour.js:2279 et :2291 state.onboarded=true sans étape ; état initial app-02:33-34 birthYear:null/isMinor:false ; index.html:311 « contrôle d'âge IA » ; skipToApp (app-08:318-320, birthYear 1995 par défaut) n'a AUCUN appelant ; onbSkipAuth (anonyme) n'a aucun appelant non plus ; base : auth.users is_anonymous = 0 sur 6 | Depuis l'activation de « Confirm email », aucun chemin réel n'affiche l'étape âge : onbNext n'est atteignable que si signUp rend une session. Conséquence secondaire : declare_birth_year (app-07:5017-5019) ne peut jamais partir puisque birthYear reste null — sans effet aujourd'hui car le drapeau IRL-proposal est OFF par défaut (app-07:4949). P1 confirmé (mineurs + mention trompeuse). |
| AUTH-03 | P1 → P1 | **CONFIRMÉ** | grep -rniE 'CGU\|conditions générales\|mentions légales\|SIREN\|hébergeur\|terms_version\|terms_accepted' index.html js/*.js → 0 ; politique app-02-state-utils.js:3139-3146 (« juin 2026 », « carnets », contrôle d'âge §6, code §7, contact §5 = adresse d'une autre société, non recopiée ici) ; À propos app-02:2549-2551 (« PASSIO SAS · France », autre adresse de contact, sans SIREN/adresse/hébergeur) ; formulaire index.html:271-297 sans case ni lien d'acceptation ; CSP netlify.toml:19 liste des tiers absents de la politique | Tout l'observé tient au SHA. La politique est incomplète (téléphone, télémétrie, sous-traitants) et contradictoire sur deux points déjà démontrés faux (âge, code). P1 juste ; c'est le seul point qui frôle P0 si « commercialisation » signifie encaisser un paiement (aucune base contractuelle), mais à ce stade (bêta gratuite) P1 tient. |
| AUTH-04 | P1 → P1 | **CONFIRMÉ** | js/telemetry.js:31 (TELEMETRY_DEFAULT_ON=true), :71 (défaut prod actif), :36-37 (passio_device_id persistant), :492-496 (user_id + user_label + device_id sur chaque événement) ; setEnabled sans aucun appelant hors telemetry.js (grep) ; aucun mot « télémétrie » dans index.html hors commentaire :51 — qui affirme « opt-in via ?telemetry=1, sans PII », contredit par :71 ; base : telemetry_events = 114 921 lignes (111 828 au moment de l'audit, +3 000 en 4 jours), 85 143 avec user_id, 1 213 device_id distincts, 1 seul cron (purge_client_errors 0 3 * * *), purge_telemetry jamais planifiée ; MAIS user_label IS NOT NULL = 0 ligne | Confirmé sur l'essentiel (actif sans consentement, device_id persistant, uuid de compte sur 85 k lignes, aucune purge, aucun interrupteur in-app). Une nuance : en production AUCUNE ligne ne porte le pseudo (user_label toujours NULL, cause non identifiée — probablement `state.user.general.username` vide et redaction), donc « profil comportemental nominatif (pseudo) » est à reformuler en « pseudonymisé par uuid de compte + identifiant d'appareil ». P1 maintenu : traceur non nécessaire déposé par défaut. |
| AUTH-05 | P1 → P1 | **CONFIRMÉ** | supabase/functions/delete-account/index.ts:47-68 (14 tables, `{error}` jamais lu, try/catch inopérant), :73-81 (bucket content seulement) ; app-02-state-utils.js:3086-3117 (11 tables + follows/blocks, erreurs ignorées, toast « Compte supprimé » inconditionnel :3130) ; base : pg_constraint → AUCUNE FK d'une table public vers auth.users (seules 8 tables du schéma auth cascadent) ; orphelins réels (user_id absent d'auth.users) : user_state 79/84, analytics_events 51/3 907, notifications.from_id 19/188, storage attachments 12 objets non couverts ; pg_policies : analytics_events, telemetry_events, reports, passion_requests, conv_reads, user_safety n'ont AUCUNE policy DELETE (donc impurgeables par le client) ; user_safety porte majority_at (pas birth_year, précision) | Confirmé et même aggravé par la base : 79 blobs user_state (état complet, téléphone inclus) appartiennent à des comptes qui n'existent plus, preuve qu'aucun chemin de suppression ne cascade. Correction : user_safety stocke `majority_at`, pas l'année de naissance (donnée personnelle tout de même). P1 juste. |
| AUTH-06 | P1 → P1 | **CONFIRMÉ** | app-02-state-utils.js:2711-2737 (ACCOUNT_SCOPED_KEYS : 12 clés, sans passio_outbox_v1 / passio_cmt_outbox_v1 / passio_post_delete_outbox_v1 / passio_event_comments_v1 ; :2723 purge « passio_cdv_geo_v1 » alors que la clé écrite est passio_geo_cache_v1, app-07:958) ; app-04-comments-shop.js:4623 (from_id: MY_UID au flush), :4644-4648 (_flushOutbox) ; app-08:5956 (setTimeout(_flushOutbox,1500) dans supaInit) ; supaAddComment app-08:3820 (author_id: MY_UID) ; test exécuté relecture/auth-rgpd/verif-auth01-auth06.js (serveur local, SDK stub, aucun réseau) → après purgeAccountScopedData() : passio_outbox_v1 INTACT (contenu « message privé de A »), cmt_outbox et delete_outbox intacts, conversations et passio_uid bien purgés ; _flushOutbox sous MY_UID=B → insert conv_messages {from_id: B, content de A} ; policy conv_messages_insert_member (from_id = auth.uid() AND is_conv_member) → accepté si B est membre | Reproduit localement au SHA. La fuite du contenu (lisible dans localStorage par le compte suivant) est inconditionnelle ; l'envoi effectif sous l'identité de B exige que B soit membre de la conversation (appareil partagé entre correspondants) — scénario étroit mais réel. P1 maintenu au titre de l'isolation inter-comptes (risque n°1 du modèle de menace), P2 défendable si l'on ne retient que l'envoi usurpé. |
| AUTH-07 | P2 → P2 | **CONFIRMÉ** | index.html:276-279 (#authPhone, affiché à l'inscription) ; app-02-state-utils.js:3403-3406 (obligatoire, 8-15 chiffres), :3418 (user_metadata), :3420-3425 (state.user.general.phone → blob user_state) ; grep general.phone → 1 écriture, 0 lecture applicative ; dashboard/server/accounts.js:4-11, :35-38 (lecture service_role) ; politique §1 app-02:3140 sans téléphone ; base : 2 comptes sur 6 portent raw_user_meta_data.phone ; le chemin Google OAuth (app-02:3305-3322) ne le demande jamais | Confirmé. Observation complémentaire : l'obligation ne vaut que par le formulaire mot de passe, l'inscription Google la contourne — preuve supplémentaire que la donnée n'a pas de finalité produit. Les 79 blobs user_state orphelins (AUTH-05) contiennent potentiellement ces numéros. P2 juste. |
| AUTH-08 | P2 → P2 | **CONFIRMÉ** | app-02-state-utils.js:3399 (inscription), :3012 (changement), :3345 (récupération) : `length < 6` sans complexité ; index.html:282 et app-02:2993/2995/3334 minlength=6 ; grep mfa\|totp\|enroll → 0 ; fait établi orchestrateur : advisor « leaked password protection » désactivé (non re-vérifié ici, cité de CONTEXTE_AUDIT.md) ; docs/SETUP_SMTP_AUTH.md §5 le liste comme restant | Confirmé par inspection. Effort 0,25 j réaliste (deux réglages Dashboard + trois constantes). P2 juste. |
| AUTH-09 | P2 → P2 | **CONFIRMÉ** | app-09-boot-pwa.js:1245-1259 (_exportConv : une conversation en .txt, texte seul, « [Media] » pour le reste) ; grep export dans app-02 → aucune entrée compte ; politique §5 app-02:3144 promet « directement dans l'app » et donne une adresse de contact différente de celle de l'À propos (app-02:2550) ; updateUser n'est appelé que pour le mot de passe (app-02:3026, :3348) — aucune rectification d'e-mail in-app | Confirmé, et légèrement élargi : ni export, ni changement d'e-mail dans l'app ; seul le mot de passe est modifiable. P2 juste. |
| AUTH-10 | P2 → P2 | **CONFIRMÉ** | netlify.toml:19 (CSP : fonts.googleapis/gstatic, tenor.googleapis, api.giphy, photon.komoot.io, tiles.openfreemap, stun Google, turn:openrelay.metered.ca) ; index.html : 3 références fonts.googleapis.com (pas 18 comme écrit — styles.css en compte 0) ; app-05-config-profil.js:480-483 (TURN openrelay avec identifiants publics « openrelayproject ») ; politique §2 « UE/US » sans liste | Confirmé ; seule la quantité « 18 références » est fausse (3), sans effet sur le fond : les polices Google sont chargées dès l'ouverture. P2 juste. |
| AUTH-11 | P2 → P2 | **CONFIRMÉ** | app-04-comments-shop.js:3340-3350 (reportUser/reportPost → toast) ; app-08-ui-modals-tour.js:5383-5389 (insert reports : id, reporter_id, target_type, target_id, reason, created_at) ; pg_policies reports = INSERT seul ; SCHEMA_PROD_REFERENCE.sql:236-242 : aucune colonne statut/traitement ; grep CGU/point de contact → 0 | Confirmé par inspection et par la base : la table ne peut même pas représenter un traitement. P2 juste. |
| AUTH-12 | P3 → P3 | **CONFIRMÉ** | tests/e2e/suppression-compte.spec.js:28 test.skip(!PASSIO_E2E_MULTI) ; grep PASSIO_E2E_MULTI .github/workflows/*.yml → uniquement le commentaire deploy.yml:155 ; job « Suites production » deploy.yml:344-348 sans cette variable ; docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:3 « déployée le 2026-06-11 » alors que la purge Storage du fichier date de 43b8ffa (2026-08-31) | Confirmé. P3 juste tant qu'AUTH-05 n'est pas corrigé (le test prouverait une suppression incomplète de toute façon) ; il devient P2 dès que la fonction est étendue. |
| AUTH-13 | P3 → P3 | **RÉFUTÉ** | base : auth.identities WHERE provider='google' → 2 identités sur 6 comptes ; index.html:294-297 (bouton) ; app-02-state-utils.js:3305-3322 (error.message brut) ; grep signInWithOAuth tests/e2e → 0 | La partie centrale (« sans preuve d'activation du provider », « bouton mort possible ») est réfutée : deux comptes réels se sont créés ou connectés par Google, le provider est actif et fonctionne. Restent deux points mineurs (message d'erreur brut, aucun test e2e du retour OAuth) qui ne justifient pas plus que P3 — et que le formulaire Google contourne le téléphone obligatoire (voir AUTH-07). |

Exécuté : PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node relecture/auth-rgpd/verif-auth01-auth06.js (chromium 1.60, http-server local :8125, requêtes externes abandonnées par page.route, SDK Supabase remplacé par un stub en page) → résultat relecture/auth-rgpd/verif-auth01-auth06.resultat.json · node -e sha256('passio-gate-v1::'+0000..9999) → 2126 essais / 7 ms · execute_sql (lecture seule, agrégats) : pg_constraint FK→auth.users ; orphelins par table ; pg_policies par cmd ; colonnes telemetry_events/user_safety/client_errors ; cron.job ; auth.identities provider=google ; raw_user_meta_data ? 'phone' ; is_anonymous · curl api.github.com/repos/PASSIO74/passio-app (visibilité)

Limites déclarées : Aucun test à comptes réels ni requête HTTP vers Supabase/Netlify (interdits) : l'acceptation réelle par la RLS de l'insert rejoué (AUTH-06) est déduite de la policy conv_messages_insert_member, pas exécutée. L'advisor « leaked password protection » (AUTH-08) est repris du fait établi par l'orchestrateur, non re-interrogé. La cause de user_label toujours NULL en prod (AUTH-04) n'a pas été élucidée (aucun trigger cherché en base ; probable username vide côté client). SET LOCAL role indisponible au connecteur : aucune vérification d'isolation sous rôle. AUTH-13 : l'activation du provider est prouvée par les identités existantes, le parcours de retour OAuth n'a pas été exercé.

### exploitation-continuite

| Id | Priorité 04 → 08 | Verdict | Preuve | Commentaire du relecteur |
|---|---|---|---|---|
| EXP-01 | P0 → P0 | **CONFIRMÉ** | migrations/SCHEMA_PROD_REFERENCE.sql:1-13 (photographie du 2026-08-17, 952 lignes dont 111 non-commentées = 91 CREATE INDEX + 0 CREATE TABLE + 0 CREATE POLICY) ; list_migrations → 4 versions (20260809182329, 20260809182411, 20260809183406, 20260824132958) vs `ls migrations/*.sql` = 64 ; execute_sql : pg_tables public = 39 (référence : 35 — absentes passion_relations, passion_requests, user_passions, user_safety), pg_policies public = 119 + storage 4 ; diff noms de policies référence/prod = 10 manquantes + 3 obsolètes (conversations::Ecriture propre, conversations::Insert conversations, conv_messages::Ecriture propre) ; docs/RECUPERATION.md:7,31,33,40 ; scripts/sauvegarde-donnees.js:117-131 (GET /auth/v1/admin/users, GoTrue n'y renvoie jamais encrypted_password) ; grep -rniE '\bRPO\b\|\bRTO\b' docs/ .passio/ hors audits → 0. | Aggravé plutôt que réfuté : la référence de schéma est un fichier de COMMENTAIRES (tables, colonnes, clés, policies, RLS, triggers sont tous préfixés `--`) ; seuls 91 CREATE INDEX sont exécutables et ils échoueraient sur une base vide. L'écart de policies est de 13 (pas 9). Aucune restauration tentée, aucune base cible, aucun RPO/RTO : P0 tenu pour une commercialisation, comme le sous-agent le nuance (P1 en bêta à 6 comptes). |
| EXP-02 | P1 → P1 | **CONFIRMÉ** | scripts/sauvegarde-donnees.js:42-59 (dashboard/.env obligatoire, exit 2) ; aucun `encrypt\|gpg\|age\|chiffr` dans le script ; .gitignore:51-55 ; `grep cron .github/workflows/*.yml` → seule sentinelle-distante.yml:5 ; docs/RECUPERATION.md:17-23 (mesure du 2026-08-16 : 220 fichiers Storage) ; execute_sql storage.objects = 70 (content 58, attachments 12) ; `.passio/sauvegardes` absent de ce poste. | Tous les faits vérifiés : manuelle, dépendante du poste, non chiffrée, jamais planifiée, dernière trace le 2026-08-16. Le Storage a perdu 150 objets depuis cette date sans qu'aucune archive postérieure n'existe hors du poste Windows. PITR/plan Supabase non vérifiables ici. |
| EXP-03 | P1 → P1 | **CONFIRMÉ** | .github/workflows/rollback.yml:27-123 (branche + PR brouillon, aucune fusion ni déploiement) ; API GitHub actions/workflows/rollback.yml/runs → total_count 0 (relu le 2026-09-08, fichier relecture/exploitation-continuite/rollback-runs.json) ; deploy.yml:476-527 (deploy exige governance+audits+test-prod+test-local) ; grep 'publish deploy\|rollback netlify\|deploy précédent' docs/ *.md → 0 ; 35 drapeaux `window.PASSIO_*` distincts dans js/ et index.html. | Confirmé par inspection : le seul rollback outillé repasse par la CI complète et n'a jamais tourné ; aucun rollback Netlify documenté ; tous les drapeaux sont côté client. La durée de 37 min du run 2494 n'a pas été re-mesurée ici (non nécessaire au verdict). |
| EXP-04 | P1 → P1 | **CONFIRMÉ** | `grep -liE 'rollback\|retour arri' migrations/*.sql` → 10-11 fichiers sur 64 (pas 5) ; list_migrations → 4 entrées ; scripts/run_migrations.js:1-19 (« n'applique AUCUNE migration ») ; docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:27-28 (`supabase functions deploy` à la main) ; supabase/functions/{ask-ai,delete-account,notify-call} ; aucun workflow ne référence supabase/functions. | Le chiffre « 5 fichiers avec retour arrière » est faux (10-11), mais 53 fichiers sur 64 en restent dépourvus et l'état appliqué reste inconnu (4 migrations enregistrées, 60 hors journal). Edge Functions déployées à la main, sans version ni CI : le verdict tient. |
| EXP-05 | P1 → P1 | **CONFIRMÉ** | deploy.yml:93 (TRUSTED_REVIEWER: PASSIO74) ; secrets référencés : CLAUDE_CODE_OAUTH_TOKEN, NETLIFY_AUTH_TOKEN, SUPABASE_SERVICE_ROLE_KEY, PASSIO (claude-pr-task.yml:6 le déclare « plus lu ») ; docs/PHONE_ONLY_AI_WORKFLOW.md:7 (abonnement Claude Pro personnel) ; docs/SETUP_SMTP_AUTH.md:16,94 (clé Brevo expire le 30 août 2027, aucun rappel) ; dashboard/Sentinelle-Demarrage.vbs, Lancer-Pilotage.cmd, supervise.mjs (poste Windows) ; grep rotation docs/ → 0 pertinent (seul faux positif lighthouse-prod.report.html) ; 1 seul project_ref Supabase. | Tous les points uniques vérifiés dans le dépôt. L'absence d'un second collaborateur GitHub/Supabase/Netlify n'est pas prouvable mécaniquement ici (API collaborators 403) mais rien dans le dépôt n'en désigne un. |
| EXP-06 | P1 → P1 | **CONFIRMÉ** | sentinelle-distante.yml:4-6 (cron 17 * * * *), :119-165 (verdict = issue GitHub uniquement, aucun e-mail/push) ; docs/SENTINELLE_DISTANTE.md:7 « Contrôles horaires » ; API GitHub 100 derniers runs (25/08 07:59 → 08/09 04:39, fichier relecture/exploitation-continuite/sentinelle-runs.json) : médiane 2,80 h, max 13,32 h, 66/99 intervalles > 2 h, 4 échecs schedule / 96 ; 7 derniers jours : médiane 3,4 h, max 6,1 h ; grep uptimerobot\|statuspage\|page de statut docs/ → 0. ⚠️ La preuve citée par le sous-agent (preuves/exploitation-continuite/sentinelle-runs.json) n'existe pas dans le dossier de preuves. | Le fichier de preuve annoncé est absent ; recalculé sur 100 runs, la cadence réelle est de 2,8 h en médiane (4,04 h annoncés sur 56 runs) et le maximum de 13,32 h est exact. L'alerte n'existe que sous forme d'issue GitHub : le verdict et la priorité tiennent, seuls les chiffres sont à remplacer. |
| EXP-07 | P1 → P1 | **CONFIRMÉ** | js/app-08-ui-modals-tour.js:237-262 (feedbackModal « Enregistré localement. Tu pourras l'exporter au créateur. » ; saveFeedback → state.feedbacks + saveState + toast, aucun réseau) ; grep feedbacks js/ → seules ces 2 lignes (aucune exportation) ; index.html:449-457 ; app-02:2549-2551 (contact@passio.app) vs :3065 et :3144 (contact@ladamemetallerie.com) ; preuves emul-support-juridique-resultat.json (feedbackRequestsAfterSave = [], feedbackStoredLocally = 1) ; aucune table `feedbacks` en prod (pg_tables). | Confirmé. Nuance : `_syncableState` (app-02:308-311) ne retire pas `feedbacks`, donc le retour part dans le blob `user_state` du compte — sur le serveur, mais illisible pour l'éditeur et jamais consulté : ce n'est pas un canal de support. |
| EXP-08 | P1 → P1 | **CONFIRMÉ** | js/app-02-state-utils.js:3134-3151 (politique de juin 2026 : « carnets », « portabilité … par e-mail », e-mail retiré « sous 30 jours ») ; supabase/functions/delete-account/index.ts:84 (deleteUser immédiat) ; grep 'portabilit\|exporter mes donn' js/ index.html → 1 occurrence (la phrase de la politique) ; grep '72 ?h\|violation de données' docs/ .passio/context .passio/adr → 0 (seul faux positif : nom d'une capture PNG) ; execute_sql : 79 lignes `user_state` sans compte auth (résidus, cf. CPL-EXP-03). | Confirmé : aucun outil d'export, aucune procédure de violation, politique inexacte sur plusieurs points (carnets retirés, délai d'effacement, télémétrie absente). |
| EXP-09 | P1 → P1 | **CONFIRMÉ** | js/app-02-state-utils.js:2542-2556 (« PASSIO SAS · France · contact@passio.app · passio.app », sans adresse, SIREN ni hébergeur) ; grep -ciE 'CGU\|conditions générales\|conditions d.utilisation' index.html js/*.js → 0 ; grep SIREN\|SIRET docs/ livrables_investisseurs/*.md → 0 ; 36 URL unsplash dans js/app-01* ; preuves emul-support-juridique-resultat.json (cguMatches = []). | Confirmé intégralement au SHA. Priorité P1 justifiée : l'obligation LCEN naît à l'ouverture publique, pas en bêta fermée. |
| EXP-10 | P1 → P1 | **CONFIRMÉ** | API GitHub repos/PASSIO74/passio-app (2026-09-08) : private=false, visibility=public, license=null, has_pages=true ; `git ls-files livrables_investisseurs` → 36 fichiers (4 pdf, 4 md, 28 png), 14 Mo ; 13 vidéos mp4 = 33 Mo ; aucun LICENSE versionné ; 21 .md/.json à la racine ; .passio/context/KNOWN_RISKS.md:21 (fuite de DM du 2026-08-09) ; total à HEAD : 1 353 fichiers / 91 Mo (dont 476 fichiers / 20 Mo de .passio/audits ajoutés APRÈS c8cb8e9, cf. CPL-EXP-01). | Confirmé (36 fichiers de livrables, pas 37 — sans incidence). GitHub Pages est bien activé (has_pages=true) mais ce qu'il sert n'a pas pu être vérifié (tunnel 403 vers github.io) : reste une action à faire. Aucun secret ni e-mail tiers trouvé ; seules 3 adresses (celle de l'éditeur, déjà dans l'app, et des domaines de test). |
| EXP-11 | P1 → P1 | **CONFIRMÉ** | 1 seul project_ref (7 occurrences dans js/app-08*, netlify.toml, _headers, tests/e2e, dashboard/server) ; deploy.yml:300-357 (job « Suites production (comptes réels) » sur push ET PR) ; deploy.yml:10-40 (incidents 2026-08-30 quota d'inscriptions saturé par la CI) ; sentinelle-distante.yml:100-117 (canari crée 2 comptes réels par passage) ; execute_sql : telemetry_events development=8 850, preview=19 328, production=86 744 dans la même table. | Confirmé : la table de télémétrie de production contient 28 178 événements de dev/preview (25 %), preuve mesurée de la pollution par absence de staging. |
| EXP-12 | P2 → P2 | **CONFIRMÉ** | deploy.yml:93 (TRUSTED_REVIEWER: PASSIO74), :106-111 (liste critique : .github/*, migrations/*, 4 fichiers dashboard, 2 scripts — ni js/, ni index.html, ni _headers/netlify.toml, ni sw.js, ni supabase/functions), :128-135 (accepte toute review de PASSIO74 contenant la chaîne) ; grep 'dossier-revue\|npm run revue\|chatgpt' deploy.yml → 0 ; preuves branch-main.json (2 checks requis), branch-protection.json (403) ; ⚠️ AGENTS.md ne contient AUCUNE mention de « contre-revue » (la citation « AGENTS.md:38-39 règle 9 » est fausse). | Le mécanisme est bien une auto-attestation par chaîne de caractères, au périmètre étroit. La référence à AGENTS.md est erronée (la promesse vient de docs/REVUE_INDEPENDANTE.md et de deploy.yml lui-même) ; le verdict et P2 tiennent. |
| EXP-13 | P2 → P2 | **CONFIRMÉ** | sw.js:125 (network-first index, cache-first le reste) ; 17 références online/offline dans js/ — toutes des files d'attente (app-04:131,158,498,533,4619), aucune ne rend un bandeau ; seuls toasts réseau : app-06:4368-4371 (publication) et app-07:3423 ; preuves supabase-coupe.json : visiteur → screen-feed, 20 cartes, 0 toast, texteHorsLigne null ; onboardé → 0 carte, landing active, 0 toast. ⚠️ Dans les deux scénarios `requetesSupabaseBloquees` = 0 : l'émulation a coupé le CDN (ERR_TUNNEL), pas Supabase à proprement parler. | Le comportement (aucune indication hors-ligne, fil de démonstration présenté comme réel) est confirmé par le code ; la preuve d'émulation est moins précise que son titre (aucune requête Supabase n'a été interceptée, le SDK n'a jamais chargé) mais le résultat visible est le même. P2 maintenu. |
| EXP-14 | P2 → P2 | **CONFIRMÉ** | index.html:311 (« contrôle d'âge IA … +18 ans ») ; js/app-02-state-utils.js:3179-3193 (accepte ≥ 13, pose isMinor) ; app-08:320 (skipToApp force 1995) ; app-02:3145 (politique : 13 ans) ; declare_birth_year appelé par app-07:5024 (proposition IRL) et app-08:5793 (au boot, best-effort, sous drapeau irlProposalEnabled) ; preuves emul (birthYearAccepted 2013, isMinor true). | Confirmé. Nuance : la déclaration serveur est aussi tentée au boot (app-08:5793), pas seulement à la proposition IRL — mais elle reste conditionnée à un drapeau et n'empêche rien à l'onboarding. |
| EXP-15 | P2 → P2 | **CONFIRMÉ** | js/telemetry.js:31 (activée par défaut), :34-38 (passio_device_id persistant en localStorage), :118 (`DEVICE_ID = _deviceIdRaw()`), :494 (`device_id: DEVICE_ID` envoyé BRUT) ; :50-56 (opt-out seulement par ?telemetry=0 ou localStorage) ; grep 'telemetry=0\|Statistiques d.usage' index.html js/ hors telemetry.js → 0 ; app-02:3134-3151 (politique sans mention de la télémétrie). | Le sous-agent écrit « haché avant envoi (:45) » : c'est faux — la ligne 45 est le hachage d'ÉCHANTILLONNAGE, l'identifiant d'appareil part en clair (telemetry.js:494). Le constat est donc plus net que décrit ; on passe de PLAUSIBLE à CONFIRMÉ pour les faits, la qualification ePrivacy restant une appréciation. |
| EXP-16 | P2 → P2 | **CONFIRMÉ** | passio_qa_registry.json : 30 incidents, statuts = FIXED_IN_PROD ×10, CLOS ×9, FIXED_LOCALLY ×6, INFIRME_PARTIELLEMENT, DETECTED, « OUVERT — bloque le deploiement », « CORRIGE, en attente de contre-revue », CORRIGE ; .claude/skills/passio-incident-report/SKILL.md:39-45 (7 statuts admis) ; .passio/playbooks/README.md:1-8 (carte de skills) ; deploy.yml:10-40 (incidents racontés en commentaires YAML). | Confirmé avec un chiffre à corriger : 5 formes hors nomenclature (14 incidents), pas 8 — 8 est le nombre TOTAL de statuts distincts. Le fond (registre hétérogène, aucun playbook utilisateur) tient. |

Exécuté : git diff --stat c8cb8e9 HEAD hors .passio/audits → vide (code identique au SHA audité) · list_migrations → 4 versions · execute_sql : pg_tables/pg_views/pg_policies/pg_extension/cron.job/storage.buckets/storage.objects/auth.users/telemetry_events par env/user_state orphelins (agrégats seulement) · diff des noms de policies SCHEMA_PROD_REFERENCE.sql vs pg_policies (relecture/exploitation-continuite/{ref,prod}_policies.txt) · curl api.github.com : repo (visibility, has_pages, license), 100 runs sentinelle-distante.yml (intervalles recalculés), runs rollback.yml (0) · git ls-files + du : dépôt, livrables_investisseurs, vidéos, .passio/audits · greps ciblés : RPO/RTO, CGU, SIREN, publish deploy, uptime/statuspage, 72 h/violation, rotation, window.PASSIO_, online/offline

Limites déclarées : Aucune écriture, aucun appel à Supabase REST/Storage, Netlify ou Render ; aucune émulation navigateur relancée (verdicts EXP-13/14 par lecture du code et des preuves existantes). Non vérifiables ici : ce que sert GitHub Pages (tunnel 403), le plan/PITR Supabase, les collaborateurs GitHub/Netlify/Supabase (API 403), la durée du job « Déploiement production ». Le fichier de preuve sentinelle-runs.json cité par le sous-agent est absent ; il a été refait à partir de l'API publique (100 runs). HEAD (08e7e7a) diffère de c8cb8e9 uniquement par l'ajout de .passio/audits — ce qui fonde CPL-EXP-01.

### irl

| Id | Priorité 04 → 08 | Verdict | Preuve | Commentaire du relecteur |
|---|---|---|---|---|
| IRL-01 | P1 → P1 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:3767-3777 (infoRow Adresse + lien maps.google + <a href="tel:…"> sans aucune condition d'inscription/compte) ; js/first-run.js:515-524 (supaLoadEvents() lancé pour le visiteur sans compte, résultat écrit dans seed.events) ; preuves/irl/policies-irl.txt [events] Lecture publique + Read events, qual=true, roles={public} ; ref_cols.txt:21 (colonnes address, lat, lng, contact) ; emulation-resultats.json « fiche evA (non inscrit) » adresseVisible=true, contactVisible=true. | Vrai au SHA : la fiche ne connaît aucune notion de « réservé aux inscrits », et le visiteur de première visite reçoit les vrais événements. Le formulaire (5294) ne prévient pas non plus l'organisateur que son numéro sera lisible par tout le monde, clé anon comprise. P1 justifié (numéro personnel + adresse exacte exposés à anon/scraping). |
| IRL-02 | P1 → P1 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:4949 (irlProposalEnabled → `return false` par défaut) ; 5015-5024 et 5041-5050 : `declare_birth_year` et `irl_interaction_allowed` uniquement derrière irlProposalEnabled() ; grep `irl_interaction_allowed\|isMinor` sur js/ : seuls appelants 4962/5024/5050, aucun dans setEventRsvp (3269), submitEvent (5568), checkInEvent (3433), _joinEventConversation (3366) ; js/app-02-state-utils.js:3179-3193 (année auto-déclarée, isMinor local) ; migrations/migration_ts_serveur_age_blocage.sql:130-152 (fonction existe, staging.md ligne 11 la liste en prod) ; policies-irl.txt [event_attendees] INSERT check = user_id = auth.uid() seulement. | Exact : la garde serveur existe en prod mais aucune écriture IRL ne la consulte, et aucune policy ne porte de condition de majorité. L'étape « age » de l'onboarding (onbSteps app-02:3160) est bien traversée par le parcours première visite, mais l'année reste locale (jamais envoyée à user_safety hors drapeau OFF). P1 tenu. |
| IRL-03 | P1 → P1 | **CONFIRMÉ** | preuves/irl/policies-irl.txt [event_attendees] Lecture publique cmd=SELECT roles={public} qual=true ; ref_cols.txt:18 (colonnes rsvp, checked_in_at, rating, feedback, rated_at) ; js/app-08-ui-modals-tour.js:4190-4193 (l'app elle-même lit event_id,user_id,rsvp,checked_in_at pour les 60 événements, y compris en mode visiteur via first-run.js:516). | La policy le garantit sans exécution : anon lit la table entière (qui, où, quand, pointé ou non, note et retour libre). Non exécuté contre la prod (interdit), mais l'émulation par qual est fidèle pour un `true`. P1 tenu (RGPD + profilage de déplacements). |
| IRL-04 | P1 → P1 | **CONFIRMÉ** | js/app-08-ui-modals-tour.js:5506-5514 : UPDATE event_attendees SET rsvp='going' WHERE user_id=<autrui>, retour `_writeVerdict(expectRows).ok` (false sur 0 ligne) ; policies-irl.txt [event_attendees] UPDATE qual/check = user_id = auth.uid() → 0 ligne pour toute promotion d'autrui ; js/app-07-ia-explore-irl.js:3355 `await supaPromoteFromWaitlist(ev.id, next);` retour ignoré, 3357-3358 notif « une place s'est libérée, tu es inscrit·e ! » ; 5476-5477 idem côté organisateur ; texte de la modale 5449 « Ils sont promus automatiquement dès qu'une place se libère » ; emulation-resultats.json « désinscription → promotion file » : supaInsertNotif envoyée après stub false. | Vrai : la fonction rend déjà `false` (moitié du correctif faite) mais ses deux appelants ne le lisent pas. La liste d'attente est donc une fonctionnalité annoncée et intégralement morte côté serveur, avec notification mensongère ; à la reconnexion, supaLoadMyRsvps ré-hydrate « waitlist » chez la personne promue. P1 tenu. |
| IRL-05 | P2 → P2 | **CONFIRMÉ** | policies-irl.txt [event_attendees] INSERT check=(user_id = auth.uid()), UPDATE idem ; ref_cols.txt:18 + liste des contraintes : seule event_attendees_pkey, aucun CHECK ; staging.md ligne 20 : triggers actifs, aucun sur event_attendees ; js/app-07-ia-explore-irl.js:3286-3290 (_eventIsFull) et 3283 (_eventIsCancelled) = gardes CLIENT uniquement. | Exact : capacité, statut et valeur du rsvp ne sont contrôlés que dans setEventRsvp ; un PATCH/POST REST ou deux clients concurrents passent. P2 correct (intégrité, pas sécurité). |
| IRL-06 | P2 → P2 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:5522-5535 : `await supaDeleteEvent(id)` sans lecture du retour, purge locale + toast « Événement supprimé » inconditionnels, aucune _notifyEventAttendees ; js/app-08-ui-modals-tour.js:4159-4175 : delete enfants (RLS DELETE user_id=auth.uid() → seules MES lignes partent, sans erreur) puis delete parent avec verdict expectRows rendu mais ignoré ; ref_cols.txt : event_attendees n'a que sa PK, aucune FK vers events ; policies-irl.txt [events] DELETE qual=author_id=auth.uid() ; emulation-resultats.json « suppression refusée par la base » encoreLocal=false, toast « Événement supprimé ». | Vrai, et un cas aggravant non cité : openEventManage (5375-5394) est ouvert à _canManageEvent (co-organisateur inclus) et propose « Supprimer définitivement », alors que la policy DELETE est auteur seul → le co-organisateur voit « Événement supprimé », l'événement revient au prochain supaLoadEvents. P2 tenu (l'annulation douce, qui prévient, reste le chemin recommandé). |
| IRL-07 | P2 → P1 **(changée)** | **CONFIRMÉ** | js/app-08-ui-modals-tour.js:4180 `.select("*").order("created_at",{ascending:false}).limit(60)` sans filtre de date ni pagination ; js/app-07-ia-explore-irl.js:2400-2403 : les événements passés sont écartés CLIENT seulement (ils consomment donc le quota de 60) ; 4018 RECURRENCE_MAX_OCCURRENCES=12 et 5255 value="6" : une série crée 6 à 12 lignes réelles ; 4276-4287 : le lien profond #irl-event-<id> (et donc le QR de pointage) ne cherche que dans allEvents() → « Événement introuvable ou supprimé » pour tout événement hors des 60. | Confirmé et sous-estimé : avec la récurrence (6 par défaut, 12 max) cinq organisateurs suffisent à saturer les 60, les événements passés y comptent, et la conséquence dépasse la liste — les liens partagés et le QR d'accueil échouent aussi. C'est un plafond de capacité atteint dès les premiers utilisateurs actifs → P1 (avant lancement public) plutôt que P2. |
| IRL-08 | P2 → P2 | **CONFIRMÉ** | policies-irl.txt [events] Update organisateurs qual=(author_id=auth.uid() OR jsonb_exists(co_organizers, auth.uid())) check=None ; [events] INSERT check=(author_id=auth.uid()) sans condition sur organizer_id ; js/app-08-ui-modals-tour.js:4113-4114 organizer_id posé côté client seulement ; 4213-4214 organizerName lu depuis organizer_id \|\| author_id ; staging.md ligne 20 : freeze author existe pour posts uniquement. | Exact sur le fond. Nuance de correction : sans WITH CHECK, Postgres applique USING à la ligne résultante, donc « WITH CHECK identique à USING » ne change RIEN — un co-organisateur qui s'écrit author_id satisfait déjà USING après coup. Seul un trigger gelant author_id/organizer_id (à l'image de trg_posts_freeze_author) et, au besoin, interdisant au co-organisateur de toucher co_organizers, ferme la prise de contrôle. P2 tenu. |
| IRL-09 | P2 → P2 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:5562-5565 reportEvent → supaReport("event", id, "") puis toast, sans confirm ni motif ; js/app-08-ui-modals-tour.js:5383-5392 : insert direct, aucun anti-doublon (seul rate_limit 10/min serveur) ; grep -i « lieu public\|conseils de sécurité\|prévenir un proche » sur app-07, ui-v4b*, ui-v4a2*, index.html : 0 résultat ; emulation-resultats-2.json « 5 signalements successifs » appels=5. | Vrai : aucun rappel de sécurité avant une rencontre physique, signalement à motif vide, pas de blocage de l'organisateur depuis la fiche (le ⋯ ne vit que sur le profil, doc lots-ui/12). P2 correct. |
| IRL-10 | P2 → P2 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:3369-3372 (_canManageEvent = auteur OU co-organisateur crée la discussion) ; js/app-08-ui-modals-tour.js:5580 created_by: MY_UID ; migrations/migration_ts_serveur_age_blocage.sql:212-232 can_join_event_conversation exige `c.created_by = e.author_id` ; policies.json conv_members INSERT check = is_conversation_creator OR (user_id=auth.uid() AND can_join_event_conversation) ; app-08:5588-5594 rend false sur erreur, app-07:3378 l'ignore ; 3380 `_ensureLocalEventConv(ev)` exécuté quel que soit le verdict. | Confirmé, et « rien ne se passe » est en-deçà de la réalité : après le refus, _ensureLocalEventConv (3380) puis openEventChat (3421-3423) fabriquent quand même une conversation locale « 📍 <titre> » dans la messagerie du participant, dont il n'est pas membre côté serveur — ses messages y seront refusés par la RLS de conv_messages. P2 tenu. |
| IRL-11 | P3 → P3 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:5582-5583 parseFloat/parseInt sans borne ; 5608 `if (!editId && ts < Date.now() - 3600000)` : la garde de date saute en édition ; 5287/5290 min="0"/min="1" sur les inputs mais le bouton est un onclick, pas un submit de formulaire → la validation native ne s'applique pas ; ref_cols.txt : aucun CHECK sur events ; emulation-resultats.json « création valide (prix -5, max -3) » full=true, prixAffiche « Gratuit » ; emulation-resultats-2.json « édition d'un événement avec une date passée » supaUpdateEvent appelé, visibleListe=false. | Exact. Conséquence supplémentaire de l'édition vers le passé : l'événement disparaît de la liste principale de son propre organisateur (visibleListe=false) sans message. P3 correct. |
| IRL-12 | P3 → P3 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:3462-3464 (erreur GPS → done()) ; 6490-6500 code = FNV(id) avec alphabet public ; 6579 comparaison côté client ; js/app-08-ui-modals-tour.js:5462-5470 supaCheckInEvent = UPDATE de MA ligne avec expectRows, retour ignoré par 3444 et 6593. | Confirmé avec une nuance : « pointage sans inscription préalable » n'est vrai que LOCALEMENT (badges) — côté serveur l'UPDATE touche 0 ligne sans ligne d'inscription et rend false, que le client ignore sans annuler son checkedInEvents local. Le fond (aucune preuve serveur, code dérivable de l'id public) tient. P3 correct tant que le pointage ne conditionne que des badges. |
| IRL-13 | P3 → P3 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:5732-5739 _notifyEventAttendees itère ev.attendees uniquement (maybes et waitlist ignorés) ; 5514 toggleCancelEvent → _notifyEventAttendees. | Exact. Même limite pour _messageEventAttendees (5557) et cancelEventSeries (5491). P3 correct. |

Exécuté : Aucun test Playwright lancé : chaque point se tranche par lecture directe du code au SHA (git diff --stat c8cb8e9 HEAD -- js/ index.html styles.css migrations/ → vide) croisée avec les preuves déjà produites (emulation-resultats*.json, policies-irl.txt, ref_cols.txt, fonctions_realtime_storage_staging.md). Commandes exécutées : sed/grep sur js/app-07-ia-explore-irl.js, js/app-08-ui-modals-tour.js, js/app-02-state-utils.js, js/first-run.js, sw.js, supabase/functions/notify-call/index.ts, migrations/migration_ts_serveur_age_blocage.sql.

Limites déclarées : Aucune requête vers la production ni exécution du moteur RLS (interdit) : les verdicts « RLS » reposent sur le dump policies.json et les sources SQL des fonctions, pas sur une écriture réelle. Les émulations du sous-agent n'ont pas été rejouées (résultats relus contre le code, cohérents). La push Web (CPL-IRL-04) n'a pas été déclenchée. Les priorités P1 des items 01/02/03 supposent la lecture « sécurité IRL » du GO : elles tiennent au regard des colonnes réellement exposées (contact, address, lat/lng, feedback), mais l'appréciation reste celle de l'orchestrateur.

### perf-capacite-couts

_Relecture non rendue._

### profils-passions

| Id | Priorité 04 → 08 | Verdict | Preuve | Commentaire du relecteur |
|---|---|---|---|---|
| PRO-01 | P1 → P1 | **CONFIRMÉ** | js/app-02-state-utils.js:1304 `supa.from("passions").select("id")` sans `.range()` ni `.limit()`, cache à un seul coup (:1302) ; :1337-1345 `estPassionCanonique` = `_referentielPassions` ∪ `PASSIONS` (19, app-01:80) et RIEN d'autre — `js/passions-flat.js` (data/passions-v1.json, 1 908 entrées, fetch :250) n'alimente jamais `_referentielPassions` (grep : aucune écriture hors app-02:1307). Comptage statique rejoué (scratchpad/relecture/profils-passions/compte-refusees.js) : total_json=1908, ids1000=1000, socle=19 dont 11 dans les 1 000 → refusees=900, sante-sport-sante et parentalite-sport-famille refusées. Studio : js/passions-flat-ui.js:134-136 `passionPubliable` = `estPassionCanonique` ; publishPost app-06:4245 refuse avant tout insert ; renderStudio app-06:3792 filtre le `<select>` par `estPassionCanonique`. Le commentaire app-02:1318-1323 nomme lui-même le plafond max-rows et ne le traite que dans le sens « ne pas rétrécir », jamais dans le sens « compléter ». | Tient au SHA. La valeur réelle de `max-rows` reste BLOQUÉE (proxy), mais le défaut de Supabase (1 000) est documenté et le code l'anticipe ; la borne exacte des refusées dépend des 1 000 lignes que PostgREST rend (sans ORDER BY : entre 889 et 908), ce qui ne change rien au verdict. P1 justifié (pas P0 : une autre passion reste publiable ; pas P2 : un compte réel est privé de l'une de ses trois passions et la page Rechercher promet 1 908). Précision de correction : `PassioPassions` est chargé à la demande (jamais au boot, invariant 160 Ko) donc ne peut pas être l'autorité seule ; la pagination `.range()` en boucle jusqu'à `data.length < 1000` est la voie sûre, et il faudra RE-RENDRE `#postPassion` à l'arrivée (voir CPL-PRO-02). |
| PRO-02 | P1 → P1 | **CONFIRMÉ** | js/app-02-state-utils.js:1670-1690 `_msgSenderMeta` : n = premier non-défaut de [g.username, state.user.name, prof.name] (état local libre), ph = toute URL `^https?://` (:1687) ; :1697-1703 injecté dans CHAQUE message sortant. Réception : app-04:4309-4312 `sp.n` écrase `fromName` ; app-08:4746-4750 liste des conversations `userName: lastSp?.n \|\| otherProf?.username`, `userPhoto: lastSp?.ph \|\| …` ; app-08:4924-4928 temps réel réécrit conv.userName/Emoji/Color/Photo depuis `sp`. Serveur : migrations/migration_identite_affichage_canonique.sql:77-87 pose `trg_identite_affichage` sur video_lives, event_comments, cdv_live_comments (+ step_interactions selon requêtes §3), JAMAIS sur conv_messages ; requêtes §1 : conv_messages/notifications n'ont aucune colonne d'identité. Notifications : app-08:4776-4781 `content = escapeHtml(currentProfile().name) + ' ' + content` écrit par l'émetteur. | Confirmé à la ligne ; l'émulation C est cohérente avec le code. Nuance : l'UI vérifie l'unicité du pseudo (`supaUsernameTaken` app-08:4487, appelé app-06:1319) mais `sp.n` ne dérive pas d'un champ validé serveur, et rien en base n'impose l'unicité (voir CPL-PRO-01). `sp.ph` mérite d'être traité dans la même correction pour une raison SUPPLÉMENTAIRE : la CSP autorise `img-src https:` (netlify.toml:19, _headers:40) et `safeUrlAttr` accepte http(s), donc une URL externe posée dans `ph` devient une balise de pistage (IP, UA, heure de lecture) par destinataire. Réserve sur le risque de régression : `sp.pid` sert UI-8, ne réécrire que n/e/c/ph. |
| PRO-03 | P2 → P2 | **CONFIRMÉ** | Serveur : requêtes-base §2 (policies user_passions = `user_id = auth.uid()` seulement, profiles « Update propre » sans contrainte sur le jsonb) ; migrations/SCHEMA_PROD_REFERENCE.sql:356-357/437 : profiles ne porte que pkey + FK passion_id (et ne connaît pas encore user_passions, référence du 2026-08-17). Client : app-08:3142-3163 `supaSavePassionState` envoie `charge.passions` (jsonb entier) + `passion_id` sans autre garde que `_writeVerdict` ; toutes les gardes de plafond/quota sont dans app-06 (`archiverPassion` :2004-2008, `ajouterPassionAuCompte` :3701) et lisent `state.user.passionChanges.entries`. CLAUDE.md (mode illimité) l'assume : « rien en base que la console ne puisse déjà écrire ». | Vrai et cohérent avec le code : le plafond est une règle d'écran, pas une règle de données. P2 maintenu — aucun paiement n'existe aujourd'hui (paywall sans montant), donc pas P1 ; mais c'est le prérequis de toute offre payante, donc pas P3. À prévoir : exemption des comptes antérieurs (5 vivantes), et `reinjecterProfilsLocauxBornes` (app-02) qui ne borne que ce qu'elle ajoute. |
| PRO-04 | P2 → P2 | **CONFIRMÉ** | Divergence = fait de base (requêtes §6, non re-vérifiable ici : connecteur interdit). Mais le DIAGNOSTIC du finding est faux sur deux points : (a) `archiverPassion` app-06:2040-2041 et `restaurerPassion` :2081 appellent DÉJÀ `supaSavePassionState()` puis `supaSaveUserState()` dans la même fonction — la correction « publier depuis la même fonction » est déjà en place ; ce qui manque est le SUIVI : `supaSavePassionState` (app-08:3142-3163) est fire-and-forget, sans reprise ni file (`_writeVerdict` app-02:376-395 se contente d'un console.warn + diagLog), alors que user_state a beacon + reprise — un seul échec réseau au moment de l'archivage donne exactement l'état observé (moto-enduro vivante côté vitrine, archivée côté état, et `passion_id` figé sur une valeur antérieure). (b) `.passio/context/MULTI_PROFILE.md` ne dit NULLE PART que `profiles.passion_id` porte l'active (grep « passion_id » : lignes 15 et 59, toutes deux sur `posts.passion_id`) ; le code dit l'inverse et l'assume : app-08:2739-2753 « préfère une passion VIVANTE mais accepte une archivée en dernier recours », `_chargeProfilComplete` « la passion principale (1ʳᵉ créée) reste dans passion_id ». | Observé confirmé, cause et correction à réécrire : file de reprise (comme `passio_post_delete_outbox_v1`) ou re-publication de `profiles.passions` au verdict RÉUSSI de `supaLoadUserState`, plus retirer l'affirmation doc inexistante. Le `passion_id = art (archivée)` s'explique aussi par PRO-01/CPL-PRO-02 : `_passionIdPubliable` filtre par `estPassionCanonique` et, si le référentiel n'est pas (ou pas entièrement) chargé, seules les passions du socle sont candidates. P2 maintenu : un visiteur voit une passion quittée, et les créateurs d'une page de passion sont cherchés sur cette colonne (app-07:430-435). |
| PRO-05 | P3 → P3 | **CONFIRMÉ** | app-06:2040-2049 `archiverPassion` ne re-rend que `renderProfilesScreen`/`renderProfileStrip` (jamais `renderStudio`) et rien du tout en mode silencieux ; app-06:4212 `publishPost` lit `#postPassion` et ne vérifie que `estPassionCanonique` (:4245) ; :4295-4296 `_matchProf` retombe sur un profil ARCHIVÉ de même passion (« \|\| find(pr.passion === passion) »). Mais la reproduction du finding (« revenir au Studio sans re-rendu ») n'est PAS un chemin réel : `goTo("studio")` appelle `renderStudio()` (app-02:2053) qui rebâtit le `<select>` sur `passionsVivantes()` (app-06:3789-3792). Le chemin réel existe ailleurs : Studio ouvert sur Y → choix d'une passion X non possédée au plafond (flat-ui :190 `ajouterPassionAuCompte` → app-06:3702 `openPassionPaywall({})`) → « échanger » (app-06:3415 → `echangerPassion` :2108-2114 = `archiverPassion(Y, true)` silencieux, puis closeModal + renderProfilesScreen, sans renderStudio) → `#postPassion` reste sur Y → publication dans la passion qu'on vient d'archiver, `profileId` archivé. | Défaut réel, reproduction à réécrire avec le chemin d'échange depuis le Studio (le seul qui laisse l'écran ouvert). P3 maintenu : la passion reste canonique et possédée-mais-archivée, aucune perte ; sous UI-6 le `<select>` natif est masqué (app-06:3806) donc l'utilisateur ne peut pas RE-choisir Y à la main, seul l'échange le laisse en place. Correction : re-rendre `#postPassion` dans `archiverPassion`/`echangerPassion` ET ne retenir dans `publishPost` qu'un profil vivant (retirer le repli de :4296). |
| PRO-06 | P3 → P3 | **CONFIRMÉ** | Chiffres = faits de base (requêtes §4-5, non re-vérifiables ici). Cause du miroir incomplet lisible dans le code : app-08:3195-3210 `supaMiroirUserPassions` saute toute passion `!estPassionCanonique(pid)` — avec PRO-01 (900 ids inconnus du client, dont sante-sport-sante et parentalite-sport-famille, précisément dans la liste des manquantes du §5) et avec la course du chargement (CPL-PRO-02), les lignes manquent DURABLEMENT, pas par accident ; `_userPassionsDispo` se désarme sur 42501 pour la session (:3186-3190). Orphelins user_state : le client supprime user_state (app-02:3094) mais l'Edge Function delete-account ne le fait pas (supabase/functions/delete-account/index.ts:46-62, ni user_state ni user_passions) — déjà rapporté et relu comme SUP-10 (06-AUDIT-SECURITE-DONNEES.md:305-312). | Confirmé ; le finding manque sa cause principale : corriger PRO-01 (référentiel complet) fait revenir la plupart des 23 entrées à la prochaine écriture, un job de réconciliation seul re-sauterait les mêmes ids. La partie « 79 orphelins » recoupe SUP-10 (résidus après suppression) : à fusionner plutôt qu'à compter deux fois. P3 maintenu tant que rien ne LIT user_passions (app-08:3172-3176). |

Exécuté : git diff --stat c8cb8e9 HEAD -- js/ index.html migrations/ data/ → vide (code audité = code relu) · node scratchpad/relecture/profils-passions/compte-refusees.js (calcul statique : data/passions-v1.json ∖ (ids-1000-ctid.txt ∪ PASSIONS app-01)) → {total_json:1908, ids1000:1000, socle:19, socle_dans_les_1000:11, refusees:900, sante_sport_sante_refusee:true, parentalite_sport_famille_refusee:true} · Rejeu de maxrows-emulation.js sur http-server :8111 → ÉCHEC D'ENVIRONNEMENT : node_modules/ absent (Cannot find module @playwright/test) ; rien installé, serveur arrêté ; conclusions par inspection + calcul statique

Limites déclarées : Aucun test navigateur possible (node_modules absent, interdiction d'installer) ; aucune requête base (connecteur interdit) : les faits de base (§2, §4-6 des requêtes du 2026-09-04) sont repris tels quels et non re-vérifiés ; la valeur réelle de `max-rows` du projet et l'absence d'index unique sur `profiles.username` en PRODUCTION restent à confirmer par l'orchestrateur (`select setting from pg_settings` ne l'expose pas ; `select indexdef from pg_indexes where tablename='profiles'`).

### robustesse-pannes

| Id | Priorité 04 → 08 | Verdict | Preuve | Commentaire du relecteur |
|---|---|---|---|---|
| ROB-01 | P2 → P2 | **CONFIRMÉ** | js/app-06-reels-partage.js:4348-4371 (Promise.race 5 s, échec → toast « Post en local », aucune file) ; grep `_pendingSync\|syncStatus` sur js/ → seul rejeu existant = bobines (`_scheduleReelRetry` app-08:1403-1420, filtre `p.isReel` 1408) ; app-09-boot-pwa.js:252-262 (`online` → supaLoadPosts seulement) ; script relecture.js T2 → `appelsPublish: 0` après dispatch `online` ; chaos C3 (chaos.spec.js:397, PAS chaos2) → insertsPosts 0 après reload. | Réel au SHA : aucun code ne rejoue `state.userPosts` (hors bobines) ni au retour du réseau ni au boot, alors que messages (`passio_outbox_v1`), commentaires (`passio_cmt_outbox_v1`), user_state (pending) et bobines ont chacun leur file. Deux corrections de formulation : la preuve cite `chaos2.spec.js` qui n'a jamais tourné (aucune ligne A7-A10 dans 04-chaos-sortie-brute.txt, pas de 02b-*.jsonl) — le scénario C3 est dans `chaos.spec.js:397` ; et `sync: syncing` n'est pas un indice de reprise avortée : c'est la valeur INITIALE (app-06:4314), jamais réécrite nulle part. L'utilisateur reçoit « Post en local (connexion lente) » + notification « Ton post est en attente » (app-06:4424) — pas silencieux, mais mensonger : rien n'attend. P2 tenu (à la limite de P1 pour un usage en événement à connexion faible). |
| ROB-02 | P2 → P2 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:3297-3305 (mutation optimiste + saveState AVANT écriture), 3319-3321 (`rsvpOk` lu puis utilisé UNIQUEMENT pour `irlFunnelTrackJoin`, aucun rollback, aucune file) ; script relecture.js T3 (supaSetEventRsvp stubé à `false`) → `rsvpLocal: going`, attendees conservés, toasts [] ; chaos C4 → repriseAutomatique = 1 GET. | Confirmé par inspection ET exécution hors réseau : l'échec réel de l'écriture ne défait jamais l'affichage optimiste, contrairement à l'invariant CLAUDE.md appliqué au like (`likePost` annule + toast). Correction la plus courte : `if (rsvpOk === false) { restaurer prev ; toast }` — la promotion en file d'attente et la conversation de groupe (3327-3328) doivent aussi être conditionnées. Voir CPL-ROB-02 pour la notification à l'organisateur envoyée malgré l'échec. |
| ROB-03 | P2 → P2 | **CONFIRMÉ** | js/app-09-boot-pwa.js:240-252 (`#offlineBanner` uniquement sur message SW OFFLINE / événement `offline`, jamais sur un statut HTTP) ; js/app-02-state-utils.js:376-400 (`_writeVerdict` → console.warn + diagLog, aucune UI) ; grep `SIGNED_OUT` sur js/ → 0 résultat ; grep « Session expirée » → seulement le formulaire mot de passe (app-02:3020, 3031) ; js/app-08-ui-modals-tour.js:5707-5722 (boucle fil 60 s) ; chaos F_500/401/429 : toasts [] et bannière none. | Le cœur tient : aucun état « serveur injoignable / session expirée » n'est jamais montré, et l'app n'a AUCUN gestionnaire `SIGNED_OUT` — après un échec de rafraîchissement du jeton, `MY_UID` (restauré de `localStorage.passio_uid`) reste posé et l'app continue comme connectée. Deux surévaluations à corriger : (1) « les écritures échouent en silence » n'est PAS mesuré par F_* (aucune écriture n'y est tentée) et est faux pour like (rollback + toast), commentaires (« Non envoyé · Réessayer »), messages (« réessayer ») et posts (« Post en local ») — il est vrai pour RSVP, suivre, bloquer, story ; (2) « 28 requêtes en 30 s de rejeux » n'est pas prouvé : la fenêtre de mesure inclut le double chargement du boot (supaInit au boot + SIGNED_IN) et la boucle à 60 s, sans backoff mais sans tempête. P2 tenu. |
| ROB-04 | P3 → P3 | **CONFIRMÉ** | js/app-04-comments-shop.js:3280-3311 (`toggleFollowUser` : aucun verrou, bascule sur `state.user.following` synchrone), 3314-3327 (`blockUser` : aucune garde « déjà bloqué », supaBlockUser + supaUnfollowUser + toast à chaque appel) ; script relecture.js T4 → follow 1 / unfollow 3 / block 2, toasts « Tu suis » puis « Tu ne suis plus », « bloqué » ×2. | Reproduit hors réseau. Nuance : le second tap arrive sur un bouton déjà passé à « ✓ Suivi », donc l'état final (non suivi, local ET serveur cohérents) est une bascule, pas une corruption ; le grief tient par incohérence avec le like, qui a un verrou anti-double-clic. Le côté « Bloquer » est le plus utile à corriger (deux INSERT blocks — le second finit en 23505 grâce à `blocks_pkey`, mais 2 DELETE follows et 2 toasts). P3 juste ; correction : sortir tôt si `state.user.blocked.includes(userId)` et verrou `_followBusy[uid]`. |
| ROB-05 | P3 → P3 | **CONFIRMÉ** | js/app-07-ia-explore-irl.js:1152-1165 (refus → `irlUserLocationError = true` PUIS `irlUserLocation = Paris`, aucun toast) ; 1240-1255 `updateIrlCityTitle` : la branche « Paris (approx) » (1250) est MORTE — `irlUserLocation` est déjà posée, donc 1247 rend « Paris » ; script relecture.js T5 → titre « Paris », toasts []. | Confirmé, mais le correctif est plus petit que proposé : l'intention d'afficher « Paris (approx) » existe déjà et n'est jamais atteinte (ordre des `if`), et la saisie de ville existe déjà (`openIrlCitySelector` app-07:1258, `#irlCitySearch` index.html:1219). Reste à ajouter : tester `irlUserLocationError` AVANT `irlUserLocation` dans le titre, un toast unique « Localisation refusée — résultats autour de Paris, change de ville », et ne pas confondre ce repli avec une position réelle dans le tri par distance. Effort 0,1 j. |
| ROB-06 | P3 → P3 | **CONFIRMÉ** | js/app-04-comments-shop.js:4644-4649 (`_flushOutbox` : pas de verrou, relit la file et renvoie tout) ; script relecture.js T1a → deux `_flushOutbox()` = 3 INSERT pour 1 message ; chaos C2 envoisDbl [ok, ok, dup, dup]. | Le défaut de verrou est réel et reproduit. Mais la partie « le statut reste failed » n'est PAS due au double flush : elle vient d'un défaut distinct et plus grave — `_sendTextToSupa` (app-04:4630-4634) traite un 23505 comme un échec et REMET le message en file — élevé en CPL-ROB-01 (P2). Sans lui, le double flush ne coûterait que du bruit réseau (T1a : les deux réponses 201 du stub laissent `sent`, outbox vide). P3 tenu pour le verrou seul ; le corriger sans CPL-ROB-01 ne réglerait pas le symptôme observé. |

Exécuté : node scratchpad/relecture/robustesse-pannes/relecture.js (Chromium 1194 via playwright 1.56.1 global, http-server 127.0.0.1:8112, toute autre origine abandonnée, écritures stubées) → T2 appelsPublish 0 après `online` ; T3 rsvpLocal going + notifsOrganisateur [event_join→u_org] + toasts [] ; T4 follow 1 / unfollow 3 / block 2, toasts ×4 ; T5 titre « Paris », toasts [] ; erreursJS [] · node scratchpad/relecture/robustesse-pannes/relecture-t1.js (idem, stub `supa` lexical) → T1a deux `_flushOutbox()` = 3 INSERT du même message (statut sent, outbox vide car le stub répond 201) ; T1b un flush avec 23505 → failed, outbox [msg_b] ; après `online` → 2e INSERT, toujours failed, outbox [msg_b] · grep : `SIGNED_OUT` js/ → 0 ; `syncStatus =` js/ → 1 écriture (app-06:4314) ; `_pendingSync` → bobines uniquement (app-08:1386-1420) ; 04-chaos-sortie-brute.txt → aucune ligne A7/A8/A9/A10/F401b (chaos2.spec.js jamais exécuté) · git diff --stat c8cb8e9 HEAD -- js/ index.html styles.css → vide (code applicatif identique au SHA audité)

Limites déclarées : node_modules du dépôt absent (pas de @playwright/test ni de SDK supabase-js local) : impossible de rejouer chaos.spec.js tel quel ; mes vérifications tournent sur playwright 1.56.1 global + chromium-1194 avec des STUBS en page (aucune requête sortante, aucune écriture en base, production jamais touchée), donc elles prouvent la logique client, pas le comportement du vrai SDK (la reprise C2 marquée failed dans le banc chaos est probablement un artefact de course entre `setOffline(false)` et `log.mode = "ok"`, mais cela ne change rien au 23505). ROB-03 : les F_* n'ont tenté aucune écriture — la partie « écritures silencieuses » est jugée par inspection ; le chemin réel d'expiration (refresh_token révoqué → SIGNED_OUT) n'a pas été exercé. CPL-ROB-04 est par inspection seule. Émulation Chromium uniquement, aucun appareil réel.

### tests-ci

| Id | Priorité 04 → 08 | Verdict | Preuve | Commentaire du relecteur |
|---|---|---|---|---|
| TCI-01 | P1 → P1 | **CONFIRMÉ** | tests/e2e/multi-comptes.spec.js:21, confidentialite.spec.js:21, suppression-compte.spec.js:28 (test.skip(!process.env.PASSIO_E2E_MULTI)), qa-campaign.spec.js:63 (PASSIO_QA_CAMPAIGN) ; grep PASSIO_E2E_MULTI .github/workflows/*.yml → une seule occurrence, en COMMENTAIRE (deploy.yml:155) ; multi-comptes.spec.js:682 openVlogViewer, :734 supaPublishCdvLive, :736/:785 supaAddCdvLiveStep, :753 supaUpdateCdvLiveStep, :774 supaAddCdvCollaborator, :828 supaAddCarnetCollaborator — aucune définie dans js/*.js ; npx playwright test --list --project=prod → 15 tests / 7 fichiers, ce qui recoupe le journal CI (3 passed = authz-critical, blocage-acces, user-state-horodatage ; 12 skipped = 8 multi-comptes + 2 confidentialite + 1 qa-campaign + 1 suppression-compte). | Tient intégralement. Précision : deux des huit tests multi-comptes (lignes 619 et 712) sont morts (ReferenceError garanti sous PASSIO_E2E_MULTI=1), les six autres restent plausibles mais jamais exécutés. Le job « Suites production » vert ne prouve que 3 tests sur 15, et CLAUDE.md:40 annonce « les 7 suites à comptes réels » pour test:prod. |
| TCI-02 | P1 → P1 | **CONFIRMÉ** | tests/e2e/confirmation-email.spec.js:47-49 (supa.auth.signUp / signInWithPassword / resend remplacés par des doublures) ; tests/e2e/compte-e2e.js:14 et :89 (POST /auth/v1/admin/users, email_confirm: true) ; grep -l resetPasswordForEmail tests/e2e/*.spec.js → 0 fichier. | Aucune machine ne parcourt le chemin SMTP Brevo → lien de confirmation → session, ni le mot de passe oublié. La porte d'entrée du produit n'est prouvée que par des doublures ; P1 justifié pour tout lancement public. |
| TCI-03 | P1 → P1 | **CONFIRMÉ** | grep -rl sauvegarde-donnees tests/ → 0 ; .github/workflows/rollback.yml:3-4 (on: workflow_dispatch uniquement), :82/:90/:121 (checkout -b, push, gh pr create --draft) ; tests/sql/socle-prod.sql = 97 lignes, 18 mentions de policy. | Rien n'exerce ni la restauration d'une sauvegarde ni le workflow de rollback. Nuance : rollback.yml est un script bash simple et lisible, le risque porte surtout sur la restauration des DONNÉES, jamais répétée. P1 maintenu (RGPD/continuité avant lancement). |
| TCI-04 | P1 → P1 | **CONFIRMÉ** | js/app-08-ui-modals-tour.js:2551 const SUPABASE_URL = "https://njkiyoklssvefstljemx.supabase.co" ; grep -c SUPABASE scripts/build.js → 0 (aucune substitution) ; deploy.yml preview needs: [governance, audits] (ligne 563) et déploie AVANT test-local/test-prod ; test-prod sans `if:` tourne aussi sur pull_request ; sentinelle-distante.yml:5 cron "17 * * * *", :117 authz-critical --project=prod ; authz-critical.spec.js:53-54 crée 2 comptes ; scripts/test-prod.js:5-8 documente 5 comptes par lancement. | Un seul projet Supabase pour tout (prod, tests, canari, previews). Le « ≈85 runs/2 jours » n'est pas retrouvé tel quel dans RESUME_PREUVES.txt (qui donne 100 runs du 02 au 04 sept) mais l'ordre de grandeur tient. P1 justifié : chaque PR écrit en production. |
| TCI-05 | P2 → P2 | **CONFIRMÉ** | REPRODUIT : bac scratchpad/relecture/tests-ci/bac-creux (copie de scripts/audit-tests-creux.js, lien js/, spec de 4 lignes page.setContent + locator) → « OK — aucun spec ne vérifie uniquement ses propres constructions », exit=0. Cause : scripts/audit-tests-creux.js:43 (regex UI) et :91 `if (UI.test(src)) continue;` — tout locator( suffit, sans goto ni fonction de production. | Contournement trivial et reproduit au SHA. P2 correct : c'est un gate de qualité, pas une barrière de sécurité. |
| TCI-06 | P3 → P3 | **CONFIRMÉ** | REPRODUIT : bac scratchpad/relecture/tests-ci/bac-iso (copie du script + socle, spec avec « sansDonneesDistantes( » en commentaire seulement, goto /index.html + #feedList) → « OK — toute suite qui navigue elle-même pose son isolation », exit=0. Cause : scripts/audit-tests-isolation.js:65-66 ne retire que les lignes require( avant la regex. | Tient. Contournement improbable par accident mais réel ; P3 correct. |
| TCI-07 | P2 → P2 | **CONFIRMÉ** | npm run couverture (2026-09-08) → « Mesurée le 2026-08-16T17:14:44Z · Interactions 355 · Exécutées 38 · Taux 10.7 % · 28 noms mesurés absents du code actuel : mesure à refaire · dénominateur 435 → 355 » ; npm run couverture:risque → « Non exercées qui ÉCRIVENT en base : 19 » avec exactement les 19 noms listés (mePublish, submitEvent, publishStoryFromComposer, …). | Chiffres reproduits à l'identique. La mesure est périmée de 23 jours et le rapport lui-même le dit. P2 correct. |
| TCI-08 | P2 → P2 | **CONFIRMÉ** | run-33861671142-jobs.json recalculé : Déploiement production 19,9 min dont Minify index 7,0 / app.js 5,2 / styles 5,0 / Netlify 2,5 ; « Installer Playwright » = 1,1+3,9+7,4+1,6+7,4+1,1+0,8+5,3 = 28,6 min cumulées (variance 0,8→7,4 min = aucun cache) ; deploy.yml:500/:511/:517 `npx --yes html-minifier-terser\|terser\|clean-css-cli` sans version ; deploy.yml:333/:390/:413 `npm install --no-save @playwright/test` (résout `latest`, pas le lock). | Tient. Nuance : package-lock.json épingle bien @playwright/test à 1.60.0 (= latest aujourd'hui), donc le dérapage n'est pas encore visible — mais la commande le contourne à chaque run. Épinglage + cache navigateurs = gain ~25 min de runner par run. |
| TCI-09 | P2 → P2 | **CONFIRMÉ** | deploy.yml:498-519 : trois étapes `npx … && mv … \|\| echo "Minification … échouée, déploiement du fichier brut"` dans le job deploy, APRÈS tous les tests ; scripts/servir-dist.js (commentaire de tête) : « Ce que ça ne prouve PAS : le comportement APRÈS minification » ; gates-artefact (deploy.yml:421) ne lance que release-integrity + passion-context sur le dist NON minifié. | Tient. Un terser qui sort 0 avec une sortie tronquée est déployé sans aucun test, et un terser en erreur déploie le brut sans rouge ni ::warning. P2 correct (un vrai rouge de prod resterait visible via la Sentinelle). |
| TCI-10 | P2 → P2 | **CONFIRMÉ** | grep -c 'upload-artifact\\|timeout-minutes' deploy.yml → 0 (sentinelle-distante.yml:43 en a un, deploy.yml aucun) ; playwright.config.js:58 retries: CI ? 2 : … sans reporter ni artefact ; REPRODUIT localement : PASSIO_RETRIES=0 --repeat-each=3 sur monitoring-file-boot.spec.js → 1 failed / 5 passed (test :49, minuteries 50 ms / 4 s / flush 500 ms contre un waitForTimeout(2000)). Réserve : le fichier deploy-runs-100.json cité comme preuve n'existe PAS dans preuves/tests-ci/ (seul run-33861671142-jobs.json y est) ; les chiffres « push/failure 4, PR/failure 8 » ne sont retrouvés que résumés dans RESUME_PREUVES.txt:22. | Le constat de code (aucun timeout, aucun artefact, reprises muettes) est vrai et le flaky des journaux est reproduit hors CI. Les 4 rouges de main ne sont pas re-vérifiables ici (fichier de preuve absent, pas d'appel GitHub). P2 maintenu. |
| TCI-11 | P2 → P2 | **CONFIRMÉ** | ls .github/dependabot.yml → absent ; grep toHaveScreenshot tests/e2e → 0 ; grep devices playwright.config.js → 0 (Chromium seul) ; tests/e2e/perf-ios.spec.js:89 toBeLessThanOrEqual(120) ; deploy.yml : aucune étape npm audit / gitleaks / lint / axe / Lighthouse. Les chiffres npm audit (1 high sharp, 4 moderate) ne sont PAS re-exécutés (registre = réseau) : repris de RESUME_PREUVES.txt:33. | Tient. Regroupement large mais chaque absence est réelle. P2 correct ; le seul point qui pourrait monter est l'absence totale de WebKit pour une PWA vendue « iOS » — à laisser au domaine mobile. |
| TCI-12 | P2 → P2 | **CONFIRMÉ** | grep -l 'RTCPeerConnection\\|startCall' tests/e2e/*.spec.js → 0 ; 'ask-ai' → 0 ; 'storage.from(' → supa-hors-ligne.spec.js seulement (stub) ; 'pushManager\\|notify-call' → supa-hors-ligne.spec.js seulement ; tests/e2e/authz-critical.spec.js:298 = fetch REST brut sur /storage/v1/ (RLS par dossier, pas le chemin applicatif). | Tient. Les fonctions les plus « démo » du produit (appel, push, upload réel, export, IA) n'ont aucune preuve automatisée. P2 correct. |
| TCI-13 | P3 → P3 | **CONFIRMÉ** | npx playwright test --list --project=local → « Total: 1103 tests in 124 files » ; --project=prod → 15 ; CLAUDE.md:40 « 897 suites navigateur » ; le fichier est .passio/context/TESTING_STRATEGY.md (PAS docs/) :7 « ~15 specs », :11 cite CDV comme couvert. | Tient, avec correction de chemin : TESTING_STRATEGY.md vit dans .passio/context/. P3 correct. |
| TCI-14 | P3 → P3 | **CONFIRMÉ** | ls tests/ hors e2e/sql/ci → 13 entrées : 10 fichiers test-*.html/js (test-debug, test-emoji, test-emoji-simple, test-final-verification, test-irl.js, test-messagerie-complet, test-panel-iso, test-quick-check, test-simple, test-time-filter.js) référencés nulle part (grep docs/ CLAUDE.md package.json → 0) ; les 3 .md : TEST_MULTISELECT.md non référencé, TEST_PUBLICATION_MULTIAPPAREILS.md cité par docs/RAPPORT_ARCHITECTURE_PUBLICATION.md, qa-report.md cité par docs/RAPPORT_QA_CAMPAGNE.md. | Tient au décompte près : 11 fichiers morts (10 test-* + TEST_MULTISELECT.md), 2 .md encore cités par des rapports. P3 correct. |
| TCI-15 | P3 → P3 | **CONFIRMÉ** | tests/sql/migration-ts-serveur.test.sh applique migration_ts_serveur_age_blocage.sql (4 policies) sur tests/sql/socle-prod.sql (97 lignes, 18 mentions policy) ; ls migrations/*.sql → 64 ; le « 125 policies » vient du dump preuves/supabase-isolation/policies.json (2026-09-04). ⚠️ grep -ci 'policy\|row level\|enable rls' migrations/SCHEMA_PROD_REFERENCE.sql → 0. | Tient, mais la CORRECTION proposée est bancale : SCHEMA_PROD_REFERENCE.sql ne contient AUCUNE policy ni RLS, donc la charger dans un Postgres jetable ne rejouerait rien des 125 policies ; il faut partir du dump policies.json (ou d'un pg_dump --schema-only incluant les policies). P3 maintenu. |
| TCI-16 | P3 → P3 | **CONFIRMÉ** | sentinelle-distante.yml:5 cron "17 * * * *", :38-40 concurrency group passio-e2e-prod (partagé avec deploy.yml:318), :108 SUPABASE_SERVICE_ROLE_KEY, :117 authz-critical --project=prod ; authz-critical.spec.js:53-54 = 2 creerCompteE2E par passage → 48 comptes/jour créés puis purgés en production. | Tient. Le verrou partagé est délibéré et commenté (sentinelle:36-37, canari ~3 min). P3 correct tant que le quota d'inscriptions horaire n'est pas atteint — c'est pourtant exactement la cause de l'incident du 2026-08-30 (deploy.yml:12-23), donc à surveiller. |

Exécuté : npm run verif → 8 gates vertes (référentiel 1908 passions) · npx playwright test --list --project=prod → 15 tests / 7 fichiers ; --project=local → 1103 tests / 124 fichiers · PASSIO_PORT=8127 PASSIO_RETRIES=0 npx playwright test --project=local tests/e2e/monitoring-file-boot.spec.js --workers=1 --repeat-each=3 → 1 failed / 5 passed (test :49 instable reproduit) · node scripts/audit-tests-creux.js --ci dans un bac scratchpad avec spec setContent+locator → exit 0 (mutation TCI-05 reproduite) · node scripts/audit-tests-isolation.js --ci dans un bac scratchpad avec sansDonneesDistantes( en commentaire → exit 0 (mutation TCI-06 reproduite) · npm run couverture ; npm run couverture:risque → chiffres TCI-07 reproduits · node sur preuves/tests-ci/run-33861671142-jobs.json → durées TCI-08 recalculées

Limites déclarées : Aucun appel réseau : npm audit (TCI-11) et les 100 runs GitHub (TCI-10) ne sont pas re-exécutés — le fichier deploy-runs-100.json cité par TCI-10 est ABSENT de preuves/tests-ci/ (seul le résumé RESUME_PREUVES.txt:22 en garde les chiffres). Les protections de branche (checks requis, approbations) ne sont pas vérifiables sans l'API GitHub : l'exploitabilité de CPL-TCI-03 en dépend. Les suites prod (multi-comptes sous PASSIO_E2E_MULTI) n'ont pas été lancées (écriture en prod interdite) : la ReferenceError de TCI-01 est déduite de l'absence des fonctions dans js/, pas observée. git status --porcelain vide avant et après.

### appareils-a11y

| Id | Priorité 04 → 08 | Verdict | Preuve | Commentaire du relecteur |
|---|---|---|---|---|
| DEV-01 | P2 → P2 | **CONFIRMÉ** | Remesuré 390×844 (relecture/appareils-a11y/mesure.json, mesure2.json) : .topbar-bell 34×34 et .hamburger 34×34 (styles.css:300-302, 3014-3016) ; #btnAttach/#btnEmoji/#btnVoice 36×36 (styles.css:6022-6023) ; .conv-fp-back 36×36 sans pseudo-élément (styles.css:5906-5907, ::before = none) ; span.post-action 55×26,5 / 47×26,5 / 31×26,5 (padding 7px 9px, styles.css:1155-1161) ; span.comment-action 26×12 / 15×12 / 12×12, padding 0, font-size 12px, line-height 1 (styles.css:3173-3179). | Deux nuances qui ne renversent pas le verdict. ① Les trois boutons du composer portent un ::before {inset:-4px} (styles.css:6030, commentaire « Zone tactile ≥ 44px ») : elementFromPoint à −3 px à gauche rend bien le bouton, à −5 px non → la zone de frappe HORIZONTALE est de 44 px, pas 36. Verticalement, à −3 px au-dessus, le point rend .conv-toolbar : l’extension n’est PAS prouvée en hauteur. Le rapport doit donc dire « 36×36 visibles, ≈44 px de large en frappe » pour #btnAttach/#btnEmoji/#btnVoice, et garder 36×36 pour .conv-fp-back qui n’a aucun pseudo-élément. ② Au regard de WCAG 2.5.8 (AA, seuil 24 px), seules les .comment-action (12 px) sont sous le seuil — et elles ne sont sauvées que par l’exception d’espacement (gap 12 px, styles.css:3170 : 12 + 12 = 24 tout juste). Tout le reste passe 24 px et échoue seulement au 44 pt des HIG/Material. P2 tenu : le cœur du défaut (actions de commentaire de 12 px, touchables à la loupe) est réel et fréquent, mais rien ne bloque une recette store. |
| DEV-02 | P2 → P2 | **CONFIRMÉ** | Comptage gabarits reproduit : 95 <div … onclick> sans tabindex sur une seule ligne dans app-02 (11), app-04 (18), app-05 (12), app-06 (18), app-07 (14), app-08 (4), app-09 (18) ; 0 d’entre eux porte role=button. Mais le chiffre est SOUS-ESTIMÉ : 60 autres gabarits <span\|img\|li…> onclick sans tabindex s’y ajoutent, et dans le DOM du fil à 390×844 (mesure.json → dev02), 271 des 285 éléments [onclick] visibles ne sont pas focusables : span.post-action ×80 (j’aime, commenter, réagir, partager), span.comment-action ×54, div.avatar ×38, div.post-author ×20, div.post-body ×20, div.comment-author ×18, div.story-item ×8. Parcours Tab ×40 depuis le corps : topbar → rail de passions → intentions → stories → boutons UI-v2/v3 (v2-reel-tile, v2-person, v3-tempt) ; AUCUNE carte, aucun j’aime, aucun commentaire n’est atteint (tab40_touchePostCardOuAction=false). .msg-card (app-04:3520) : div sans tabindex ni role (mesure2.json → conv.msgCard). | Le mécanisme d’activation d’app-08:2059-2069 ne couvre que [role=button] : il existe, il suffit de l’alimenter. Les modules récents (ui-v2-shell, ui-v3-passerelle, contextual-nav) rendent de vrais <button> nommés — le patron correct est déjà dans le dépôt. À l’usage mobile, VoiceOver/TalkBack activent un div onclick par double-tap (ils émettent un click), donc l’exclusion complète ne vaut que pour le clavier physique et les lecteurs d’écran de bureau ; c’est pourquoi P2 et non P1. Le titre du problème devrait dire « ~155 gabarits (95 div + 60 span/img) ; j’aime, commenter et ouvrir une conversation inaccessibles au clavier ». |
| DEV-03 | P3 → P3 | **CONFIRMÉ** | Remesuré (mesure.json → dev03, page Mes passions au plafond) : #nouveauProfilSous rgb(110,105,135) 12px/400 et #nouveauProfilTitre 14,5px/800, tous deux sur le fond opaque de #nouveauProfilLien rgb(239,233,253) = --bg-tint #efe9fd (styles.css:7515) → 4,40:1 recalculé (formule WCAG). --muted #6e6987 (styles.css:45) fait 5,21:1 sur blanc, 4,63:1 sur --accent-tile #f5efff, mais 4,40 sur --bg-tint et 3,88 sur --accent-tile-on #e5d9fc. .passio-hint-ok : blanc sur rgba(255,255,255,.22) composé sur --accent v2 #6D32F4 = #8d5ff6 → 4,12:1 (styles.css:7432, 7447-7448), 12px/700. | Le titre à 14,5 px gras n’est PAS du « texte large » (seuil 18,66 px gras) : 4,5 s’applique, l’écart est marginal (−0,1). Le commentaire de styles.css:45 (« 4.5+ sur blanc ») est vrai sur blanc et faux sur les lavis. P3 tenu. |
| DEV-04 | P3 → P3 | **CONFIRMÉ** | Gabarits : app-06:2511 et app-06:2557 rendent <div class="passion-photo-badge" onclick=…>📷</div> sans aria-label, title, role ni tabindex ; avatars : app-02:6683, 6739, 6831, app-04:634, 662, 675, 687, 820 (div.avatar onclick=openUserProfile sans nom). DOM du profil à 390×844 (mesure.json → dev04) : 3 pastilles (une par passion) sans nom ni tabindex, 57 .avatar[onclick] sans aria-label/title. | Les ×28 / ×17 du rapport sont des cumuls sur les 79 combinaisons, pas des occurrences par écran (3 et 57 à 390×844). Deux poids différents : l’avatar double le nom d’auteur adjacent (cible redondante, impact faible) ; la pastille 📷 est la SEULE porte pour changer la photo d’une passion, et elle est à la fois sans nom, hors clavier et à 20×20 px (styles.css:2217-2218) — c’est elle qui justifie la fiche. P3 tenu. |
| DEV-05 | P3 → P3 | **RÉFUTÉ** | `PassioPlatform` n’existe NULLE PART dans le dépôt (grep js/, index.html, sw.js, scripts/build.js : 0 occurrence) : la sonde de permissions-offline.js:153 interroge un global que le code n’a jamais défini. La détection réelle est platform.js:97-101 (`_isStandalone` = matchMedia display-mode standalone \|\| navigator.standalone ; `_pwaInstalled` persisté dans localStorage passio_pwa_installed), et elle est bien présente en page (mesure.json → dev05 : _isStandalone et _pwaInstalled de type boolean). Le champ `platform` de la télémétrie est l’OS (telemetry.js:84-92 detectPlatform → ios/android/…), pas le mode installé. Enfin la mesure n’était pas hors ligne : permissions-offline.js:147-153 fait setOffline(false) + reload AVANT de lire standaloneDetection. | Aucun défaut de chargement à corriger : en prod, scripts/build.js:105-106 inline TOUT <script src="js/…"> (platform.js compris) dans index.html, lui-même pré-caché par sw.js:17-24 ; le SW ne peut donc pas « oublier » platform.js. Fiche à retirer du rapport ; si l’on veut un point réel sur ce sujet, c’est l’absence d’un champ « standalone » dans la télémétrie (le mode installé n’est mesuré nulle part), qui est une lacune d’observabilité et non un défaut de détection. |

Exécuté : git diff --stat c8cb8e9 HEAD -- js styles.css index.html sw.js → vide (code audité = code relu) · http-server -p 8114 -s -c-1 . (dépôt, port dédié, arrêté en fin de relecture) · PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node relecture/appareils-a11y/mesure.js → mesure.json (Chromium 1194 via playwright global 1.56.1 ; 0 erreur JS ; réseau hors 127.0.0.1 bloqué par route().abort + WebSocket neutralisé) · PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node relecture/appareils-a11y/mesure2.js → mesure2.json (0 erreur JS) · node (formule WCAG) : recalcul des 9 contrastes cités (DEV-03) · grep -c / grep -o sur js/app-0*.js : 95 <div onclick> sans tabindex reproduits + 60 <span\|img\|li…> non comptés par le rapport · grep PassioPlatform sur js/, index.html, sw.js, scripts/build.js → 0 occurrence (DEV-05)

Limites déclarées : Émulation Chromium uniquement (aucun appareil réel, aucun WebKit/Firefox, aucun lecteur d’écran VoiceOver/TalkBack — DEV-C14 reste BLOQUÉ). Le composer a été mesuré en forçant la classe active sur #conv-fullpage puis avec une vraie conversation de démo (conv_lea) ; l’extension verticale du ::before à −4 px n’a pas pu être prouvée (elementFromPoint rend .conv-toolbar à −3 px au-dessus). Les hit-tests « au-dessus » de .post-action/.comment-action dans mesure2 sont INVALIDES (panneau de conversation encore en transition au moment du test) — seules les boîtes (rects) et le CSS font foi pour ces deux classes. Playwright n’est pas installé dans le dépôt (pas de node_modules) : scripts exécutés avec le paquet global /opt/node22/lib/node_modules/playwright, sans aucun `playwright install`. Aucune requête n’a atteint Supabase ni Netlify ; aucune écriture en base ; git status vide.

## 3. Contrôles débloqués le 2026-09-08

| Contrôle | Statut au 04 | Statut au 08 | Méthode | Constat | Preuve |
|---|---|---|---|---|---|
| C10 — isolation par requête sous rôle anon/authenticated | BLOQUÉ (42501) | **PROUVÉ (sur réplique)** | requête base, PostgreSQL 16 jetable chargé des 123 policies réelles du 2026-09-08 | Données « propres » : 0 ligne pour anon et pour un tiers sur 14 tables ; comptes privés respectés ; les seules lectures publiques sont celles déjà rapportées (conv_reads, event_attendees, events, storage attachments, follows, user_passions, profiles…) | `preuves/complement-2026-09-08/banc-isolation-role/` (README, matrice.txt, sondes.txt, mutations.txt) |
| C13 — UPDATE/DELETE sous rôle tiers | BLOQUÉ | **PROUVÉ (sur réplique)** | requête base | 0 ligne touchée sur 40 tables ; usurpation d'auteur, auto-invitation, message hors conversation, notification signée par autrui, dépôt Storage chez autrui, télémétrie au nom d'autrui : tous refusés ; 3 mutations (policy retirée → verdict inversé) prouvent que le banc mesure les policies | idem |
| TCI-C04 — flakiness des derniers runs | BLOQUÉ (403 proxy) | **PROUVÉ** | journaux CI lus (`get_job_logs`) | 1 instable sur 1 103 (`monitoring-file-boot.spec.js:49`, repris) ; les shards longs le sont par l'installation Playwright, pas par des reprises | `preuves/complement-2026-09-08/ci-run-33861671142/journaux-resume.md` |
| TCI-C22 / CARTO-C25 / NET-C24 / C36 / MOD-C29 — suites prod | BLOQUÉ (« vert en CI ») | **PRÉCISÉ : le vert ne couvre que 3 tests sur 15** | journaux CI | 12 tests `skipped` (multi-comptes, confidentialité, suppression de compte…) faute de `PASSIO_E2E_MULTI` ; `mesure-passions.js` échoue (`fetch failed`) et l'étape reste verte ; purge Storage « ignorée » | idem |
| MOD-C30 — irl-trust-safety.spec.js | BLOQUÉ (non lancée) | **PROUVÉ** | test exécuté localement | 23/23 (5,7 min) — prouve le lot sous drapeau OFF, ne lève pas IRL-02/MOD-08 | `preuves/complement-2026-09-08/suites-locales/irl-trust-safety.md` |

| DEV-C13 (appareils-a11y) | BLOQUÉ | **DÉFAILLANT** | émulation + inspection code | FOCUS — (a) openModal (app-08:15-27) ne déplace jamais le focus : après Enter sur la cloche, document.activeElement reste la cloche derrière la modale (mesure.json → c13_modal.focusApresOuverture ; matrice : modaleFocusInitial=false). (b) Aucun piège : Tab ×8 depuis la modale ouverte parcourt hamburger → rail de passions → intentions, tous HORS #modalContent (tab8_sortDeLaModale=true ; matrice focusPiegeModale=[false×6]) ; l’arrière-plan n’est ni inert ni aria-hidden (arrierePlanInert=false) alors que #modalContent porte role=dialog aria-modal=true (index.html:1355) — un aria-modal sans confinement réel est une promesse non tenue au lecteur d’écran. (c) closeModal (app-08:28-40) ne restitue rien : si le focus est entré dans la modale (× de fermeture) puis Escape, le focus tombe sur <body> (mesure2.json → c13_focusDedans.focusApresEscape = BODY) ; le cas « Enter puis Escape immédiat » ne rend le focus à la cloche QUE parce qu’il ne l’a jamais quittée (c13_pur). (d) openModal remplace innerHTML sans empiler : depuis une modale, activer un bouton qui ouvre une autre modale détruit l’élément focalisé et le focus tombe sur <body> (c13_remplacement.focusApres = BODY). 64 appels à openModal( dans js/ sont concernés. (e) Panneaux plein écran : #conv-fullpage (index.html:1381) sans role, sans titre h1/h2, focus laissé sur <body> à l’ouverture au toucher (voulu et documenté app-04:3721-3739, focus sur le champ seulement au pointeur fin), Escape NE le ferme PAS (mesure2.json → conv.escapeFerme=false ; seul le keydown Escape d’app-08:44-48 existe, pour la modale ; closeCurrentOverlay app-02:2063 n’est câblé qu’au popstate) ; closeConversation (app-04:4811-4827), closePassionManager (app-06:1874-1880), closeReels (app-05:2301-2318), closeEventDetail (app-07:4048-4056) ne restituent aucun focus. Trois modules font le travail correctement et montrent le patron à copier : ui-v2-shell.js:352-393 (lastFocused + piège Tab + Escape + restitution), contextual-nav.js:165-242 (idem + prefersReduced), ui-v3-passerelle.js:787-800. PREFERS-REDUCED-MOTION — partiel : 22 blocs dans styles.css (527 .shimmer ; 6717-6720 .screen/.live-dot ; 7389-7394 et 7417 ctx ; 7458 .passio-hint ; 7960, 8186, 8357, 8808, 9081, 9167, 10019, 11065, 11293, 11363 pour les lots UI-2→4A5) + access-gate.js:169 + lecture JS dans contextual-nav.js:53. Sous reduce (mesure.json → reduce, modale ouverte + toast) 7 animations tournent encore : #modalContent slideUp 320 ms (styles.css:1390), #modalBackdrop fadeIn 220 ms (1377), .toast toastIn/toastOut (1488, 1532), #postDetailPage fadeIn (index.html:1026 en ligne), #eventDetailPage fadeIn, #pwa-overlay pwaFadeIn ; hors DOM mesuré, .onb-logo-big logoFloat 4 s infinite (1587) et .onb-step fadeIn (1574) ne sont pas couverts non plus (matrice : 10 animations sous reduce). Les animations fonctionnelles (mePulse 3992, pulse-rec 6033/6242, spin 6280) sont légitimes. | js/app-08-ui-modals-tour.js:15-48 ; index.html:1355, 1381 ; js/app-02-state-utils.js:2063-2170 ; js/app-04-comments-shop.js:3721-3739, 4811-4827 ; styles.css:1377, 1390, 1488, 1532, 1587, 6717-6720, 7389-7394 ; mesures relecture/appareils-a11y/mesure.json (c13_modal, reduce) et mesure2.json (c13_pur, c13_focusDedans, c13_remplacement, conv) |

Toujours BLOQUÉ (rapport 13, inchangé) : fichier servi en production (proxy), REST anon direct (proxy), plans/quotas/réglages des tableaux de bord Supabase-Netlify-Brevo, appareils et navigateurs réels, carte, lecteur d'écran, restauration et capacité (pas de staging). Précision utile pour PRO-01 : `pgrst.db_max_rows` n'est pas posé au niveau du rôle `authenticator` (défaut plateforme 1 000), et le journal CI lit bien « 1000 identifiants » côté `service_role` ; aucun index d'unicité sur `profiles.username` (0 doublon aujourd'hui).

## 4. Oublis trouvés par les relecteurs (nouveaux problèmes, format du registre)

### CPL-AUTH-01 — P2 — Aucune clé étrangère vers auth.users : toute suppression administrative (Dashboard, purge e2e REST, Edge Function) laisse des blobs d'état complets orphelins — 79 lignes user_state sur 84 aujourd'hui

- **Fonctionnalité** : Modèle de données / cycle de vie d'un compte (user_state, analytics_events, notifications, Storage attachments)
- **Attendu** : Supprimer un compte auth entraîne (par FK ON DELETE CASCADE ou par job) la disparition de tout ce qui lui est rattaché, quel que soit le chemin de suppression
- **Observé** : pg_constraint : aucune FK d'une table public vers auth.users (seules auth.identities/sessions/mfa_factors/… cascadent). En prod : user_state 79/84 lignes dont user_id n'existe plus dans auth.users (blob d'état complet : profils, téléphone, notifications, likes), analytics_events 51 orphelines, notifications.from_id 19, 12 objets du bucket attachments jamais couverts. La purge e2e utilisée en CI (scripts/purge-e2e-accounts.js, DELETE auth/v1/admin/users) ne nettoie pas user_state — seul scripts/purge_e2e_accounts.sql:34 le fait, et il n'est pas celui que global-teardown lance (tests/e2e/global-teardown.js:60)
- **Reproduction** : SELECT count(*) FROM public.user_state WHERE user_id::text NOT IN (SELECT id::text FROM auth.users) → 79 ; SELECT conrelid::regclass FROM pg_constraint WHERE contype='f' AND confrelid='auth.users'::regclass → aucune table public
- **Preuve** : requêtes execute_sql de cette relecture (agrégats) ; scripts/purge-e2e-accounts.js:125 ; tests/e2e/global-teardown.js:60-63 ; scripts/purge_e2e_accounts.sql:34
- **Impact** : Données personnelles de comptes disparus conservées sans limite (contradiction avec la politique §4) ; 10 Mo de blobs morts ; tout futur chemin de suppression (support, RGPD manuel via Dashboard) reproduit le défaut
- **Correction** : Ajouter `user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE` (ou un job hebdomadaire d'orphelins) sur user_state, analytics_events, telemetry_events, user_safety, push_subscriptions, notifications.from_id (SET NULL) ; purge immédiate des 79 lignes ; aligner purge-e2e-accounts.js sur purge_e2e_accounts.sql
- **Effort** : 0,5 j (migration DDL + purge) — attention : colonnes `text`, un cast est nécessaire
- **Confiance** : PROUVÉ (requête base)

### CPL-AUTH-02 — P3 — La suppression de compte efface aussi le contrôle parental de l'appareil (passio_parental_code, passio_limit_sec), que la déconnexion protège délibérément

- **Fonctionnalité** : Paramètres → Supprimer mon compte (doDeleteAccount) vs contrôle parental posé sur l'appareil
- **Attendu** : Le code parental appartient à l'APPAREIL (commentaire app-02:2727-2730 : le purger « offrirait à l'enfant un contournement en un clic ») et doit survivre à tout geste de compte
- **Observé** : doDeleteAccount purge par motif `k.indexOf("passio") !== -1` (app-02-state-utils.js:3123-3125), ce qui emporte passio_parental_code et passio_limit_sec, alors que purgeAccountScopedData (déconnexion) les épargne explicitement. Un enfant peut créer un compte jetable puis le supprimer pour lever la limite de temps
- **Reproduction** : localStorage.passio_parental_code posé → Paramètres → Supprimer mon compte → SUPPRIMER → après rechargement la clé est absente
- **Preuve** : app-02-state-utils.js:3123-3125 vs :2727-2732 et :2463
- **Impact** : Contournement du contrôle parental, contraire à l'intention documentée dans le code
- **Correction** : Dans doDeleteAccount, purger ACCOUNT_SCOPED_KEYS + le jeton auth + les files (AUTH-06) au lieu du motif « passio » ; ou exclure explicitement les clés d'appareil
- **Effort** : 0,1 j
- **Confiance** : CONFORME PAR INSPECTION (non exécuté)

### CPL-AUTH-03 — P3 — Le commentaire d'intégration de la télémétrie dans index.html annonce « opt-in via ?telemetry=1, sans PII » — l'inverse du comportement en production

- **Fonctionnalité** : index.html (chargement de js/telemetry.js) / documentation embarquée
- **Attendu** : Un commentaire qui décrit l'activation réelle (ON par défaut en prod, opt-out) et les données réellement envoyées (uuid de compte, device_id persistant)
- **Observé** : index.html:51 : « opt-in via ?telemetry=1, sans PII » ; telemetry.js:31 et :71 : ON par défaut hors localhost ; telemetry.js:492-496 : user_id et device_id sur chaque ligne (85 143 lignes avec user_id en prod)
- **Reproduction** : Lire index.html:51 puis telemetry.js:71
- **Preuve** : index.html:51 ; js/telemetry.js:31, 71, 492-496 ; requête base (tel_avec_user_id = 85 143)
- **Impact** : Un relecteur ou un juriste qui s'appuie sur le code conclura à tort que la télémétrie est opt-in ; entretient l'écart de la politique de confidentialité (AUTH-04)
- **Correction** : Corriger le commentaire en même temps qu'AUTH-04
- **Effort** : 5 min
- **Confiance** : PROUVÉ (inspection + base)

### CPL-EXP-01 — P1 — Le dossier d'audit complet (défauts de sécurité non corrigés, captures, reproductions) est poussé sur le dépôt PUBLIC avant toute correction

- **Fonctionnalité** : Exposition du dépôt / gestion de la divulgation
- **Attendu** : Un audit qui liste des défauts exploitables (06-AUDIT-SECURITE-DONNEES, registre des risques, scripts de reproduction) reste privé jusqu'à correction, ou est publié expurgé.
- **Observé** : Depuis le SHA audité c8cb8e9, les commits babf09f, 1712683, 08e7e7a (« [skip ci] ») ajoutent .passio/audits/BILAN_PASSIO_09-26 au dépôt public : 473 fichiers, 20 Mo, dont 17 rapports (06-AUDIT-SECURITE-DONNEES.md, 11-REGISTRE-DES-RISQUES.md, 17-…PLAN-DE-CORRECTION.md), 105 png + 40 jpg de captures, 64 scripts .js et 7 .py de reproduction, résultats d'orchestrateur en JSON. Aucune donnée personnelle tierce trouvée (3 adresses : celle de l'éditeur déjà dans l'app, et des domaines de test).
- **Reproduction** : git diff --stat c8cb8e9 HEAD → 471 fichiers, +65 791 lignes, tous sous .passio/audits ; git ls-files .passio/audits \| wc -l → 476 ; curl api.github.com/repos/PASSIO74/passio-app → private=false.
- **Preuve** : git log c8cb8e9..HEAD ; git ls-files -z .passio/audits \| xargs -0 du -ch → 20 Mo ; relecture/exploitation-continuite/repo.json (visibility public).
- **Impact** : Un tiers dispose d'une carte à jour des faiblesses (P0/P1 de sécurité, RLS, buckets publics) avec les scripts pour les rejouer, avant que quoi que ce soit ne soit corrigé — aggravation directe d'EXP-10 postérieure au SHA audité.
- **Correction** : Passer le dépôt en privé AVANT de poursuivre la publication de l'audit (ou déplacer .passio/audits dans un dépôt privé et réécrire l'historique des 3 commits) ; règle : un audit de sécurité ne se versionne en public qu'expurgé et après correction.
- **Effort** : 0,5 jour
- **Confiance** : CONFIRMÉ

### CPL-EXP-02 — P2 — `schema-baseline.js` promet de « semer une base identique à la prod » mais ne produit aucun DDL exécutable

- **Fonctionnalité** : Outil de référence de schéma / restauration
- **Attendu** : Le fichier généré par `npm run schema:baseline` permet de recréer tables, contraintes et policies sur une base vide (usage ② annoncé en tête du script).
- **Observé** : scripts/schema-baseline.js:1-16 annonce « ② semer une base de non-production identique à la prod » ; son produit migrations/SCHEMA_PROD_REFERENCE.sql ne contient que des commentaires pour les tables, colonnes, clés, policies, RLS, fonctions et triggers, et 91 `CREATE INDEX` seuls exécutables — qui échouent sans les tables. Le script dépend en outre de la CLI `supabase`, retirée par ADR-012.
- **Reproduction** : grep -ciE '^\s*create (table\|policy)' migrations/SCHEMA_PROD_REFERENCE.sql → 0 ; grep -ciE '^\s*create ' → 91 (tous INDEX) ; grep -vE '^\s*(--\|$)' \| wc -l → 111.
- **Preuve** : scripts/schema-baseline.js:1-16 ; migrations/SCHEMA_PROD_REFERENCE.sql (952 lignes, 841 de commentaires).
- **Impact** : Le seul outil désigné par CLAUDE.md et RECUPERATION.md comme repli hors ligne ne peut pas servir de baseline de reconstruction ; l'opérateur le découvrirait le jour de la panne (cause racine d'EXP-01 non nommée par le sous-agent).
- **Correction** : Faire générer du DDL réel (`pg_dump --schema-only` via psql, ou requêtes qui émettent CREATE TABLE/POLICY/FUNCTION) ; retirer l'usage ② de l'en-tête tant que ce n'est pas fait ; tester le fichier sur une base vide.
- **Effort** : 1 jour
- **Confiance** : CONFIRMÉ

### CPL-EXP-03 — P2 — 79 blobs `user_state` orphelins (4,8 Mo) survivent à la suppression des comptes : delete-account ne purge pas l'état client

- **Fonctionnalité** : Suppression de compte / résidus / périmètre des sauvegardes
- **Attendu** : Après suppression d'un compte (Edge Function ou purge e2e), aucune ligne ne porte encore son état client (profil, bio, feedbacks, préférences).
- **Observé** : execute_sql : auth.users = 6, user_state = 84, dont 79 lignes sans compte correspondant (4 844 Ko) ; profiles orphelins = 0. supabase/functions/delete-account/index.ts:48-62 liste 15 tables et omet user_state (ainsi que telemetry_events, blocks, reports, user_passions, user_safety, comment_interactions, event_reactions, conv_reads, comment_likes, analytics_events) ; seul scripts/purge-e2e-rest.js:62 connaît user_state.
- **Reproduction** : SELECT count(*) FROM user_state us WHERE NOT EXISTS (SELECT 1 FROM auth.users u WHERE u.id::text = us.user_id) → 79.
- **Preuve** : execute_sql (agrégats) ; delete-account/index.ts:48-62 ; purge-e2e-rest.js:62.
- **Impact** : RGPD : l'état client complet d'un compte supprimé (nom, bio, feedbacks, préférences, éventuellement conversations) reste en base ; continuité : toute sauvegarde/restauration réimporte ces résidus. Distinct de SUP-10 par la mesure et la table visée.
- **Correction** : Ajouter user_state (et les autres tables à user_id) à la liste de delete-account ; purge unique des orphelins ; contrainte FK ou trigger sur auth.users → user_state.
- **Effort** : 0,5 jour
- **Confiance** : CONFIRMÉ (mesure) — origine des orphelins PLAUSIBLE (purges e2e sans user_state avant purge-e2e-rest)

### CPL-EXP-04 — P2 — La configuration hors base (Auth/SMTP, secrets des Edge Functions, cron, config Netlify) n'est sauvegardée nulle part

- **Fonctionnalité** : Sauvegarde / reconstruction du projet
- **Attendu** : Une reprise après perte du projet Supabase ou Netlify peut rejouer aussi la configuration : réglages Auth (confirmation e-mail, SMTP Brevo, modèles, URL de redirection), secrets des Edge Functions (clé Anthropic d'ask-ai, VAPID de notify-call), tâche pg_cron, variables et en-têtes Netlify.
- **Observé** : scripts/sauvegarde-donnees.js:10-17 borne le périmètre aux tables exposées + comptes + Storage ; rien n'exporte la configuration Auth ni les secrets de fonctions ; docs/SETUP_SMTP_AUTH.md et docs/EDGE_FUNCTION_*.md décrivent des gestes manuels sans inventaire de valeurs ; cron.job = purge_client_errors (0 3 * * *) n'apparaît que dans SCHEMA_PROD_REFERENCE en commentaire ; docs/RECUPERATION.md:37-44 ne mentionne aucune de ces étapes.
- **Reproduction** : grep -nE 'auth/v1/admin\|settings\|config' scripts/sauvegarde-donnees.js → seul /auth/v1/admin/users ; lire docs/RECUPERATION.md:37-44.
- **Preuve** : scripts/sauvegarde-donnees.js:10-17,117-131 ; docs/RECUPERATION.md:37-44 ; execute_sql cron.job.
- **Impact** : Même avec données et schéma restaurés, les inscriptions (SMTP), les appels (VAPID) et l'assistant IA ne repartent pas sans reconstituer de mémoire des secrets que seule une personne détient (EXP-05).
- **Correction** : Inventaire versionné (sans valeurs) de chaque réglage et secret avec son emplacement de coffre ; export périodique de la config Auth via l'API de gestion ; étapes ajoutées à RECUPERATION.md.
- **Effort** : 0,5 jour
- **Confiance** : CONFIRMÉ

### CPL-IRL-01 — P2 — RSVP refusé par le serveur : affichage optimiste jamais annulé, organisateur notifié « a rejoint ton événement » et discussion rejointe quand même

- **Fonctionnalité** : IRL — participation (setEventRsvp)
- **Attendu** : Un échec réel de l'écriture (RLS, réseau, 23505 « ligne filtrée ») annule l'affichage optimiste, prévient l'utilisateur et n'envoie aucune notification à l'organisateur (invariant CLAUDE.md « Échec réel = annuler l'affichage optimiste »).
- **Observé** : js/app-07-ia-explore-irl.js:3327-3337 : `rsvpOk = await supaSetEventRsvp(id, rsvp)` n'alimente que irlFunnelTrackJoin ; la notification à l'organisateur (3332-3334) et _joinEventConversation (3337) partent sans condition sur rsvpOk ; l'état local (3301-3306) et le pushNotification « Tu rejoins … » (3319) ne sont jamais défaits. supaSetEventRsvp rend pourtant explicitement false dans le cas 23505 (app-08:5433-5437).
- **Reproduction** : Stub `window.supaSetEventRsvp = async () => false` (ou réseau coupé après le chargement, ou refus RLS), fiche → « J'y vais » : compteur +1, « Tu rejoins », organisateur notifié ; au rechargement supaLoadMyRsvps ré-hydrate l'absence d'inscription.
- **Preuve** : js/app-07-ia-explore-irl.js:3301-3337 ; js/app-08-ui-modals-tour.js:5421-5447 (inspection code).
- **Impact** : L'organisateur compte un participant qui n'existe pas en base ; l'utilisateur croit être inscrit et découvre le contraire plus tard (ou jamais, s'il ne recharge pas avant de venir). Même famille que IRL-04.
- **Correction** : Si `rsvpOk === false` : restaurer prev via _setMyRsvpLocal + listes, toast d'échec, ne pas notifier ni rejoindre la conversation ; ne notifier l'organisateur qu'après verdict positif.
- **Effort** : 0,25 jour.
- **Confiance** : CONFORME PAR INSPECTION (non exécuté)

### CPL-IRL-02 — P2 — Le blocage n'existe pas pour les événements : les rencontres d'un compte bloqué me sont listées, et un compte que j'ai bloqué peut s'inscrire à la mienne et y lire l'adresse

- **Fonctionnalité** : IRL — liste, RSVP, participants
- **Attendu** : Comme pour le fil et la messagerie, un blocage (dans un sens ou l'autre) masque les événements du bloqué et refuse son inscription à mes événements.
- **Observé** : grep `isBlocked(` dans app-07 : deux usages seulement (4983 proposition IRL sous drapeau OFF, 5879 suggestions), aucun dans allEvents() (677), _filterIrlEvents (2395), setEventRsvp (3269) ni supaLoadEvents (app-08:4178) ; policies-irl.txt [event_attendees] INSERT check = user_id = auth.uid() sans `NOT is_blocked_with(...)` (contrairement à conv_members) ; emulation-resultats.json « liste initiale » evenementDuBloqueVisible=true. Le bloqué figure alors dans mon trombinoscope (_faces, 3755-3760) et accède à l'adresse et au contact.
- **Reproduction** : Compte B bloqué par A. B ouvre l'événement de A → « J'y vais » accepté ; A voit B dans ses participants. A voit toujours les événements de B dans Rencontrer.
- **Preuve** : js/app-07-ia-explore-irl.js:677-690, 2395-2420, 3269-3337 ; policies-irl.txt ; emulation-resultats.json (inspection code + émulation du sous-agent).
- **Impact** : Le blocage est le seul outil d'auto-protection d'une personne face à quelqu'un qu'elle veut éviter PHYSIQUEMENT ; ici il ne protège que le fil et les messages. Sécurité IRL.
- **Correction** : Filtrer allEvents()/supaLoadEvents par isBlocked (organisateur) ; refuser le RSVP côté client si bloqué ; côté serveur, ajouter `NOT is_blocked_with((SELECT author_id FROM events WHERE id = event_id))` au WITH CHECK d'INSERT/UPDATE d'event_attendees.
- **Effort** : 0,5 à 1 jour.
- **Confiance** : CONFORME PAR INSPECTION + émulation

### CPL-IRL-03 — P3 — Annulation d'une série : le verdict serveur est ignoré pour chaque date, mais chaque inscrit reçoit « a annulé » et l'organisateur lit « inscrits prévenus »

- **Fonctionnalité** : IRL — annulation de série récurrente
- **Attendu** : Comme toggleCancelEvent (5511-5513), une annulation non synchronisée n'annonce rien et prévient d'un défaut.
- **Observé** : js/app-07-ia-explore-irl.js:5486-5497 cancelEventSeries : `await supaCancelEvent(canon.id, true)` sans lecture du booléen, puis _notifyEventAttendees et toast « Série annulée — inscrits prévenus » inconditionnels ; les dates restent `active` en base si la policy filtre (co-organisateur retiré entre-temps, réseau).
- **Reproduction** : Stub supaCancelEvent → false, gérer une série → « Annuler toute la série » : notifications envoyées, toast de succès, dates encore actives au rechargement.
- **Preuve** : js/app-07-ia-explore-irl.js:5482-5497 vs 5504-5516 (inspection code).
- **Impact** : Inscrits prévenus d'une annulation qui n'a pas eu lieu (ou l'inverse après rechargement). Faible fréquence.
- **Correction** : Lire le retour par date, ne notifier que les dates réellement annulées, toast d'échec partiel.
- **Effort** : 0,25 jour.
- **Confiance** : CONFORME PAR INSPECTION

### CPL-IRL-04 — P3 — Message groupé aux inscrits : le texte est échappé HTML avant stockage, et la notification push l'affiche avec ses entités (« l&#39;entrée »)

- **Fonctionnalité** : IRL — « Prévenir les inscrits » (_messageEventAttendees)
- **Attendu** : Le texte tapé par l'organisateur s'affiche tel quel dans le centre de notifications ET dans la push système.
- **Observé** : js/app-07-ia-explore-irl.js:5557 `_notifyEventAttendees(ev, escapeHtml(txt.slice(0,200)))` ; js/app-08-ui-modals-tour.js:4781-4792 : la même chaîne est stockée puis passée à notify-call (text) ; supabase/functions/notify-call/index.ts:61 la transmet telle quelle ; sw.js:55 `body: data.text` (texte brut, pas de HTML). Dans l'app, _notifTexteHtml (1804) ne neutralise que les chevrons et l'innerHTML décode `&#39;` → rendu correct ; dans la push, l'entité est littérale.
- **Reproduction** : Gérer l'événement → « Prévenir les inscrits » → « Rendez-vous à l'entrée » → sur l'appareil d'un inscrit avec push active, la notification système lit « … à l&#39;entrée ».
- **Preuve** : fichiers:lignes ci-dessus (inspection code ; push non exercée ici).
- **Impact** : Cosmétique mais visible sur le seul message que l'organisateur adresse à ses participants ; touche aussi tout pseudo avec apostrophe (safeName escapé) dans toute push.
- **Correction** : Ne pas échapper à l'émission (le rendu non fiable neutralise déjà) ou décoder les entités avant de composer le corps de la push.
- **Effort** : 0,25 jour.
- **Confiance** : PROBABLE (chaîne lue de bout en bout, push non déclenchée)

### CPL-PRO-01 — P2 — Aucune contrainte d'unicité sur `profiles.username` en base : l'unicité du pseudo n'est qu'un contrôle client, contournable par REST ou par simple course

- **Fonctionnalité** : Identité centralisée (pseudo unique du compte), messagerie, mentions @
- **Attendu** : Deux comptes ne peuvent pas porter le même pseudo (à la casse près) — c'est le socle de l'identité centralisée d'ADR-011 et de la confiance en messagerie.
- **Observé** : L'unique garde est `supaUsernameTaken` (app-08:4487-4497 : `ilike("username", clean).limit(10)` puis comparaison en minuscules, exclusion de MY_UID), appelée à l'enregistrement du pseudo (app-06:1319-1320). En base, `profiles` ne porte que `profiles_pkey` et `profiles_passion_fk` (migrations/SCHEMA_PROD_REFERENCE.sql:356-357, index :437) — aucun index unique, aucun CHECK ; les triggers d'identité propagent le pseudo tel quel. Un `PATCH /rest/v1/profiles?id=eq.<moi>` avec le pseudo d'un autre compte passe la policy « Update propre ».
- **Reproduction** : Compte A = « benjamin ». Compte B authentifié : `await supa.from("profiles").update({ username: "benjamin" }).eq("id", MY_UID)` → 1 ligne, aucune erreur. Ou deux inscriptions simultanées du même pseudo (le contrôle client n'est pas transactionnel).
- **Preuve** : app-08:4487-4497 ; app-06:1319-1320 ; migrations/SCHEMA_PROD_REFERENCE.sql:356-357,437 ; requêtes-base §1 (aucune colonne d'identité sur conv_messages : l'affichage repose sur `profiles.username`, donc sur son unicité).
- **Impact** : Usurpation d'identité SANS toucher au payload `sp` (PRO-02) : le pseudo affiché par les embeds `profiles!author_id(username…)` (app-03:17) devient ambigu sur les posts, commentaires et conversations ; la recherche de profil par pseudo (`ilike` app-04:2717, `.limit(1)`) rend le mauvais compte. Aggrave PRO-02 et le hameçonnage interne.
- **Correction** : `create unique index profiles_username_lower_key on public.profiles (lower(username)) where username is not null` (canal ③ DDL, ADR-012), après dédoublonnage des homonymes existants ; côté client, traiter le 23505 dans `supaSavePublicProfile` par un message honnête (« pseudo déjà pris »).
- **Effort** : 0,25 jour + vérification des homonymes existants en base.
- **Confiance** : CONFORME PAR INSPECTION (code + schéma de référence ; l'absence d'index en PROD reste à confirmer par `pg_indexes` — BLOQUÉ ici, connecteur interdit).

### CPL-PRO-02 — P3 — La canonicité est décidée AU MOMENT de l'écriture ou du rendu alors que le référentiel serveur n'est ni attendu ni suivi : Studio et `passion_id` figés sur le socle des 19 tant que la réponse n'est pas arrivée, et jamais repeints à son arrivée

- **Fonctionnalité** : Studio (sélecteur de passion), `profiles.passion_id`, miroir `user_passions`
- **Attendu** : Une passion possédée et réellement dans le référentiel est publiable dès l'ouverture du Studio, et `passion_id`/`user_passions` reflètent les passions vivantes du compte quel que soit l'instant de l'écriture.
- **Observé** : `chargerReferentielPassions` est lancée sans await à la création du client (app-08:2695), sans callback ni promesse exposée (app-02:1300-1311). Or : (a) `renderStudio` (app-06:3789-3792) filtre le `<select>` par `estPassionCanonique` à l'instant du rendu et n'est jamais rappelé quand le référentiel arrive → un compte dont les passions sont hors socle (ex. moto-enduro, outdoor-randonnee) ouvre le Studio tôt (ou sur réseau lent) et voit un `<select>` vide + « on ne peut pas encore y publier », jusqu'à une nouvelle navigation ; (b) la sauvegarde de fin d'onboarding part à +800 ms (app-02:4368-4384 → `supaSavePassionState`) : `_passionIdPubliable` (app-08:2753-2767) et `supaMiroirUserPassions` (:3210) ne voient alors que le socle → `passion_id` retombe sur une passion du socle (éventuellement archivée, cf. le `art` de PRO-04) et le miroir saute les autres.
- **Reproduction** : Émulation : `_referentielPassions = null` (état avant réponse), compte avec passion vivante « moto-enduro », `goTo("studio")` → `#postPassion` sans option moto-enduro ; puis `_referentielPassions = new Set(["moto-enduro"])` → le `<select>` ne change pas tant qu'on ne quitte pas l'écran. Onboarding : throttler la réponse de `/rest/v1/passions` au-delà de 800 ms et observer `passion_id` envoyé.
- **Preuve** : app-08:2695 ; app-02:1300-1311 ; app-06:3789-3792 ; app-02:4368-4384 ; app-08:2753-2767 et :3210.
- **Impact** : Transitoire mais visible (Studio vide juste après le lancement), et durable côté données (`passion_id` et miroir faux jusqu'à la prochaine écriture réussie) ; c'est l'un des mécanismes derrière PRO-04 et PRO-06.
- **Correction** : Faire rendre une promesse à `chargerReferentielPassions`, et à sa résolution : invalider/repeindre `#postPassion` si le Studio est affiché, et re-jouer `supaSavePassionState` si une écriture est partie avant la réponse (ou attendre la réponse, bornée à 2 s, avant l'écriture de fin d'onboarding). À traiter dans le même correctif que PRO-01.
- **Effort** : 0,25 jour (dans le lot PRO-01).
- **Confiance** : PROBABLE (mécanisme lu dans le code ; l'occurrence en production n'est pas mesurée — Playwright indisponible ici).

### CPL-PRO-03 — P3 — `chargerReferentielPassions` avale toutes ses erreurs sans `diagLog` : une panne ou une troncature du référentiel est invisible au Centre de pilotage et à la Sentinelle

- **Fonctionnalité** : Référentiel des passions — observabilité
- **Attendu** : Un chemin critique (il conditionne la publication) ne masque pas ses échecs (invariant CLAUDE.md « catch large » ; précédent diagLog = fil vide 6 jours).
- **Observé** : app-02:1300-1311 : `try { … } catch (e) {}` autour de l'appel, `catch (e) {}` autour du traitement, `.catch(function () {})` sur la promesse — aucun `diagLog`, aucun `tel.error`, aucune comparaison de `r.data.length` avec un `count` attendu. PRO-01 (900 passions impubliables depuis le 2026-09-01) n'a produit aucun signal ; le finding le note (« visibilité_pilotage : aucune ») sans en faire un défaut à corriger.
- **Reproduction** : Bloquer `/rest/v1/passions` (route Playwright ou hors ligne) puis publier dans une passion hors socle : refus « on ne peut pas encore y publier », aucune ligne dans client_errors ni telemetry_events.
- **Preuve** : js/app-02-state-utils.js:1300-1311 ; CLAUDE.md « Catch large ».
- **Impact** : Le défaut PRO-01 pourrait se reproduire (changement de `max-rows`, panne PostgREST) sans qu'aucun tableau de bord ne le montre ; le diagnostic dépend d'un utilisateur qui se plaint.
- **Correction** : `diagLog("passions referentiel KO: …")` sur erreur, et `diagLog`/`tel.error` si `r.data.length` est un multiple exact de 1 000 ou inférieur à `PassioPassions.taille()` quand celui-ci est connu (signal de troncature).
- **Effort** : 0,1 jour.
- **Confiance** : CONFORME PAR INSPECTION.

### CPL-ROB-01 — P2 — Un message déjà livré (23505) est affiché « réessayer » pour toujours, remis en file et renvoyé à chaque reconnexion

- **Fonctionnalité** : Messagerie — envoi, file `passio_outbox_v1`, bouton « réessayer »
- **Attendu** : Une clé dupliquée sur `conv_messages_pkey` (id client) signifie que le message EST sur le serveur : statut « sent », retrait de la file — comme `supaFollowUser` (app-08:5303) et `supaSetPostLike` le font pour leur 23505.
- **Observé** : `_sendTextToSupa` (js/app-04-comments-shop.js:4630-4634) : `if (res && res.error) { _setMsgStatus(failed); _outboxAdd(...) }` sans distinguer le code. Toute réponse perdue après commit serveur (coupure mobile mi-requête, timeout, double flush, tap « réessayer » pendant un envoi en vol) produit un 23505 au renvoi → « failed » définitif, file jamais vidée, renvoi à chaque événement `online` (app-04:4651) et à chaque boot (app-08:5956), toujours en 23505.
- **Reproduction** : Script relecture-t1.js (hors réseau, `supa` stubé) : un flush avec réponse 23505 → status failed, outbox [msg_b] ; dispatch `online` → 2e INSERT du même id, toujours failed. Banc chaos C2 : troisiemeFlush envois3 [dup, dup] → status failed, outbox 2.
- **Preuve** : js/app-04-comments-shop.js:4630-4634, 4644-4651 ; migrations/SCHEMA_PROD_REFERENCE.sql:337 (`conv_messages_pkey` PRIMARY KEY (id)) ; relecture.json T1 unFlush23505 / apresOnline ; preuves/robustesse-pannes/02-chaos-observations.jsonl C2 (troisiemeFlush).
- **Impact** : Le destinataire a reçu le message, l'expéditeur voit un échec permanent et le retape → vrai doublon chez le destinataire ; la file grossit et rejoue en 23505 à chaque ouverture (bruit + télémétrie d'échec mensongère). Fréquent sur mobile (réponse perdue après commit).
- **Correction** : Dans `_sendTextToSupa` : `if (res.error && String(res.error.code) === "23505") { _setMsgStatus(sent); _outboxRemove(msgId); return; }` ; plus un verrou `_outboxFlushBusy` dans `_flushOutbox` (ROB-06) et exclure les messages déjà `sending` du renvoi.
- **Effort** : 0,25 jour.
- **Confiance** : CONFIRMÉ (exécuté hors réseau + banc chaos + inspection)

### CPL-ROB-02 — P3 — L'organisateur est notifié « a rejoint ton événement » même quand l'inscription a ÉCHOUÉ

- **Fonctionnalité** : IRL — RSVP « J'y vais », notifications cross-compte
- **Attendu** : La notification à l'organisateur (et la notification locale « Tu rejoins ») ne partent que si `supaSetEventRsvp` a rendu `true` — règle déjà appliquée au suivi (app-08:5308 : « la notification ne part QUE si la relation existe vraiment »).
- **Observé** : js/app-07-ia-explore-irl.js:3311 `pushNotification(« Tu rejoins … »)` AVANT l'écriture ; 3323-3326 `supaInsertNotif(ev.organizerId, "event_join", …)` conditionné à `rsvp === "going" && ev.fromSupabase && organizerId` mais JAMAIS à `rsvpOk`. Script T3 (supaSetEventRsvp → false) : notifsOrganisateur ["event_join→u_org"].
- **Reproduction** : Stub `supaSetEventRsvp = async () => false` puis `await setEventRsvp(id, "going")` sur un événement `fromSupabase` d'un autre organisateur → compter les appels à `supaInsertNotif`.
- **Preuve** : js/app-07-ia-explore-irl.js:3311, 3319-3326 ; relecture.json T3.
- **Impact** : L'organisateur compte un participant qui n'existe pas dans `event_attendees` (refus RLS, complet entre-temps, 0 ligne touchée branche 23505 app-08:5433-5436, 5xx sur l'une des deux requêtes). Cumulé à ROB-02 : deux comptes croient à une inscription que la base ignore.
- **Correction** : Conditionner `pushNotification`, `supaInsertNotif`, `_joinEventConversation` et `_promoteNextWaitlisted` à `rsvpOk === true` (ou `null` en mode local), dans le même correctif que ROB-02.
- **Effort** : 0,1 jour (dans le lot ROB-02).
- **Confiance** : CONFIRMÉ (exécuté hors réseau + inspection)

### CPL-ROB-03 — P3 — L'étiquette de synchronisation d'un post ment : « Sync… » est la seule valeur jamais écrite, « En ligne »/« Local » sont du code mort

- **Fonctionnalité** : Fil — carte d'une publication de l'utilisateur (`renderPostHTML`)
- **Attendu** : Après le verdict de `supaPublishPostWithRetry`, la carte dit « En ligne » ou « Local / Réessayer » ; un post jamais envoyé n'affiche pas un état transitoire à vie.
- **Observé** : `syncStatus` n'est écrit qu'une fois, à `"syncing"` (js/app-06-reels-partage.js:4314) ; aucun autre `syncStatus =` dans js/ (grep). Les branches `synced`/`offline` de app-02:6746-6747 sont inatteignables. Un post publié avec succès disparaît de l'étiquette seulement parce que la copie `supabasePosts` gagne le dédoublonnage (app-02:4585, 4602) ; un post jamais envoyé (ROB-01) affiche « Sync… » indéfiniment, y compris après rechargement (chaos C3 apresReload sync: syncing), et la notification dit « en attente » (app-06:4424).
- **Reproduction** : Publier hors ligne, recharger : `state.userPosts[0].syncStatus === "syncing"` et carte « Sync… » ; aucune requête ne part (C3).
- **Preuve** : js/app-06-reels-partage.js:4314, 4424 ; js/app-02-state-utils.js:6744-6747 ; grep `syncStatus` js/ → 5 occurrences, 1 seule écriture.
- **Impact** : L'utilisateur ne peut pas distinguer « en cours » de « perdu » ; aucune porte de réessai. Aggrave ROB-01.
- **Correction** : Écrire `post.syncStatus = ok ? "synced" : "offline"` dans le `.then` de app-06:4354 et dans la file de rejeu de ROB-01 ; rendre « Local » cliquable → réessai. À faire DANS le correctif ROB-01.
- **Effort** : 0,25 jour (dans le lot ROB-01).
- **Confiance** : CONFIRMÉ (inspection + banc chaos C3)

### CPL-ROB-04 — P3 — Un envoi LENT mais réussi est annoncé « Post en local (connexion lente) » puis jamais confirmé : l'utilisateur republie et double sa publication

- **Fonctionnalité** : Studio — publication avec média (photo/vidéo/audio)
- **Attendu** : Si l'upload aboutit après le délai d'affichage, la carte et un toast passent à « publié » ; tant qu'un envoi est en vol pour ce post, on ne laisse pas croire à un échec.
- **Observé** : js/app-06-reels-partage.js:4348-4357 : `Promise.race(sync, timeout 5 s)` ; à 5 s le toast « Post en local (connexion lente) » (4368) et la notification « en attente » (4424) partent alors que `supaPublishPostWithRetry` continue (timeout d'upload jusqu'à 180 s, app-08:3355-3359). Le `.then` de 4354 ne fait que `saveState(); renderFeed()` — aucun toast, aucun `syncStatus` (CPL-ROB-03). `_publishInProgress` est relâché dans le `finally` (4373) → une nouvelle publication est possible pendant que la première s'envoie.
- **Reproduction** : Stub `supaPublishPostWithRetry` résolvant `true` après 8 s, publier une photo : toast « Post en local » à 5 s, puis rien ; le post est pourtant inséré côté serveur. Non exécuté ici (inspection).
- **Preuve** : js/app-06-reels-partage.js:4348-4373, 4424 ; js/app-08-ui-modals-tour.js:3355-3359 (`_uploadTimeoutMs`).
- **Impact** : Sur réseau lent (photo de galerie, vidéo), message d'échec systématique suivi d'un succès muet → republication → doublon visible par tout le fil, double stockage facturé.
- **Correction** : Au succès tardif : toast « Post publié » + `syncStatus = synced` + repeindre ; garder un marqueur « envoi en vol » sur le post et l'afficher « Envoi… » plutôt que « Local » ; ne pas dire « connexion lente » avant l'échec réel des 2 tentatives.
- **Effort** : 0,25 jour (dans le lot ROB-01).
- **Confiance** : PROBABLE (inspection ; non exécuté)

### CPL-TCI-01 — P2 — L'étape « Mesure passion_id » est verte sur `fetch failed`, doublement (exit 0 dans le catch + continue-on-error), et sa mesure est tronquée à 1 000 lignes même quand elle réussit

- **Fonctionnalité** : CI deploy.yml job test-prod, étape scripts/mesure-passions.js
- **Attendu** : Une mesure d'exploitation qui échoue se voit en rouge (ou au moins en ::warning/annotation), et compte le référentiel entier (1 908 passions).
- **Observé** : scripts/mesure-passions.js:101-104 : `.catch((e) => { console.log("MESURE PASSIONS — échec :", …); process.exit(0); })` ; deploy.yml:355 `continue-on-error: true` par-dessus. Journal du run 33861671142 : « Référentiel passions : 1000 identifiant(s). » puis « MESURE PASSIONS — échec : fetch failed », étape et job verts. mesure-passions.js:68 GET /rest/v1/passions?select=id sans Range ni pagination → plafond PostgREST max-rows (1 000) alors que le référentiel en compte 1 908 (npm run verif : « 1908 passions »).
- **Reproduction** : sed -n 101,104p scripts/mesure-passions.js ; sed -n 354,358p .github/workflows/deploy.yml ; journaux-resume.md (run 33861671142, job Suites production).
- **Preuve** : scripts/mesure-passions.js:68-72, :101-104 ; .github/workflows/deploy.yml:354-358 ; preuves/complement-2026-09-08/ci-run-33861671142/journaux-resume.md
- **Impact** : Une mesure censée piloter la migration passion_id (PRO-01) n'existe plus depuis un temps indéterminé sans qu'aucun voyant ne l'indique ; quand elle marche, elle compte au plus 1 000 identifiants sur 1 908, donc classe à tort ~900 passions en « hors référentiel ». Aucun effet sur le déploiement.
- **Correction** : Dans le catch : `process.exitCode = 1` + `::warning` ; retirer `continue-on-error` ou le remplacer par une annotation visible ; paginer la lecture du référentiel (Range 0-999, 1000-1999… ou Prefer: count=exact + HEAD) ; épreuver par un test unitaire qui rejoue un fetch en échec.
- **Effort** : S (1 h)
- **Confiance** : haute (lu au SHA + journal CI)

### CPL-TCI-02 — P2 — La purge Storage des comptes e2e n'a JAMAIS tourné en CI : `purge-e2e-storage.js` exige SUPABASE_URL en variable d'environnement, qu'aucun workflow ne pose

- **Fonctionnalité** : tests/e2e/global-teardown.js → scripts/purge-e2e-storage.js --appliquer
- **Attendu** : Après chaque run prod (deploy.yml test-prod et sentinelle horaire), les objets Storage laissés par authz-critical (bloc « storage cloisonné », upload via /storage/v1/) sont supprimés comme le sont les comptes.
- **Observé** : scripts/purge-e2e-storage.js:27-42 env() lit process.env puis dashboard/.env et rend null sans SUPABASE_URL → :105 « [purge:storage] clés Supabase absentes — ignoré. » ; grep -rn SUPABASE_URL .github/workflows/ → 0 (seule SUPABASE_SERVICE_ROLE_KEY est posée) ; à l'inverse tests/e2e/compte-e2e.js:54-57 (configAdmin) retombe sur la constante de js/app-08:2551, ce qui explique que purge:rest réussit (« 5 compte(s) … comptes restants : 0 ») pendant que la purge Storage est ignorée, sur le même run.
- **Reproduction** : sed -n 27,42p scripts/purge-e2e-storage.js ; grep -rn SUPABASE_URL .github/workflows/ ; journal run 33861671142 job Suites production.
- **Preuve** : scripts/purge-e2e-storage.js:27-42,105 ; tests/e2e/compte-e2e.js:54-57 ; preuves/complement-2026-09-08/ci-run-33861671142/journaux-resume.md
- **Impact** : Chaque run CI prod (≈35-50/jour avec la sentinelle horaire) laisse ses objets de test dans le bucket public de PRODUCTION : résidus orphelins (rejoint SUP-10 / AUTH-05), coût de stockage, et objets d'e2e visibles publiquement si le bucket est public.
- **Correction** : Faire de purge-e2e-storage.js un consommateur de `configAdmin()` (même repli sur app-08 que compte-e2e.js), ou poser `SUPABASE_URL` dans l'env des jobs test-prod et sentinelle ; ajouter au journal un `::warning` quand la purge est ignorée sous CI ; purge unique de rattrapage avec `--appliquer` depuis un poste.
- **Effort** : S (30 min + purge de rattrapage)
- **Confiance** : haute

### CPL-TCI-03 — P1 — Le secret service_role de PRODUCTION est exposé à toute pull_request du dépôt AVANT la gouvernance : test-prod n'a ni `needs` ni `if`

- **Fonctionnalité** : deploy.yml jobs test-prod (et test-local), déclencheur pull_request
- **Attendu** : Un secret qui contourne toute la RLS n'est remis qu'à du code déjà relu (après le job « Gouvernance critique » qui vérifie la contre-revue), ou seulement sur push main / via un GitHub Environment à approbation.
- **Observé** : deploy.yml:300-347 : test-prod sans `needs:` et sans `if:` (contrairement à deploy :478-479 et preview :531/:563), avec `SUPABASE_SERVICE_ROLE_KEY: ${{ secrets… }}` ; `on: pull_request` (deploy.yml:7). Le job Gouvernance (deploy.yml:85-136) tourne en PARALLÈLE, pas en amont. Tout push sur une branche du dépôt (dont celles créées par claude-code.yml, permissions contents: write, ligne 82) qui modifie tests/e2e/authz-critical.spec.js, compte-e2e.js ou playwright.config.js obtient le secret dans un processus Node avec accès réseau sortant. Les PR de forks n'ont pas les secrets (mesure-passions.js:63 le rappelle), mais le flux Claude Code n'utilise pas de fork.
- **Reproduction** : sed -n 300,347p .github/workflows/deploy.yml (aucun needs/if) ; comparer avec :478-479 et :563 ; grep -n 'contents: write' .github/workflows/claude-code.yml.
- **Preuve** : deploy.yml:300-347, :85-136, :478, :563 ; claude-code.yml:81-83
- **Impact** : Une branche non relue (erreur, injection de prompt dans une issue traitée par le bot, compromission d'un jeton) peut exfiltrer la clé service_role → lecture/écriture/suppression de TOUTES les données de production, RLS incluse. GitHub masque la valeur dans les journaux, pas dans une requête sortante.
- **Correction** : `needs: [governance]` sur test-prod (coût : ~6 s), et/ou déplacer les étapes qui portent le secret dans un GitHub Environment `production-tests` à approbateur requis ; à terme, staging (TCI-04) et clé service_role de staging seulement dans les PR.
- **Effort** : S (workflow) ; M avec Environment
- **Confiance** : haute sur le fait ; moyenne sur l'exploitabilité (dépend des protections de branche, non vérifiables ici)

### CPL-TCI-04 — P3 — `migrations/SCHEMA_PROD_REFERENCE.sql`, désignée par CLAUDE.md comme référence hors ligne du schéma, ne contient aucune policy RLS : aucun banc local ne peut la rejouer

- **Fonctionnalité** : Référence de schéma pour les bancs SQL (tests/sql/) et les corrections proposées TCI-15 / TCI-01
- **Attendu** : La référence hors ligne porte les 125 policies de production pour qu'un Postgres jetable puisse les rejouer.
- **Observé** : grep -ciE 'policy\|row level\|enable rls' migrations/SCHEMA_PROD_REFERENCE.sql → 0 ; les 125 policies n'existent que dans preuves/supabase-isolation/policies.json (dump du 2026-09-04) ; tests/sql/socle-prod.sql en recopie 18 à la main.
- **Reproduction** : grep -ciE 'policy\|row level' migrations/SCHEMA_PROD_REFERENCE.sql
- **Preuve** : migrations/SCHEMA_PROD_REFERENCE.sql ; tests/sql/socle-prod.sql ; .passio/audits/…/10-AUDIT-MODERATION…md:88 (mention du dump 125 policies)
- **Impact** : Toute extension du banc RLS (TCI-15) ou test hors prod de la messagerie (TCI-01) échouerait faute de source rejouable ; la « référence » ne décrit que la moitié du modèle de sécurité.
- **Correction** : Régénérer la référence par `pg_dump --schema-only` (policies incluses) ou ajouter un fichier `migrations/POLICIES_PROD_REFERENCE.sql` généré depuis pg_policies, et le brancher dans scripts/schema-baseline.js.
- **Effort** : S
- **Confiance** : haute

### CPL-DEV-01 — P2 — Modale générique : focus jamais déplacé ni restitué, aucun piège, arrière-plan non inerte, et remplacement de modale qui fait tomber le focus sur <body> (WCAG 2.4.3 / 2.1.2)

- **Fonctionnalité** : Toutes les modales openModal (64 appels : notifications, options de post, partage, paywall, aides, profil visité…)
- **Attendu** : À l’ouverture, focus sur le conteneur (#modalContent tabindex=-1) ou le premier focusable ; Tab confiné ; arrière-plan inert ; à la fermeture, focus rendu à l’élément déclencheur mémorisé (uniquement quand le backdrop n’était pas déjà actif, pour respecter le non-empilement).
- **Observé** : Focus reste derrière la modale ; Tab ×8 sort ; Escape avec focus dans la modale → activeElement=BODY ; openModal depuis une modale → activeElement=BODY.
- **Reproduction** : relecture/appareils-a11y/mesure2.js (c13_pur, c13_focusDedans, c13_remplacement) ; à la main : Tab jusqu’à la cloche, Entrée, Tab, Tab…
- **Preuve** : js/app-08-ui-modals-tour.js:15-40 (aucun .focus(), aucun activeElement mémorisé) ; index.html:1355 ; mesure2.json
- **Impact** : Utilisateur clavier et lecteur d’écran de bureau perdu à chaque modale (64 points d’entrée) ; RGAA 7.1/7.3 non conformes ; aria-modal=true trompe le lecteur d’écran.
- **Correction** : Dans openModal : si !backdrop.active, _modalTrigger = document.activeElement ; après innerHTML, #modalContent.tabIndex=-1 + focus({preventScroll:true}) ; ajouter un keydown Tab confiné (copier ui-v2-shell.js:381-393) ; poser inert sur .app-shell (ou aria-hidden) pendant l’ouverture. Dans closeModal : restituer _modalTrigger si encore connecté.
- **Effort** : 0,5 jour.

### CPL-DEV-02 — P2 — Dialogue sans nom accessible : #modalContent porte role=dialog aria-modal=true mais ni aria-label ni aria-labelledby ; les panneaux plein écran (#conv-fullpage, #eventDetailPage, #postDetailPage, #passionManager) n’ont ni rôle ni titre

- **Fonctionnalité** : Modales et panneaux plein écran
- **Attendu** : Tout dialogue est nommé (WCAG 4.1.2) : aria-labelledby vers son .modal-title, ou aria-label ; les panneaux plein écran portent role=dialog + nom + un titre h1/h2.
- **Observé** : 0 aria-labelledby dans app-08 ; mesure : dialogNom {ariaLabel:null, ariaLabelledby:null} ; #conv-fullpage {role:null, h1:false}.
- **Reproduction** : mesure.json → c13_modal.dialogNom ; mesure2.json → conv.roleConv
- **Preuve** : index.html:1355, 1381, 1026, 1035 ; js/app-08-ui-modals-tour.js:22-26
- **Impact** : Le lecteur d’écran annonce « dialogue » sans dire lequel ; navigation par titres impossible dans une conversation ou une fiche d’activité.
- **Correction** : Dans openModal, après injection : si un .modal-title existe, lui donner un id et poser aria-labelledby ; sinon aria-label="Fenêtre". Poser role=dialog + aria-label dynamique sur #conv-fullpage (nom du contact), #eventDetailPage, #postDetailPage ; rendre le nom en h1 dans l’en-tête.
- **Effort** : 0,25 jour.

### CPL-DEV-03 — P2 — Escape ne ferme pas les panneaux plein écran (#conv-fullpage, fiche activité, page post, Mes passions) : closeCurrentOverlay n’est câblé qu’au bouton retour

- **Fonctionnalité** : Messagerie, IRL, fil, profil — clavier
- **Attendu** : Escape ferme la couche du dessus, comme pour la modale (WCAG 2.1.2 : pas de piège).
- **Observé** : Conversation ouverte, Escape → toujours active (mesure2.json → conv.escapeFerme=false) ; seul le bouton « Retour » (36×36, focusable) ferme, et le focus reste alors sur ce bouton devenu invisible (focusApresFermeture = .conv-fp-back dans le panneau translaté hors écran).
- **Reproduction** : mesure2.js § conv
- **Preuve** : js/app-08-ui-modals-tour.js:44-48 (Escape → closeModal seulement) ; js/app-02-state-utils.js:2063 (closeCurrentOverlay, appelée par popstate) ; js/app-04-comments-shop.js:4811-4827
- **Impact** : Utilisateur clavier bloqué dans une conversation tant qu’il ne retrouve pas le bouton Retour ; après fermeture, le focus est sur un élément hors écran.
- **Correction** : Remplacer le keydown d’app-08:44 par `if (e.key==="Escape") closeCurrentOverlay();` (l’ordre des couches y est déjà géré) ; dans closeConversation/closeEventDetail/closePost/closePassionManager, restituer le focus au déclencheur mémorisé ou, à défaut, au .nav-item actif.
- **Effort** : 0,25 jour.

### CPL-DEV-04 — P3 — prefers-reduced-motion incomplet : modale (slideUp 320 ms), backdrop, toasts, pages post/activité, overlay PWA et logo d’onboarding (logoFloat infinite) continuent d’animer sous reduce

- **Fonctionnalité** : Transitions globales
- **Attendu** : Sous prefers-reduced-motion: reduce, aucune animation décorative (WCAG 2.3.3 AAA, 2.2.2 pour les boucles infinies) ; les indicateurs fonctionnels (enregistrement, chargement) peuvent rester.
- **Observé** : 7 animations actives sous reduce avec une modale et un toast (mesure.json → reduce.animesActives), 10 dans la matrice avec l’onboarding.
- **Reproduction** : mesure.js (contexte reducedMotion:"reduce")
- **Preuve** : styles.css:1377, 1390, 1488, 1532, 1574, 1587 hors des 22 blocs @media (prefers-reduced-motion: reduce) ; index.html:1026 (animation en ligne sur #postDetailPage)
- **Impact** : Faible : inconfort vestibulaire pour les utilisateurs ayant activé la préférence ; le glissement de la modale est la surface la plus fréquente.
- **Correction** : Un bloc unique en fin de la partie « socle » : @media (prefers-reduced-motion: reduce) { .modal, .modal-backdrop, .toast, #postDetailPage, #eventDetailPage, #pwa-overlay, .onb-step, .onb-logo-big { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; } } ; retirer l’animation en ligne d’index.html:1026 au profit d’une classe.
- **Effort** : 0,25 jour.

### CPL-DEV-05 — P3 — Fil, Profil et Studio sans aucun titre h1–h6 ; la navigation par titres des lecteurs d’écran n’a rien à saisir sur trois des six écrans

- **Fonctionnalité** : Structure des écrans
- **Attendu** : Un h1 par écran (RGAA 9.1, WCAG 1.3.1/2.4.6), au moins visuellement masqué.
- **Observé** : matrice-synthese.md § Titres : feed AUCUN, profiles AUCUN, studio AUCUN ; irl h1, messages h1, explore h3 seulement.
- **Reproduction** : preuves/appareils-a11y/matrice.js (section Titres)
- **Preuve** : preuves/appareils-a11y/matrice-synthese.md:255-263 ; index.html (#screen-feed, #screen-profiles, #screen-studio sans h1)
- **Impact** : Faible mais transverse : orientation au lecteur d’écran dégradée sur les écrans principaux.
- **Correction** : Ajouter un h1 visuellement masqué (classe .sr-only) en tête de chaque .screen (« Fil », « Mon profil », « Créer »), h2 sur Explorer.
- **Effort** : 0,1 jour.

## 5. Ce que cette relecture change au verdict

Rien sur le fond : les huit P0 sont confirmés (EXP-01 et PERF-01, qui n'avaient pas été relus, le sont désormais avec preuve) et les sept critères d'interdiction restent vrais. Ce qui change est dans `17-BILAN-CONSOLIDE-ET-PLAN-DE-CORRECTION.md` : le critère « isolation non prouvée » devient « isolation prouvée sous rôle, avec trois fuites voulues par les policies à fermer », et les oublis ci-dessus rejoignent les chantiers CH02 (messagerie), CH05 (IRL), CH11 (produit) et CH12 (CI).

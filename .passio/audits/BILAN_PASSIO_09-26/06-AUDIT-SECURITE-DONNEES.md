# Audit sécurité et données — BILAN PASSIO 09/26

> SHA audité : `c8cb8e995b88159a1e9d4c2f7dc196ad93a133bf` (main, 2026-09-04). Modèle : Claude Fable 5.1 (auditeurs de domaine et relecteurs adversariaux). Statuts : PROUVÉ · CONFORME PAR INSPECTION · PROBABLE · DÉFAILLANT · BLOQUÉ · NON APPLICABLE. Priorités : P0 bloque la commercialisation · P1 avant lancement public · P2 amélioration importante · P3 optimisation future. Chaque problème a été soumis à une relecture adversariale indépendante (angles reproduction / impact / contexte) ; la priorité retenue est celle de la relecture quand elle diffère.


## Domaine « supabase-isolation »

Audit en lecture seule du projet Supabase njkiyoklssvefstljemx (PostgreSQL 17.6) sur le SHA c8cb8e99, via le connecteur supabase-passio-readonly (execute_sql, get_advisors, list_migrations) et l'inspection du dépôt. Dump complet des 125 policies (119 public + 4 storage + 2 realtime, toutes PERMISSIVE) déposé dans policies.json et analysé table par table ; prosrc des 20 fonctions maison, 12 triggers, grants, publication realtime, buckets et 25 chemins d'objets (masqués) relevés.
Isolation par requête : SET LOCAL ROLE anon/authenticated est REFUSÉ au rôle du connecteur (ERROR 42501) et l'accès HTTPS à supabase.co est bloqué par le proxy (CONNECT 403) → la preuve « moteur RLS » n'a pas pu être exécutée ici ; elle a été remplacée par une ÉMULATION fidèle (quals exacts des policies et prosrc des helpers inlinés, auth.uid() = NULL puis uuid inexistant) sur les 39 tables et storage.objects, et par la lecture de authz-critical.spec.js (12 blocs d'invariants, run CI 33861671142 vert).
Verdict côté base : isolation des comptes CONFORME sur le noyau (conv_messages, conv_members, conversations, notifications, user_state, blocks, reports, push_subscriptions, story_views, user_safety, telemetry, analytics, client_errors : 0 ligne pour anon et pour un tiers ; 0 ligne UPDATE/DELETE touchable par un tiers) mais NON PROUVÉE au sens strict ici (émulation, pas exécution) et DÉFAILLANTE sur trois surfaces : (1) conv_reads en lecture publique (34 accusés de lecture de 19 conversations privées lisibles par anon et diffusés en realtime à tous) ; (2) les 12 pièces jointes vocales de conversations privées dans un bucket public LISTABLE avec la seule clé anon (R2 « P0 » ouvert depuis le 2026-08-08) ; (3) la policy UPDATE de conv_messages sans WITH CHECK, qui laisse déplacer un message dans n'importe quelle conversation.
Staging séparé : NON — un seul project_ref sert le dev local, les previews Netlify de PR, les suites CI à comptes réels (créés puis purgés EN PROD) et la production. Schéma : 4 migrations enregistrées contre 64 fichiers, et SCHEMA_PROD_REFERENCE.sql (photographie du 2026-08-17) est périmé (4 tables absentes, passions 5 → 13 colonnes, 116 policies vs 125) → non reconstructible depuis le dépôt.
Clés : aucune valeur service_role ni JWT de service dans le dépôt ; service_role uniquement en secret GitHub (deploy.yml, sentinelle-distante.yml), dans dashboard/.env (serveur) et dans les Edge Functions, qui vérifient toutes le JWT par auth.getUser(). 18 WARN advisors, 0 ERROR (relevé identique au 2026-09-03).

### Contrôles (33)

| Id | Contrôle | Statut | Méthode | Preuve |
|---|---|---|---|---|
| C01 | Dump complet des policies (pg_policies public/storage/realtime) et analyse table par table | **PROUVÉ** | requête base | execute_sql pg_policies → 125 policies (public=119, realtime=2, storage=4), toutes PERMISSIVE ; fichier policies.json (avec listes : tables sans SELECT/UPDATE/DELETE, UPDATE sans WITH CHECK ×9, SELECT USING true ×20, doublons ×5) |
| C02 | RLS activée sur toutes les tables public + storage.objects + realtime.messages | **PROUVÉ** | requête base | pg_class.relrowsecurity = true sur 39/39 tables public, storage.objects, storage.buckets, realtime.messages ; relforcerowsecurity=false partout (attendu) |
| C03 | USING (true) sur des données privées | **DÉFAILLANT** | requête base | conv_reads.reads_select USING (true) — 34 lignes (conv_id, user_id, last_read_at) de 19 conversations privées ; émulation anon = 34 (attendu 0). Voir SUP-02. Les autres USING true (profiles, events, event_attendees, follows, user_passions, passions, comment_likes, event_comments, event_reactions, video_lives, step_interactions, post_collaborators, cdv_*) sont publics par conception ou données mortes (cdv_*) |
| C04 | Absence de policy UPDATE/DELETE = écriture ignorée en silence (0 ligne) | **DÉFAILLANT** | inspection code | conversations : aucune policy UPDATE ni DELETE, or js/app-05-config-profil.js:1624 fait conversations.update({description}) et js/app-08-ui-modals-tour.js:4518 conversations.delete() → 0 ligne, erreur avalée par .then(function(){},function(){}) / try-catch (SUP-16). Autres tables sans UPDATE (post_likes, follows, comment_interactions, blocks…) : le client ne fait que delete+insert (grep js/, aucun .update() sur ces tables) |
| C05 | Policies d'écriture UPDATE sans WITH CHECK (le USING s'applique à la nouvelle ligne mais les colonnes non citées restent libres) | **DÉFAILLANT** | requête base | 9 policies UPDATE sans with_check : conv_messages « Update propre » (from_id seul contraint → conv_id libre, SUP-03), events « Update organisateurs » (author_id réassignable par un co-organisateur, SUP-09), post_comments « Update propre » (post_id libre, SUP-09), cdv_lives, conv_reads, notifications, profiles, push_subscriptions, video_lives (sans conséquence : la clé est dans le USING) |
| C06 | Colonnes de profiles exposées à anon (e-mail, téléphone, année de naissance, localisation ?) | **PROUVÉ** | requête base | information_schema.columns profiles = id, username, emoji, color, passion_id, bio, created_at, avatar_url, cover_url, passions, is_private, rs_links — aucune PII forte. Téléphone/e-mail/majorité vivent dans auth.users (raw_user_meta_data porte la clé phone pour 1 compte) et user_safety (SELECT propre, aucun grant anon) : non exposés par PostgREST |
| C07 | follows / event_attendees publics (choix assumé) | **CONFORME PAR INSPECTION** | requête base | Lecture publique USING true sur follows (7 lignes) et event_attendees (17 lignes, y compris checked_in_at, rating, feedback) ; choix écrit dans .passio/context/KNOWN_RISKS.md (remédiation 2026-08-09). event_attendees.feedback/rating publics n'y sont PAS mentionnés |
| C08 | reports sans SELECT, telemetry/analytics/client_errors INSERT-only, user_state privé | **PROUVÉ** | requête base | policies.json : reports = reports_insert seul ; telemetry_events = telemetry_insert_own (user_id IS NULL OR own) ; analytics_events = analytics_insert_own ; client_errors = INSERT WITH CHECK true ; user_state = 4 policies propres. Émulation anon/tiers = 0 ligne lisible sur chacune |
| C09 | notifications : insertion par un tiers (notifications_insert_own_author) | **CONFORME PAR INSPECTION** | requête base | WITH CHECK (from_id = auth.uid()) : l'auteur est contraint, le destinataire user_id, kind, ref_id, content restent libres et aucun trigger rate_limit sur notifications (pg_trigger : 0) → SUP-07. Lecture scellée (user_id = auth.uid()), émulation tiers = 0 |
| C10 | Preuve d'isolation par requête sous rôle anon (SET LOCAL ROLE anon ; count par table) | **BLOQUÉ** | requête base | begin; set local role anon; … → ERROR 42501 permission denied to set role "anon" (idem authenticated). Remplacé par l'émulation C12. Il faudrait un rôle de connecteur membre de anon/authenticated, ou une base jetable (docker postgres + tests/sql/socle-prod.sql) avec les policies réelles |
| C11 | Preuve d'isolation par API REST anon (clé anon publique, GET count=exact) | **BLOQUÉ** | non réalisé | curl https://njkiyoklssvefstljemx.supabase.co/rest/v1/ → « CONNECT tunnel failed, response 403 » (proxy de l'environnement). Le script rest_anon.sh a en outre été refusé par le classificateur de permissions. Il faudrait un poste avec accès réseau à supabase.co |
| C12 | ÉMULATION anon et tiers : quals exacts + helpers inlinés, count par table (39 tables + storage.objects) | **CONFORME PAR INSPECTION** | requête base | isolation_par_table.md : anon et tiers (uuid 0000…) → conversations 0/117, conv_members 0/7, conv_messages 0/68, notifications 0/188, user_state 0/84, blocks 0, reports 0/2, push 0/5, story_views 0/7, user_safety 0/2, telemetry 0/111 829, analytics 0/3 855, client_errors 0/16 ; profiles 5, posts 32, events 3, follows 7, passions 1 908 (>0 attendu) ; EXCEPTIONS : conv_reads 34/34 et storage.objects 70/70 (dont 12 attachments) |
| C13 | UPDATE/DELETE ciblé sous rôle tiers dans une transaction annulée (update posts … returning id → 0 ligne) | **BLOQUÉ** | requête base | begin; set local role authenticated; set local request.jwt.claims=…; update … ; rollback → refusé dès le SET ROLE (42501) ; le connecteur est de plus en transaction_read_only=on (ADR-012). Émulation des USING : posts/profiles/conv_messages/notifications/events/user_state/conv_members/stories/storage → 0 ligne touchable par le tiers. Preuve exécutée réelle : authz-critical.spec.js blocs 2–3 (PATCH/DELETE cross-compte = 0 ligne), CI 33861671142 |
| C14 | Compte privé : un post d'un profil is_private est-il invisible pour le tiers ? | **CONFORME PAR INSPECTION** | requête base | 0 profil is_private=true en prod (posts_auteur_prive = 0) → NON VÉRIFIABLE sur données. Policy « Lecture respectant les comptes prives » (posts) et « Lecture stories… » : author_id = uid OR NOT EXISTS(profil privé) OR EXISTS(follow) ; post_likes/post_comments/comment_interactions dérivent via post_is_visible/comment_target_visible. Suite confidentialite.spec.js (projet prod) la couvre avec 3 comptes réels |
| C15 | Storage : policies storage.objects, fonction storage_chemin_autorise, buckets publics | **DÉFAILLANT** | requête base | buckets content et attachments : public=true, 50 Mo, allowed_mime_types NULL ; passio_media_read SELECT USING bucket_id IN (content, attachments) pour {public} → LISTING /storage/v1/object/list autorisé à anon ; 12 objets attachments/conv_<id>/<epoch>_voice.webm (vocaux de DM) ; 5 conv_messages contiennent une URL /object/public/attachments/. INSERT/UPDATE cloisonnés par storage_chemin_autorise (content : dossier[2]=uid ; attachments : is_conv_member), DELETE owner = auth.uid() (70/70 objets avec owner). Voir SUP-01 |
| C16 | Signed URLs utilisées ? | **PROUVÉ** | inspection code | grep createSignedUrl js/ → seule occurrence js/app-08-ui-modals-tour.js:2644-2645 (stub hors-ligne _ko). getPublicUrl partout : app-08:3593, app-09:886, app-09:1599 ; app-02:1089-1090 transforme en /render/image/public/. Aucune URL signée en production |
| C17 | Realtime : publication (25 tables) et respect de la RLS pour postgres_changes | **CONFORME PAR INSPECTION** | requête base | pg_publication_tables supabase_realtime = 25 tables (cdv_* ×6, comment_interactions, comment_likes, conv_members, conv_messages, conv_reads, conversations, event_comments, event_reactions, events, notifications, post_collaborators, post_comments, post_likes, posts, profiles, step_interactions, stories, telemetry_events, video_lives). postgres_changes est filtré par la RLS du JWT souscripteur (comportement Supabase, non ré-exécuté ici) ; conséquence : conv_reads (SELECT true) est diffusé à TOUT client abonné (app-08 dbChan.on postgres_changes conv_reads) |
| C18 | Realtime : policies realtime.messages (broadcast privé user:<uid>) et fonction broadcast_conv_message_to_users | **PROUVÉ** | requête base | realtime.messages RLS on, 2 policies identiques « Utilisateur recoit/reçoit ses messages » : SELECT authenticated, realtime.topic() = 'user:'\|\|auth.uid(). Trigger AFTER INSERT conv_messages → broadcast_changes('user:'\|\|member) pour chaque conv_members (prosrc lu). Le canal v2 conv:<id> (private) n'a AUCUNE policy → mort (v3 actif par défaut, app-08:4833-4843) |
| C19 | Realtime : canaux broadcast publics écoutés/émis par tous (grep channel( js/) | **DÉFAILLANT** | inspection code | app-05:493-497 _callChannel = canal broadcast PUBLIC ; ring:<uid> (app-05:556-564, payload.from choisi par l'émetteur ; réception app-05:1105 + filtre isBlocked(payload.from) app-05:832 seulement), call:<callId> avec callId = MY_UID + Date.now().toString(36) (app-05:531), typing:<convId> (app-04:4755), vlive:<id> presence keyed MY_UID (app-05:3237, 3497). Voir SUP-06 |
| C20 | Realtime : limites du plan (connexions simultanées, msgs/s) | **BLOQUÉ** | non réalisé | Le plan Supabase n'est pas lisible par le connecteur (features=database,debugging,docs). Capacité non prouvée. Il faudrait le tableau de bord Supabase (Settings → Billing) ; au repos l'app tient 3 canaux par client (app-08:5027-5029) |
| C21 | Fonctions : 13 SECURITY DEFINER, celles exécutables par anon (prosrc lu), fuite par oracle | **DÉFAILLANT** | requête base | pg_proc : 13 secdef ; EXECUTE anon+authenticated sur can_edit_post, comment_target_visible, is_conv_member, post_is_visible ; authenticated sur is_blocked_with, irl_interaction_allowed, can_join_event_conversation, is_conversation_creator, declare_birth_year. is_conv_member(_conv_id,_uid) répond pour N'IMPORTE quel couple sans lire auth.uid() → oracle d'appartenance ; post_is_visible(pid) : inexistant → true, existant-privé → false → oracle d'existence. Voir SUP-05 |
| C22 | search_path des 3 fonctions non épinglées (storage_chemin_autorise, unaccent_immutable, rechercher_passions) | **CONFORME PAR INSPECTION** | requête base | Toutes trois SECURITY INVOKER (prosecdef=false). storage_chemin_autorise : références toutes qualifiées (storage.foldername, public.is_conv_member, auth.uid). rechercher_passions : similarity/unnest non qualifiés MAIS has_schema_privilege('anon'\|'authenticated','public','CREATE') = false → aucune usurpation possible. WARN advisor à corriger par hygiène (SET search_path = '') |
| C23 | declare_birth_year non avançable : fonction + trigger | **CONFORME PAR INSPECTION** | requête base | prosrc : INSERT … ON CONFLICT DO UPDATE … WHERE s.majority_at IS NULL OR EXCLUDED.majority_at > s.majority_at ; trigger trg_user_safety_majorite BEFORE UPDATE → RAISE si NEW.majority_at < OLD.majority_at ; bornes 1900..année courante. Limite : la PREMIÈRE déclaration est libre (auto-déclaration), user_safety n'a ni INSERT ni UPDATE policy (seule la fonction écrit) |
| C24 | Clé service_role absente du dépôt ; clé anon dans app-08 | **PROUVÉ** | inspection code | grep -rn service_role\|eyJhbGciOi (hors node_modules, dist) : une seule valeur JWT = clé anon js/app-08-ui-modals-tour.js:2552 (attendu) ; toutes les autres occurrences sont des commentaires, docs, dashboard/.env.example:33 (vide) et .claude/scripts/compact-permissions.js:41 (regex de détection). .mcp.json absent |
| C25 | Secrets GitHub (noms), Edge Functions (JWT vérifié ?), CSP | **CONFORME PAR INSPECTION** | inspection code | Secrets référencés : SUPABASE_SERVICE_ROLE_KEY (deploy.yml:108,346,357 ; sentinelle-distante.yml:108), NETLIFY_AUTH_TOKEN, CLAUDE_CODE_OAUTH_TOKEN, GITHUB_TOKEN, PASSIO ; NETLIFY_SITE_ID en clair (identifiant). Edge : ask-ai:51-58, notify-call:29-35, delete-account:27-33 → auth.getUser() avant tout, 401 sinon ; service_role via Deno.env. CSP _headers/netlify.toml : connect-src limité au projet + tuiles/BAN/Photon/Tenor/Giphy/STUN/TURN |
| C26 | API exposée : tables PostgREST, grants anon, RPC | **PROUVÉ** | requête base | relacl : anon = arwdDxtm (tous droits) sur toutes les tables public sauf conv_members/conv_messages/conversations (sans INSERT) et user_safety (aucun) ; 3 vues security_invoker=on (telemetry_last24h, client_errors_*) → vides sous RLS ; RPC anon : can_edit_post, comment_target_visible, is_conv_member, post_is_visible, rechercher_passions, storage_chemin_autorise, unaccent_immutable. RPC utilisées par l'app : declare_birth_year (app-07:5024), irl_interaction_allowed (app-07:5050), rechercher_passions (passions-flat.js:405) |
| C27 | Séparation staging / production (project_ref, previews Netlify, comptes CI) | **DÉFAILLANT** | inspection code | grep supabase.co : un seul ref njkiyoklssvefstljemx dans js/app-08:2551, index.html, _headers, netlify.toml, dashboard/.env.example, tests/e2e/*.js, scripts/*.html ; aucune variable *_STAGING. deploy.yml:530-586 preview PR = même dist → même prod. deploy.yml:301-357 « Suites production (comptes réels) » PASSIO_E2E_PROD=1 crée des comptes %@passio-e2e.test en prod puis purge (scripts/purge-e2e-rest.js). Voir SUP-04 |
| C28 | Schéma prod vs migrations : migrations enregistrées, SCHEMA_PROD_REFERENCE.sql à jour ? | **DÉFAILLANT** | requête base | list_migrations = 4 (20260809182329, 20260809182411, 20260809183406, 20260824132958) vs 64 fichiers migrations/. Comparaison md5 colonnes (schema_reference_vs_prod.txt) : 36 relations identiques, passions et video_lives différentes, passion_relations/passion_requests/user_passions/user_safety ABSENTES de la référence ; policies 116 (réf.) vs 125 (prod) : 12 nouvelles, 3 disparues. Voir SUP-08 |
| C29 | Les invariants de authz-critical.spec.js sont-ils portés par une policy réelle ? | **CONFORME PAR INSPECTION** | inspection code | Bloc 0 profil usurpé → profiles Upsert propre (id = uid) ✓ ; 1 post forgé → posts Ecriture propre ✓ ; 2 PATCH cross → posts Update propre can_edit_post ✓ (+ trigger posts_freeze_author) ; 3 DELETE cross → Suppression propre ✓ ; 4 intégrité ✓ ; 5 notifications d'autrui → Lecture propre ✓ ; 6 DM hors conv → conv_messages_select_member ✓ ; 7 télémétrie → aucune policy SELECT ✓ ; 8 identité canonique → trg_identite_affichage BEFORE INSERT OR UPDATE video_lives ✓ ; 9 notif forgée → notifications_insert_own_author ✓ ; 10 anon sans donnée privée → ✓ pour conv_messages MAIS faux si étendu à conv_reads ; 11 storage dépôt/move/copy/traversal/attachments → passio_media_insert_cloisonne + passio_media_update_cloisonne (storage_chemin_autorise) ✓. Exécution réelle : CI 33861671142, job « Suites production (comptes réels) » vert (non relancé ici, comptes réels + service_role requis) |
| C30 | Advisors sécurité (relevé du jour) | **PROUVÉ** | requête base | get_advisors security 2026-09-04 12:49 UTC : 0 ERROR, 18 WARN — search_path mutable ×3, pg_trgm in public, secdef exécutable par anon ×4, par authenticated ×9, auth_leaked_password_protection désactivée. Identique aux faits établis |
| C31 | Anonymous sign-in (signInAnonymously appelé par app-02:3530) : provider activé côté Auth ? | **BLOQUÉ** | requête base | auth.users : 5 comptes, 0 is_anonymous, 5 confirmés, 0 avec phone. La configuration Auth (anonymous provider, MFA, captcha) n'est pas lisible via SQL/MCP ; il faudrait le tableau de bord Auth. first-run.js:35 affirme qu'aucun signInAnonymously n'est fait en mode invité |
| C32 | Triggers anti-abus et rate limiting serveur | **CONFORME PAR INSPECTION** | requête base | rate_limit_insert sur comment_interactions (60/min), event_reactions (30/min), reports (10/min) ; passion_requests bornée à 5/24 h par la policy. AUCUN rate-limit sur notifications, conv_messages, posts, telemetry_events (INSERT anon autorisé, 111 829 lignes/30 j) ni client_errors (INSERT true) → SUP-07 / notes |
| C33 | Suppression de compte (Edge delete-account) : résidus de données et Storage | **DÉFAILLANT** | inspection code | supabase/functions/delete-account/index.ts:46-62 purge 15 couples table/colonne et :67-76 seulement content/photos\|videos\|audios/<uid> ; non purgés : comment_interactions, event_comments, event_reactions, blocks, reports, user_state, user_safety, user_passions, analytics_events, telemetry_events, video_lives, cdv_*, conv_reads, comment_likes, post_collaborators, step_interactions ; Storage covers/avatars/passion_*/cdv_steps/<uid> et attachments. Voir SUP-10 |

### Problèmes (16)

| Id | Priorité retenue | Relecture | Titre |
|---|---|---|---|
| SUP-01 | **P0** | CONFIRMÉ par la relecture | Pièces jointes vocales de conversations privées dans un bucket public, listables avec la seule clé anon (R2 « P0 » ouvert depuis le 2026-08-08) |
| SUP-02 | **P1** | CONFIRMÉ par la relecture | conv_reads en lecture publique : appartenance et activité des conversations privées lisibles par anon et diffusées en realtime à tous |
| SUP-03 | **P1** | CONFIRMÉ par la relecture | conv_messages « Update propre » sans WITH CHECK : un utilisateur peut déplacer son propre message dans n'importe quelle conversation (injection dans des DM dont il n'est pas membre, contournement du blocage) |
| SUP-04 | **P0** | CONFIRMÉ par la relecture | Un seul projet Supabase pour le dev, les previews de PR, les tests CI à comptes réels et la production (staging séparé : NON) |
| SUP-05 | **P2** | CONFIRMÉ par la relecture | Fonctions SECURITY DEFINER exposées en RPC à anon : oracle d'appartenance (is_conv_member) et d'existence de contenu privé (post_is_visible, comment_target_visible) ; is_blocked_with révèle un blocage |
| SUP-06 | **P1** | CONFIRMÉ par la relecture | Canaux Realtime broadcast publics sans autorisation : invitation d'appel usurpable (ring:<uid>), signalisation WebRTC injectable (call:<callId> devinable), frappe (typing:<convId>) écoutable |
| SUP-07 | **P1** | CONFIRMÉ par la relecture | Écritures de masse non bornées : notifications vers n'importe qui (contenu et lien libres, aucun rate-limit), telemetry_events et client_errors insérables par anon sans limite |
| SUP-08 | **P2** | CONFIRMÉ par la relecture | Schéma de production non reconstructible : 4 migrations enregistrées sur 64 fichiers et SCHEMA_PROD_REFERENCE.sql périmé depuis le 2026-08-17 |
| SUP-09 | **P2** | CONFIRMÉ par la relecture | UPDATE sans WITH CHECK sur events et post_comments : un co-organisateur peut s'approprier un événement (author_id), un auteur peut déplacer son commentaire vers un autre post |
| SUP-10 | **P1** | CONFIRMÉ par la relecture | Suppression de compte incomplète : résidus dans 16 tables et 6 dossiers Storage après delete-account |
| SUP-11 | **P3** | CONFIRMÉ par la relecture | Dette de policies : données du Carnet de voyage (retiré) toujours exposées publiquement, 5 jeux de policies dupliquées, 78 auth_rls_initplan, données orphelines |
| SUP-12 | **P3** | CONFIRMÉ par la relecture | Réglages Auth : protection contre les mots de passe compromis désactivée ; état du provider anonyme non vérifiable |
| SUP-13 | **P3** | CONFIRMÉ par la relecture | search_path non épinglé sur 3 fonctions exposées et pg_trgm dans public (WARN advisors, exploitabilité nulle aujourd'hui) |
| SUP-14 | **P3** | CONFIRMÉ par la relecture | Pages HTML du dépôt invitant à coller la clé service_role dans un navigateur |
| SUP-15 | **P3** | CONFIRMÉ par la relecture | Faux accusés de lecture : conv_reads acceptable pour toute conv_id sans appartenance |
| SUP-16 | **P3** | CONFIRMÉ par la relecture | Écritures qui échouent en silence : aucune policy UPDATE/DELETE sur conversations alors que le client les tente (description de groupe non persistée, conversation jamais supprimée côté serveur) |

### SUP-01 — Pièces jointes vocales de conversations privées dans un bucket public, listables avec la seule clé anon (R2 « P0 » ouvert depuis le 2026-08-08)

| Champ | Valeur |
|---|---|
| Identifiant | SUP-01 |
| Priorité retenue | **P0** (proposée par l'auditeur : P1) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Messagerie privée — vocaux et pièces jointes (bucket attachments) ; médias du bucket content |
| Résultat attendu | Un média d'une conversation privée n'est lisible que par ses membres (bucket privé + URL signée ou proxy), et la liste des objets n'est pas énumérable |
| Résultat observé | storage.buckets : attachments et content public=true, allowed_mime_types NULL, 50 Mo ; policy passio_media_read SELECT USING bucket_id IN (content, attachments) pour {public} → l'endpoint POST /storage/v1/object/list/attachments répond au rôle anon ; 12 objets attachments/conv_<id>/<epoch_ms>_voice.webm (audio/webm) ; 5 conv_messages portent une URL /object/public/attachments/. Aucune URL signée dans le code (createSignedUrl = stub hors-ligne app-08:2644) |
| Reproduction | 1) Avec la clé anon (publique, index.html/app-08:2552) : POST {url}/storage/v1/object/list/attachments body {"prefix":"attachments/","limit":100} → liste des dossiers conv_* et fichiers. 2) GET {url}/storage/v1/object/public/attachments/<chemin listé> → le vocal est servi sans authentification. (Non exécuté ici : proxy 403 ; étapes déduites de la policy et des paramètres de bucket) |
| Preuve | execute_sql storage.buckets (public=true ×2) ; pg_policies storage.objects passio_media_read ; storage.objects (25 chemins masqués, fonctions_realtime_storage_staging.md) ; js/app-09-boot-pwa.js:886,1599 getPublicUrl(attachments) ; .passio/audits/SECURITY_AUDIT.md S10 « ❌ P0 » ; KNOWN_RISKS R2 |
| Impact utilisateur et commercial | Confidentialité des messages privés rompue pour tout média : toute personne disposant de la clé anon (donc tout le monde) peut énumérer et écouter les vocaux des DM. Juridiquement (RGPD art. 32) et commercialement disqualifiant pour une messagerie |
| Visibilité dans le Centre de pilotage | non — aucun événement télémétrique ni panneau ne mesure les lectures Storage anonymes |
| Détection par la Sentinelle | non — la Sentinelle lit telemetry_events/client_errors, pas les logs Storage |
| Proposition de correction | Passer attachments en public=false ; lecture par createSignedUrl (TTL court) côté client au rendu du message, ou policy SELECT storage.objects sur attachments = is_conv_member(dossier[2], auth.uid()) ; remplacer getPublicUrl (app-09:886,1599) ; migrer les 5 messages portant une URL publique ; ajouter un cas dans authz-critical (tiers → 400 sur l'objet). Pour content, au minimum retirer la policy SELECT (le bucket public sert les URLs sans policy ; la policy n'ajoute que le LISTING) |
| Risque de régression | Moyen : les vocaux déjà envoyés avec URL publique cessent d'être lisibles sans migration des contenus ; cache du service worker et CDN à invalider ; risque SYNC-B64-005 (repli base64) si l'upload échoue |
| Effort estimé | 1 à 2 jours (migration + client + test authz) |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P1). Reproduction sur le SHA audité (HEAD f501fb7 = c8cb8e99 + 2 commits d'audit ; `git diff --stat c8cb8e99 HEAD -- js index.html migrations supabase` vide). 1) Preuve déposée preuves/supabase-isolation/policies.json:126 (source pg_policies, 2026-09-04) : `storage.objects` policy `passio_media_read` cmd SELECT roles {public} qual `(bucket_id = ANY (ARRAY['content','attachments']))` — aucune condition sur auth.uid()/appartenance → le listing `/storage/v1/object/list/attachments` est ouvert à anon. Corroboré par policies_liste_live.txt:110 et isolation_par_table.md:33 (70/70 objets lisibles par anon, 12 vocaux `attachments/conv_<id>/<ts>_voice.webm`, buckets public=true) et :40 (5 conv_messages portent une URL /object/public/attachments/). 2) Dépôt : migrations/migration_storage_buckets.sql:20-21 crée les DEUX buckets avec `public = true` et :31-32 la policy SELECT `bucket_id in ('content','attachments')` sans autre garde ; migration_storage_cloisonnement.sql:115-117 ne modifie que file_size_limit (le drapeau public n'est jamais remis à false). 3) Client : js/app-09-boot-pwa.js:1597-1599 (`storagePath = "attachments/" + convId + "/" + Date.now() + "_voice." + ext`, puis `getPublicUrl(storagePath)`) et :877-886 (pièces jointes, `getPublicUrl` + cdnUrl) — l'URL PUBLIQUE est écrite dans le message ; `createSignedUrl` n'existe qu'en stub hors-ligne app-08:2644-2645 (grep js/ : aucun autre usage). 4) tests/e2e/authz-critical.spec.js:339-341 ne couvre que l'ÉCRITURE cloisonnée (INSERT sur conv_absente) ; aucun cas de LECTURE/LISTING par un tiers. 5) Citations anciennes exactes : .passio/context/KNOWN_RISKS.md:9 (R2, « URLs signées (P0) ») et .passio/audits/SECURITY_AUDIT.md:23 (S10 ❌ P0). Appel REST réel (POST list avec la clé anon) : REFUSÉ par le classificateur de permissions (lecture prod interdite dans ce mode) → la reproduction en ligne reste déduite, mais policy + drapeau bucket + chemins d'objets + code client concordent sans ambiguïté. P1 maintenu (prioriser P0 si l'on suit SECURITY_AUDIT S10 ; 5 comptes, 12 vocaux : périmètre encore petit, mais disqualifiant avant ouverture publique). — Correction de formulation : Formulation juste ; deux précisions. (a) Dans « preuve », le chemin d'objet est `attachments/conv_<id>/…` DANS le bucket `attachments` (app-09:1597), donc le body de listing doit être `{"prefix":"attachments"}` (dossier de premier niveau) — l'étape 1 de reproduction le dit correctement mais la preuve peut le rendre explicite. (b) Dans « correction », la variante « policy SELECT = is_conv_member(dossier[2], auth.uid()) » ne suffit PAS tant que le bucket reste `public=true` : le endpoint /object/public/ sert les objets SANS évaluer la policy SELECT (la policy ne gouverne que list/download authentifiés) ; il faut d'abord `public=false`, puis URLs signées côté client — à écrire dans cet ordre. Ajouter que l'appel REST anon n'a pas été exécuté ici (refus de permission), pas seulement « proxy 403 ».
- **impact** → CONFIRMÉ (priorité proposée P0). Défaut réel, mais sous-classé : il relève de P0, pas de P1. 1) Ce n'est PAS une décision produit documentée : le dépôt lui-même le classe P0 à quatre endroits — .passio/context/KNOWN_RISKS.md:9 (R2 « URLs signées (P0) »), .passio/audits/SECURITY_AUDIT.md:23 et :35 (S10 « ❌ P0 »), .passio/context/SECURITY_MODEL.md:14 (« À faire (P0) »), .passio/adr/ADR-004-media-storage-no-base64.md:10 (« URLs signées à mettre en place (P0) »). Le commentaire de migrations/migration_storage_buckets.sql:30 (« Lecture publique des deux buckets ») décrit une implémentation, pas une acceptation du risque ; aucun ADR n'assume la lecture publique des pièces jointes de DM (KNOWN_RISKS n'assume que follows/event_attendees). 2) Critères du GO grande échelle : « isolation des comptes non prouvée » interdit le GO ; ici elle est INFIRMÉE pour les médias de conversations privées (preuves/supabase-isolation/isolation_par_table.md:33 : storage.objects anon = 70/70, attendu 0 pour attachments ; fonctions_realtime_storage_staging.md:30-33 : buckets public=true, policy SELECT `bucket_id in (content, attachments)` pour {public}, 12 objets attachments/conv_*/…_voice.webm, 5 conv_messages portant une URL /object/public/attachments/). La fuite de DM textuels du 2026-08-09 a été traitée en « FUITE CRITIQUE » (KNOWN_RISKS.md:21) ; la même donnée sous forme vocale ouverte à anon ne peut pas être d'un cran inférieur. 3) Le code confirme l'absence de toute alternative : js/app-09-boot-pwa.js:886 et :1599 (getPublicUrl sur `attachments`), app-08:2644 (createSignedUrl = stub hors-ligne uniquement). La reproduction n'a pas été exécutée (proxy 403), mais elle est déterministe : policy SELECT accordée à {public} → l'endpoint de listing répond à anon ; bucket public → l'objet est servi sans jeton. Le seul rempart restant serait la non-devinabilité du chemin (17 car. + epoch ms), que le listing annule. 4) Impact : confidentialité de la messagerie privée rompue pour toute personne disposant de la clé anon (publiée dans index.html/app-08:2552), RGPD art. 32 — cela « bloque la commercialisation » (définition de P0), pas seulement « avant lancement public ». Invisible du pilotage ET de la Sentinelle (second critère d'interdiction cumulé). git status --short : vide. — Correction de formulation : Priorité : P1 → **P0** (bloque la commercialisation ; le dépôt le classe déjà P0 dans KNOWN_RISKS R2, SECURITY_AUDIT S10, SECURITY_MODEL et ADR-004 — le finding cite ces sources mais n'en tire pas la priorité). Ajouter dans « impact » que ce défaut coche DEUX critères d'interdiction du GO grande échelle : isolation des comptes infirmée (pas seulement « non prouvée ») ET fonction critique invisible du pilotage et de la Sentinelle. Dans « observé », préciser que c'est le LISTING (policy SELECT pour {public}) qui rend l'exploitation triviale — sans lui, le bucket public restait « peu devinable » ; la correction minimale d'urgence (retirer `attachments` de passio_media_read + passer le bucket en privé) est à distinguer de la correction complète (URLs signées / policy is_conv_member). Le champ « confiance : CONFIRMÉ » devrait dire « CONFIRMÉ par inspection (policy + bucket), reproduction HTTP non exécutée (proxy) ».
- **contexte** → CONFIRMÉ (priorité proposée P0). Risque CONNU et ASSUMÉ, jamais traité sur le SHA audité (c8cb8e99, ancêtre de HEAD f501fb7 qui n'ajoute que des fichiers d'audit) : `.passio/context/KNOWN_RISKS.md:9` (R2, « URLs signées (P0) »), `.passio/audits/SECURITY_AUDIT.md:23,35` (S10 ❌ P0), `.passio/context/SECURITY_MODEL.md:14` (« À faire (P0) »), `.passio/context/CURRENT_PRIORITIES.md:22`, `.passio/adr/ADR-004-media-storage-no-base64.md:10` (conséquence négative explicitement acceptée « P0 »), `.claude/skills/storage/SKILL.md:27`. Aucune migration du dépôt ne passe un bucket en `public=false` ni ne restreint la lecture : `migrations/migration_storage_buckets.sql:20-32` crée les deux buckets publics ET la policy `passio_media_read` SELECT sans condition de rôle (commentaire : « en plus du flag public, pour les SDK ») ; `migration_storage_cloisonnement.sql` (appliquée 2026-08-17) n'a cloisonné que INSERT/UPDATE et son propre relevé ③ (« relire les deux sans aucun jeton → HTTP 200 ») documente que la lecture est restée ouverte. Preuve prod déposée : `preuves/supabase-isolation/policies_liste_live.txt:110` (SELECT|storage.objects|passio_media_read) et `fonctions_realtime_storage_staging.md:30-33`. Code vérifié : `js/app-09-boot-pwa.js:886` et `:1599` utilisent `getPublicUrl("attachments")`, `createSignedUrl` n'existe qu'en stub hors-ligne `js/app-08-ui-modals-tour.js:2644`. Le finding apporte un élément NEUF par rapport à R2 : l'ÉNUMÉRATION par anon via `/object/list/attachments` (R2 ne parlait que d'URL devinée ; le chemin `conv_<17 car.>/<epoch>` n'est peu devinable que sans listing). Aucune contradiction avec un invariant ; la correction proposée est compatible : ADR-003 (RLS = frontière), ADR-004 (médias en Storage), et `storage_chemin_autorise` réutilise déjà `is_conv_member((storage.foldername(name))[2], auth.uid())` — le même prédicat sert pour SELECT. Retirer la policy SELECT publique ne casse aucun consommateur applicatif : les seuls appels de listing (`scripts/sauvegarde-donnees.js:152`, `scripts/purge-e2e-storage.js:69`, `supabase/functions/delete-account/index.ts:76`) passent par service_role. `git status --short` : vide. — Correction de formulation : 1) Formulation : dire explicitement « risque connu et assumé depuis le 2026-08-08, classé P0 par le projet lui-même dans QUATRE documents (KNOWN_RISKS R2, SECURITY_AUDIT S10, SECURITY_MODEL, ADR-004) et resté ouvert sur le SHA audité ; la migration de cloisonnement du 2026-08-17 a fermé l'ÉCRITURE seulement et a mesuré la lecture anonyme à 200 sans la traiter ». 2) Priorité : le finding titre « R2 P0 » mais propose P1 sans justifier la rétrogradation, alors que son propre impact (« commercialement disqualifiant », RGPD art. 32) correspond à la définition P0 du cadre (bloque la commercialisation) — soit remonter à P0, soit écrire pourquoi P1 (beta privée à 5 comptes, 12 vocaux). 3) Ce qui est neuf vs R2 (à mettre en avant) : le LISTING anon rendu possible par `passio_media_read` — sans lui les chemins sont peu devinables ; c'est la sous-mesure la moins coûteuse (retirer/restreindre la policy SELECT) à séparer de la migration vers bucket privé + URLs signées. 4) Correction : préciser que la DDL passe par le canal ③ d'ADR-012 (psql/SQL Editor, jamais la CI) avec fichier `migrations/` + `migration-checker` (R3 : divergence 4 migrations enregistrées / 64 fichiers) ; que la lecture `content` doit rester publique pour avatars/covers/passion_* mais que les posts de comptes `is_private` y sont AUSSI exposés (skill storage:27) — deux périmètres à distinguer ; que « ajouter un cas dans authz-critical » (tests/) est licite pour un correctif humain (l'exclusion de tests/ ne vise que la réparation automatique du dashboard). 5) Risque de régression à compléter : `app-09:886` retombe DÉJÀ en base64 en DB quand l'upload échoue, ce qui contredit ADR-004/CLAUDE.md (« jamais de base64 en DB ») — le durcissement ne doit pas augmenter la fréquence de ce repli. 6) Doublons probables : SUP-10 (résidus Storage après delete-account : voisin, pas doublon) ; aucun domaine « sécurité » n'a encore déposé de résultat — attendre un SEC-xx reprenant S10/R2 et fusionner sous un seul identifiant.

### SUP-02 — conv_reads en lecture publique : appartenance et activité des conversations privées lisibles par anon et diffusées en realtime à tous

| Champ | Valeur |
|---|---|
| Identifiant | SUP-02 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Accusés de lecture ✓✓ de la messagerie (table conv_reads, canal realtime:db) |
| Résultat attendu | Qui a lu quelle conversation et quand n'est lisible que par les membres de la conversation (comme conv_members) |
| Résultat observé | policy reads_select USING (true) ; 34 lignes (conv_id, user_id, last_read_at) couvrant 19 conversations ; émulation anon = 34, tiers = 34 (attendu 0). La table est dans la publication realtime et app-08-ui-modals-tour.js:5039-5052 s'abonne à postgres_changes sur conv_reads sans filtre → chaque client reçoit en direct les lectures de tout le monde. Combinée à is_conv_member exposée à anon (SUP-05), elle reconstruit la carte des DM (qui parle avec qui, à quelle heure) |
| Reproduction | Émulation : select count(*) from public.conv_reads (qual = true) → 34 pour anon comme pour un tiers. REST (non exécuté, proxy 403) : GET /rest/v1/conv_reads?select=conv_id,user_id,last_read_at avec la clé anon |
| Preuve | policies.json conv_reads.reads_select ; isolation_par_table.md ligne conv_reads ; pg_publication_tables (conv_reads) ; js/app-08-ui-modals-tour.js:5039 ; migrations/migration_conv_reads.sql |
| Impact utilisateur et commercial | Métadonnées de messagerie privée (graphe des conversations, horaires d'activité) exposées à tout internaute ; charge realtime inutile (N clients × chaque lecture) |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | DROP POLICY reads_select ; CREATE POLICY … FOR SELECT USING (is_conv_member(conv_id, (select auth.uid())::text)) ; exiger l'appartenance aussi à l'INSERT/UPDATE (27 lignes existantes sont posées par des non-membres) ; ajouter conv_reads au bloc 10 d'authz-critical |
| Risque de régression | Faible : supaLoadOtherReads lit les lectures des autres membres d'une conv dont on est membre — reste vrai sous la nouvelle policy ; realtime conv_reads filtré par RLS continue de livrer aux membres |
| Effort estimé | 2 heures (migration + test) |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P1). 1. Policy : preuves/supabase-isolation/policies.json (dump pg_policies du 2026-09-04, projet prod) → {"t":"conv_reads","p":"reads_select","cmd":"SELECT","roles":"{public}","qual":"true"} ; policies_liste_live.txt:81 « SELECT|public.conv_reads|reads_select ». Le qual `true` est bien celui du dépôt : migrations/migration_conv_reads.sql:13 `CREATE POLICY "reads_select" ON conv_reads FOR SELECT USING (true);` et migrations/SCHEMA_PROD_REFERENCE.sql:595 le liste. Aucune migration ultérieure ne le resserre (grep reads_select sur migrations/ ne rend que ce fichier). 2. Grant : policies.json:135 « grants_table_anon : arwdDxtm sur toutes les tables public sauf conv_members/conv_messages/conversations (pas d'INSERT) et user_safety » → conv_reads est lisible par anon (grant + RLS USING true), donc l'émulation « anon = tiers = total » est correcte par construction, pas seulement par le comptage. 3. Contraste avec les tables sœurs : conv_messages_select_member, conv_members_select_member, conversations_select_member portent toutes `is_conv_member(conv_id, (select auth.uid())::text)` (policies.json) — conv_reads est la SEULE table de la messagerie sans garde d'appartenance, ce qui prouve que l'« attendu » du finding est le contrat réel du domaine. 4. Realtime : ref_cols.txt / fonctions_realtime_storage_staging.md:23 → conv_reads dans la publication supabase_realtime ; js/app-08-ui-modals-tour.js:5043 (SHA c8cb8e99, vérifié par `git show`) : `dbChan.on("postgres_changes", { event: "*", schema: "public", table: "conv_reads" }, …)` sans `filter`, sur le canal partagé « realtime:db » ouvert par supaSubscribe(). Avec USING (true), le filtrage RLS de postgres_changes ne retient rien : tout client abonné reçoit toutes les lectures (le tri `r.user_id === MY_UID` est côté client, après réception). 5. Les colonnes exposées (conv_id, user_id, last_read_at — ref_cols.txt:16) suffisent à relier deux user_id partageant un conv_id avec l'heure de lecture : la reconstruction du graphe des DM est réelle. Limites : les chiffres 34 lignes / 19 conversations / 27 lignes posées par des non-membres viennent de l'execute_sql de l'auditeur (isolation_par_table.md:21,39) et n'ont pas été re-exécutés ici (connecteur interdit) ; l'accès REST anon direct est bloqué par le proxy (403). Le défaut STRUCTUREL (policy + grant + publication + abonnement non filtré) est, lui, reproduit intégralement depuis le code et les dumps. Pas d'écriture : `git status --short` vide. — Correction de formulation : Formulation exacte ; deux précisions à apporter. (1) Risque de régression « Faible » à nuancer : isolation_par_table.md:39 relève 113 conversations sur 117 SANS conv_members et 27 conv_reads posés par des non-membres — sous une policy `is_conv_member`, ces accusés de lecture deviennent invisibles (✓✓ absent) pour ces conversations. C'est cohérent (leurs conv_messages sont déjà illisibles sous conv_messages_select_member), mais le finding doit le dire : la correction doit être accompagnée d'une vérification que la création de conversation écrit bien ses deux lignes conv_members (app-08:4512-4513) et d'un rattrapage des orphelines, sinon les ✓✓ disparaissent sur l'existant. (2) Préciser que le grant anon SELECT (policies.json:135) fait partie de la cause : sans lui, USING (true) ne suffirait pas ; la correction peut aussi révoquer SELECT à anon sur conv_reads (aucun chemin client anon ne lit cette table : supaMarkRead/supaLoadOtherRead exigent MY_UID, app-08:4651/4663).
- **impact** → CONFIRMÉ (priorité proposée P1). 1) Le défaut est réel et non une décision produit : migrations/migration_conv_reads.sql:1-3 annonce l'intention « chaque MEMBRE voit l'état de lecture des autres », mais ligne 13 implémente `CREATE POLICY "reads_select" ... USING (true)` — c'est un raccourci, pas un choix documenté. preuves/supabase-isolation/policies.json confirme en prod : conv_reads.reads_select qual=true, roles={public} (donc anon inclus). 2) .passio/context/KNOWN_RISKS.md:21 (correctif du 2026-08-09) scelle conversations/conv_members/conv_messages et déclare explicitement follows/event_attendees comme « choix assumé » ; conv_reads n'y figure NULLE PART → oubli, pas décision. Aucune mention dans CLAUDE.md, docs/lots-ui, ADR. 3) Exposition realtime confirmée : js/app-08-ui-modals-tour.js:5043 s'abonne à postgres_changes sur conv_reads sans filtre ; la table est dans supabase_realtime (migration_conv_reads.sql:21-24). Avec USING(true) le filtrage RLS du realtime ne retire rien → chaque client reçoit les lectures de tous. 4) Priorité : P1 est juste, pas P0. La fuite porte des MÉTADONNÉES (conv_id, user_id, last_read_at) et non le contenu des messages (conv_messages reste scellé, isolation_par_table.md : 0/68 pour anon et tiers). C'est une atteinte à la confidentialité du graphe de DM (RGPD, « qui parle avec qui, quand ») inacceptable pour un lancement public, mais elle ne bloque pas la commercialisation au sens P0 (aucun contenu privé, aucune écriture croisée, 5 comptes réels). Elle nourrit cependant le critère d'interdiction du GO « isolation des comptes non prouvée » : tant que ce finding est ouvert, l'isolation de la messagerie est démontrée DÉFAILLANTE sur une de ses quatre tables. 5) Aucune couverture de test : grep conv_reads sur tests/e2e/*.spec.js = 0 résultat (authz-critical ne la connaît pas) — la correction proposée d'ajouter conv_reads au bloc d'invariants est pertinente. — Correction de formulation : Le champ « risque_regression : Faible » est sous-estimé et doit être corrigé. Les preuves du domaine lui-même (isolation_par_table.md:39) montrent que 113 conversations sur 117 n'ont AUCUN membre dans conv_members et que 27 des 34 conv_reads sont posés par un non-membre : une policy SELECT/INSERT fondée sur is_conv_member(conv_id, auth.uid()) masquerait donc la majorité des accusés de lecture existants et REFUSERAIT le upsert de supaMarkRead (app-08:4652, erreur avalée par try/catch → ✓✓ silencieusement mort) pour toute conversation dont les membres ne sont pas inscrits dans conv_members. Le correctif doit être précédé d'un rattrapage de conv_members (ou d'une policy tolérante fondée sur la conversation ET ses membres), et le risque doit être requalifié « Moyen : dépend de l'intégrité de conv_members ». L'effort « 2 heures » devient plutôt 0,5 à 1 jour (migration + backfill + test authz-critical + suite multi-comptes). Le reste du finding (attendu/observé/preuve/impact/priorité P1) est exact.
- **contexte** → CONFIRMÉ (priorité proposée P1). 1. Non traité sur le SHA audité : migrations/migration_conv_reads.sql:13 pose encore `CREATE POLICY "reads_select" ON conv_reads FOR SELECT USING (true)` ; preuves/supabase-isolation/policies.json (conv_reads.reads_select, qual "true") et migrations/SCHEMA_PROD_REFERENCE.sql:595-597 le confirment côté prod ; aucune autre migration ne touche conv_reads (grep migrations/ : seul migration_conv_reads.sql + le schéma de référence). 2. C'est un SURVIVANT de la remédiation du 2026-08-09 (.passio/context/KNOWN_RISKS.md:21) : migration_rls_private_dms_stories.sql a fermé conv_messages/conv_members/conversations (helper is_conv_member) mais n'a pas visité conv_reads (grep : 0 occurrence). La doc du même fichier n'assume en « choix » que follows/event_attendees ; conv_reads n'y figure pas → risque NI connu NI assumé. 3. Le seul « choix » écrit est le commentaire d'en-tête de migration_conv_reads.sql:2 (« Lecture publique (chaque membre voit l'état de lecture des autres) ») : intention = membres, implémentation = tout le monde. Le finding ne contredit aucun invariant ; il l'applique. 4. Aucune fiche de docs/PIEGES_CONNUS.md, CLAUDE.md, ADR ni docs/lots-ui ne mentionne conv_reads (seuls docs/HISTORIQUE_PROJET.md:27 — fusion des canaux realtime — et docs/RAPPORT_SESSION_2026-07-02.md:52 — supaTs). TECH_DEBT.md n'existe pas à la racine. 5. Correction compatible : DDL par canal ③ d'ADR-012 (psql/SQL Editor, jamais CI) ; le client (js/app-08-ui-modals-tour.js:4661-4664 supaLoadOtherRead .eq(conv_id) et 5039-5052 handler realtime) ne lit que des convs dont il est membre → tient sous la nouvelle policy. Ajouter un bloc à tests/e2e/authz-critical.spec.js est un geste développeur normal (l'interdit « tests/ » ne vise que la réparation automatique du dashboard). 6. Verrou absent : grep tests/ conv_reads → 0 fichier ; le bloc 10 (« Sans jeton, aucune donnée privée », authz-critical.spec.js:276) ne couvre pas conv_reads. 7. git status --short : vide. — Correction de formulation : Formulation à ajuster : (a) dire que c'est un SURVIVANT de la fermeture des DM du 2026-08-09 (KNOWN_RISKS.md:21 — conv_messages/conv_members/conversations corrigés, conv_reads oublié) et que l'intention documentée était bien « membres » (commentaire migration_conv_reads.sql:2), donc défaut d'implémentation, pas choix assumé ; (b) le risque de régression « faible » doit mentionner que 113/117 conversations n'ont AUCUN membre et 27/34 conv_reads viennent de non-membres (isolation_par_table.md:39) : ces accusés deviendront invisibles après correctif — sans perte réelle car conv_messages exige déjà l'appartenance, mais à écrire ; (c) la policy corrigée doit utiliser le schéma où vit réellement is_conv_member en prod (public vs passio_private), et garder la forme `(select auth.uid())` (advisor auth_rls_initplan). DOUBLON INTERNE : SUP-15 (P3, « faux accusés de lecture », INSERT/UPDATE sans appartenance, « même migration que SUP-02 ») recouvre le volet « exiger l'appartenance aussi à l'INSERT/UPDATE » déjà inclus dans la correction de SUP-02 → fusionner SUP-15 dans SUP-02 ou retirer ce volet de SUP-02. Lien (pas doublon) : SUP-05 (is_conv_member exposée à anon). Aucun autre domaine (carto, code-nettoyage, pilotage-sentinelle) ne rapporte conv_reads.

### SUP-03 — conv_messages « Update propre » sans WITH CHECK : un utilisateur peut déplacer son propre message dans n'importe quelle conversation (injection dans des DM dont il n'est pas membre, contournement du blocage)

| Champ | Valeur |
|---|---|
| Identifiant | SUP-03 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Messagerie privée — modification de message |
| Résultat attendu | Un UPDATE de message ne peut jamais changer sa conversation ; seul le contenu de son propre message dans une conversation dont on est membre est modifiable |
| Résultat observé | pg_policies : UPDATE conv_messages qual = (from_id = auth.uid()::text), with_check = NULL → PostgreSQL applique le USING à la nouvelle ligne (from_id doit rester le sien) mais conv_id, content, created_at sont libres. L'INSERT exige is_conv_member (conv_messages_insert_member), l'UPDATE non. Les conv_id cibles sont obtenables via conv_reads (SUP-02) ou events.conv_id public (format evgrp_<event_id>, 1 en prod). Les membres de la conversation cible lisent le message (conv_messages_select_member) ; le trigger broadcast (AFTER INSERT) ne le diffuse pas, mais le prochain chargement l'affiche |
| Reproduction | Compte B : créer une conversation où il est membre (conversations_insert_creator + conv_members via is_conversation_creator), insérer un message m ; PATCH /rest/v1/conv_messages?id=eq.m body {"conv_id":"<conv de A et C>"} avec le JWT de B → 1 ligne modifiée (attendu 0). Non exécuté ici (aucune écriture autorisée) ; déduit de la sémantique documentée des policies UPDATE sans WITH CHECK |
| Preuve | policies.json conv_messages « Update propre » (check null) vs conv_messages_insert_member ; base : 2 messages existants proviennent déjà d'un non-membre ; docs PostgreSQL CREATE POLICY : « If no WITH CHECK is specified, USING is used for new rows » |
| Impact utilisateur et commercial | Harcèlement ciblé et contournement du blocage (is_blocked_with n'est vérifié qu'à l'INSERT de conv_members) ; possibilité d'injecter du contenu dans une conversation de groupe d'événement ; perte de confiance dans la messagerie |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non — aucun signal (le message ressemble à un message normal) |
| Proposition de correction | ALTER POLICY « Update propre » ON conv_messages … WITH CHECK (from_id = (select auth.uid())::text AND is_conv_member(conv_id, (select auth.uid())::text)) ; idéalement rendre conv_id immuable par trigger (comme posts_freeze_author) ; ajouter un bloc dans authz-critical (PATCH conv_id → 0 ligne) |
| Risque de régression | Nul pour l'app : le client ne modifie jamais conv_id (grep conv_messages update = édition de contenu uniquement) |
| Effort estimé | 1 heure (migration + test) |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P1). Chaque maillon de la chaîne est vérifié contre les preuves déposées (dump pg_policies du 2026-09-04) et le code du SHA audité ; seule l'exécution du PATCH n'a pas pu être faite (aucune écriture autorisée), mais la sémantique PostgreSQL est déterministe. 1. preuves/supabase-isolation/policies.json (« policies », t=conv_messages) : « Update propre » cmd=UPDATE roles={public} qual=(from_id = auth.uid()::text) check=null ; l'INSERT « conv_messages_insert_member » exige is_conv_member(conv_id, uid), l'UPDATE non. La liste « policies_update_sans_with_check » du même dump contient « conv_messages.Update propre ». policies_liste_live.txt:113 confirme (UPDATE|public.conv_messages|Update propre). Docs PostgreSQL CREATE POLICY : sans WITH CHECK, l'USING sert de contrôle sur la nouvelle ligne → seul from_id est contraint, conv_id/content/created_at libres. 2. Grant : policies.json « grants_table_anon » = arwdDxtm sur toutes les tables public sauf INSERT sur conv_members/conv_messages/conversations → le droit UPDATE (w) sur conv_messages est bien accordé à anon/authenticated ; aucun FORCE RLS. 3. Aucune garde de colonne : fonctions_realtime_storage_staging.md:20 — le seul trigger sur conv_messages est broadcast_conv_message_users_trigger, AFTER INSERT (migrations/migration_realtime_user_topic.sql:40-42) ; aucun trigger « freeze » comme trg_posts_freeze_author. ref_cols.txt:15 : conv_id est un text avec FK vers conversations(id) → il suffit d'un conv_id existant, obtenable via conv_reads (reads_select USING true, policies.json) ou events.conv_id (ref_cols.txt:21, colonne publique). 4. Prérequis trivial : tout compte ayant déjà envoyé un message dans une conversation dont il est membre possède une ligne à déplacer (pas besoin de créer une conversation). 5. Effet côté lecteurs : js/app-08-ui-modals-tour.js:4566-4569 (supaLoadMessages : select … .eq("conv_id", convId)) et :4692-4695 (aperçus, .in("conv_id", convIds)) chargent par conv_id sous conv_messages_select_member → les membres de la conversation cible voient le message déplacé au prochain chargement. is_blocked_with n'est vérifié qu'au WITH CHECK de conv_members INSERT (policies.json) → contournement du blocage réel. 6. Non-régression : grep js/ → 0 appel .update() sur conv_messages (les seuls accès sont insert/delete/select) ; authz-critical.spec.js ne teste que SELECT (l.175) et PATCH sur posts (l.144-148), jamais PATCH conv_id → le défaut est hors filet de tests. git status --short : vide (aucune modification). — Correction de formulation : Le fond tient ; trois retouches de formulation : (1) « risque_regression » dit « grep conv_messages update = édition de contenu uniquement » — en réalité le client ne fait AUCUN update sur conv_messages (0 occurrence dans js/ ; la suppression passe par delete + insert d'une pierre tombale, app-04:4227-4231), ce qui renforce la nullité du risque ; (2) « 2 messages existants proviennent déjà d'un non-membre » (isolation_par_table.md:39) n'est pas une preuve de CE défaut (peut venir de l'ancienne policy INSERT « Ecriture propre » sans is_conv_member, ou d'un membre parti) — à citer comme indice, pas comme preuve ; (3) « confiance: CONFIRMÉ » devrait se lire « CONFORME PAR INSPECTION / non exécuté » selon le vocabulaire du cadre, le PATCH n'ayant pas été joué ; le correctif proposé (WITH CHECK from_id = auth.uid() AND is_conv_member(conv_id, auth.uid()) + trigger d'immutabilité de conv_id + bloc authz-critical) est adapté.
- **impact** → CONFIRMÉ (priorité proposée P1). Défaut réel, non documenté comme choix produit : preuves/supabase-isolation/policies.json → conv_messages « Update propre » cmd=UPDATE roles={public} qual=(from_id = auth.uid()::text) check=null, alors que conv_messages_insert_member exige is_conv_member(conv_id, uid). Sémantique PostgreSQL : sans WITH CHECK, USING s'applique à la nouvelle ligne → seul from_id est contraint, conv_id reste libre. migrations/migration_rls_v2_appliquee.sql:94 crée bien cette policy sans WITH CHECK ; migration_ts_serveur_age_blocage.sql:284-294 n'a durci que l'INSERT (REVOKE/GRANT INSERT seulement) et fonctions_realtime_storage_staging.md:43 note des grants arwdDxtm larges (UPDATE inclus) ; aucun trigger BEFORE UPDATE sur conv_messages (seul broadcast AFTER INSERT, ligne 20). Le vecteur est exploitable par tout compte authentifié disposant d'un conv_id (fourni par conv_reads en lecture publique, SUP-02, ou par le format evgrp_<event_id> des events publics). KNOWN_RISKS.md:21 ne cite pas ce cas ; aucun ADR ni doc ne l'assume. Le client n'utilise jamais ce chemin (grep js/ : seul un .delete() sur conv_messages, app-04:4227 ; aucun .update()) et authz-critical.spec.js ne le couvre pas (blocs 6 et 277 : lecture seulement). Priorité : P1 est juste. Pas P0 car (a) aucune fuite de confidentialité — c'est une injection/intégrité, la lecture reste scellée (isolation_par_table.md:20 et 36 : 0 ligne pour un tiers) ; (b) l'attaquant n'est pas anonyme (from_id = son uuid, compte à e-mail confirmé) et son message est supprimable/signalable ; (c) le gain réel par rapport à ce qu'un compte peut déjà faire (créer une conversation et y ajouter A via is_conversation_creator) se limite au contournement du blocage et à l'injection dans les conversations de tiers/de groupe d'événement ; (d) correctif d'1 h sans régression client ; (e) fermer SUP-02 retire la principale source de conv_id. En revanche le contournement de blocage touche le critère « sécurité IRL ou modération insuffisante » du GO grande échelle → obligatoire avant lancement public, donc P1, pas P2. — Correction de formulation : Deux retouches de formulation : (1) « confiance: CONFIRMÉ » doit devenir « CONFORME PAR INSPECTION / PROBABLE » — la reproduction est explicitement non exécutée, la conclusion est déduite de la sémantique documentée (fiable) mais le grant UPDATE à authenticated n'est cité qu'indirectement (arwdDxtm) ; (2) retirer de la preuve « 2 messages existants proviennent déjà d'un non-membre » : isolation_par_table.md:39 montre 113 conversations sur 117 sans aucun membre (orphelines) et un INSERT ouvert avant le durcissement, donc ces 2 lignes ne prouvent en rien une exploitation de l'UPDATE ; les citer affaiblit le finding. Impact à préciser : atteinte d'INTÉGRITÉ (injection dans un fil privé, contournement du blocage), pas de confidentialité — l'attaquant reste identifié par from_id. Correction à compléter : la policy est TO public ; la réécrire TO authenticated avec WITH CHECK (from_id = (select auth.uid())::text AND is_conv_member(conv_id, (select auth.uid())::text)), et ajouter un cas authz-critical PATCH conv_id → 0 ligne. Priorité P1 maintenue.
- **contexte** → CONFIRMÉ (priorité proposée P1). Finding NOUVEAU, nulle part connu ni décidé : aucune mention de « Update propre » sans WITH CHECK, ni d'immuabilité de conv_id, dans CLAUDE.md, .passio/context/KNOWN_RISKS.md (R1–R11 : rien sur l'UPDATE de conv_messages ; la remédiation du 2026-08-09 ne traite que le SELECT « USING true »), .passio/context/TECH_DEBT.md (aucune occurrence de conv_messages/RLS), docs/PIEGES_CONNUS.md, ni les ADR (grep « conv_messages|with check » sur .passio/adr → seul ADR-005 sur les timestamps). État sur le SHA audité : la policy vient de migrations/migration_rls_v2_appliquee.sql:94 (`CREATE POLICY "Update propre" ON conv_messages FOR UPDATE USING (from_id = auth.uid()::text)`, sans WITH CHECK) et migrations/SCHEMA_PROD_REFERENCE.sql:589-591 (« check : - ») ; le dump live preuves/supabase-isolation/policies.json:39 confirme `check:null`. Aucune migration ultérieure ne la remplace (migration_fonctions_rls_hors_schema_expose.sql:153-162 et migration_ts_serveur_age_blocage.sql:284-294 ne redéfinissent que SELECT et INSERT). Aucun trigger BEFORE UPDATE sur conv_messages (fonctions_realtime_storage_staging.md:20 : broadcast AFTER INSERT seulement ; le seul freeze existant est posts_freeze_author). Toujours OUVERT. Prémisse « conv_id cible obtenable » vérifiée : events.conv_id existe (ref_cols.txt:21) et events est en SELECT USING true (policies.json:58-59). Le projet connaît DÉJÀ ce mécanisme exact et l'a corrigé ailleurs : docs/PIEGES_CONNUS.md:70 « Un WITH CHECK sur can_edit_post(id) ne protège PAS la propriété → le trigger posts_freeze_author rend author_id immuable » (migration_carnet_collaborators.sql:75). La correction proposée (WITH CHECK + trigger d'immuabilité) reproduit ce précédent : compatible avec les invariants, is_conv_member est déjà exécutable par authenticated (fonctions_realtime_storage_staging.md:9), aucune porte ouverte, ADR-009 non concerné. Risque de régression client confirmé nul : grep js/ → aucun `.update()` sur conv_messages (app-04:4227/4231 = delete+insert de pierre tombale, app-04:4281/4622, app-05:1340, app-08:4552, app-09 = insert ; app-08:4566/4692 = select). Git status : propre (0 ligne). — Correction de formulation : Formulation à ajuster, fond inchangé : (1) préciser que ce n'est PAS un risque connu/assumé (absent de KNOWN_RISKS, TECH_DEBT, PIEGES_CONNUS, ADR) mais que le dépôt a déjà traité le même mécanisme sur posts (PIEGES_CONNUS.md:70, posts_freeze_author) — la correction doit citer ce précédent comme modèle. (2) La correction est du DDL : par ADR-012 elle passe par psql/SQL Editor + fichier migrations/, JAMAIS depuis la CI ni via `supabase db query --linked` ; le finding doit le dire. (3) La policy est `roles {public}` : mentionner que l'UPDATE anon est de facto neutralisé par from_id = auth.uid() (NULL), donc surface = comptes authentifiés uniquement. (4) Le volet « contournement du blocage » recoupe MSG-10 (messagerie-notifs : conv_messages_insert_member n'inclut pas is_blocked_with) — ce n'est pas un doublon strict (MSG-10 = INSERT par un membre bloqué, SUP-03 = UPDATE par un NON-membre), mais les deux corrections touchent les mêmes policies conv_messages et devraient être livrées dans la même migration et le même bloc authz-critical. Effort « 1 h » plausible ; l'ajout dans tests/e2e/authz-critical.spec.js est légitime (l'exclusion de tests/ ne vise que la réparation automatique de la Sentinelle).

### SUP-04 — Un seul projet Supabase pour le dev, les previews de PR, les tests CI à comptes réels et la production (staging séparé : NON)

| Champ | Valeur |
|---|---|
| Identifiant | SUP-04 |
| Priorité retenue | **P0** (proposée par l'auditeur : P1) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Infrastructure de données — cycle de vie dev/test/preview/prod |
| Résultat attendu | Un projet de staging (schéma identique, données factices) reçoit les previews, les suites e2e qui écrivent et les essais de migration ; la production ne reçoit que du trafic réel |
| Résultat observé | Un seul project_ref (njkiyoklssvefstljemx) dans js/app-08:2551, index.html, _headers, netlify.toml, dashboard/.env.example, tests/e2e/*.js et scripts/*.html ; aucune variable de staging. deploy.yml:530-586 : la preview Netlify de chaque PR est le même dist → même base prod. deploy.yml:301-357 : « Suites production (comptes réels) » (PASSIO_E2E_PROD=1, --project=prod) crée 2+ comptes %@passio-e2e.test, des posts, des lives, des objets Storage EN PROD à chaque push, puis purge avec la clé service_role. 113 conversations sur 117 sont orphelines (sans membre), trace probable de ces cycles |
| Reproduction | grep -rn supabase.co js/ dashboard/ tests/ .github/ \| grep -v node_modules → un seul ref ; lire deploy.yml:301-357 et :530-586 |
| Preuve | fonctions_realtime_storage_staging.md § Staging ; PASSIO_PRODUCTION_READINESS.md:35-40 (« bloqués par une base qui ne soit pas la production ») ; ADR-012 (le canal ③ DDL n'a aucune cible de répétition) |
| Impact utilisateur et commercial | Toute migration (RLS, DDL) est essayée directement en prod ; un test rouge ou une purge ratée laisse des données de test visibles des vrais utilisateurs (comptes e2e, posts « authz canary ») ; impossible de répéter une restauration ; critère d'interdiction du GO grande échelle selon le mandat |
| Visibilité dans le Centre de pilotage | partiel — le dashboard exclut les comptes de test des KPI (DASH_ONLY_PROD_EVENTS, testusers) mais ne signale pas leur présence en prod |
| Détection par la Sentinelle | non |
| Proposition de correction | Créer un second projet Supabase (staging) alimenté par SCHEMA_PROD_REFERENCE régénéré + migrations rejouées ; router previews et suites prod dessus (SUPABASE_URL/ANON par environnement, injectés au build) ; garder en prod uniquement des tests en lecture ou un smoke unique post-déploiement ; c'est aussi la base cible de la restauration (readiness §35) |
| Risque de régression | Moyen : le build inline la clé anon (scripts/build.js) → il faut paramétrer app-08 par environnement sans casser la CSP (_headers) ; les suites prod devront viser le staging |
| Effort estimé | 3 à 5 jours (création, schéma, CI, CSP, docs) |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P1). Reproduit sur l'arbre audité (branche audit à f501fb7 ; `git diff --stat c8cb8e99 HEAD` VIDE sur js/app-08, deploy.yml, index.html, _headers, netlify.toml, scripts/build.js, playwright.config.js, tests/e2e/compte-e2e.js, dashboard/.env.example → même état que le SHA c8cb8e99). 1) Un seul project_ref : `grep -rhoE "[a-z]{20}\.supabase\.co"` sur js/, dashboard/, tests/, .github/, index.html, _headers, netlify.toml, scripts/, supabase/ → 12 occurrences, UNE seule valeur `njkiyoklssvefstljemx.supabase.co`. js/app-08-ui-modals-tour.js:2551 `const SUPABASE_URL = "https://njkiyoklssvefstljemx.supabase.co"` en dur ; scripts/build.js ne lit que COMMIT_REF/GITHUB_SHA/PASSIO_BUILD_TIME (l.80, 90), aucune substitution d'URL/clé par environnement. Les 17 fichiers contenant « staging » sont des faux positifs (dashboard/.env.example:8 = NODE_ENV du dashboard ; .claude/skills/passio-release-guard/SKILL.md:10 écrit explicitement « ni staging, ni canary »). 2) deploy.yml:531-586 job `preview` : `node scripts/build.js dist/index.html` puis `netlify-cli deploy --dir dist --alias pr-N` sur le MÊME NETLIFY_SITE_ID que la prod (l.583) → même dist, même base. 3) deploy.yml:300-357 job `test-prod` : `PASSIO_E2E_PROD: "1"`, `npx playwright test --project=prod` avec SUPABASE_SERVICE_ROLE_KEY ; le commentaire du job (l.303-308) dit lui-même « crée de vrais comptes sur la prod » et « purge TOUS les comptes %@passio-e2e.test ». playwright.config.js:79 projet `prod`, :65 globalTeardown ; tests/e2e/global-teardown.js:58 conditionne la purge à PASSIO_E2E_PROD. authz-critical / multi-comptes / qa-campaign écrivent bien posts, video_lives et Storage (grep storage.from|video_lives|from('posts')). 4) Preuves citées existantes et concordantes : preuves/supabase-isolation/fonctions_realtime_storage_staging.md §« Staging vs production » (l.45-49, verdict NON) ; PASSIO_PRODUCTION_READINESS.md:35 et :40 (« bloqués par … une base qui ne soit pas la production ») ; isolation_par_table.md:39 (113/117 conversations sans membre). 5) La « trace probable » des cycles CI est même RENFORCÉE : scripts/purge-e2e-rest.js purge 21 tables (conv_messages, conv_members, conv_reads, posts, profiles…) mais `conversations` en est ABSENTE, et la table n'a aucune policy DELETE (contrôle C04) → chaque cycle laisse mécaniquement des conversations orphelines en prod. git status --short : vide (0 ligne). — Correction de formulation : Formulation juste ; deux précisions : (a) dans « observé », remplacer « trace probable de ces cycles » par « conséquence structurelle : scripts/purge-e2e-rest.js ne purge pas `conversations` et la table n'a aucune policy DELETE » ; (b) dans « preuve », ajouter deploy.yml:583 (NETLIFY_SITE_ID identique preview/prod) et scripts/build.js:80-90 (aucune variable d'environnement d'URL/clé Supabase au build), qui prouvent l'absence de tout point d'injection par environnement. Le chiffre « 2+ comptes » par run est cohérent avec compte-e2e.js mais non compté ici. Priorité P1 maintenue.
- **impact** → CONFIRMÉ (priorité proposée P0). Défaut réel et non documenté comme décision produit : aucun ADR ni fiche ne choisit un projet unique ; au contraire PASSIO_PRODUCTION_READINESS.md:35-40 et docs/RECUPERATION.md:50 réclament un second projet Supabase (« C'est une décision, pas du développement — et la seule de cette liste qui ne s'achète pas avec du temps »). Faits revérifiés sur le SHA audité (c8cb8e9 ancêtre de HEAD, git status vide) : `grep -rn supabase.co js/ dashboard/ tests/ .github/ scripts/ index.html _headers netlify.toml` → un seul ref (12 occurrences de njkiyoklssvefstljemx.supabase.co, 0 autre) ; deploy.yml:301-357 job « Suites production (comptes réels) » avec PASSIO_E2E_PROD=1 et SUPABASE_SERVICE_ROLE_KEY, dont le commentaire admet lui-même que deux runs « s'effacent mutuellement leurs comptes » et ont laissé main rouge le 2026-09-01 ; deploy.yml:530-586 preview de PR = même dist, donc même base ; tests/e2e/global-teardown.js + scripts/purge-e2e-rest.js purgent EN PROD. La priorité P1 est incohérente avec le propre champ `impact` du finding (« critère d'interdiction du GO grande échelle selon le mandat ») : « staging et prod non séparés » est cité textuellement dans les critères d'interdiction, et ce même défaut est la cause racine de deux autres critères (restauration non prouvée, capacité non mesurée : readiness §35 « bloqués par exactement la même chose »). Un défaut qui à lui seul déclenche trois critères d'interdiction du GO bloque la commercialisation → P0, pas « avant lancement public ». Nuance : il ne bloque pas la poursuite de la beta privée actuelle (5 comptes), ce qui explique sans le justifier le P1 de l'auditeur. — Correction de formulation : Priorité : P1 → P0 (critère d'interdiction du GO explicite, et cause commune de « restauration non prouvée » et « capacité non mesurée »). Observé : l'affirmation « 113 conversations sur 117 orphelines, trace probable de ces cycles » doit être étiquetée PROBABLE et séparée des faits PROUVÉS (ref unique, jobs CI, preview) ; l'impact « données de test visibles des vrais utilisateurs » n'est pas démontré (le teardown purge, seul l'échec de purge l'exposerait) — le formuler comme risque, non comme constat. Preuve : ajouter deploy.yml:305-312 (commentaire reconnaissant l'incident du 2026-09-01 dû au partage de la prod) et docs/RECUPERATION.md:50. Effort : 3-5 jours plausibles mais il faut aussi prévoir un mécanisme d'injection de l'URL/clé par environnement dans scripts/build.js et la CSP de _headers (déjà noté) — et l'évaluer comme « décision + travail », pas seulement du développement.
- **contexte** → CONFIRMÉ (priorité proposée P1). Finding CONNU et TOUJOURS OUVERT sur le SHA c8cb8e99, non traité, non décidé : - PASSIO_PRODUCTION_READINESS.md:39-41 (« Ce qui ferait passer à PUBLIC BETA READY » §3) : « un second projet Supabase ou Docker… C'est une décision, pas du développement » ; §« deux d'entre eux — 3 et 4 — sont bloqués par exactement la même chose : une base qui ne soit pas la production ». Aucune décision inscrite depuis (aucun ADR sur le sujet dans .passio/adr/, KNOWN_RISKS.md n'a pas de ligne dédiée — R3 « schéma diverge » et R7 « tout push main = prod » sont voisins mais distincts). - docs/RECUPERATION.md:31-51 : « ni Docker, ni psql, ni base cible » ; « second projet Supabase (gratuit) dédié aux essais de restauration » proposé, non fait. - .github/workflows/deploy.yml:302-313 assume explicitement que le job test-prod « crée de vrais comptes sur la prod » et est « LE VERROU DE LA PROD SUPABASE » (concurrency passio-e2e-prod) ; docs/HOOKS_ET_PERMISSIONS.md:45 liste « la prod Supabase » parmi les biens partagés entre sessions. Le risque est donc assumé et mitigé (verrou de concurrence, purge, secret par étape), pas résolu. - js/app-08-ui-modals-tour.js:2551 : SUPABASE_URL codée en dur, aucune variable *_STAGING dans .github/workflows/, scripts/build.js, netlify.toml, _headers (grep exécuté). - Aucun invariant contredit ; la correction ne rouvre ni ADR-009 ni une liste noire ni tests/. Deux contraintes documentées à respecter : ADR-012 ③ (le DDL ne part JAMAIS depuis la CI — le peuplement du staging se fait par psql/SQL Editor) et docs/PIEGES_CONNUS.md:70 (les suites à comptes réels sont « la seule preuve possible des policies », donc un staging aux policies non identiques rendrait cette preuve creuse — dépendance directe à SUP-08). git status --short : vide. — Correction de formulation : Reformuler comme « risque CONNU et ASSUMÉ depuis le 2026-08-16, décision non prise » en citant PASSIO_PRODUCTION_READINESS.md §3-4 (« une base qui ne soit pas la production ») et docs/RECUPERATION.md:48-51 ; ajouter que deploy.yml:302-313 mitige (verrou de concurrence passio-e2e-prod, purge teardown, secret porté par étape) — la prod n'est pas exposée par négligence mais par absence de cible. Dans « correction » : préciser que le peuplement du schéma staging respecte ADR-012 ③ (DDL par psql/SQL Editor, jamais depuis la CI) et qu'il DÉPEND de SUP-08 (SCHEMA_PROD_REFERENCE périmé, 4 migrations sur 64 enregistrées) : sans schéma reconstructible, un staging aux policies divergentes rendrait la suite prod « verte pour rien » (docs/PIEGES_CONNUS.md:70 : ces suites sont la seule preuve des policies). Nuancer « garder en prod uniquement des tests en lecture » : la doctrine actuelle du dépôt fait de ces suites la preuve RLS, il faut la remplacer par un smoke authz-critical sur le staging PLUS une vérification d'identité des policies staging/prod (diff pg_policies), pas la supprimer. Le point « scripts/build.js inline la clé anon » est juste (invariant BUILD:APP) ; la CSP (_headers) porte le ref en dur, à paramétrer aussi. Doublons / recoupements : MSG-13 (messagerie-notifs, mêmes 113 conversations orphelines — ne pas compter deux fois), SUP-08 (prérequis), PIL-05 et PIL-06 (pilotage-sentinelle : suites prod lancées depuis le dashboard sans verrou — même cause racine « pas d'autre base »), readiness §3 restauration jamais exécutée (probablement repris par le domaine sauvegarde/ops s'il existe). Priorité P1 maintenue : bloque le lancement public selon le mandat, pas la beta privée actuelle.

### SUP-05 — Fonctions SECURITY DEFINER exposées en RPC à anon : oracle d'appartenance (is_conv_member) et d'existence de contenu privé (post_is_visible, comment_target_visible) ; is_blocked_with révèle un blocage

| Champ | Valeur |
|---|---|
| Identifiant | SUP-05 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | API PostgREST /rest/v1/rpc |
| Résultat attendu | Les helpers de policies ne sont pas appelables depuis l'API (EXECUTE réservé au moteur RLS via un rôle interne ou schéma non exposé), ou ne répondent que sur l'appelant |
| Résultat observé | pg_proc : EXECUTE anon+authenticated sur is_conv_member(_conv_id,_uid) — répond true/false pour tout couple (conv, uid) ; post_is_visible(pid) — true si le post n'existe pas, false s'il existe et est privé non suivi → révèle l'existence de contenu caché ; comment_target_visible idem ; is_blocked_with(_other) (authenticated) répond à « X me bloque-t-il ? » ; irl_interaction_allowed révèle la majorité déclarée d'autrui. Advisor : 4 WARN anon + 9 WARN authenticated. ADR-012 (Vérification) l'avait déjà noté « à traiter par le canal ③ » sans suite |
| Reproduction | POST /rest/v1/rpc/is_conv_member {"_conv_id":"<conv obtenue via conv_reads>","_uid":"<uid d'un profil public>"} avec la clé anon → true/false (non exécuté ici, proxy 403 ; grants lus dans pg_proc) |
| Preuve | execute_sql pg_proc/has_function_privilege (fonctions_realtime_storage_staging.md) ; get_advisors anon_security_definer_function_executable ×4 ; migrations/migration_fonctions_rls_hors_schema_expose.sql existe mais n'est PAS appliquée (fonctions toujours dans public) |
| Impact utilisateur et commercial | Énumération des relations privées (avec SUP-02 : carte complète des DM) ; détection d'un blocage (escalade de harcèlement) ; fuite d'existence de posts de comptes privés |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | REVOKE EXECUTE … FROM anon, authenticated sur les 4 helpers (les policies s'exécutent avec les droits du propriétaire de la fonction, pas de l'appelant) ; ou appliquer migration_fonctions_rls_hors_schema_expose.sql (déplacement hors schéma exposé) ; borner is_conv_member à _uid = auth.uid() ; pour is_blocked_with, ne répondre que sur le sens « je bloque » ou réserver au serveur |
| Risque de régression | Moyen : storage_chemin_autorise (SECURITY INVOKER) appelle public.is_conv_member avec les droits de l'appelant → il faut garder EXECUTE pour le rôle qui évalue la policy Storage ou passer cette fonction en SECURITY DEFINER ; à vérifier par authz-critical bloc 11 |
| Effort estimé | 0,5 jour |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P2). Reproduction par les preuves déposées et le code au SHA c8cb8e99 (aucun diff sur migrations/ ni js/ entre ce SHA et HEAD) : 1) Grants : preuves/supabase-isolation/fonctions_realtime_storage_staging.md:6-11 (tableau pg_proc/has_function_privilege) donne EXECUTE anon+authenticated sur can_edit_post, comment_target_visible, post_is_visible, is_conv_member, et authenticated sur is_blocked_with / irl_interaction_allowed ; ligne 43 confirme que ces quatre fonctions sont des routes RPC anon exposées. Corroboré par le code : migrations/migration_rls_private_dms_stories.sql:21-22 (`grant execute on function public.is_conv_member(text,text) to anon, authenticated`) et migrations/migration_ts_serveur_age_blocage.sql:125-126, 153-154 ; et par le fait établi de l'orchestrateur (advisor anon_security_definer_function_executable ×4, authenticated ×9). 2) Comportement d'oracle : corps recopiés « à l'octet près » dans migrations/migration_fonctions_rls_hors_schema_expose.sql:80-95 — post_is_visible rend TRUE si aucune ligne posts, FALSE si auteur privé non suivi (oracle d'existence) ; comment_target_visible délègue à post_is_visible (l.100-110) ; is_conv_member(_conv_id,_uid) = simple EXISTS sur conv_members sans référence à auth.uid() (l.120-122 et migration_rls_private_dms_stories.sql:18-20) ; is_blocked_with teste les DEUX sens (blocker_id=_other AND blocked_id=auth.uid(), migration_ts_serveur_age_blocage.sql:117-122) donc répond « X me bloque-t-il ? » ; irl_interaction_allowed rend la majorité de _other (l.145-149) mais seulement si l'appelant est lui-même majeur et non bloqué. 3) Migration non appliquée : migration_fonctions_rls_hors_schema_expose.sql:7-9 porte « ⚠️ NON APPLIQUÉE … jamais exécutée » ; supabase_migrations = 4 entrées (fait établi) ; ADR-012 l.167-172 note le point « à traiter par le canal ③ » sans suite. La preuve HTTP directe (POST /rest/v1/rpc/is_conv_member) n'a pas été exécutée (proxy 403), mais les grants pg_proc et les prosrc suffisent : le défaut est réel sur le SHA audité. P2 maintenu : fuite de structure sociale, pas de contenu ; les conv_id (17 car. aléatoires) ne deviennent énumérables qu'en combinaison avec SUP-02 (conv_reads USING true). — Correction de formulation : Le champ « correction » est en partie FAUX et dangereux : « REVOKE EXECUTE … FROM anon, authenticated (les policies s'exécutent avec les droits du propriétaire de la fonction, pas de l'appelant) » — c'est l'inverse en PostgreSQL : une expression de policy est évaluée avec les privilèges de l'utilisateur COURANT, qui doit détenir EXECUTE sur les fonctions appelées. Révoquer anon casserait la lecture de post_comments/post_likes (policies `post_is_visible(post_id)` pour {public}, SCHEMA_PROD_REFERENCE.sql:705,717) pour tout visiteur sans compte (first-run), et is_conv_member pour les membres. Le dépôt le dit lui-même (migration_fonctions_rls_hors_schema_expose.sql:33-52 « Ce serait une panne, pas un correctif »). Ne garder que : appliquer cette migration (déplacement hors schéma exposé, EXECUTE conservé pour anon/authenticated sur le nouveau schéma, l.132-135) — et requalifier l'appel dans storage_chemin_autorise (SECURITY INVOKER) vers passio_private.is_conv_member, à vérifier par authz-critical bloc « storage » (l.344). Le « risque de régression » cité est donc juste mais mal orienté : le risque vient du REVOKE, pas du déplacement. Nuancer aussi irl_interaction_allowed : la révélation de la majorité d'autrui est un choix produit (le client l'appelle, js/app-07-ia-explore-irl.js:5050) et n'est possible que pour un appelant déjà majeur et non bloqué — à sortir de la liste des oracles ou marquer P3. Pour is_blocked_with, le sens « je suis bloqué par X » est bien un oracle authenticated (P2 justifié), la migration existante ne le couvre pas (l.56-58).
- **impact** → CONFIRMÉ (priorité proposée P2). Défaut réel et non une décision produit : ADR-012 (.passio/adr/ADR-012-canal-acces-base-de-donnees.md:167-172) qualifie explicitement les 4 RPC anon (post_is_visible, comment_target_visible, can_edit_post, is_conv_member) de défaut « à traiter par le canal ③ », et migrations/migration_fonctions_rls_hors_schema_expose.sql (en-tête : « ⚠️ NON APPLIQUÉE. Écrite le 2026-09-03 ») confirme que le correctif est écrit mais pas appliqué — preuves/supabase-isolation/fonctions_realtime_storage_staging.md:6-11 et :43 montrent les grants encore en place en prod. KNOWN_RISKS.md:24 (2026-08-09) ne documente comme « volontaire » que le maintien d'EXECUTE pour authenticated, pas l'exposition à anon ni le caractère d'oracle. Priorité : P2 est juste. Aucun contenu ne fuit (structure sociale seulement : oui/non sur un couple conv/uid, existence d'un post privé, état de blocage) ; l'exploitation d'is_conv_member suppose de connaître un conv_id, ce qui dépend de SUP-02 (conv_reads USING true) — c'est SUP-02 qui porte la gravité, SUP-05 est un amplificateur. Ce n'est pas P0 (ne bloque pas la commercialisation, aucune donnée privée lisible) ; pas P1 non plus au sens strict, mais il touche au critère GO « isolation des comptes » et le correctif existe déjà (0,5 j) : à traiter dans le même geste que SUP-02. Nuance à corriger dans le finding : irl_interaction_allowed est appelée légitimement par le client (js/app-07-ia-explore-irl.js:5050) et déjà bornée à authenticated par migrations/migration_ts_serveur_age_blocage.sql:153-154 ; elle ne rend qu'un booléen combiné (blocage OU minorité de l'un des deux, :132-142), donc « révèle la majorité déclarée d'autrui » est exagéré : c'est un canal d'inférence, pas une lecture directe. Idem is_blocked_with (symétrique par conception ADR/migration ts_serveur_age_blocage) : l'exposition à authenticated est un choix documenté dans la migration hors-schéma (« être appelable par un compte connecté est un choix, pas un défaut »). — Correction de formulation : Garder P2. Reformuler : (1) « correction » : retirer l'option « REVOKE EXECUTE FROM anon » (casserait la lecture des commentaires/likes par les visiteurs first-run, cf. en-tête de la migration) ; ne retenir que le déplacement hors schéma exposé, en AJOUTANT la requalification de storage_chemin_autorise vers passio_private.is_conv_member, absente de la migration existante — sans quoi son DROP FUNCTION public.is_conv_member casse l'upload Storage (à couvrir par authz-critical bloc 11). (2) « observé » : is_blocked_with et irl_interaction_allowed sont réservées à authenticated par migration_ts_serveur_age_blocage.sql:153-154 et la seconde est appelée par le client (app-07:5050) : les mentionner comme exposition ASSUMÉE (décision documentée), pas comme défaut ; nuancer « révèle la majorité d'autrui » en « permet d'inférer blocage-ou-minorité, booléen combiné ». (3) « impact » : préciser que l'oracle is_conv_member n'est exploitable qu'avec un conv_id, donc dépend de SUP-02 — traiter les deux ensemble. (4) « risque_regression » : passer de Moyen à Élevé tant que la migration ne traite pas storage_chemin_autorise.

### SUP-06 — Canaux Realtime broadcast publics sans autorisation : invitation d'appel usurpable (ring:<uid>), signalisation WebRTC injectable (call:<callId> devinable), frappe (typing:<convId>) écoutable

| Champ | Valeur |
|---|---|
| Identifiant | SUP-06 |
| Priorité retenue | **P1** (proposée par l'auditeur : P2) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Appels audio/vidéo WebRTC, indicateur « est en train d'écrire », lives vidéo |
| Résultat attendu | Seuls les pairs légitimes peuvent émettre sur le canal de sonnerie d'un utilisateur et sur le canal de signalisation d'un appel ; les canaux sont privés (config.private + policy realtime.messages) |
| Résultat observé | app-05:493-497 _callChannel crée des canaux broadcast PUBLICS (commentaire « Canal Realtime broadcast (public) ») ; ring:<peerId> reçoit un payload {callId, from, kind, name, emoji} où from est fourni par l'émetteur (app-05:556-564) ; à la réception le seul filtre est isBlocked(payload.from) (app-05:832) ; callId = MY_UID + Date.now().toString(36) (app-05:531) donc prédictible à la seconde ; typing:<convId> (app-04:4755) et vlive:<id> (presence keyed MY_UID) sont publics. realtime.messages n'a de policy que pour user:<uid> |
| Reproduction | Client anon (clé anon) : supa.channel('ring:<uid public>').send({type:'broadcast',event:'invite',payload:{callId:'x',from:'<uid d un contact de confiance>',kind:'video',name:'…'}}) → sonnerie affichée chez la victime avec l'identité choisie (non exécuté ici) |
| Preuve | js/app-05-config-profil.js:493-497, 531, 556-564, 832, 1101-1107 ; js/app-04-comments-shop.js:4755 ; policies.json realtime.messages (topic user:* seulement) |
| Impact utilisateur et commercial | Usurpation d'identité dans un canal de confiance (appel entrant), contournement du blocage, détournement d'un appel (offer/answer/ICE injectés), fuite du nom de qui écrit dans une conversation |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Passer ring:, call:, typing: en canaux privés ({config:{private:true}}) avec policies realtime.messages : ring → topic = 'ring:'\|\|auth.uid() en SELECT et INSERT autorisé pour tout authenticated non bloqué ; call → topic lié aux deux uids ; typing → is_conv_member ; callId aléatoire (crypto.randomUUID) ; vérifier payload.from contre le JWT côté serveur (realtime.messages INSERT policy avec realtime.topic() et auth.uid()) |
| Risque de régression | Moyen : Realtime Authorization exige que chaque client soit authentifié (visiteur first-run non connecté ne peut plus écouter, acceptable) ; tests appels multi-comptes à rejouer |
| Effort estimé | 1 à 2 jours |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P2). Code identique au SHA audité : `git diff --stat c8cb8e99 HEAD -- js/app-05-config-profil.js js/app-04-comments-shop.js` est vide (les deux commits d'audit ne touchent que .passio/). - js/app-05-config-profil.js:493-496 : `_callChannel` = `supa.channel(name, { config: { broadcast: { self:false, ack:false } } })` — aucun `private: true`, commentaire « Canal Realtime broadcast (public) ». - :531 `callId = MY_UID + "_" + Date.now().toString(36)` ; :559-562 `invitePayload = { callId, from: MY_UID, kind, name, emoji }` émis sur `ring:<peer.id>` (:563) — `from` et `name` sont posés par l'ÉMETTEUR. - :818-836 `_callOnInvite` : filtres = doublon de callId, occupé, puis `isBlocked(payload.from)` seulement ; :1105 réception sur `ring:<MY_UID>` sans autre contrôle ; :1047-1054 `_callRenderIncomingUI` affiche `inv.name` et `acceptIncomingCall` (:851) adopte `inv.from` comme pair. - :762-807 `_callBindChannelEvents` : offer/answer/ice/hangup/decline acceptés sur `call:<callId>` sans vérifier l'expéditeur. - js/app-04-comments-shop.js:4755 `supa.channel("typing:" + convId)` public, payload.user affiché (:4757). - preuves/supabase-isolation/policies.json : realtime.messages ne porte que 2 policies SELECT authenticated sur `realtime.topic() = 'user:'||auth.uid()` — rien pour ring:/call:/typing:/vlive:, cohérent avec des canaux publics (un projet en « private only » casserait d'ailleurs l'appel). - Test exécuté (émulation Chromium, serveur local port 8120, supabase.co coupé par route.abort) : `_callOnInvite({callId:'forge-123', from:'0000…0042', kind:'video', name:'Faux contact de confiance'})` → overlayActive=true, `.call-name`="Faux contact de confiance", `.call-status`="Appel vidéo entrant…", `_callIncoming.from` = l'uuid forgé. La partie « transport » (envoi effectif par un client anon sur le canal public) n'a pas pu être exécutée (CONNECT 403 vers supabase.co) mais découle du code et de Supabase Realtime (broadcast public = aucune autorisation). — Correction de formulation : Deux précisions de formulation, sans effet sur le verdict : (1) le callId est `MY_UID + "_" + Date.now().toString(36)` (séparateur « _ » omis dans le finding) ; surtout, la prédictibilité n'est même pas nécessaire — le canal `ring:<uid>` étant public, un tiers abonné y LIT le callId de chaque invitation en clair, ce qui rend l'injection sur `call:<callId>` triviale ; (2) la « reproduction » n'a été exécutée ici que côté récepteur (rendu de la sonnerie forgée, preuve déposée) ; l'émission réelle par un client anon reste non exécutée (réseau bloqué) — à marquer « inspection code + émulation partielle » plutôt que « non exécuté ». Le rôle « anon » dans la reproduction suppose que le projet n'impose pas « private channels only » — hypothèse cohérente avec le fonctionnement actuel des appels mais non lisible ici (tableau de bord Realtime).
- **impact** → CONFIRMÉ (priorité proposée P1). Défaut réel, vérifié dans le code du SHA audité (branche à c8cb8e9 + 2 commits d'audit hors code) : - js/app-05-config-profil.js:493-497 : `_callChannel` crée un canal broadcast sans `config.private` (commentaire « Canal Realtime broadcast (public) ») ; app-08:4977 et 4992 montrent que le projet SAIT faire des canaux privés (`conv:`/`user:` avec `{ config: { private: true } }`) et que Realtime Authorization est active (policies.json l.122-123 : seules 2 policies realtime.messages, topic `user:<auth.uid()>`, rôle authenticated). Un canal public reste donc ouvert à tout porteur de la clé anon. - app-05:559-562 : `invitePayload.from = MY_UID` est fourni par l'émetteur ; app-05:832 : le seul filtre à la réception est `isBlocked(payload.from)`, donc un compte bloqué (ou un anonyme) choisit un `from` non bloqué et fait sonner la victime avec le nom/emoji de son choix ; app-05:851 : `acceptIncomingCall` prend `inv.from`/`inv.name` comme identité du pair → la victime décroche un appel vidéo P2P avec un inconnu qui se présente sous une identité de confiance. - app-05:531 : `callId = MY_UID + "_" + Date.now().toString(36)` (uid public via profiles SELECT true) → `call:<callId>` devinable, signalisation injectable ; app-04:4743/4755 : `typing:<convId>` public, payload `user` en clair. Pas une décision produit : docs/PIEGES_CONNUS.md:80 décrit « canaux publics, AUCUNE table/migration » comme état d'implémentation (2026-06-25), sans analyse de risque ; .passio/context/KNOWN_RISKS.md ne mentionne ni les appels, ni les canaux broadcast ; aucun ADR. Rien ne documente une acceptation du contournement du blocage. Priorité : P2 est trop bas. Le blocage (`blocks`, `isBlocked`) est LA mesure de sécurité utilisateur contre le harcèlement ; ici il est contournable par un tiers pour déclencher l'interaction la plus intrusive de l'app (sonnerie + push via notify-call, appel vidéo en direct). Cela touche le critère d'interdiction du GO « sécurité IRL ou modération insuffisante » et l'invisibilité totale (pilotage : non, Sentinelle : non). Pas P0 : exploitation technique (clé anon, uid, envoi manuel), aucune fuite de données persistantes, 5 comptes en beta privée. Donc P1 : à corriger avant tout lancement public. — Correction de formulation : Le finding est exact ; corriger sa priorité de P2 en P1 et étoffer l'impact : « contournement du blocage » doit être qualifié de défaut de sécurité utilisateur / modération (critère d'interdiction du GO grande échelle), pas seulement d'usurpation ; préciser dans « attendu » qu'aucune décision documentée (ADR, KNOWN_RISKS, PIEGES_CONNUS) n'accepte des canaux publics — PIEGES_CONNUS.md:80 décrit l'implémentation, pas un choix de risque. Ajouter en preuve app-05:851 (`acceptIncomingCall` adopte `inv.from`/`inv.name` sans vérification) et app-08:4977/4992 (canaux privés déjà en place → le risque de régression « Realtime Authorization à activer » est déjà absorbé, l'effort 1-2 j tient). Nuance de portée : l'invite forgée n'atteint la victime qu'app ouverte (pas de push sans l'Edge Function notify-call, qui vérifie le JWT).

### SUP-07 — Écritures de masse non bornées : notifications vers n'importe qui (contenu et lien libres, aucun rate-limit), telemetry_events et client_errors insérables par anon sans limite

| Champ | Valeur |
|---|---|
| Identifiant | SUP-07 |
| Priorité retenue | **P1** (proposée par l'auditeur : P2) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Notifications in-app ; télémétrie ; monitoring client_errors |
| Résultat attendu | Un compte ne peut notifier que dans le cadre d'une interaction réelle (ou à un débit borné) ; les tables d'ingestion anonymes sont bornées (rate-limit, taille, TTL) |
| Résultat observé | notifications_insert_own_author ne contraint que from_id ; user_id, kind (10 valeurs distinctes en prod), ref_id, content, created_at libres ; 0 trigger rate_limit sur notifications, conv_messages, posts, telemetry_events, client_errors (pg_trigger). telemetry_insert_own accepte user_id IS NULL (anon), 111 829 lignes/30 j, 54 Mo ; client_errors WITH CHECK true. Le rendu des notifications est échappé par défaut (app-08:1804 deConfiance = n.html === true \|\| kind === local) donc pas de XSS, mais le contenu et le lien sont ceux de l'attaquant |
| Reproduction | Compte B : boucle POST /rest/v1/notifications {user_id:<A>, from_id:<B>, kind:'like', content:'…', ref_id:'…'} → accepté sans borne. Anon : POST /rest/v1/telemetry_events (user_id null) en boucle → accepté |
| Preuve | policies.json notifications/telemetry_events/client_errors ; execute_sql pg_trigger (0 rate_limit sur ces tables) ; volumes telemetry 111 829 |
| Impact utilisateur et commercial | Spam/hameçonnage dans le seul canal de confiance de l'app ; saturation de la base (coût, quota Supabase) par un script anonyme ; bruit dans le Centre de pilotage |
| Visibilité dans le Centre de pilotage | partiel — le dashboard verrait une explosion de telemetry_events (KPI) mais rien sur les notifications |
| Détection par la Sentinelle | partiel — anomalies de volume télémétrie possibles, pas de règle dédiée |
| Proposition de correction | Triggers rate_limit_insert('from_id', N) sur notifications et ('user_id'/'uid', N) sur telemetry_events et client_errors (le mécanisme existe déjà) ; kind contraint par CHECK sur une liste ; borne de taille sur content/stack ; cron purge_telemetry (fonction existante, aucun job cron ne l'appelle : seul purge_client_errors est planifié) |
| Risque de régression | Faible : choisir des seuils au-dessus de l'usage réel (le client batch la télémétrie) |
| Effort estimé | 0,5 jour |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P2). Reproduction par inspection (aucune écriture en base possible ni autorisée ; REST vers supabase.co bloqué par le proxy) — chaque élément factuel du finding est retrouvé dans les preuves déposées et le code du SHA audité (arbre js/ et migrations/ identique à c8cb8e99, les deux commits de la branche ne touchent que .passio/audits) : - preuves/supabase-isolation/policies.json : notifications · notifications_insert_own_author INSERT roles={public} check "(from_id = ((SELECT auth.uid()))::text)" — aucune contrainte sur user_id, kind, ref_id, content, created_at ; telemetry_events · telemetry_insert_own check "(user_id IS NULL) OR (user_id = auth.uid()::text)" (anon accepté) ; client_errors · "Insert erreurs" check "true". - preuves/supabase-isolation/fonctions_realtime_storage_staging.md:20 et ref_cols.txt (liste des triggers) : trg_rate_limit uniquement sur comment_interactions, event_reactions, reports ; aucun trigger sur notifications, telemetry_events, client_errors. preuves/pilotage-sentinelle/requetes-supabase-lecture-seule.txt:6 : « [pg_trigger telemetry_events/client_errors] aucun trigger … [pg_constraint] seule PRIMARY KEY (id) : aucune borne de longueur serveur » ; :2-3 : un seul cron (purge_client_errors), purge_telemetry jamais planifiée ; :7 : 111 829 lignes / 54 Mo. - migrations/migration_anti_flood_interactions.sql:98-139 : rate_limit_insert existe et n'est posé que sur ces trois tables ; migrations/migration_indexes_et_monitoring.sql:26 : WITH CHECK (true) sur client_errors ; migrations/migration_telemetry.sql:63-66 : user_id IS NULL toléré ; aucun REVOKE INSERT anon sur ces tables dans migrations/ ; aucun CHECK sur notifications.kind dans SCHEMA_PROD_REFERENCE.sql. - Chemin client : js/app-08-ui-modals-tour.js:4776-4790 supaInsertNotif insère user_id/kind/ref_id/content librement (seul from_id = MY_UID) ; js/telemetry.js:347-358 POST direct /rest/v1/telemetry_events avec apikey anon ; js/platform.js:46 insert client_errors sans borne. - « Pas de XSS » vérifié : app-08:1860-1867 mergeSupaNotifs neutralise < > à l'ingestion pour toute notif fromSupabase, indépendamment du kind (un kind='local' injecté ne contourne pas l'échappement), puis app-08:1804 au rendu. Seul point non vérifiable depuis les preuves déposées : « 10 valeurs distinctes de kind en prod » (issu d'execute_sql, non déposé) — non porteur pour le verdict. — Correction de formulation : Deux nuances de formulation, sans changer la priorité : (1) l'attaquant de notifications est ATTRIBUABLE — from_id = auth.uid() est imposé, donc le spam est traçable à un compte confirmé (modérable/bannissable), ce qui distingue ce cas de la télémétrie anonyme ; (2) un INSERT REST brut dans notifications ne déclenche PAS le push Web (l'appel à l'Edge Function notify-call est fait côté client dans supaInsertNotif, app-08:4787-4792, après l'insert) — le spam atteint la cloche in-app et le realtime du destinataire, pas la notification système. Reformuler « contenu et lien libres » en « contenu et ref_id libres, mais neutralisés à l'ingestion (app-08:1866) et rendus comme texte : hameçonnage par texte uniquement, pas par lien cliquable injecté ». Le champ « preuve » doit citer les fichiers déposés (policies.json ; fonctions_realtime_storage_staging.md:20 ; pilotage-sentinelle/requetes-supabase-lecture-seule.txt:2-7) plutôt que « execute_sql pg_trigger », non déposé. La mention « 10 valeurs distinctes en prod » reste à étiqueter « requête base, non déposée ».
- **impact** → CONFIRMÉ (priorité proposée P1). Défaut réel, non documenté comme décision : la seule doc sur le sujet (migrations/migration_anti_flood_interactions.sql:11-16, docs/RAPPORT_SESSION_2026-07-02.md:95) présente le rate limiting comme un P0 « soldé » alors qu'il ne couvre que comment_interactions/event_reactions/reports — notifications, telemetry_events et client_errors n'y figurent pas (policies.json : notifications_insert_own_author CHECK from_id seul ; telemetry_insert_own accepte user_id IS NULL ; client_errors WITH CHECK true ; contrôle C32 : 0 trigger sur ces tables). L'insert anon de télémétrie est voulu (visiteurs sans compte, first-run, opt-out ?telemetry=0) mais son ABSENCE DE BORNE ne l'est pas : purge_telemetry existe (migration_telemetry.sql:86-88) et aucun cron ne l'appelle. Priorité : P2 est trop bas au regard des critères. (1) Le même défaut est coté P1 par trois autres domaines (MSG-04, PIL-03, MOD-06) avec des mesures supplémentaires : notify-call (supabase/functions/notify-call/index.ts:22-46) relaie un push système avec toUserId/text libres, sans relation ni quota, donc le canal ne se limite pas à l'in-app ; 3 684 lignes/appareil/jour de télémétrie, 54 Mo/30 j pour 3-4 comptes. (2) Un script anonyme, sans compte, peut saturer la base et déclencher des alertes critical qui consomment le budget Sentinelle : cela touche les critères GO « capacité » et « modération/harcèlement insuffisants ». (3) Le harcèlement par notifications vers un inconnu, sans cadence, avec seul un blocage client a posteriori (app-08:1869), est exactement le cas « avant lancement public ». Atténuations vérifiées : pas de XSS (mergeSupaNotifs neutralise les chevrons à l'entrée, app-08:1854-1858) ; le « lien » n'est jamais une URL, openNotifTarget (app-08:1976-2005) route ref_id vers des cibles internes seulement (post, profil, événement, conversation) ; un kind inconnu ne mène nulle part et prend l'emoji par défaut. — Correction de formulation : Priorité P2 → P1 (avant lancement public). Reformuler « contenu et lien libres » : le lien n'est pas une URL mais un ref_id qui n'ouvre que des cibles internes (post/profil/événement/conversation, app-08:1976-2005) — l'hameçonnage possible est purement textuel, pas par redirection. Ajouter à l'observé que notify-call (Edge) relaie un push système avec text/toUserId libres sans quota, ce qui aggrave l'impact par rapport à l'in-app seul. Préciser que l'insertion anonyme de télémétrie est un comportement attendu (visiteurs sans compte, opt-out documenté dans CLAUDE.md) : le défaut est l'absence de borne (débit, taille, TTL/cron purge_telemetry), pas l'ouverture elle-même. Signaler le chevauchement avec MSG-04 / PIL-03 / MOD-06 pour éviter quatre entrées du même risque au registre. Effort 0,5 j plausible pour les triggers + CHECK ; ajouter le cron purge_telemetry (canal ③ ADR-012, DDL hors CI).

### SUP-08 — Schéma de production non reconstructible : 4 migrations enregistrées sur 64 fichiers et SCHEMA_PROD_REFERENCE.sql périmé depuis le 2026-08-17

| Champ | Valeur |
|---|---|
| Identifiant | SUP-08 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Gouvernance du schéma (staging, restauration, revue des policies hors ligne) |
| Résultat attendu | Une photographie fidèle de la prod (tables, colonnes, policies, fonctions) régénérée à chaque migration, et un journal de migrations appliqué dans l'ordre |
| Résultat observé | supabase_migrations = 4 entrées (3 du 2026-08-09, 1 du 2026-08-24) ; 64 fichiers dont des lots retirés (cdv_*, wallet). SCHEMA_PROD_REFERENCE.sql (« NE PAS ÉDITER À LA MAIN », généré par scripts/schema-baseline.js) date du 2026-08-17 : 4 tables absentes (passion_relations, passion_requests, user_passions, user_safety), passions à 5 colonnes au lieu de 13, video_lives différente, 116 policies contre 125 (12 nouvelles, 3 disparues). CLAUDE.md et ADR-012 le présentent pourtant comme « la photographie de la structure RÉELLE » |
| Reproduction | node script de comparaison md5 colonnes/policies (schema_reference_vs_prod.txt) ; list_migrations |
| Preuve | preuves/supabase-isolation/schema_reference_vs_prod.txt ; ref_cols.txt ; list_migrations ; git log -1 migrations/SCHEMA_PROD_REFERENCE.sql = 2026-08-31 (commit sans régénération du contenu) |
| Impact utilisateur et commercial | Toute décision prise hors ligne sur la référence est fausse pour 4 tables et le référentiel des passions ; impossible de monter un staging ou de restaurer sur une base neuve (prérequis de SUP-04 et du plan de reprise) |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Régénérer la référence (node scripts/schema-baseline.js) et l'imposer en gate CI (diff vide contre la prod, via le canal ① ou un dump) ; consolider les 64 fichiers en un jeu ordonné rejouable (supabase/migrations/<ts>_*.sql) et enregistrer l'historique réel dans supabase_migrations |
| Risque de régression | Faible (documentaire) ; le gate CI doit tolérer la fenêtre entre migration appliquée et référence commitée |
| Effort estimé | 1 jour + discipline |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P2). Reproduit sur le SHA audité (c8cb8e99 ; `git diff --stat c8cb8e99 HEAD -- migrations/` vide, le fichier de référence est identique) : - migrations/SCHEMA_PROD_REFERENCE.sql:2 « photographie du 2026-08-17 », :4 « Généré par node scripts/schema-baseline.js. NE PAS ÉDITER À LA MAIN » ; `git log -1 -- migrations/SCHEMA_PROD_REFERENCE.sql` = 43b8ffa 2026-08-31 (#226), contenu toujours daté du 17/08. - Référence :183-188 : `passions` à 5 colonnes (id, emoji, label, color, sort_order) alors que migrations/migration_passions_plat.sql:84 sq. ajoute popularity, normalized_label, aliases, status, source, is_broad… (migration appliquée en prod le 2026-09-01 selon CLAUDE.md) → écart réel. - `grep -nE "^-- (passion_relations|passion_requests|user_passions|user_safety)$"` sur la référence : aucune entrée, alors que migrations/migration_ts_serveur_age_blocage.sql:22 crée public.user_safety et que preuves/supabase-isolation/policies.json (125 policies) contient user_passions_*, passion_relations_select_all, passion_requests_*, user_safety_select_own. - Policies dans la référence : `grep -c "\[(SELECT|INSERT|UPDATE|DELETE)\]"` = 116 ; policies.json = 125 ; la liste des 12 (+) / 3 (−) de schema_reference_vs_prod.txt est cohérente avec ces deux jeux. - `ls migrations/*.sql | wc -l` = 64 ; fonctions_realtime_storage_staging.md:52 : supabase_migrations = 4 (20260809 ×3, 20260824), fait aussi établi par l'orchestrateur. - ADR-012 :120-123 présente bien le fichier comme « la photographie de la structure réelle de la production » ; CLAUDE.md:68 « Hors ligne : migrations/SCHEMA_PROD_REFERENCE.sql, jamais les migrations/*.sql seuls ». Aucun schema:baseline dans la CI (package.json:30 seulement). git status --short : vide. — Correction de formulation : Aucune correction de fond. Précision de formulation : la « 13 colonnes » de passions n'est pas comptée dans les preuves déposées (ref_cols.txt ne liste que la référence) — elle repose sur l'orchestrateur et migration_passions_plat.sql ; écrire « ≥ 11 colonnes d'après migration_passions_plat.sql:113 (13 selon la lecture prod de l'orchestrateur) ». Le libellé « commit sans régénération du contenu » est exact (43b8ffa, 2026-08-31, en-tête toujours daté 2026-08-17).
- **impact** → CONFIRMÉ (priorité proposée P2). Défaut réel, pas une décision produit : migrations/SCHEMA_PROD_REFERENCE.sql:1-6 porte lui-même « photographie du 2026-08-17 » et git log ne montre qu'un commit 43b8ffa (2026-08-31) sans régénération ; pourtant CLAUDE.md:68 et .passio/adr/ADR-012:120-124 en font « la photographie de la structure RÉELLE » à privilégier hors ligne, et docs/DETTES_ADR-010_2026-08-31.md:38 raisonne dessus (D2, unicité de username) comme s'il était à jour. L'écart mesuré (preuves/supabase-isolation/schema_reference_vs_prod.txt : 4 tables absentes dont user_safety et user_passions, passions 5→13 colonnes, 125 vs 116 policies, 12 policies nouvelles/3 disparues) est incontestable. Aggravant non relevé par l'auditeur : scripts/schema-baseline.js:42 régénère par `supabase db query --linked`, canal RETIRÉ par ADR-012 (« la CLI n'est installée nulle part ») ; le script s'auto-protège (ligne 196-207 : laisse le fichier intact si aucun bloc récupéré, mesuré le 2026-09-03) — donc AUCUN moyen documenté de régénérer la référence n'existe aujourd'hui. Priorité : au regard des définitions, ce n'est ni un P0 (aucun compte, aucune donnée exposée ; l'isolation a été prouvée par l'auditeur sur pg_policies LIVE, pas sur la référence) ni un P1 autonome : les critères d'interdiction du GO qu'il touche (« staging et prod non séparés », « restauration non prouvée ») sont portés par SUP-04 (P1), dont SUP-08 n'est qu'un prérequis documentaire — le staging comme la restauration peuvent partir d'un dump live (pg_dump via canal ③) ou des sauvegardes Supabase, sans la référence. P2 est donc juste : amélioration importante de gouvernance, effort ~1 jour. Aucune modification de fichier suivi ; git status --short vide. — Correction de formulation : Trois retouches de formulation. ① OBSERVÉ : les « 4 entrées supabase_migrations sur 64 fichiers » ne sont pas un défaut en soi mais la conséquence attendue du canal ③ d'ADR-012 (DDL appliqué par psql / SQL Editor, qui n'écrivent pas dans supabase_migrations) ; le vrai défaut est l'absence de tout journal ordonné rejouable, à dire ainsi. ② IMPACT : remplacer « impossible de monter un staging ou de restaurer sur une base neuve » par « pas de socle vérifié pour un staging ou une base neuve depuis le dépôt » — un pg_dump live ou les sauvegardes Supabase restent possibles ; la restauration non prouvée relève de SUP-04 / du skill sauvegarde, pas de SUP-08. ③ CORRECTION/EFFORT : « node scripts/schema-baseline.js » ne peut PAS tourner tel quel (ligne 42 exige la CLI retirée) ; la correction doit d'abord réécrire le script sur psql / information_schema (canal ③) avant toute idée de gate CI, ce qui rehausse l'effort à ~1,5–2 jours. Priorité P2 confirmée.

### SUP-09 — UPDATE sans WITH CHECK sur events et post_comments : un co-organisateur peut s'approprier un événement (author_id), un auteur peut déplacer son commentaire vers un autre post

| Champ | Valeur |
|---|---|
| Identifiant | SUP-09 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Événements IRL (co-organisation) ; commentaires |
| Résultat attendu | author_id d'un événement est immuable ; post_id d'un commentaire est immuable |
| Résultat observé | events « Update organisateurs » qual = author_id = uid OR jsonb_exists(co_organizers, uid), with_check NULL → un co-organisateur peut PATCH author_id=<lui> (la nouvelle ligne satisfait toujours le USING) et devenir propriétaire (DELETE, conv d'événement, retrait des autres) ; aucun trigger freeze sur events (seul posts en a un). post_comments « Update propre » qual author_id = uid, with_check NULL → post_id modifiable (commentaire injecté sous un post d'un compte privé sans le suivre, ou déplacé pour tromper) |
| Reproduction | Co-organisateur : PATCH /rest/v1/events?id=eq.<ev> {"author_id":"<moi>"} → 1 ligne (non exécuté ici) |
| Preuve | policies.json events.Update organisateurs / post_comments.Update propre (check null) ; pg_trigger : trg_posts_freeze_author seulement sur posts |
| Impact utilisateur et commercial | Prise de contrôle d'un événement par un co-organisateur ; intégrité des fils de commentaires |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | WITH CHECK identiques aux USING + trigger BEFORE UPDATE gelant author_id (events) et post_id/author_id (post_comments), sur le modèle de posts_freeze_author |
| Risque de régression | Nul pour l'app (aucun client ne modifie ces colonnes) |
| Effort estimé | 1 heure |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P2). 1. Preuve déposée relue : preuves/supabase-isolation/policies.json (dump pg_policies du 2026-09-04) contient exactement `{"t":"events","p":"Update organisateurs","cmd":"UPDATE","qual":"(author_id = (auth.uid())::text) OR jsonb_exists(co_organizers, (auth.uid())::text)","check":null}` et `{"t":"post_comments","p":"Update propre","cmd":"UPDATE","qual":"(author_id = (auth.uid())::text)","check":null}` ; policies_liste_live.txt:116 et :118 les listent aussi. La liste des triggers dans ref_cols.txt ne montre `trg_posts_freeze_author` que sur `posts` (events et post_comments n'ont aucun trigger). 2. Origine dans le dépôt, sur le SHA audité (c8cb8e99, ancêtre de HEAD, migrations/ et js/ inchangés) : migrations/migration_irl_v2.sql:41-45 (`CREATE POLICY "Update organisateurs" ON events FOR UPDATE USING (...)` sans WITH CHECK) et migrations/migration_rls_v2_appliquee.sql:46 (`CREATE POLICY "Update propre" ON post_comments FOR UPDATE USING (author_id = auth.uid()::text)`, sans WITH CHECK). SCHEMA_PROD_REFERENCE.sql:652-654 confirme `check : -`. 3. Sémantique PostgreSQL : sans WITH CHECK, l'expression USING est appliquée à la nouvelle ligne. Nouvelle ligne events avec `author_id = <co-organisateur>` → `author_id = auth.uid()` vrai → acceptée ; nouvelle ligne post_comments avec `post_id` changé et `author_id` inchangé → acceptée. Le raisonnement est déterministe à partir des quals ; aucune requête d'écriture n'a été exécutée (interdit en prod), le finding le dit lui-même (« non exécuté ici »). 4. Le projet a déjà reconnu ce défaut exact sur `posts` : docs/PIEGES_CONNUS.md:70 (« un WITH CHECK sur can_edit_post(id) ne protège PAS la propriété → trigger posts_freeze_author, sinon un co-auteur pouvait s'approprier le carnet »). `events` avec co-organisateurs est le même schéma sans le même gel. 5. Côté client, `_eventRow` (js/app-08-ui-modals-tour.js:4060-4079) n'envoie jamais `author_id` et `supaUpdateEvent` (:4141) ne filtre pas sur `author_id` (voulu, pour les co-organisateurs) ; aucun `update` sur post_comments dans js/. « Risque de régression nul » est donc cohérent. 6. Vecteur limité : il faut être co-organisateur (ajouté par l'auteur, app-07:5434-5436) ou auteur du commentaire ; 3 événements et 5 comptes en prod → P2 tenable. — Correction de formulation : Nuance à apporter dans « observé » pour post_comments : l'injection d'un commentaire sous un post d'un compte privé sans le suivre est DÉJÀ possible par INSERT (policy « Ecriture propre » WITH CHECK `author_id = auth.uid()` seulement, sans test de visibilité du post) — l'UPDATE sans WITH CHECK n'ouvre donc pas ce vecteur, il n'ajoute que le déplacement d'un commentaire existant (avec ses likes/interactions) vers un autre post. Le reste (events : appropriation par un co-organisateur ; absence de trigger de gel) est exact tel quel.
- **impact** → CONFIRMÉ (priorité proposée P2). 1. Défaut réel côté events : preuves/supabase-isolation/policies.json l.61 « Update organisateurs » UPDATE qual = author_id = uid OR jsonb_exists(co_organizers, uid), check = null ; l.60 « Suppression propre » DELETE qual author_id = uid. En PostgreSQL, sans WITH CHECK le USING est appliqué à la nouvelle ligne : un co-organisateur qui PATCH author_id=<lui> satisfait le USING sur la nouvelle ligne → il devient propriétaire, gagne le DELETE et peut évincer le créateur (l'app ne pose aucun filtre .eq("author_id") — js/app-08-ui-modals-tour.js:4141, commentaire l.4130-4136 l'explique). Seul posts porte un trigger freeze (migrations/migration_carnet_collaborators.sql:77-90 ; fonctions_realtime_storage_staging.md l.20). Aucune décision produit ne documente cette liberté : migrations/migration_irl_v2.sql:36-45 ne parle que de « corriger l'événement », docs/PIEGES_CONNUS.md:66 ⑥ idem, KNOWN_RISKS.md n'en dit rien. 2. Impact borné : le co-organisateur est CHOISI par l'auteur (app-07:5434-5443, openEventCoOrganizers), donc relation de confiance ; il peut déjà, légitimement, modifier lieu/date/statut et la liste co_organizers (colonnes libres dans la même policy). L'escalade marginale est l'appropriation + suppression. Prod : 3 events, 5 comptes. Pas de PII, pas d'isolation de lecture, pas de critère bloquant du GO (l'« isolation des comptes » vise la lecture ; la « sécurité IRL » n'est pas plus atteinte que par les pouvoirs déjà accordés au co-organisateur). → P2 juste, pas P1. 3. Volet post_comments SURÉVALUÉ : policies.json l.78 INSERT « Ecriture propre » check = author_id = uid SEULEMENT — un commentaire sous le post d'un compte privé non suivi est DÉJÀ insérable directement, sans passer par un UPDATE de post_id. Le WITH CHECK manquant n'ajoute que le « déplacement » d'un commentaire existant (intégrité, cosmétique). Le client n'appelle jamais post_comments.update (grep js/ : select l.3662/3948, insert l.3817 seulement) → régression nulle, comme dit. 4. git status --short vide, HEAD f501fb7 (branche d'audit). — Correction de formulation : Garder P2. Reformuler le volet post_comments : « post_id modifiable » n'est pas la porte d'entrée de « commentaire injecté sous un post d'un compte privé sans le suivre » — l'INSERT « Ecriture propre » (WITH CHECK author_id = uid seul, policies.json l.78) le permet DÉJÀ ; le WITH CHECK manquant sur UPDATE n'ajoute que le déplacement d'un commentaire existant (intégrité). Si l'injection sous compte privé doit être signalée, c'est un finding distinct sur la policy INSERT (ajouter post_is_visible(post_id) au WITH CHECK, en cohérence avec la policy SELECT l.79). Préciser dans « impact » que le co-organisateur est désigné par l'auteur (relation de confiance) et qu'il dispose déjà de tous les pouvoirs d'édition (lieu, date, statut, liste co_organizers) : l'escalade réelle est la prise de propriété (DELETE + éviction du créateur). Ajouter à la correction : les 9 policies UPDATE sans WITH CHECK (C05) se traitent en un seul lot, avec un test dans authz-critical.spec.js (« co-organisateur PATCH author_id → 0 ligne »), et poser WITH CHECK explicitement sur posts-style : USING/WITH CHECK identiques + trigger BEFORE UPDATE gelant author_id sur events.

### SUP-10 — Suppression de compte incomplète : résidus dans 16 tables et 6 dossiers Storage après delete-account

| Champ | Valeur |
|---|---|
| Identifiant | SUP-10 |
| Priorité retenue | **P1** (proposée par l'auditeur : P2) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | RGPD — suppression de compte (Edge Function delete-account) |
| Résultat attendu | Toutes les données rattachées à l'uid disparaissent (ou sont anonymisées), Storage compris |
| Résultat observé | delete-account/index.ts:46-62 purge 15 couples table/colonne ; oubliés : comment_interactions, event_comments, event_reactions, blocks, reports, user_state (jusqu'à 4,8 Mo par ligne), user_safety, user_passions, analytics_events, telemetry_events (user_id), video_lives, cdv_* , conv_reads, comment_likes, post_collaborators, step_interactions ; Storage : seuls photos/videos/audios/<uid> (index.ts:67-76), pas covers/avatars/passion_photos/passion_covers/cdv_steps/<uid> ni attachments. FK profiles sans ON DELETE CASCADE (confdeltype = a) : la suppression de profiles échoue si posts/conv_messages restent (ordre des jobs le gère partiellement, best-effort silencieux) |
| Reproduction | Lire supabase/functions/delete-account/index.ts:46-76 ; select count(*) from <table> where user_id = <uid supprimé> après un parcours de suppression (suppression-compte.spec.js, projet prod) |
| Preuve | supabase/functions/delete-account/index.ts:46-76 ; execute_sql pg_constraint confdeltype ; chemins Storage relevés (covers/, avatars/, passion_*/, cdv_steps/) |
| Impact utilisateur et commercial | Non-conformité RGPD (droit à l'effacement), données orphelines publiques (event_comments, video_lives avec author_name) après départ de l'utilisateur, coût Storage |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Compléter la liste des tables et des préfixes Storage ; remplacer le best-effort par une fonction SQL SECURITY DEFINER transactionnelle (ou ON DELETE CASCADE sur les FK vers profiles) ; test suppression-compte à étendre aux tables oubliées |
| Risque de régression | Faible |
| Effort estimé | 0,5 jour |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P2). - SHA : `git diff --stat c8cb8e99..HEAD -- supabase/ js/ migrations/` est vide → le code lu est celui du SHA audité. - supabase/functions/delete-account/index.ts:47-63 : exactement 15 couples table/colonne (posts, post_likes, post_comments, stories, events, event_attendees, conv_messages, conv_members, notifications, story_views, push_subscriptions, client_errors, follows×2, profiles) ; :73-81 : Storage limité à `content/photos|videos|audios/<uid>`. Aucune fonction SQL de suppression de compte dans migrations/ ni js/ (grep `delete_my_account|supprimer_compte` : 0). - Tables oubliées vérifiées contre preuves/supabase-isolation/ref_cols.txt (colonnes user_id/author_id/reporter_id présentes) et policies.json : `tables_sans_policy_delete` = analytics_events, conv_reads, reports, telemetry_events, user_safety → AUCUN chemin (ni client RLS ni Edge) ne peut les purger. comment_interactions, comment_likes, event_comments, event_reactions, post_collaborators, step_interactions, user_passions, video_lives, cdv_* ont une policy DELETE propriétaire mais ne sont appelées ni par l'Edge ni par le client. - Storage : chemins hors purge RÉELS dans le code — app-08:3578 `filePath = ${folder}/${MY_UID}/…` avec folder = "covers" (app-06:956), "avatars" (app-06:977), "passion_photos" (app-06:1002), "passion_covers" (app-06:1039) ; bucket `attachments` (app-09:877, :1598) jamais touché — docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:51-53 l'admet (« Limites connues »). - FK : SCHEMA_PROD_REFERENCE.sql:334-361, les 5 FK vers profiles (conv_members, conv_messages, post_comments, posts, stories) sont sans clause ON DELETE (= NO ACTION, cohérent avec confdeltype='a') ; aucune FK vers auth.users → la suppression auth ne cascade rien. - Test : tests/e2e/suppression-compte.spec.js:86-95 n'asserte que user_state écrit, média `photos/` retiré, Edge 200, reconnexion refusée — aucune mesure de résidus. - Aucun fichier suivi modifié (`git status --short` = 0 ligne). — Correction de formulation : Observé : retirer `user_state` et `blocks` (côté bloqueur) de la liste des « oubliés » sans réserve — ils sont supprimés par le client (app-02:3096, :3101-3102) avant l'appel de l'Edge, via des policies DELETE existantes ; préciser « non purgés par l'Edge Function seule ; purgés par le client de façon best-effort sans lecture de {error} ; `blocks` côté BLOQUÉ (blocked_id) reste orphelin (policy blocker_id uniquement) ». Reformuler la phrase sur les FK : l'ordre des jobs (index.ts:48-62) place les 5 tables référençant profiles AVANT profiles, donc l'échec n'arrive que si un DELETE amont a échoué — et il est alors silencieux (:67). Preuve : remplacer « execute_sql pg_constraint confdeltype » (non déposé dans preuves/) par SCHEMA_PROD_REFERENCE.sql:334-361 (FK sans clause ON DELETE) + ref_cols.txt, ou déposer la sortie de la requête. Compléter la preuve Storage par les lignes de code réelles : app-08:3578 (`${folder}/${MY_UID}/`), app-06:956/977/1002/1039 (covers, avatars, passion_photos, passion_covers), app-09:877/1598 (bucket attachments) et docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:51-53 qui reconnaît la limite. Ajouter : suppression-compte.spec.js:86-95 ne mesure aucun résidu (couverture absente, pas seulement « à étendre »). Priorité P2 maintenue (5 comptes réels, purge manuelle sous 30 j annoncée à l'écran app-02:3068) ; à requalifier P1 avant lancement public si l'engagement RGPD affiché doit être tenu automatiquement.
- **impact** → CONFIRMÉ (priorité proposée P1). Défaut réel et NON documenté comme décision produit : supabase/functions/delete-account/index.ts:46-62 purge 15 couples table/colonne et :67-76 seulement content/photos|videos|audios/<uid> ; or js/app-06-reels-partage.js:956,977,1002,1039 dépose avatars/, covers/, passion_photos/, passion_covers/ sous l'uid dans le bucket `content` (PUBLIC, listable par anon — preuve fonctions_realtime_storage_staging.md:31-32) : le visage d'une personne qui a supprimé son compte reste servi publiquement. Tables à USING true jamais purgées : event_comments (author_name + text), video_lives (author_name, author_photo), comment_likes, event_reactions… (ref_cols.txt, policies_liste_live.txt). Ce n'est pas un comportement attendu : docs/CHECKLIST_COMMERCIALISATION.md:45 coche « [x] RGPD : suppression de compte réelle », le commentaire js/app-02-state-utils.js:3106-3117 affirme la suppression « complète », et la modale app-02:3058-3063 promet à l'utilisateur la suppression de « tous tes posts, photos, vidéos… likes, commentaires ». Seule limite documentée : docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:52-55 (bucket `attachments` non purgé) — le reste n'est écrit nulle part. Le verrou tests/e2e/suppression-compte.spec.js:30-99 ne mesure que user_state + photos/<uid>/temoin.png + reconnexion : il reste vert sur tous les résidus listés. Priorité : P2 est trop bas. L'art. 17 RGPD est une obligation légale opposable dès l'ouverture au public, la promesse in-app est fausse, et des données NOMMÉES (author_name, photo) restent publiques après départ → « avant lancement public » = P1. Pas P0 : aucun critère d'interdiction du GO ne vise ce point, la beta est privée (5 comptes), le noyau (auth, profil, posts, messages, user_state côté client) est bien effacé, et le correctif est borné. — Correction de formulation : 1) Priorité P2 → P1 (obligation légale avant tout lancement public, promesse in-app et checklist commerciale fausses). 2) « Observé » : retirer user_state et blocks de la liste des oublis de l'Edge Function — le client les purge d'abord via RLS (app-02:3092-3102, policies user_state_delete_own / blocks_delete_own), mais en best-effort seulement (session expirée, hors-ligne, onglet fermé) ; formuler « purgés côté client uniquement, sans rattrapage serveur ». 3) Distinguer deux familles : tables purgeables par le client mais non purgées (comment_interactions, event_comments, event_reactions, comment_likes, video_lives, post_collaborators, step_interactions, conv_reads — policies DELETE own existantes) et tables purgeables UNIQUEMENT par service_role (reports sans policy DELETE, user_safety, telemetry_events/analytics_events INSERT-only) — la correction n'est pas la même. 4) « Impact » : ajouter le point le plus visible — avatar/couverture restent servis dans un bucket PUBLIC listable par anon (visage d'une personne partie), et la modale de suppression promet explicitement l'effacement des photos, likes et commentaires. 5) « Preuve » : ajouter docs/CHECKLIST_COMMERCIALISATION.md:45 (case cochée), app-02:3058-3063 (promesse), docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:52-55 (seule limite documentée : attachments) et le fait que suppression-compte.spec.js ne mesure que photos/<uid>. 6) Effort 0,5 j → 1 j (fonction SQL SECURITY DEFINER transactionnelle + 5 préfixes Storage + extension du test prod + redéploiement Edge hors dépôt).

### SUP-11 — Dette de policies : données du Carnet de voyage (retiré) toujours exposées publiquement, 5 jeux de policies dupliquées, 78 auth_rls_initplan, données orphelines

| Champ | Valeur |
|---|---|
| Identifiant | SUP-11 |
| Priorité retenue | **P3** (proposée par l'auditeur : P3) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Hygiène RLS et données |
| Résultat attendu | Une fonctionnalité retirée n'a plus de surface exposée ; une policy par commande et par intention |
| Résultat observé | cdv_lives (2), cdv_live_steps (4, lat/lng/photos), cdv_live_comments, cdv_live_collaborators, cdv_live_followers, cdv_live_reactions, step_interactions (6) : SELECT true, tables dans la publication realtime, alors qu'ADR-011 a retiré la fonctionnalité (aucune donnée détruite, choix documenté). Doublons : events SELECT ×2, follows SELECT ×2, profiles SELECT ×2, conv_members DELETE ×2, realtime.messages ×2 (recoit/reçoit). auth.uid() non enveloppé dans (select …) sur 78 policies (advisor perf). Intégrité : 113 conversations sans membre, 2 messages d'un non-membre, 27 conv_reads hors membre, 92 comment_interactions ciblant une cible introuvable |
| Reproduction | policies.json ; execute_sql (isolation_par_table.md § Intégrité) ; get_advisors performance |
| Preuve | policies.json § policies_dupliquees ; isolation_par_table.md ; faits établis (auth_rls_initplan ×78, multiple_permissive_policies ×30) |
| Impact utilisateur et commercial | Surface d'attaque et coût de maintenance inutiles ; performance RLS dégradée à l'échelle ; données fantômes dans les exports RGPD |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Retirer cdv_* et step_interactions de la publication et passer leur SELECT en propriétaire (ou archiver/dropper après export) ; supprimer les doublons ; réécrire auth.uid() en (select auth.uid()) ; purger les conversations orphelines et les reads hors membre |
| Risque de régression | Faible (fonctionnalité morte) ; vérifier goTo('cdv') redirigé |
| Effort estimé | 1 jour |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P3). 1) cdv_* / step_interactions exposés : preuves/supabase-isolation/policies.json (dump pg_policies 2026-09-04) — cdv_collab_select, cdv_comments_select, cdv_followers_select, cdv_reactions_select, cdv_steps_select, si_select : cmd SELECT, roles {public}, qual "true" (6 tables) ; colonnes cdv_live_steps = lat, lng, photos, video, city (ref_cols.txt:7). Publication : ref_cols.txt (dump live pg_publication_tables) liste 25 entrées « supabase_realtime:: » dont public.cdv_lives, cdv_live_collaborators/comments/followers/reactions/steps et step_interactions — recoupé par js/app-08-ui-modals-tour.js:5273-5275 qui admet que ces tables « restent dans la publication ». Retrait fonctionnel : .passio/adr/ADR-011-refonte-multi-passion.md:146-156 (« Aucune migration destructive ») et js/app-02-state-utils.js:1978 (goTo cdv → feed) ; seul résidu client vivant : js/app-04-comments-shop.js:1452,1504 (routage lc_ → cdv_live_comments). 2) Doublons : policies.json § policies_dupliquees et entrées brutes — events « Lecture publique »/« Read events » (qual true ×2), follows idem, profiles idem, conv_members DELETE « Suppression propre » (user_id=uid) ⊂ « Suppression admin » (user_id=uid OR created_by), realtime.messages « recoit »/« reçoit » (quals identiques). 5 jeux confirmés. 3) auth_rls_initplan ×78 et multiple_permissive_policies ×30 : faits établis par l'orchestrateur (CONTEXTE_AUDIT.md, get_advisors) ; cohérent avec les quals du dump (auth.uid() nu partout sauf cdv_lives_select, conv_members_select_member). 4) Intégrité (113 conversations sans membre, 2 messages non-membre, 27 conv_reads hors membre, 92 comment_interactions orphelines) : preuve déposée isolation_par_table.md:38-39 et :16 (rôle connecteur, execute_sql) — non ré-exécutable ici sans SQL, admise comme preuve de l'auditeur. SHA : HEAD f501fb78 a c8cb8e99 pour ancêtre, aucun diff sur js/index.html/migrations/styles.css → état audité identique. Priorité P3 justifiée : fonctionnalité morte, 2+4 lignes, aucune PII forte, dette/hygiène. — Correction de formulation : Observé : « cdv_lives (2) … : SELECT true » est inexact — policies.json : cdv_lives_select = (visibility <> 'private') OR (author_id = (SELECT auth.uid())::text), donc un carnet « Privé » n'est PAS lisible par un tiers (c'est la garantie de confidentialité ADR-011 §5). Le USING true ne vaut que pour les 5 tables filles (cdv_live_steps, _comments, _collaborators, _followers, _reactions) et step_interactions ; or ces filles n'héritent pas de la visibilité du carnet parent : les étapes (lat/lng/photos/video) d'un carnet privé restent lisibles par anon via la table fille — c'est la formulation exacte de l'exposition à retenir. Ajouter au « observé » que le client vivant ne lit plus aucune table cdv_* sauf le routage des commentaires lc_ (js/app-04-comments-shop.js:1452,1504), et que la présence dans la publication est ASSUMÉE par le code (js/app-08:5273-5275), donc un choix documenté plutôt qu'un oubli. Correction : préciser que passer cdv_live_steps & co. en « lecture selon visibilité du carnet parent » (EXISTS cdv_lives l WHERE l.id=live_id AND (l.visibility<>'private' OR l.author_id=auth.uid())) suffit sans dropper. Effort 1 jour plausible ; risque de régression faible confirmé (aucune requête client sur cdv_* hors commentaires).

### SUP-12 — Réglages Auth : protection contre les mots de passe compromis désactivée ; état du provider anonyme non vérifiable

| Champ | Valeur |
|---|---|
| Identifiant | SUP-12 |
| Priorité retenue | **P3** (proposée par l'auditeur : P3) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | PLAUSIBLE |
| Fonctionnalité | Authentification Supabase |
| Résultat attendu | Leaked password protection activée (gratuit) ; provider anonyme désactivé si non utilisé (first-run.js:35 affirme ne pas l'utiliser, app-02:3530 l'appelle pourtant dans un chemin d'onboarding) |
| Résultat observé | get_advisors : auth_leaked_password_protection WARN (déjà noté le 2026-08-09 comme « toggle gratuit » et toujours ouvert) ; auth.users : 0 compte is_anonymous, mais la configuration n'est pas lisible ici |
| Reproduction | get_advisors security |
| Preuve | get_advisors 2026-09-04 ; js/app-02-state-utils.js:3529-3530 ; js/first-run.js:35 |
| Impact utilisateur et commercial | Comptes créés avec des mots de passe connus des fuites ; si le provider anonyme est actif, un script peut obtenir un JWT authenticated sans e-mail et exercer toutes les policies {public}/{authenticated} (dont SUP-07) |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Activer la protection HIBP dans Auth ; vérifier et désactiver Anonymous sign-ins si inutilisé, ou borner les policies d'écriture à (auth.jwt()->>'is_anonymous') = 'false' |
| Risque de régression | Faible |
| Effort estimé | 1 heure |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P3). 1) Volet « leaked password protection désactivée » : RÉEL. Le fait est établi indépendamment par l'orchestrateur (CONTEXTE_AUDIT.md, « Advisors sécurité … protection contre les mots de passe compromis DÉSACTIVÉE », get_advisors du 2026-09-04), et le dépôt le reconnaît : .passio/context/KNOWN_RISKS.md:24 laisse « volontairement » le WARN auth_leaked_password_protection ouvert depuis le 2026-08-09, docs/SETUP_SMTP_AUTH.md:82 le liste encore comme « durcissement restant » à faire. Aucune preuve dans preuves/ ni dans le code ne contredit ce WARN ; aucun contrôle côté app ne compense (validation à 6 caractères, doc §5). 2) Volet « provider anonyme » : le finding est lui-même INCERTAIN sur la configuration, et sa citation de code est MAL LUE. js/app-02-state-utils.js:3511-3548 définit bien `onbSkipAuth()` qui appelle `supa.auth.signInAnonymously()` (l.3530), mais `grep -rn "onbSkipAuth(" index.html js/ dist/` ne trouve AUCUN appelant (ni onclick dans index.html, ni appel JS, ni dans dist/app.js hors la définition l.5743) : c'est du code MORT sur le SHA audité (git diff c8cb8e99..HEAD sur app-02/first-run/app-08/index.html : vide). Le parcours actif (js/first-run.js:35-36) ne crée aucune session anonyme. Le compteur auth.users is_anonymous = 0 rapporté par l'auditeur va dans le même sens. Le risque « JWT authenticated sans e-mail » n'est donc qu'une hypothèse sur un réglage Dashboard non lisible ici, pas un défaut reproduit. Le défaut réel qui subsiste est le toggle HIBP ouvert — P3 (hygiène gratuite, aucune exploitation démontrée), priorité conservée. — Correction de formulation : Reformuler l'attendu/observé : retirer « app-02:3530 l'appelle pourtant dans un chemin d'onboarding » — `onbSkipAuth` n'a AUCUN appelant sur le SHA audité (code mort, à signaler plutôt en dette de nettoyage : une fonction globale qui crée une session anonyme et reste appelable depuis la console). Scinder le finding : (a) HIBP désactivée = DÉFAILLANT, prouvé par get_advisors, correction « activer le toggle » (effort 5 min, pas 1 h) ; (b) provider anonyme = BLOQUÉ/INCERTAIN (réglage non lisible en SQL, 0 compte anonyme, aucun chemin actif dans l'app) — l'impact « JWT authenticated sans e-mail » est conditionnel et ne doit pas être présenté comme observé. Preuve à corriger : citer `grep onbSkipAuth(` vide plutôt que la seule ligne 3530.

### SUP-13 — search_path non épinglé sur 3 fonctions exposées et pg_trgm dans public (WARN advisors, exploitabilité nulle aujourd'hui)

| Champ | Valeur |
|---|---|
| Identifiant | SUP-13 |
| Priorité retenue | **P3** (proposée par l'auditeur : P3) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Fonctions SQL exposées (rechercher_passions, storage_chemin_autorise, unaccent_immutable) |
| Résultat attendu | SET search_path = '' sur toute fonction exposée, extensions hors du schéma public |
| Résultat observé | Les trois sont SECURITY INVOKER ; storage_chemin_autorise n'a que des références qualifiées ; rechercher_passions appelle similarity/unnest non qualifiés mais anon/authenticated n'ont pas CREATE sur public (has_schema_privilege = false) → aucune usurpation possible tant que ce grant ne change pas |
| Reproduction | get_advisors security ; execute_sql pg_proc.proconfig ; has_schema_privilege |
| Preuve | fonctions_realtime_storage_staging.md § Fonctions |
| Impact utilisateur et commercial | Aucun aujourd'hui ; risque latent si un futur GRANT CREATE ou un schéma extensions apparaît |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | ALTER FUNCTION … SET search_path = '' avec références qualifiées (public.similarity) ; déplacer pg_trgm dans extensions |
| Risque de régression | Faible (tester rechercher_passions) |
| Effort estimé | 1 heure |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P3). 1) Le défaut est établi indépendamment du finding : CONTEXTE_AUDIT.md (« Faits déjà établis par l'orchestrateur ») cite les advisors sécurité WARN « search_path mutable ×3 (storage_chemin_autorise, unaccent_immutable, rechercher_passions) » et « pg_trgm en public » — exactement les 3 fonctions et l'extension du finding. 2) Source des fonctions dans le dépôt (SHA c8cb8e99, ancêtre de HEAD f501fb7, migrations/ et js/ sans diff) : migrations/migration_passions_plat.sql:59-66 (unaccent_immutable, `language sql immutable strict`, aucun SET search_path), :6218-6255 (rechercher_passions, `language plpgsql stable`, aucun SET search_path, appel `similarity(p.normalized_label, n)` NON qualifié à la ligne ~6250), migrations/migration_storage_cloisonnement.sql:60-76 (storage_chemin_autorise, `language sql stable`, aucun SET search_path, références toutes qualifiées storage.foldername / public.is_conv_member / auth.uid). `grep -n -i search_path` sur ces deux fichiers : 0 résultat. 3) pg_trgm : migration_passions_plat.sql:6123 `create extension if not exists pg_trgm;` sans clause `schema extensions` → installée dans public (concordant avec l'advisor « pg_trgm en public » et l'extension listée par l'orchestrateur). 4) SECURITY INVOKER : aucune clause `security definer` dans les trois CREATE FUNCTION (cohérent avec preuves/supabase-isolation/fonctions_realtime_storage_staging.md:14-16, secdef = non). 5) Exploitabilité nulle : la seule pièce non reproductible ici est `has_schema_privilege('anon'|'authenticated','public','CREATE') = false`, rapportée dans le tableau de preuve (md:14) et non dans un dump brut. Elle ne change pas la réalité du défaut (WARN advisor réel), seulement sa gravité ; P3 reste justifié dans les deux cas, car même avec CREATE sur public un attaquant ne gagnerait, en SECURITY INVOKER, que ses propres privilèges. — Correction de formulation : Précision dans « observé » : `unnest` (comme `translate`, `lower`, `coalesce`, `regexp_replace`) est dans pg_catalog, qui est implicitement consulté EN PREMIER quel que soit search_path — il n'est pas usurpable ; le seul appel réellement exposé à une usurpation par search_path dans rechercher_passions est `similarity` (fonction de pg_trgm, dans public). Remplacer « similarity/unnest non qualifiés » par « similarity non qualifié (public.similarity de pg_trgm) ». La preuve `has_schema_privilege = false` devrait être déposée en sortie brute dans preuves/ (elle n'existe aujourd'hui que comme affirmation dans le tableau du md). Le reste (priorité P3, correction, effort) est juste.

### SUP-14 — Pages HTML du dépôt invitant à coller la clé service_role dans un navigateur

| Champ | Valeur |
|---|---|
| Identifiant | SUP-14 |
| Priorité retenue | **P3** (proposée par l'auditeur : P3) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Outillage (scripts/setup-storage.html, scripts/create-buckets-auto.html) |
| Résultat attendu | La clé service_role ne transite jamais par un navigateur ni par un fichier ouvert en file:// |
| Résultat observé | scripts/setup-storage.html:26-46 : textarea « Colle ta clé service_role ici » ; create-buckets-auto.html idem ; ces pages référencent le project_ref prod. Contredit S4 de SECURITY_AUDIT (« service_role côté serveur dashboard uniquement ») sur la pratique, pas sur le code déployé (hors build) |
| Reproduction | cat scripts/setup-storage.html |
| Preuve | scripts/setup-storage.html:26,27,46 ; scripts/create-buckets-auto.html |
| Impact utilisateur et commercial | Habitude dangereuse (historique du navigateur, extensions) ; incident déjà vécu avec settings.local.json |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Supprimer les deux pages (buckets déjà créés) ou les remplacer par un script Node lisant la variable d'environnement |
| Risque de régression | Nul |
| Effort estimé | 15 minutes |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P3). - `git diff --stat c8cb8e99 HEAD -- scripts/setup-storage.html scripts/create-buckets-auto.html` : vide → les fichiers lus sont ceux du SHA audité. - scripts/setup-storage.html:26 : `<textarea id="serviceRoleKey" … placeholder="Colle ta clé service_role ici">` ; :27 « Supabase Dashboard → Settings → API → service_role secret » ; :38 `SUPABASE_URL = "https://njkiyoklssvefstljemx.supabase.co"` (project_ref prod) ; :58 et :71 `authorization: Bearer ${serviceRoleKey}` envoyé depuis le navigateur vers storage/v1. - scripts/create-buckets-auto.html:104 même project_ref ; :105 `const SERVICE_ROLE_KEY = "COLLE_TA_CLE_SERVICE_ROLE_ICI";` ; :116 et :139 `Authorization: Bearer ${SERVICE_ROLE_KEY}`. Pas de textarea : la page invite à ÉCRIRE la clé dans un fichier versionné, ce qui est un vecteur de fuite plus grave que le collage (commit accidentel). - Aucune valeur réelle de clé dans les deux fichiers (grep `eyJ` : 0 résultat) → aucun secret commité, ce que le finding ne prétend pas. - Hors build/déploiement : `scripts/build.js` n'inline que le bloc BUILD:APP d'index.html ; aucune référence à ces pages dans build.js, netlify.toml ni .github/workflows → jamais servies en prod. Seule référence : docs/RESUME_FINAL.md:90 « (legacy, ignoré) ». - .passio/audits/SECURITY_AUDIT.md:17 (S4) : « service_role côté serveur dashboard uniquement, jamais au navigateur » — contredit par ces deux pages sur la pratique, comme le dit le finding. - Priorité P3 justifiée : outillage mort, non déployé, sans secret présent ; le risque est comportemental (historique, extensions, commit accidentel), pas une exposition actuelle. - `git status --short` : vide. — Correction de formulation : Observé : remplacer « create-buckets-auto.html idem » par « create-buckets-auto.html:105 : constante `SERVICE_ROLE_KEY = "COLLE_TA_CLE_SERVICE_ROLE_ICI"` en dur dans le fichier versionné (pas de textarea), envoyée en Bearer aux lignes 116 et 139 ». Preuve : ajouter setup-storage.html:38,58,71 et create-buckets-auto.html:104,105,116,139. Impact : ajouter le risque de commit accidentel de la clé dans un fichier suivi (cas create-buckets-auto.html). Correction : préciser que les deux pages visent des buckets « media »/« storage » inexistants en prod (buckets réels attachments/content), donc suppression pure sans remplacement.

### SUP-15 — Faux accusés de lecture : conv_reads acceptable pour toute conv_id sans appartenance

| Champ | Valeur |
|---|---|
| Identifiant | SUP-15 |
| Priorité retenue | **P3** (proposée par l'auditeur : P3) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Accusés de lecture ✓✓ |
| Résultat attendu | Un accusé de lecture n'est accepté que d'un membre de la conversation |
| Résultat observé | reads_upsert WITH CHECK (user_id = uid) sans is_conv_member ; 27 lignes existantes proviennent de non-membres ; app-08:5040-5044 marque « lu par l'autre » dès qu'un conv_reads porte le conv_id (x.id === r.conv_id) → un tiers connaissant un conv_id peut faire croire qu'un message a été lu |
| Reproduction | POST /rest/v1/conv_reads {conv_id:<conv d autrui>, user_id:<moi>, last_read_at:now} → accepté (non exécuté ici) |
| Preuve | policies.json conv_reads ; js/app-08-ui-modals-tour.js:5039-5052 ; execute_sql reads_hors_membre = 27 |
| Impact utilisateur et commercial | Signal ✓✓ falsifiable ; faible gravité, corrigé avec SUP-02 |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Même migration que SUP-02 (WITH CHECK is_conv_member) |
| Risque de régression | Faible |
| Effort estimé | inclus dans SUP-02 |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P3). 1. Policy : preuves/supabase-isolation/policies.json (dump live des 125 policies) donne pour public.conv_reads : reads_upsert INSERT check=(user_id = auth.uid()::text), reads_update USING (user_id = auth.uid()::text), reads_select USING true — aucun appel à is_conv_member. Identique à migrations/migration_conv_reads.sql:12-17 et à SCHEMA_PROD_REFERENCE.sql:592-600. ref_cols.txt:16 confirme que conv_reads n'a que la PK (conv_id,user_id), sans FK vers conversations : n'importe quel conv_id (même inexistant) est accepté à l'insert. 2. Code sur le SHA audité c8cb8e99 (git diff --stat c8cb8e99..HEAD sur js/app-08 et migrations/ : vide, donc HEAD = SHA audité pour ces fichiers) : js/app-08-ui-modals-tour.js:5043-5052, handler realtime conv_reads : `if (!r || r.user_id === MY_UID) return;` puis `x.id === r.conv_id` → `c._otherRead = true` et re-rendu du ✓✓. Aucune vérification que r.user_id est membre de la conversation. Second chemin non cité par le finding, même défaut : supaLoadOtherRead app-08:4661-4671 — `.eq("conv_id", convId).neq("user_id", MY_UID)` prend le max de last_read_at de TOUT user_id ≠ moi et pose `_otherRead = true`. 3. Chaîne d'attaque cohérente : un compte authentifié quelconque connaît les conv_id (reads_select USING true les expose à anon — SUP-02), fait un upsert conv_reads {conv_id: <conv d'autrui>, user_id: <lui>} accepté par reads_upsert, et la victime voit ses messages « lus » (realtime, puis à chaque ouverture via supaLoadOtherRead). 4. Les « 27 lignes de non-membres » figurent bien dans isolation_par_table.md:39 (relevé au rôle connecteur), donc la preuve citée existe. 5. Le POST n'a pas été exécuté (le finding le dit honnêtement) ; l'émulation par lecture des quals est sans ambiguïté, aucune requête n'est nécessaire pour trancher. git status --short : vide. — Correction de formulation : Le finding est juste sur le fond ; trois retouches de formulation : (a) « 27 lignes existantes proviennent de non-membres » ne doit pas être lu comme une preuve d'abus — isolation_par_table.md:39 note aussi que 113 conversations sur 117 n'ont AUCUN membre (orphelines), les 27 lignes sont donc très probablement des lectures légitimes sur des conversations dont conv_members manque, pas des falsifications ; écrire « 27 lignes sans appartenance vérifiable (conversations majoritairement orphelines) ». (b) Ajouter le second chemin d'exploitation, supaLoadOtherRead app-08:4661-4671 (.neq user_id MY_UID), qui rend le faux ✓✓ persistant à chaque ouverture et pas seulement en direct ; la correction (WITH CHECK is_conv_member) couvre les deux, mais un filtre client sur les membres serait une défense en profondeur. (c) Préciser que conv_reads n'a pas de FK vers conversations (ref_cols.txt:16) : un conv_id inexistant est aussi accepté. Lignes citées : le handler est à 5043-5052 (pas 5039/5040). Priorité P3 maintenue (signal cosmétique, correctif inclus dans SUP-02).

### SUP-16 — Écritures qui échouent en silence : aucune policy UPDATE/DELETE sur conversations alors que le client les tente (description de groupe non persistée, conversation jamais supprimée côté serveur)

| Champ | Valeur |
|---|---|
| Identifiant | SUP-16 |
| Priorité retenue | **P3** (proposée par l'auditeur : P2) |
| Relecture adversariale | CONFIRMÉ par la relecture |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Messagerie — édition de la description d'un groupe, suppression de conversation |
| Résultat attendu | Soit la policy existe (créateur), soit le client ne tente pas l'écriture et l'utilisateur est informé |
| Résultat observé | pg_policies conversations = INSERT (créateur) + SELECT (membre) seulement. js/app-05-config-profil.js:1624 conversations.update({description}) → 0 ligne, rejet avalé ; js/app-08-ui-modals-tour.js:4517-4518 conv_members.delete(conv_id) puis conversations.delete(id) dans un nettoyage → 0 ligne. Invariant CLAUDE.md « UPDATE/DELETE qui touche 0 ligne = RLS manquante ; toujours lire { error } » non respecté (le SDK ne lève pas sur 0 ligne) |
| Reproduction | Créateur d'un groupe : modifier la description, recharger sur un autre appareil → ancienne description (déduit des policies ; non exécuté) |
| Preuve | policies.json § tables_sans_policy_update/delete ; js/app-05-config-profil.js:1624 ; js/app-08-ui-modals-tour.js:4517-4518 |
| Impact utilisateur et commercial | Fonction visible qui ne fait rien (description de groupe) ; conversations orphelines qui s'accumulent (113/117) |
| Visibilité dans le Centre de pilotage | non — aucune erreur remontée (0 ligne = succès HTTP 200/204) |
| Détection par la Sentinelle | non |
| Proposition de correction | CREATE POLICY conversations_update_creator FOR UPDATE USING/WITH CHECK (created_by = auth.uid()) et conversations_delete_creator FOR DELETE ; côté client lire { data, error } et le count (Prefer: return=representation) ; test e2e mesurant la persistance |
| Risque de régression | Faible |
| Effort estimé | 2 heures |

Relecture (angles indépendants) :

- **reproduction** → CONFIRMÉ (priorité proposée P2). Reproduit sur le SHA audité (git diff c8cb8e99..HEAD sur js/app-05, js/app-08, migrations/ : vide ; seuls deux commits d'audit .passio/ sont au-dessus). 1) Preuve base : preuves/supabase-isolation/policies.json → `tables_sans_policy_update` et `tables_sans_policy_delete` contiennent toutes deux "conversations" ; policies_liste_live.txt:47 et :82 ne listent que INSERT (conversations_insert_creator) et SELECT (conversations_select_member). Aucune migration du dépôt ne crée jamais de policy UPDATE/DELETE sur conversations (grep migrations/ : uniquement SELECT/INSERT, drops compris). 2) Preuve code : js/app-05-config-profil.js:1624 `supa.from("conversations").update({ description })…then(function(){},function(){})` — rejet et 0 ligne avalés, `{ error }` jamais lu ; js/app-08-ui-modals-tour.js:4517-4518 `conv_members.delete` puis `conversations.delete` sous try/catch vide. Colonne `description` bien présente (ref_cols.txt:17), donc l'UPDATE n'échoue pas en 400 : il touche 0 ligne (RLS) en silence. 3) Aggravant non cité : la description n'est JAMAIS relue depuis le serveur (aucun `description`/`groupDesc` mappé après les `select("*")` de conversations aux lignes 4683/4892/5077 d'app-08 ; l'unique `r.description` à 4223 concerne les events). La fonction n'est donc persistée que localement même si la policy existait. Défaut réel : invariant CLAUDE.md « UPDATE/DELETE 0 ligne = RLS manquante ; toujours lire { error } » violé sur deux points d'écriture. — Correction de formulation : Le cœur (policies absentes + écritures tentées et avalées) tient. À corriger dans la formulation : (a) IMPACT — l'attribution « conversations orphelines qui s'accumulent (113/117) » à ce défaut est non prouvée et probablement fausse : le DELETE de app-08:4518 n'est qu'un chemin de repli (échec d'insert conv_members, rare) et la suppression par l'utilisateur (_deleteConv) ne tente rien côté serveur ; les orphelines viennent vraisemblablement des purges e2e qui n'effacent pas `conversations`. Retirer le « conversation jamais supprimée côté serveur » du titre ou le requalifier « suppression locale seulement, par conception ». (b) OBSERVÉ — ajouter que `conversations.description` n'est jamais relu par le client (select * non mappé), donc la correction proposée (policy UPDATE) ne suffit pas : il faut aussi hydrater `groupDesc` depuis `description` au chargement (sinon le test e2e de persistance proposé restera rouge). (c) EFFORT — 2 h → ~3-4 h avec l'hydratation et le test multi-appareils. Priorité P2 maintenue (fonction visible sans effet, pas de fuite ni de perte de données).
- **impact** → CONFIRMÉ (priorité proposée P3). Défaut réel mais surévalué et mal attribué. (1) policies.json (dump prod 2026-09-04) : conversations = conversations_insert_creator (INSERT authenticated) + conversations_select_member (SELECT) — aucune UPDATE ni DELETE, confirmé. (2) js/app-05-config-profil.js:1624 : `supa.from("conversations").update({description}).eq("id", convId).then(function(){}, function(){})` → l'échec RLS (0 ligne, pas d'erreur SDK) est avalé ; invariant CLAUDE.md « toujours lire { error } » non respecté, confirmé. (3) MAIS la description n'est JAMAIS lue depuis le serveur non plus : les trois hydratations de conversations (js/app-08-ui-modals-tour.js:4744, 4901, 5102) ne mappent que `group_name`, jamais `description` → `groupDesc` ; le seul `r.description` lu (app-08:4223) concerne les events. La description de groupe est donc un champ purement LOCAL (localStorage/IDB), persistant sur l'appareil, jamais partagé aux membres — avec ou sans policy. Créer la policy seule (correction proposée) ne rendrait pas la fonction opérationnelle. (4) app-08:4517-4518 : le `conversations.delete()` n'est qu'un nettoyage best-effort sur le chemin d'ÉCHEC de `supaCreateConversation` (insert conv_members refusé) — pas une suppression de conversation par l'utilisateur ; aucune fonction « supprimer une conversation » côté serveur n'existe (leaveGroup app-05:1604 ne touche que conv_members, dont la DELETE policy existe). (5) Les « 113/117 orphelines » ne viennent pas de ce chemin : scripts/purge-e2e-rest.js:44-64 purge conv_members/conv_messages/conv_reads par user_id mais ne liste jamais `conversations` → chaque campagne CI à comptes réels laisse ses conversations sans membre. Impact commercial : nul (aucune fuite, aucune perte de données locale, aucun critère d'interdiction du GO touché — ni isolation, ni restauration, ni sécurité IRL/modération) ; impact utilisateur : une fonctionnalité secondaire (description de groupe) silencieusement non partagée. Ni ADR ni docs/lots-ui ne documentent ce comportement comme voulu (grep editGroupDescription/groupDesc dans docs/, .passio/adr/ : 0 résultat ; aucun test e2e ne le couvre). git status --short : vide. — Correction de formulation : Titre/observé : remplacer « conversation jamais supprimée côté serveur » par « nettoyage best-effort du chemin d'échec de supaCreateConversation inopérant » (aucune suppression de conversation utilisateur n'existe côté serveur). Ajouter l'observation décisive : `description` n'est jamais hydratée depuis le serveur (app-08:4744/4901/5102 ne mappent que group_name) → champ local-only, la policy seule ne corrige rien. Impact : retirer « conversations orphelines qui s'accumulent (113/117) » — cause principale = scripts/purge-e2e-rest.js qui ne purge pas `conversations` (à rattacher au finding staging/purge, SUP-04/C27), pas ce chemin client. Correction : soit supprimer l'UPDATE serveur et assumer le champ local (ou retirer la fonction), soit policy UPDATE (created_by = auth.uid()) + lecture de `description` dans les trois hydratations + lecture de { error } + ajout de `conversations` à la purge e2e + test e2e à deux comptes. Effort : 2 h → ½ journée (trois chemins d'hydratation, test multi-comptes en prod). Priorité : P2 → P3 (fonction secondaire, aucune perte ni fuite, aucun critère GO ; reste un défaut, pas un comportement documenté).

### Surfaces saines

- Noyau d'isolation par requête (émulation des quals exacts, anon et tiers) : conv_messages, conv_members, conversations, notifications, user_state, blocks, reports, push_subscriptions, story_views, user_safety, telemetry_events, analytics_events, client_errors, passion_requests → 0 ligne lisible ; 0 ligne UPDATE/DELETE touchable par un tiers sur posts, profiles, conv_messages, notifications, events, user_state, conv_members, stories, storage.objects
- profiles : aucune PII forte exposée (pas d'e-mail, téléphone, année de naissance) ; auth.users et user_safety hors de portée de PostgREST anon
- Usurpation d'identité d'affichage neutralisée par trigger identite_affichage_canonique (BEFORE INSERT OR UPDATE) sur video_lives, event_comments, cdv_live_comments, step_interactions, et propagation depuis profiles
- posts : author_id gelé par trigger posts_freeze_author ; UPDATE gardé par can_edit_post des deux côtés (USING + WITH CHECK)
- conv_members INSERT : créateur OU auto-inscription à une conversation d'événement actif (RSVP going/maybe) ET non bloqué (is_blocked_with) — policy la plus riche du schéma, cohérente avec tests/sql/socle-prod.sql
- conv_messages INSERT : from_id = auth.uid() ET is_conv_member ; conversations INSERT : created_by = auth.uid() (les policies WITH CHECK true de la référence 2026-08-17 ont bien disparu)
- Storage : écriture cloisonnée (INSERT et UPDATE via storage_chemin_autorise, DELETE owner = auth.uid(), 70/70 objets avec owner) — prouvé en CI par authz-critical bloc 11
- Realtime v3 : canal privé user:<uid> avec policy realtime.messages sur realtime.topic() = 'user:'||auth.uid() ; trigger broadcast un topic par membre ; realtime.messages RLS activée
- Aucune valeur service_role ni JWT de service dans le dépôt ; service_role confiné aux secrets GitHub (étapes nommées), à dashboard/.env (serveur) et aux Edge Functions ; .mcp.json absent
- Edge Functions ask-ai, notify-call, delete-account : JWT vérifié par auth.getUser() avant toute action, 401 sinon
- Fonctions de maintenance (purge_telemetry, rate_limit_insert, broadcast_conv_message_to_users, posts_freeze_author) : EXECUTE révoqué à anon/authenticated ; search_path épinglé sur 17/20 fonctions
- Vues telemetry_last24h, client_errors_top_24h, client_errors_par_heure en security_invoker=on → vides sous RLS
- Anti-flood serveur sur comment_interactions (60/min), event_reactions (30/min), reports (10/min), passion_requests (5/24 h)
- declare_birth_year + trigger : majorité non avançable (fonction et trigger concordants)
- CSP connect-src bornée au projet Supabase et aux services cartographiques/GIF/STUN-TURN ; anon/authenticated sans CREATE sur public
- 0 base64 en conv_messages.content et posts.media_url (ADR-004 respecté sur les données vivantes)

### Non vérifié (BLOQUÉ) et ce qu'il faudrait

- Isolation par requête SOUS LE MOTEUR RLS (SET LOCAL ROLE anon/authenticated) : refusé au rôle du connecteur (42501). Il faudrait un rôle de connecteur membre de anon/authenticated, ou une base jetable (Postgres Docker + policies réelles, comme tests/sql/socle-prod.sql) — ce que le banc CI « Banc Trust & Safety serveur (RLS, PostgreSQL jetable) » fait partiellement.
- Appels REST/Storage/RPC anon contre la production (counts, listing des buckets, oracles RPC) : proxy de l'environnement → CONNECT 403 sur supabase.co ; script rest_anon.sh également refusé par le classificateur. Il faudrait un poste avec accès réseau ; les étapes sont écrites dans les findings.
- UPDATE/DELETE ciblés sous rôle tiers dans une transaction annulée : impossible (SET ROLE refusé + transaction_read_only). Émulation des USING seulement ; la preuve exécutée reste authz-critical.spec.js en CI (run 33861671142 vert, non relancé ici : comptes réels + SUPABASE_SERVICE_ROLE_KEY).
- Compte privé : 0 profil is_private en prod → invisibilité d'un post privé non vérifiable sur données ; portée par la policy et par confidentialite.spec.js (projet prod, non lancé).
- Limites Realtime du plan (connexions simultanées, messages/s) et réglages Auth (provider anonyme, HIBP, captcha, MFA) : non lisibles par le connecteur (features=database,debugging,docs) ; il faudrait le tableau de bord Supabase.
- Suites du projet prod (authz-critical, confidentialite, multi-comptes, blocage-acces, user-state-horodatage, qa-campaign, suppression-compte) : NON LANCÉES (interdit : comptes réels en prod) ; citées via le job CI vert.
- Vérification que les previews Netlify pointent la prod : déduite de deploy.yml (même dist, même clé inlinée) — netlify.app inaccessible (403) donc non observée en ligne.

### Affirmations des anciens rapports confrontées au code actuel

- SECURITY_AUDIT S1 « RLS par propriétaire en place, à auditer par table » → toujours vraie pour le noyau ; l'audit par table révèle 2 exceptions non documentées (conv_reads SELECT true, storage listing anon) et 9 UPDATE sans WITH CHECK — preuve : policies.json
- SECURITY_AUDIT S4 / CHECKLIST « aucun secret en clair, service_role côté serveur uniquement » → toujours vraie pour le code (grep sans valeur), nuancée par scripts/setup-storage.html qui fait coller la clé dans un navigateur
- SECURITY_AUDIT S10 / KNOWN_RISKS R2 « médias privés en bucket public, URLs signées à faire (P0) » → toujours vraie et AGGRAVÉE : la policy SELECT autorise le LISTING à anon ; aucune URL signée dans le code (createSignedUrl = stub)
- KNOWN_RISKS 2026-08-09 « conv_messages/conv_members/conversations : lecture réservée aux membres » → toujours vraie (émulation 0/0/0) ; « follows/event_attendees publics = choix assumé » → toujours vrai ; conv_reads n'y figure pas et est public
- KNOWN_RISKS 2026-08-09 « notifications déjà scellé user_id = auth.uid() » → vrai en lecture ; l'INSERT est depuis contraint sur from_id (notifications_insert_own_author) mais reste libre sur destinataire/contenu et sans rate-limit
- KNOWN_RISKS 2026-08-09 « purge_telemetry n'était appelable par n'importe qui → révoqué » → toujours vraie (exec_roles null) ; mais aucun cron n'appelle purge_telemetry (seul purge_client_errors est planifié)
- KNOWN_RISKS R3 « schéma prod diverge des migrations, migration-checker en gate » → toujours vraie et non mitigée : 4 migrations enregistrées / 64 fichiers, référence périmée (4 tables absentes)
- PASSIO_PRODUCTION_READINESS.md:18 « Confidentialité — base PROUVÉ, RLS sur 34/34 tables » → dépassée : 39/39 tables avec RLS ; la mention PROUVÉ ne couvre ni conv_reads ni le Storage
- PASSIO_PRODUCTION_READINESS.md:35-40 « restauration et staging bloqués par une base qui ne soit pas la production » → toujours vraie (aucun second projet)
- CHECKLIST_COMMERCIALISATION « RLS v2 (26 policies) + migration conv_members appliquée » → périmée : 119 policies public ; conv_members INSERT bien appliquée (is_conversation_creator/can_join_event_conversation/is_blocked_with)
- ADR-012 « canal ① en read_only=on ; 4 fonctions SECURITY DEFINER appelables par anon à traiter par le canal ③ » → read-only confirmé indirectement (SET ROLE refusé, aucune écriture possible) ; les 4 fonctions sont TOUJOURS exposées (advisor identique le 2026-09-04) — non traité
- DATABASE_MODEL.md / CLAUDE.md « SCHEMA_PROD_REFERENCE.sql = photographie de la structure RÉELLE » → FAUSSE aujourd'hui (schema_reference_vs_prod.txt)
- DATABASE_MODEL.md « Realtime : livraison respecte la RLS (prouvée par tests multi-comptes) » → non vérifiable ici (suite prod non lancée) ; cohérent avec les policies pour postgres_changes ; les canaux broadcast publics (ring/call/typing) n'ont AUCUNE RLS
- Faits établis de l'orchestrateur « 128 policies public » → recompté : 119 public + 4 storage + 2 realtime = 125 (pg_policies, 2026-09-04)

### Fichiers de preuve

- `preuves/supabase-isolation/policies.json`
- `preuves/supabase-isolation/policies_liste_live.txt`
- `preuves/supabase-isolation/isolation_par_table.md`
- `preuves/supabase-isolation/fonctions_realtime_storage_staging.md`
- `preuves/supabase-isolation/schema_reference_vs_prod.txt`
- `preuves/supabase-isolation/ref_cols.txt`

### Notes de l'auditeur

VERDICTS DEMANDÉS. Isolation des comptes côté base : NON PROUVÉE ICI au sens strict (SET ROLE et REST bloqués) ; CONFORME PAR ÉMULATION sur le noyau messagerie/notifications/état ; DÉFAILLANTE sur conv_reads (SUP-02), Storage attachments (SUP-01) et l'UPDATE de conv_messages (SUP-03). La preuve exécutée qui existe est authz-critical en CI (run 33861671142). Staging séparé : NON (SUP-04) — critère d'interdiction du GO grande échelle selon le mandat.
CAPACITÉ / COÛTS (non prouvés, relevés) : base 92 Mo dont telemetry_events 54 Mo (111 829 lignes en 30 jours, aucun cron de purge, fonction purge_telemetry inutilisée) et user_state 10 Mo (84 lignes, max 4,8 Mo pour UNE ligne — le blob d'état contient probablement des médias, à croiser avec le domaine perf/sync) ; Storage 171 Mo/70 objets, vidéos jusqu'à 9,9 Mo sans filtre MIME ni quota par compte (50 Mo par fichier) ; plan Supabase inconnu → limites Realtime/Storage non prouvées. auth_rls_initplan ×78 : coût RLS par ligne à corriger avant montée en charge.
RECOMMANDATIONS À SOUMETTRE À BENJAMIN : (1) traiter SUP-01/02/03 en une seule migration RLS + Storage avant toute ouverture publique, avec extension d'authz-critical (conv_reads, PATCH conv_id, lecture d'un attachment par un tiers) ; (2) décider le statut des tables cdv_*/step_interactions (retirer de la publication et fermer la lecture, ou archiver) ; (3) créer le projet de staging et y router previews + suites prod — c'est aussi la base cible de la restauration jamais exécutée ; (4) régénérer SCHEMA_PROD_REFERENCE.sql et l'imposer en gate ; (5) révoquer EXECUTE anon/authenticated sur les 4 helpers de policies (ADR-012 l'a noté le 2026-09-03 sans suite) en vérifiant storage_chemin_autorise ; (6) supprimer scripts/setup-storage.html et create-buckets-auto.html ; (7) documenter dans KNOWN_RISKS que event_attendees expose checked_in_at/rating/feedback publiquement (choix ou défaut ?) ; (8) rate-limit sur notifications, telemetry_events, client_errors, conv_messages, posts.
CONSERVER : le socle de policies propriétaire, les triggers d'identité canonique et de gel d'auteur, le cloisonnement Storage en écriture, le canal realtime privé user:<uid>, la vérification JWT des Edge Functions, l'absence de secret dans le dépôt.
LIMITES DE L'AUDIT : Chromium/Playwright non utilisés (aucune mesure navigateur nécessaire dans ce domaine) ; aucune écriture en base ; git status vide à la fin (vérifié deux fois) ; aucun fichier suivi modifié ; rest_anon.sh n'a jamais été créé (commande refusée). Comptage des policies : 125 et non 128 comme dans les faits établis.

## Domaine « auth-rgpd »

Domaine auth-rgpd audité en lecture seule sur le SHA c8cb8e99 (la branche d'audit n'ajoute que des fichiers d'audit ; `git status --short` vide à la fin). Méthode : lecture intégrale de js/access-gate.js, js/telemetry.js, supabase/functions/delete-account/index.ts, des fonctions d'auth/session d'app-02 (onbDoAuth, onbForgotPassword, onbResendConfirmation, doLogout, purgeAccountScopedData, purgerJetonAuthLocal, adopterCompteConnecte, _peutPousserEtat, doDeleteAccount, openPrivacyPolicy, openAbout) et d'app-08 (boot, onAuthStateChange, createClient), du formulaire d'auth et du panneau Paramètres d'index.html, des docs CONFIRMATION_EMAIL / SETUP_SMTP_AUTH / SECURITE_CODE_ACCES / EDGE_FUNCTION_DELETE_ACCOUNT ; croisement avec le dump des 125 policies (preuves/supabase-isolation/policies.json) pour établir, table par table, ce que la suppression de compte n'atteint pas. Exécution des 8 suites e2e demandées : 58/58 vertes en 16,6 min (workers=1, port 8107). Trois attaques émulées en Chromium, toutes les requêtes *.supabase.co interceptées (rien n'a atteint la production) : contournement du gate par sessionStorage (réussi), rejeu au démarrage de la file de messages d'un compte A sous l'identité du compte B (réussi : POST conv_messages avec from_id = B), télémétrie d'un visiteur sans compte (10 événements avec device_id persistant, sans un mot de consentement). Force brute hors ligne du hash du gate : 6 ms. Dépôt GitHub PUBLIC avec le code d'accès en clair dans 19 fichiers. Verdict du domaine : l'authentification (confirmation d'e-mail, anti-énumération, déconnexion complète, isolation de l'état d'exploration) est solide et prouvée par tests ; en revanche le volet juridique/RGPD est INSUFFISANT POUR UN LANCEMENT PUBLIC : aucune CGU, aucune mention légale, politique de confidentialité incomplète et périmée, aucun recueil d'acceptation, télémétrie sans consentement, vérification d'âge morte sur le chemin nominal, suppression de compte qui laisse des résidus dans ~18 tables et un bucket, et deux défauts d'isolation inter-comptes côté appareil.

### Contrôles (44)

| Id | Contrôle | Statut | Méthode | Preuve |
|---|---|---|---|---|
| AUTH-C01 | Inscription : confirmation d'e-mail obligatoire (signUp sans session, compte inutilisable avant confirmation) | **PROUVÉ** | test exécuté | PASSIO_PORT=8107 npx playwright test --project=local tests/e2e/confirmation-email.spec.js … --workers=1 → 7/7 passed (preuves/auth-rgpd/playwright-bilan.txt) ; app-02-state-utils.js:3468-3477 (branche « pas de session → confirme ») |
| AUTH-C02 | Renvoi du lien de confirmation et anti-énumération (identities vides, message de succès neutre, délai anti-abus traduit) | **PROUVÉ** | test exécuté | confirmation-email.spec.js 7/7 ; app-02:3255-3302 (onbResendConfirmation, message « Si ce compte attend une confirmation… ») ; app-02:3455-3466 (identities.length===0 → « déjà utilisé » sans distinguer) |
| AUTH-C03 | Connexion : message d'erreur générique (« E-mail ou mot de passe incorrect »), pas de fuite d'existence du compte | **CONFORME PAR INSPECTION** | inspection code | app-02-state-utils.js:3431-3432 ; Supabase rend « Invalid login credentials » indistinct pour e-mail inconnu / mauvais mot de passe |
| AUTH-C04 | Mot de passe oublié : flux hash type=recovery, lien à usage unique, adoption différée au changement effectif, pas de rechargement en cours de route | **CONFORME PAR INSPECTION** | inspection code | app-02:3238-3251 (resetPasswordForEmail, redirectTo origin) ; app-02:3325-3371 (_showPasswordRecoveryUI, min 6 car., adoption puis reload) ; app-08:2176-2178, 2235-2262 (_recuperationEnCours, _exigerRestaurationAvantEcriture) |
| AUTH-C05 | Sessions : jeton sb-<ref>-auth-token en localStorage, persistSession/autoRefreshToken (défauts SDK), durée du JWT et du refresh token | **BLOQUÉ** | inspection code | app-08:2688 createClient(SUPABASE_URL, SUPABASE_KEY) sans option → défauts du SDK (persistSession=true, autoRefreshToken=true, storage localStorage). La durée du JWT (défaut 3600 s) et la rotation du refresh token sont des réglages du Dashboard Supabase (Authentication → Sessions), non lisibles ici. Il faudrait une capture du Dashboard ou `get_project_config`. |
| AUTH-C06 | Déconnexion complète : lecture de {error} de signOut, purge du jeton SDK en repli, purge des clés de compte + IndexedDB, rechargement | **PROUVÉ** | test exécuté | connexion-compte-existant.spec.js 16/16 passed ; app-02:2945-2985 (doLogout), 2932-2943 (purgerJetonAuthLocal par motif /^sb-.+-auth-token$/), 2741-2779 (purgeAccountScopedData, idbConvClear), idb-store.js:107 |
| AUTH-C07 | Connexion sur appareil neuf / après déconnexion : intention passio_auth_intent_v1 horodatée (TTL 10 min), posée APRÈS la purge, consommée par boot() | **PROUVÉ** | test exécuté | connexion-compte-existant.spec.js 16/16 ; app-02:2588-2602 (AUTH_INTENT_KEY, AUTH_INTENT_TTL_MS = 600 000) ; app-08:2228-2233 (branche déconnexion inachevée : signOut → purgerJetonAuthLocal → purge → intention → reload) |
| AUTH-C08 | Gate 2125 : robustesse du hash (4 chiffres, sel public), contournement, code en clair, anti-brute-force | **DÉFAILLANT** | émulation | preuves/auth-rgpd/gate-bruteforce.txt : 2 126 essais, 6 ms, code retrouvé ; émulation A (emul-auth-resultats.json) : sessionStorage.setItem('passio_gate_v1', <hash public>) → gate absent, app rendue (A-gate-contourne.png) ; access-gate.js:24-27 (GATE_SALT/GATE_HASH embarqués, aucun compteur d'essais) ; code en clair dans CLAUDE.md:31,38, docs/SECURITE_CODE_ACCES.md:44, tests/e2e/gate-helper.js:34 (19 fichiers) ; dépôt PASSIO74/passio-app visibility=public (list_repos) |
| AUTH-C09 | Tests du gate (access-gate, gate-sans-app) | **PROUVÉ** | test exécuté | access-gate.spec.js 6/6, gate-sans-app.spec.js 3/3 passed (playwright-bilan.txt) — ils prouvent que le gate s'affiche et bloque boot(), pas qu'il résiste (il ne résiste pas, cf. AUTH-C08) |
| AUTH-C10 | Protection contre les mots de passe compromis (HaveIBeenPwned) | **DÉFAILLANT** | requête base | Fait établi par l'orchestrateur (get_advisors) : « protection contre les mots de passe compromis DÉSACTIVÉE » ; docs/SETUP_SMTP_AUTH.md §5 le liste comme durcissement « restant » depuis le 2026-08-30 ; .passio/context/KNOWN_RISKS.md:24 le laisse « volontairement » |
| AUTH-C11 | Force du mot de passe (longueur minimale, complexité) | **DÉFAILLANT** | inspection code | app-02:3401 (pwd.length < 6), app-02:2977 (« 6 caractères minimum »), app-02:3345 (récupération : < 6), index.html:282 minlength=6 ; aucune règle de complexité ; réglage serveur non lisible ici |
| AUTH-C12 | MFA utilisateur | **DÉFAILLANT** | inspection code | grep -i '\.mfa\.\|enroll\|totp' js/*.js → 0 résultat : aucune MFA proposée (acceptable pour un réseau social grand public à ce stade, à documenter) |
| AUTH-C13 | Verrouillage / rate-limit des tentatives de connexion (GoTrue) | **BLOQUÉ** | non réalisé | Aucun compteur côté client (onbDoAuth). Les limites GoTrue (Authentication → Rate Limits) ne sont pas lisibles ici et un test de charge contre la prod est interdit. Il faudrait la capture du Dashboard « Rate Limits ». |
| AUTH-C14 | OAuth social (Google) : présence, activation du provider, couverture de test | **BLOQUÉ** | inspection code | index.html:294-297 bouton « Continuer avec Google » visible ; app-02:3305-3322 signInWithOAuth ; grep tests/e2e → aucune suite ; activation du provider = Dashboard Supabase, non vérifiable ici |
| AUTH-C15 | Numéro de téléphone à l'inscription : obligatoire, vérifié ?, où stocké, déclaré ? | **DÉFAILLANT** | inspection code | app-02:3400-3406 (8-15 chiffres obligatoires en signup, aucune vérification OTP), 3418 (user_metadata.phone), 3421-3424 (state.user.general.phone → blob user_state via _syncableState) ; dashboard/server/accounts.js:35-38 le lit ; politique §1 (app-02:3137) ne le mentionne pas |
| AUTH-C16 | Vérification d'âge (13+) : atteinte sur le chemin d'inscription nominal ? exactitude des promesses | **DÉFAILLANT** | inspection code | onbValidateAge app-02:3179-3193 n'est appelée que par l'étape « age » atteinte via onbNext() après une inscription QUI REND UNE SESSION (app-02:3553) — impossible depuis « Confirm email » (2026-08-30) ; chemin réel : signup → app-02:3468-3477 (retour onglet signin) → lien de confirmation → app-08:2278 `state.onboarded = true` OU signin → app-02:3506-3511 `onboarded=true; reload` ; index.html:311 « contrôle d'âge IA » ; politique §6 (app-02:3145) « l'inscription demande l'âge » |
| AUTH-C17 | Suppression in-app : parcours (confirmation SUPPRIMER, tables purgées côté client sous RLS, purge locale, IDB, reload) | **CONFORME PAR INSPECTION** | inspection code | app-02:3053-3072 (modale, saisie SUPPRIMER), 3075-3132 (13 tables + follows×2 + blocks×2, invoke delete-account, signOut, purge localStorage 'passio*' + jeton, idbConvClear, sessionStorage.clear, reload) |
| AUTH-C18 | Edge Function delete-account : périmètre EXACT des tables purgées vs 39 tables de prod | **DÉFAILLANT** | inspection code | index.ts:45-61 : 15 jobs sur 14 tables (posts, post_likes, post_comments, stories, events, event_attendees, conv_messages, conv_members, notifications[user_id], story_views, push_subscriptions, client_errors[uid], follows×2, profiles) + index.ts:66-75 bucket content/{photos,videos,audios}/<uid> + deleteUser. JAMAIS purgées par personne : analytics_events (3 855 l., aucune policy DELETE), telemetry_events (user_id + user_label=pseudo, aucune DELETE), comment_interactions, comment_likes, conv_reads (aucune DELETE), event_comments (author_name), event_reactions, reports (reporter_id, aucune DELETE), user_safety (année de naissance, SELECT seule), user_passions, passion_requests (aucune DELETE), video_lives (author_name/author_photo), post_collaborators, step_interactions, cdv_* (6 tables), notifications.from_id (le `content` chez les autres porte le pseudo), bucket attachments ; user_state et blocks : purgés côté CLIENT seulement (RLS) — cf. policies.json |
| AUTH-C19 | Edge Function : gestion d'erreur (le SDK ne lève pas ; try/catch inopérant) | **DÉFAILLANT** | inspection code | index.ts:62-65 `try { await admin.from(table).delete().eq(col, uid); } catch (_e) {}` — `{ error }` jamais lu, aucun journal, réponse `{ok:true}` même si 14 DELETE ont échoué ; même défaut côté client app-02:3100-3117 |
| AUTH-C20 | Edge Function déployée et identique au dépôt ; suite suppression-compte en CI | **BLOQUÉ** | inspection code | docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:3 « déployée le 2026-06-11 via le Dashboard » ; le fichier du dépôt date du 2026-08-31 (git log 43b8ffa) ; tests/e2e/suppression-compte.spec.js:28 `test.skip(!process.env.PASSIO_E2E_MULTI)` et PASSIO_E2E_MULTI n'est posé dans AUCUN workflow (grep .github/workflows → seulement un commentaire deploy.yml:155) → le job « Suites production » vert du run 33861671142 NE prouve PAS la fonction. Il faudrait un run manuel PASSIO_E2E_MULTI=1 ou les logs Edge Functions du Dashboard. |
| AUTH-C21 | Bucket attachments (pièces jointes et vocaux de conversations) purgé à la suppression ? | **DÉFAILLANT** | inspection code | app-09:867 et 1597 : chemins attachments/<convId>/… (sans uid) ; index.ts ne touche que le bucket content ; docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:50-52 l'assume ; buckets public=true (fait établi) |
| AUTH-C22 | Résidus après suppression (anciens rapports : 76 user_state, 54 notifications, 19 médias) | **DÉFAILLANT** | requête base | Fait établi (orchestrateur) : 84 lignes user_state et 188 notifications pour 5 comptes auth → au moins 79 user_state orphelines (blob d'état complet, 10 Mo) ; PASSIO_PRODUCTION_READINESS.md:19 les attribue à des suppressions administratives sans les avoir purgées |
| AUTH-C23 | Délai, confirmation et message de suppression | **CONFORME PAR INSPECTION** | inspection code | app-02:3062 « e-mail retiré sous 30 jours » alors que deleteUser est immédiat (sur-promesse prudente, acceptable) ; saisie SUPPRIMER exigée |
| AUTH-C24 | Politique de confidentialité in-app : existence, exhaustivité, exactitude | **DÉFAILLANT** | inspection code | app-02:3133-3150 (modale, « Dernière mise à jour : juin 2026 ») : absents = base légale, téléphone, télémétrie/device_id, géolocalisation, sous-traitants (Netlify, Brevo, OpenFreeMap, Photon/komoot, BAN, Tenor, Giphy, Google Fonts, jsdelivr/unpkg, STUN Google, TURN openrelay.metered.ca — netlify.toml:19), durée de conservation de la télémétrie, DPO, transferts (« UE/US » sans précision) ; périmés = « carnets » (retirés ADR-011), « l'inscription demande l'âge » (faux, C16), « accès protégé par code » (contournable, C08) ; contact = adresse d'une autre entreprise |
| AUTH-C25 | CGU / conditions d'utilisation (contrat, licence sur les contenus, règles de modération, résiliation) | **DÉFAILLANT** | inspection code | grep -i 'CGU\|conditions générales\|conditions d.utilisation' index.html js/*.js → 0 ; aucune page, aucune modale, aucun lien |
| AUTH-C26 | Mentions légales (LCEN art. 6-III : éditeur, adresse, SIREN, directeur de publication, hébergeur) | **DÉFAILLANT** | inspection code | grep -i 'siren\|siret\|siège social\|directeur de la publication\|hébergeur' → 0 ; openAbout app-02:2539-2555 affiche « PASSIO SAS · France · contact@passio.app » sans SIREN ni adresse, contredit par la politique (autre adresse de contact) ; existence de la société non vérifiable |
| AUTH-C27 | Recueil de l'acceptation (CGU/politique) à l'inscription | **DÉFAILLANT** | inspection code | index.html:255-300 : formulaire e-mail / téléphone / mot de passe / Google, aucune case, aucun lien vers la politique ; grep openPrivacyPolicy index.html → uniquement Paramètres → Support (l.457) |
| AUTH-C28 | Télémétrie : base de licéité, consentement ePrivacy, interrupteur, données envoyées | **DÉFAILLANT** | émulation | émulation C (emul-auth-resultats.json) : visiteur sans compte, 10 événements captés, champs device_id (23 car., localStorage passio_device_id persistant), user_id/user_label (pseudo quand connecté, telemetry.js:238-249), platform/browser/screen_size ; C_consentement_demande=false ; telemetry.js:71 défaut ON en prod ; opt-out ?telemetry=0 (l.53) sans aucun interrupteur dans Paramètres (grep setEnabled → 0 appelant UI) ; telemetrie-preauth.spec.js 3/3 verts confirment l'envoi pré-auth |
| AUTH-C29 | Durée de conservation (télémétrie, analytics, client_errors) | **DÉFAILLANT** | requête base | Fait établi : 1 seul cron `purge_client_errors` ; telemetry_events = 111 828 lignes (54 Mo) ; migration_telemetry.sql:85-97 définit purge_telemetry(keep_days) jamais planifiée ; analytics_events 3 855 lignes sans purge ni DELETE |
| AUTH-C30 | Localisation : déclenchement, précision, stockage, tiers, désactivable | **CONFORME PAR INSPECTION** | inspection code | app-07:2758-2762 (position demandée seulement si écran IRL visible et sans marqueur UI-4A0), 1120-1150 (irlUserLocation en MÉMOIRE, coordonnées exactes envoyées à photon.komoot.io / api-adresse pour le nom de ville), 3451-3460 (check-in : distance calculée localement, GPS refusé toléré), app-09:993-1010 (partage de position à 5 décimales dans un message, geste explicite) ; first-run.js:35 aucune permission en mode visiteur. Manque : mention dans la politique (C24) |
| AUTH-C31 | Export des données / portabilité (art. 20) | **DÉFAILLANT** | inspection code | Seul export : app-09:1245-1259 `_exportConv` (une conversation en .txt) ; aucun export de compte (profil, posts, événements, messages) ; politique §5 promet la portabilité « dans l'app » |
| AUTH-C32 | Droits d'accès / rectification / effacement : chemin et interlocuteur | **PROBABLE** | inspection code | Effacement : in-app (C17). Accès/rectification : par e-mail vers l'adresse d'une autre société (app-02:3144) ; aucun formulaire, aucun délai annoncé, aucun DPO |
| AUTH-C33 | Transferts hors UE et sous-traitants | **BLOQUÉ** | inspection code | Région Supabase : aucun indice dans le dépôt (grep region/eu-west/frankfurt → 0), non vérifiable sans Dashboard ; Netlify (CDN mondial), Tenor (Google, US), Giphy (US), Google Fonts (fonts.googleapis.com, index.html), TURN openrelay.metered.ca (relais des flux WebRTC), Photon komoot (DE) — netlify.toml:19 ; aucun DPA ni liste dans le dépôt |
| AUTH-C34 | Registre des traitements, DPO, analyse d'impact | **BLOQUÉ** | inspection code | grep -ri 'registre des traitements\|DPO\|délégué à la protection\|AIPD\|DPIA' docs/ .passio/ → 0 ; documents hors dépôt possibles, non fournis |
| AUTH-C35 | Mineurs : âge minimum, vérification, statut isMinor | **DÉFAILLANT** | inspection code | cf. C16 ; state.user.isMinor (app-02:3192) n'est posé que par l'étape morte ; app-07:4962 l'utilise pour l'IRL ; declare_birth_year (app-07:5017-5034) seulement à la proposition d'un événement → user_safety (2 lignes) |
| AUTH-C36 | Propriété intellectuelle des contenus (licence accordée à PASSIO) | **DÉFAILLANT** | inspection code | Aucune CGU (C25) → aucune licence, aucune clause de retrait, aucune règle sur le contenu d'autrui |
| AUTH-C37 | LCEN / DSA : signalement de contenu illicite, point de contact, accusé de réception | **DÉFAILLANT** | inspection code | Signalement in-app : app-04:3340-3350 (reportUser/reportPost) → app-08:5385 insert reports (policy INSERT seule, 2 lignes en prod) ; aucun point de contact publié, aucune procédure de notification, aucun retour au signalant, aucune conservation motivée |
| AUTH-C38 | Séparation des comptes : ACCOUNT_SCOPED_KEYS vs clés réellement écrites | **DÉFAILLANT** | émulation | Inventaire `localStorage.setItem` (js/*.js) : non purgées = passio_outbox_v1 (messages, app-04:4580), passio_cmt_outbox_v1 (commentaires, app-04:462), passio_post_delete_outbox_v1 (app-04:64), passio_event_comments_v1 (app-07:4458), passio_geo_cache_v1 (app-07:958 — la liste purge « passio_cdv_geo_v1 », clé morte), passio_first_run_v1 ; émulation B (B-outbox-rejouee-sous-identite-B.json) : au boot, POST /rest/v1/conv_messages {id: msg_prive_de_A, conv_id: conv_A_B, from_id: <uid B>, content: « message privé écrit par A »} sans aucune action de B (app-08:5956 setTimeout(_flushOutbox,1500) ; app-04:4644-4649 ; app-04:4616-4620 from_id: MY_UID) |
| AUTH-C39 | IndexedDB des conversations purgée à la déconnexion et à la suppression | **CONFORME PAR INSPECTION** | inspection code | idb-store.js:107 window.idbConvClear ; app-02:2775 (purgeAccountScopedData), app-02:3128 (doDeleteAccount) ; un seul store IDB (conversations_v1) |
| AUTH-C40 | Beacon pagehide et état d'exploration anonyme : jamais poussés sous une autre identité | **PROUVÉ** | test exécuté | exploration-anonyme-vs-compte.spec.js 16/16 passed ; app-02:438-451 (_peutPousserEtat), 630-648 (beacon : _accountPurged, MY_UID, onboarded, _peutPousserEtat, _stateDirty), 2855-2893 (adopterCompteConnecte) |
| AUTH-C41 | Service worker : ne met pas en cache les réponses Supabase (pas de données personnelles dans Cache Storage) | **CONFORME PAR INSPECTION** | inspection code | sw.js:127-133 : GET seulement, `url.hostname !== self.location.hostname → return` |
| AUTH-C42 | Suites e2e demandées (confirmation-email, connexion-compte-existant, exploration-anonyme-vs-compte, access-gate, gate-sans-app, audit-identite-emoji, telemetrie-preauth, user-passions-miroir) | **PROUVÉ** | test exécuté | PASSIO_PORT=8107 npx playwright test --project=local <8 specs> --workers=1 --reporter=line → « 58 passed (16.6m) », EXIT=0 (preuves/auth-rgpd/playwright-suites-auth.log, playwright-bilan.txt : 6+4+7+16+16+3+3+5 = 60 lignes de progression dont 2 reprises, 58 tests) |
| AUTH-C43 | Suites à comptes réels (suppression-compte, user-state-horodatage) | **BLOQUÉ** | non réalisé | Interdites ici (écriture en base réelle, SUPABASE_SERVICE_ROLE_KEY). user-state-horodatage : dans le projet prod, job « Suites production (comptes réels) » vert au run 33861671142. suppression-compte : SKIPPÉE même en CI (C20). |
| AUTH-C44 | Exposition du dépôt public (documentation interne, code d'accès, adresse de contact) | **DÉFAILLANT** | inspection code | list_repos → PASSIO74/passio-app visibility=public ; CLAUDE.md, docs/SECURITE_CODE_ACCES.md:44 (code et hash), tests/e2e/gate-helper.js:34, adresse e-mail de contact en clair dans app-02:3062 et 3144 |

### Problèmes (13)

| Id | Priorité retenue | Relecture | Titre |
|---|---|---|---|
| AUTH-01 | **P1** | NON VÉRIFIÉ (pas de relecture) | Le gate « bêta privée » (2125) n'a aucune valeur : force brute en 6 ms, contournement par sessionStorage, code en clair dans un dépôt GitHub public |
| AUTH-02 | **P1** | NON VÉRIFIÉ (pas de relecture) | La vérification d'âge n'est jamais atteinte sur le chemin d'inscription nominal, et l'écran promet un « contrôle d'âge IA » inexistant |
| AUTH-03 | **P1** | NON VÉRIFIÉ (pas de relecture) | Aucune CGU, aucune mention légale, aucun recueil d'acceptation ; politique de confidentialité incomplète et périmée |
| AUTH-04 | **P1** | NON VÉRIFIÉ (pas de relecture) | Télémétrie active par défaut sans consentement, sans interrupteur in-app, avec identifiant d'appareil persistant, pseudo et identifiant de compte, conservée sans limite |
| AUTH-05 | **P1** | NON VÉRIFIÉ (pas de relecture) | La suppression du compte laisse des données personnelles dans ~18 tables et un bucket, et ignore toutes ses erreurs |
| AUTH-06 | **P1** | NON VÉRIFIÉ (pas de relecture) | Files locales non purgées à la déconnexion : un message privé du compte A est rejoué et envoyé sous l'identité du compte B au démarrage suivant |
| AUTH-07 | **P2** | NON VÉRIFIÉ (pas de relecture) | Numéro de téléphone obligatoire à l'inscription, non vérifié, sans finalité déclarée, dupliqué dans le blob user_state |
| AUTH-08 | **P2** | NON VÉRIFIÉ (pas de relecture) | Mot de passe : 6 caractères sans complexité, protection contre les mots de passe compromis désactivée, aucune MFA |
| AUTH-09 | **P2** | NON VÉRIFIÉ (pas de relecture) | Aucun export de données de compte (portabilité art. 20) ; accès/rectification par e-mail seulement, vers l'adresse d'une autre société |
| AUTH-10 | **P2** | NON VÉRIFIÉ (pas de relecture) | Sous-traitants et transferts hors UE non maîtrisés ni documentés (Google Fonts, Tenor/Giphy, TURN tiers, Photon, région Supabase inconnue) |
| AUTH-11 | **P2** | NON VÉRIFIÉ (pas de relecture) | LCEN / DSA : signalement in-app sans point de contact, sans accusé de réception ni procédure de retrait |
| AUTH-12 | **P3** | NON VÉRIFIÉ (pas de relecture) | Le déploiement et la conformité de l'Edge Function delete-account ne sont prouvés par aucun test automatisé (suite skippée en CI) |
| AUTH-13 | **P3** | NON VÉRIFIÉ (pas de relecture) | Bouton « Continuer avec Google » sans preuve d'activation du provider ni test ; erreur brute en anglais en cas d'échec |

### AUTH-01 — Le gate « bêta privée » (2125) n'a aucune valeur : force brute en 6 ms, contournement par sessionStorage, code en clair dans un dépôt GitHub public

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-01 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Verrouillage pré-lancement js/access-gate.js |
| Résultat attendu | Un accès réellement restreint (vérification serveur, compteur d'essais, code non retrouvable) ou l'abandon assumé du gate — et une politique de confidentialité qui ne promet pas une protection inexistante |
| Résultat observé | Code à 4 chiffres haché SHA-256 avec un sel public embarqué (access-gate.js:24-25) : 10 000 candidats, retrouvé en 2 126 essais / 6 ms hors ligne ; aucun compteur d'essais dans l'UI ; `sessionStorage.setItem('passio_gate_v1', <hash public>)` fait disparaître le gate (émulation A) ; le code figure en clair dans 19 fichiers du dépôt PUBLIC (CLAUDE.md:31, docs/SECURITE_CODE_ACCES.md:44, tests/e2e/gate-helper.js:34) ; docs/SECURITE_CODE_ACCES.md:47 affirme « le hash empêche seulement de retrouver le code », ce qui est faux ; la politique §7 (app-02:3146) affirme « l'accès est protégé par code » |
| Reproduction | node -e "…sha256('passio-gate-v1::'+code) sur 0000-9999…" (preuves/auth-rgpd/gate-bruteforce.txt) ; ou dans la console : sessionStorage.setItem('passio_gate_v1','67a2ba44…390f') puis recharger |
| Preuve | preuves/auth-rgpd/gate-bruteforce.txt ; preuves/auth-rgpd/emul-auth-resultats.json (A_gate_absent_apres_setItem=true) ; preuves/auth-rgpd/A-gate-contourne.png ; list_repos visibility=public |
| Impact utilisateur et commercial | Toute personne peut entrer dans la « bêta privée » : les contenus publics (profils, posts, événements, lieux) sont exposés à des tiers non invités ; la promesse faite aux testeurs est fausse ; en cas de contrôle, une mesure de sécurité annoncée mais inopérante est un aggravant. Commercialement : le gate ne peut pas servir de mécanisme d'invitation payante ou de rareté. |
| Visibilité dans le Centre de pilotage | non — le gate n'émet aucun événement, un accès non autorisé est indiscernable d'un testeur |
| Détection par la Sentinelle | non — aucune tentative n'est journalisée (la vérification est purement locale) |
| Proposition de correction | Soit retirer le gate et cesser de le promettre (la RLS est la vraie frontière), soit le remplacer par la migration déjà prévue dans docs/SECURITE_CODE_ACCES.md (Edge Function verify-access avec limite d'essais, ou liste blanche d'e-mails, ou codes d'invitation à usage unique en table `invites`) ; dans tous les cas retirer le code du dépôt, changer le code, passer le dépôt en privé ou déplacer CLAUDE.md/docs sensibles |
| Risque de régression | Faible : verifyCode() est le seul point à remplacer ; 9 tests (access-gate, gate-sans-app) et gate-helper.js à adapter |
| Effort estimé | 0,5 j (retrait) à 1,5 j (Edge Function + invitations) |

### AUTH-02 — La vérification d'âge n'est jamais atteinte sur le chemin d'inscription nominal, et l'écran promet un « contrôle d'âge IA » inexistant

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-02 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Onboarding / inscription (étape data-onb-step="age") |
| Résultat attendu | Tout nouveau compte déclare son année de naissance avant d'entrer dans l'app (13+), la mention est exacte, la donnée sert la protection des mineurs (isMinor, user_safety) |
| Résultat observé | onbValidateAge (app-02:3179-3193) n'est appelée que par l'étape « age », atteinte via onbNext() à app-02:3553 seulement si signUp RENVOIE UNE SESSION — impossible depuis « Confirm email » (2026-08-30) : signup → app-02:3468-3477 revient sur l'onglet connexion ; lien de confirmation → app-08:2278 pose `state.onboarded = true` et entre dans l'app ; connexion → app-02:3506-3511 `onboarded=true; reload`. Résultat : birthYear reste null, isMinor false. index.html:311 : « PASSIO protège les mineurs avec un contrôle d'âge IA » ; politique §6 : « l'inscription demande l'âge à l'onboarding » |
| Reproduction | Créer un compte (e-mail à confirmer), cliquer le lien de confirmation ou se connecter : l'app s'ouvre sur le fil sans jamais afficher « Vérification d'âge » ; `state.user.birthYear` = null |
| Preuve | app-02-state-utils.js:3468-3477, 3506-3511, 3553 ; app-08-ui-modals-tour.js:2278-2280 ; index.html:308-316 ; app-02:3145 |
| Impact utilisateur et commercial | Des enfants de moins de 13 ans peuvent s'inscrire et rencontrer des adultes IRL ; la politique et l'écran mentent (pratique commerciale trompeuse) ; obligations DSA art. 28 / CNIL sur les mineurs non tenues |
| Visibilité dans le Centre de pilotage | non — aucun événement « âge déclaré » ; le dashboard ne montre pas le taux de comptes sans année de naissance |
| Détection par la Sentinelle | non |
| Proposition de correction | Dans boot() (branche session, app-08:2278) et après signin : si `!state.user.birthYear` afficher l'étape « age » AVANT d'entrer ; écrire l'année via declare_birth_year (user_safety, non avançable) ; retirer « IA » du texte ; ajouter un test qui suit le chemin réel (compte pré-confirmé via compte-e2e.js) |
| Risque de régression | Moyen : touche boot() et les 16 cas de connexion-compte-existant (à rejouer) |
| Effort estimé | 1 j |

### AUTH-03 — Aucune CGU, aucune mention légale, aucun recueil d'acceptation ; politique de confidentialité incomplète et périmée

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-03 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Documents juridiques in-app (Paramètres → Support → Politique de confidentialité ; À propos ; formulaire d'inscription) |
| Résultat attendu | CGU (contrat, licence sur les contenus, modération, résiliation), mentions légales LCEN (éditeur, adresse, SIREN, directeur de publication, hébergeur), politique RGPD art. 13 complète (base légale, finalités, destinataires/sous-traitants, transferts, durées, droits, DPO/contact, mineurs), acceptation horodatée à l'inscription |
| Résultat observé | Politique (app-02:3133-3150, « juin 2026 ») : ne nomme pas le téléphone collecté, la télémétrie (device_id, pseudo), la géolocalisation, aucun sous-traitant hors Supabase (« UE/US ») alors que la CSP autorise Netlify, Brevo, OpenFreeMap, Photon/komoot (DE), BAN, Tenor et Giphy (US), Google Fonts (US), jsdelivr/unpkg, STUN Google, TURN openrelay.metered.ca ; cite les « carnets » retirés ; affirme un contrôle d'âge et un accès par code qui ne tiennent pas ; contact = adresse d'une autre société. CGU : 0 occurrence. Mentions légales : « PASSIO SAS · France · contact@passio.app » (app-02:2545-2547) sans SIREN ni adresse ni hébergeur, contredisant l'adresse de la politique. Formulaire (index.html:255-300) : aucune case ni lien d'acceptation |
| Reproduction | Paramètres → Support → Politique de confidentialité ; Paramètres → Support → À propos ; Créer un compte |
| Preuve | app-02-state-utils.js:3133-3150, 2539-2555 ; index.html:255-300, 457 ; grep CGU/SIREN/hébergeur → 0 ; netlify.toml:19 |
| Impact utilisateur et commercial | Bloquant juridiquement pour un lancement public : sans CGU, PASSIO n'a aucune licence sur les contenus publiés, aucune base contractuelle pour modérer ou résilier ; sans mentions légales, infraction LCEN (jusqu'à 75 000 € pour une personne physique) ; information RGPD incomplète = manquement art. 13/14 sanctionnable par la CNIL ; un investisseur ou un store le relèvera immédiatement |
| Visibilité dans le Centre de pilotage | non applicable |
| Détection par la Sentinelle | non |
| Proposition de correction | Rédiger (avec validation juridique) CGU + mentions légales + politique v2 ; les servir comme pages statiques versionnées (docs légales dans le dépôt, date de version) ; case « J'accepte les CGU et la politique » obligatoire à l'inscription, horodatage écrit dans user_metadata (terms_version, terms_accepted_at) ; lien permanent dans Paramètres ; unifier l'adresse de contact ; ajouter la liste des sous-traitants et la région d'hébergement |
| Risque de régression | Faible (contenu) ; le test confirmation-email et first-run.spec devront cocher la case |
| Effort estimé | 2-3 j de rédaction technique + relecture juridique externe |

### AUTH-04 — Télémétrie active par défaut sans consentement, sans interrupteur in-app, avec identifiant d'appareil persistant, pseudo et identifiant de compte, conservée sans limite

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-04 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | js/telemetry.js → table telemetry_events → Centre de pilotage |
| Résultat attendu | Soit un consentement préalable (bandeau) avec refus aussi simple que l'acceptation et un réglage dans Paramètres, soit une télémétrie strictement anonyme relevant de l'exemption CNIL « mesure d'audience » (pas d'identifiant de compte, durée de vie du traceur ≤ 13 mois, données ≤ 25 mois) ; durée de conservation définie et appliquée |
| Résultat observé | telemetry.js:71 : ON par défaut en production ; :36-37 device_id persistant en localStorage ; :491-498 chaque événement porte user_id (uuid) et user_label (pseudo) une fois connecté, platform, browser, screen_size, connection ; :53 opt-out uniquement par ?telemetry=0, aucun bouton dans Paramètres (Telemetry.setEnabled sans appelant UI) ; la politique ne la mentionne pas ; émulation C : un VISITEUR sans compte émet 10 événements avec device_id de 23 caractères et aucun texte de consentement à l'écran ; 111 828 lignes (54 Mo) et purge_telemetry() jamais planifiée (1 seul cron : purge_client_errors) |
| Reproduction | Ouvrir https://passio-app.netlify.app sur un appareil vierge (ou localhost ?telemetry=1) et observer les POST /rest/v1/telemetry_events ; preuves/auth-rgpd/emul-auth.js scénario C |
| Preuve | preuves/auth-rgpd/emul-auth-resultats.json (C_*) ; telemetry.js:15, 36-37, 52-71, 238-249, 491-498 ; migration_telemetry.sql:85-97 ; fait établi « 1 job cron purge_client_errors » |
| Impact utilisateur et commercial | Traceur non strictement nécessaire déposé sans consentement (art. 82 loi Informatique et Libertés / ePrivacy) : sanction CNIL possible dès le premier contrôle ; profil comportemental nominatif (pseudo + uuid + écran par écran) conservé indéfiniment ; coût de stockage croissant (54 Mo pour 5 comptes) |
| Visibilité dans le Centre de pilotage | partiel — le dashboard consomme cette télémétrie mais n'affiche ni le statut de consentement ni la rétention |
| Détection par la Sentinelle | non |
| Proposition de correction | Option 1 (rapide) : anonymiser — retirer user_id/user_label des événements (garder session_id éphémère), device_id renouvelé tous les 13 mois, finalité limitée, et déclarer l'exemption ; Option 2 : bandeau de consentement avant le premier envoi + interrupteur « Télémétrie » dans Paramètres → Confidentialité (Telemetry.setEnabled existe déjà, telemetry.js:665). Dans les deux cas : cron `SELECT cron.schedule('purge_telemetry','0 4 * * *',$$SELECT purge_telemetry(90)$$)` et purge d'analytics_events ; mettre la politique à jour |
| Risque de régression | Moyen : le Centre de pilotage relie des événements par user_id (traçage bout-en-bout, Sentinelle) — à rebrancher sur session_id ; telemetrie-preauth.spec.js à adapter |
| Effort estimé | 1-2 j |

### AUTH-05 — La suppression du compte laisse des données personnelles dans ~18 tables et un bucket, et ignore toutes ses erreurs

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-05 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Paramètres → Supprimer mon compte → doDeleteAccount (app-02) + Edge Function delete-account |
| Résultat attendu | Effacement complet (art. 17) de tout ce qui identifie la personne dans les 39 tables et les 2 buckets, avec journal des échecs ; la modale promet « tes messages, conversations et notifications… tous tes posts, photos, vidéos… supprimés » |
| Résultat observé | Edge Function (index.ts:45-61) : 14 tables + bucket content/{photos,videos,audios}/<uid>. Jamais purgés par personne (aucune policy DELETE côté client non plus, policies.json) : analytics_events (user_id, 3 855 l.), telemetry_events (user_id + pseudo), comment_interactions, comment_likes, conv_reads, event_comments (author_name), event_reactions, reports, user_safety (année de naissance), user_passions, passion_requests, video_lives (author_name, author_photo), post_collaborators, step_interactions, cdv_* (6 tables), notifications.from_id (chez les destinataires, `content` porte le pseudo), bucket attachments/<convId>/… (photos, fichiers et vocaux de conversations privées, bucket public). user_state et blocks : purgés côté client seulement (RLS) — si l'appareil est hors ligne à cet instant ils restent (84 user_state pour 5 comptes en prod). index.ts:62-65 et app-02:3100-3117 : `{ error }` jamais lu, `catch` inopérant (le SDK ne lève pas), réponse `{ok:true}` quoi qu'il arrive |
| Reproduction | Après suppression d'un compte, `SELECT count(*) FROM analytics_events WHERE user_id = '<uid>'` (idem telemetry_events, comment_interactions, user_safety…) ; `SELECT name FROM storage.objects WHERE bucket_id='attachments'` — requêtes à faire exécuter par l'orchestrateur (notes) |
| Preuve | supabase/functions/delete-account/index.ts:45-75 ; app-02-state-utils.js:3084-3104 ; preuves/supabase-isolation/policies.json (tables sans DELETE : analytics_events, conv_reads, conversations, passion_requests, reports, telemetry_events, user_safety) ; ref_cols.txt (colonnes author_name/author_photo/user_label) ; fait établi 84 user_state / 5 comptes |
| Impact utilisateur et commercial | Droit à l'effacement non honoré ; médias privés de conversations restent servis publiquement après suppression (R2 aggravé) ; promesse contractuelle fausse ; résidus nominatifs (pseudo dans telemetry_events et notifications) exploitables |
| Visibilité dans le Centre de pilotage | partiel — le dashboard liste les comptes (accounts.js) mais aucun panneau « résidus après suppression » |
| Détection par la Sentinelle | non — l'Edge Function ne journalise rien et répond toujours ok |
| Proposition de correction | Étendre `jobs` dans index.ts (+ user_state, blocks×2, analytics_events, telemetry_events, comment_interactions, comment_likes, conv_reads, event_comments, event_reactions, user_safety, user_passions, passion_requests, video_lives, post_collaborators, step_interactions, cdv_* si conservées, notifications WHERE from_id) ; anonymiser plutôt que supprimer ce qui doit rester (reports → reporter_id NULL) ; purger attachments des conversations dont l'utilisateur était membre (lister conv_members AVANT de supprimer) ; lire `{ error }` de chaque DELETE, journaliser et renvoyer `{ok, echecs:[…]}` ; job hebdomadaire d'orphelins (lignes dont user_id n'existe plus dans auth.users) ; poser PASSIO_E2E_MULTI=1 sur le job prod pour que suppression-compte.spec.js tourne enfin |
| Risque de régression | Faible côté app ; la fonction se redéploie hors CI (à documenter comme changement critique, AGENTS.md) |
| Effort estimé | 1 j + 0,5 j de test en réel |

### AUTH-06 — Files locales non purgées à la déconnexion : un message privé du compte A est rejoué et envoyé sous l'identité du compte B au démarrage suivant

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-06 |
| Priorité retenue | **P1** (proposée par l'auditeur : P1) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Isolation inter-comptes côté appareil (ACCOUNT_SCOPED_KEYS, files hors-ligne) |
| Résultat attendu | Après déconnexion, aucune donnée du compte A ne reste lisible ni rejouable ; une entrée de file porte l'identité de son auteur et n'est jamais envoyée sous une autre |
| Résultat observé | passio_outbox_v1 (messages, app-04:4580), passio_cmt_outbox_v1 (commentaires, app-04:462), passio_post_delete_outbox_v1, passio_event_comments_v1, passio_geo_cache_v1 (la liste purge « passio_cdv_geo_v1 », clé qui n'est plus écrite) et passio_first_run_v1 sont absents d'ACCOUNT_SCOPED_KEYS (app-02:2711-2739). _sendTextToSupa insère `from_id: MY_UID` au moment du flush (app-04:4620) et _flushOutbox est lancé 1,5 s après le boot (app-08:5956). Émulation B : A laisse un message en file (hors ligne), se déconnecte ; B se connecte sur le même navigateur → au boot, sans aucune action, POST /rest/v1/conv_messages {id: msg_prive_de_A, conv_id: conv_A_B, from_id: <uid B>, content: « message privé écrit par A »}. Si B est membre de la conversation (cas typique d'un appareil partagé entre proches), la RLS accepte : B « envoie » le message privé de A. Même mécanisme pour les commentaires (author_id: MY_UID, app-08:3820) au prochain enqueue |
| Reproduction | preuves/auth-rgpd/emul-outbox-B.js (SDK stub, requêtes interceptées) : inserts_captes_au_boot = 1 avec from_id = uid B |
| Preuve | preuves/auth-rgpd/B-outbox-rejouee-sous-identite-B.json ; app-02-state-utils.js:2711-2739 ; app-04-comments-shop.js:4580-4649 ; app-08-ui-modals-tour.js:5956 |
| Impact utilisateur et commercial | Fuite de messages privés vers le compte suivant (lisibles dans localStorage) et usurpation d'auteur ; sur appareil partagé/familial, un message envoyé « par » la mauvaise personne dans la mauvaise conversation ; contredit l'engagement d'isolation corrigé le 2026-08-12 |
| Visibilité dans le Centre de pilotage | non — le message paraît légitime (from_id valide, RLS satisfaite) |
| Détection par la Sentinelle | non |
| Proposition de correction | Ajouter les six clés à ACCOUNT_SCOPED_KEYS ; figer `from_id`/`author_id` dans l'entrée de file à l'enqueue et, au flush, ignorer (et purger) toute entrée dont l'identité ≠ MY_UID ; test e2e : file laissée par A, boot en B, aucune requête conv_messages |
| Risque de régression | Faible : messagerie hors-ligne (messages-offline specs) à rejouer |
| Effort estimé | 0,5 j |

### AUTH-07 — Numéro de téléphone obligatoire à l'inscription, non vérifié, sans finalité déclarée, dupliqué dans le blob user_state

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-07 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Formulaire « Créer un compte » (index.html:276-279, onbDoAuth) |
| Résultat attendu | Minimisation (art. 5-1-c) : ne collecter le téléphone que s'il sert une finalité déclarée (et alors le vérifier), l'annoncer dans la politique |
| Résultat observé | app-02:3403-3406 exige 8-15 chiffres ; :3418 l'écrit dans auth.users.user_metadata ; :3421-3424 dans state.user.general.phone → blob user_state synchronisé ; lu par le dashboard (accounts.js:35-38) ; aucun usage produit (grep general.phone → 1 écriture, 0 lecture applicative) ; aucune vérification OTP ; absent de la politique (§1 « e-mail et nom d'utilisateur ») |
| Reproduction | Créer un compte sans numéro → « Numéro de téléphone invalide » |
| Preuve | index.html:276-279 ; app-02-state-utils.js:3400-3424 ; dashboard/server/accounts.js:4-9, 35-38 ; app-02:3137 |
| Impact utilisateur et commercial | Donnée de contact sensible collectée sans base ni finalité, exposée à toute fuite du blob user_state ; frein à l'inscription |
| Visibilité dans le Centre de pilotage | oui — panneau comptes (accounts.js) affiche le numéro |
| Détection par la Sentinelle | non |
| Proposition de correction | Rendre le champ facultatif ou le retirer ; sinon déclarer la finalité (récupération de compte / anti-abus), vérifier par OTP (Supabase phone auth) et ne pas le recopier dans user_state |
| Risque de régression | Faible ; confirmation-email.spec.js et first-run.spec.js remplissent le champ |
| Effort estimé | 0,5 j |

### AUTH-08 — Mot de passe : 6 caractères sans complexité, protection contre les mots de passe compromis désactivée, aucune MFA

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-08 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Inscription, changement et récupération de mot de passe |
| Résultat attendu | Recommandation CNIL 2022 : ≥ 12 caractères, ou ≥ 8 avec complexité et limitation des tentatives ; vérification HIBP activée ; MFA au moins proposée |
| Résultat observé | Minimum 6 dans les trois formulaires (app-02:3401, 2977-2979, 3345 ; index.html:282 minlength=6) ; advisor Supabase « leaked password protection » désactivé (fait établi ; SETUP_SMTP_AUTH.md §5 le liste comme restant) ; grep mfa/totp/enroll → 0 |
| Reproduction | Créer un compte avec « 123456 » |
| Preuve | app-02-state-utils.js:3401, 2977, 3345 ; index.html:282 ; docs/SETUP_SMTP_AUTH.md:78-86 ; KNOWN_RISKS.md:24 |
| Impact utilisateur et commercial | Comptes facilement compromis (credential stuffing) ; messages privés et position partagée exposés |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Activer HIBP dans le Dashboard, régler le minimum serveur à 10 avec lettres+chiffres, aligner les trois contrôles client (doc §5 prévient du désalignement) ; MFA TOTP en option ultérieure |
| Risque de régression | Faible ; tests créant des comptes via compte-e2e.js (service_role, non soumis à la règle) |
| Effort estimé | 0,25 j |

### AUTH-09 — Aucun export de données de compte (portabilité art. 20) ; accès/rectification par e-mail seulement, vers l'adresse d'une autre société

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-09 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Exercice des droits RGPD |
| Résultat attendu | Un bouton « Exporter mes données » (profil, passions, posts, commentaires, événements, messages) en format structuré, un canal d'accès/rectification identifié, un délai d'un mois annoncé |
| Résultat observé | Seul export : une conversation en .txt (app-09:1245-1259) ; la politique §5 promet la portabilité « directement dans l'app » ; contact = adresse d'une métallerie (app-02:3144), différente de celle de l'écran À propos |
| Reproduction | Paramètres → aucune entrée d'export ; Messages → ⋯ → Exporter la conversation (texte) |
| Preuve | app-09-boot-pwa.js:1245-1259 ; app-02-state-utils.js:3144, 2546 |
| Impact utilisateur et commercial | Demande de portabilité impossible à satisfaire sans intervention manuelle ; promesse fausse dans la politique |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Edge Function export-account (service_role, JWT de l'appelant) renvoyant un JSON zippé de toutes les tables + liens Storage ; bouton dans Paramètres → Compte ; adresse privacy@ dédiée |
| Risque de régression | Nul (ajout) |
| Effort estimé | 1 j |

### AUTH-10 — Sous-traitants et transferts hors UE non maîtrisés ni documentés (Google Fonts, Tenor/Giphy, TURN tiers, Photon, région Supabase inconnue)

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-10 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Chaîne d'hébergement et services tiers (netlify.toml CSP) |
| Résultat attendu | Liste des sous-traitants avec DPA, hébergement UE identifié, aucun appel tiers non nécessaire transmettant l'IP sans consentement |
| Résultat observé | CSP netlify.toml:19 : fonts.googleapis.com/gstatic (IP transmise à Google à chaque chargement — jurisprudence Munich 2022), tenor.googleapis.com et api.giphy.com (US), turn:openrelay.metered.ca (les flux audio/vidéo WebRTC peuvent transiter par un relais tiers gratuit sans DPA), photon.komoot.io (DE, reçoit les coordonnées GPS exactes), tiles.openfreemap.org ; région du projet Supabase non lisible dans le dépôt ; politique : « hébergement UE/US » |
| Reproduction | Ouvrir l'app, onglet Réseau : requêtes fonts.googleapis.com dès le chargement |
| Preuve | netlify.toml:19 ; index.html (18 références fonts.googleapis.com) ; app-07:1120-1150 (_resolveUserCityName) ; grep région → 0 |
| Impact utilisateur et commercial | Transferts non encadrés (art. 44-46), IP transmise à Google sans base ; relais TURN tiers = point de fuite des appels |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Auto-héberger les polices (fonts locales, CSP sans Google) ; TURN propre (coturn) ou service sous DPA ; documenter la région Supabase (Dashboard → Settings → General) et signer les DPA Supabase/Netlify/Brevo ; lister le tout dans la politique |
| Risque de régression | Faible (polices) ; TURN : à tester sur réseaux NAT stricts |
| Effort estimé | 1 j |

### AUTH-11 — LCEN / DSA : signalement in-app sans point de contact, sans accusé de réception ni procédure de retrait

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-11 |
| Priorité retenue | **P2** (proposée par l'auditeur : P2) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | Modération / signalement (reportUser, reportPost → table reports) |
| Résultat attendu | Point de contact unique publié (DSA art. 11-12), mécanisme de notification de contenu illicite accessible sans compte, accusé de réception et information de la décision (art. 16-17), conservation motivée des signalements |
| Résultat observé | Signalement possible seulement connecté (app-04:3340-3350 → app-08:5385 insert reports, policy INSERT seule, 2 lignes en prod) ; aucune page « signaler un contenu illicite », aucun e-mail dédié, aucun retour au signalant, aucune durée de conservation, reporter_id jamais anonymisé |
| Reproduction | Sans compte : aucun moyen de signaler ; connecté : ⋯ → Signaler → toast, rien d'autre |
| Preuve | app-04-comments-shop.js:3340-3350 ; app-08-ui-modals-tour.js:5385 ; policies.json reports (INSERT) ; grep contact/notification → 0 |
| Impact utilisateur et commercial | Non-conformité DSA (obligations de base des hébergeurs) ; responsabilité engagée si un contenu illicite notifié n'est pas retiré promptement |
| Visibilité dans le Centre de pilotage | partiel — le dashboard liste les reports |
| Détection par la Sentinelle | non |
| Proposition de correction | Page « Signaler un contenu illicite » (formulaire public + e-mail), accusé de réception automatique, statut du signalement dans reports (traité/rejeté, date), mention dans les CGU, désignation du point de contact |
| Risque de régression | Nul (ajout) |
| Effort estimé | 0,5 j technique + process |

### AUTH-12 — Le déploiement et la conformité de l'Edge Function delete-account ne sont prouvés par aucun test automatisé (suite skippée en CI)

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-12 |
| Priorité retenue | **P3** (proposée par l'auditeur : P3) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | CONFIRMÉ |
| Fonctionnalité | CI deploy.yml → « Suites production » |
| Résultat attendu | Le rapport de production readiness affirme « couvert par suppression-compte.spec.js » : la suite doit tourner à chaque déploiement |
| Résultat observé | tests/e2e/suppression-compte.spec.js:28 `test.skip(!process.env.PASSIO_E2E_MULTI)` ; PASSIO_E2E_MULTI n'apparaît dans aucun workflow (seul un commentaire deploy.yml:155) ; la doc dit « déployée le 2026-06-11 » alors que le fichier du dépôt (purge Storage) est du 2026-08-31 : l'adéquation code déployé / dépôt repose sur une vérification manuelle du 2026-08-17 |
| Reproduction | Lire le log du job « Suites production » du run 33861671142 : suppression-compte apparaît en skipped |
| Preuve | tests/e2e/suppression-compte.spec.js:18-28 ; .github/workflows/deploy.yml:155, 344-348 ; docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:3 ; git log 43b8ffa |
| Impact utilisateur et commercial | Une régression ou un redéploiement raté de la fonction passerait inaperçu ; le rapport d'août sur-affirme |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Poser PASSIO_E2E_MULTI=1 sur le job prod (compte jetable via service_role, déjà prévu) ou un canari hebdomadaire ; versionner la fonction (en-tête de version renvoyé dans la réponse) |
| Risque de régression | Nul |
| Effort estimé | 0,25 j |

### AUTH-13 — Bouton « Continuer avec Google » sans preuve d'activation du provider ni test ; erreur brute en anglais en cas d'échec

| Champ | Valeur |
|---|---|
| Identifiant | AUTH-13 |
| Priorité retenue | **P3** (proposée par l'auditeur : P3) |
| Relecture adversariale | NON VÉRIFIÉ (pas de relecture) |
| Confiance de l'auditeur | PLAUSIBLE |
| Fonctionnalité | OAuth Google (onbGoogleAuth) |
| Résultat attendu | Le bouton n'est affiché que si le provider est configuré ; un test couvre le retour OAuth (passio_oauth_pending) |
| Résultat observé | index.html:294 bouton toujours visible ; app-02:3305-3322 affiche error.message brut ; aucune suite e2e ; activation non vérifiable ici |
| Reproduction | Cliquer « Continuer avec Google » en production (non réalisé ici : netlify.app inaccessible) |
| Preuve | index.html:294-297 ; app-02-state-utils.js:3305-3322 ; grep tests/e2e signInWithOAuth → 0 |
| Impact utilisateur et commercial | Si le provider n'est pas activé, un bouton mort sur l'écran d'inscription ; si activé, un chemin d'auth non testé |
| Visibilité dans le Centre de pilotage | non |
| Détection par la Sentinelle | non |
| Proposition de correction | Vérifier le provider dans le Dashboard ; masquer le bouton derrière un drapeau ; ajouter un test de retour OAuth avec session simulée |
| Risque de régression | Nul |
| Effort estimé | 0,25 j |

### Surfaces saines

- Confirmation d'e-mail obligatoire : signUp sans session, compte inutilisable avant confirmation, renvoi du lien disponible, messages neutres — 7/7 tests verts (confirmation-email.spec.js)
- Anti-énumération : identities vides traitées, message de renvoi qui n'affirme rien, erreur de connexion générique (app-02:3431-3466)
- Déconnexion complète : {error} de signOut lu, purge du jeton SDK par motif, purge des clés de compte, IndexedDB vidé, rechargement forcé, intention de reconnexion horodatée (TTL 10 min) — 16/16 tests verts (connexion-compte-existant.spec.js)
- Isolation de l'état d'exploration anonyme : adopterCompteConnecte aux trois entrées, _peutPousserEtat sur les deux chemins d'écriture, beacon gardé — 16/16 tests verts (exploration-anonyme-vs-compte.spec.js)
- Mot de passe oublié : fragment type=recovery jamais rechargé en cours de route, adoption différée au changement effectif, écriture d'état interdite pendant la récupération (app-08:2176-2262, app-02:3325-3371)
- Localisation : demandée uniquement sur geste explicite (UI-4A0), jamais en mode visiteur, gardée en mémoire seulement, jamais écrite dans un événement (irl-trust-safety), partage de position = action volontaire
- Service worker : n'intercepte jamais les hôtes externes, aucune réponse Supabase en Cache Storage (sw.js:127-133)
- Téléphone et e-mail : jamais dans la table profiles (lecture publique) — user_metadata et user_state (RLS propriétaire) seulement
- Télémétrie : filtre PII par liste noire + primitives seules, e-mails/JWT expurgés, backlog d'un autre compte neutralisé (telemetry.js:139-160, 338-341), opt-in explicite en local — 3/3 tests verts
- Suppression in-app : confirmation textuelle SUPPRIMER, purge locale complète (localStorage passio*, jeton SDK, IndexedDB, sessionStorage), Edge Function avec JWT de l'appelant et service_role côté serveur uniquement, CORS assumé

### Non vérifié (BLOQUÉ) et ce qu'il faudrait

- Durée du JWT / rotation du refresh token / expiration d'inactivité (BLOQUÉ : réglages Dashboard Supabase → Authentication → Sessions ; il faudrait une capture ou l'API de configuration du projet)
- Limites de tentatives de connexion et d'envoi d'e-mails GoTrue (BLOQUÉ : Dashboard → Rate Limits ; test de charge contre la prod interdit)
- Activation réelle du provider Google OAuth (BLOQUÉ : Dashboard → Providers)
- Déploiement effectif et version de l'Edge Function delete-account, et purge réelle des médias (BLOQUÉ : suppression-compte.spec.js skippée en CI, comptes réels interdits ici ; il faudrait un run manuel PASSIO_E2E_MULTI=1 ou les logs Edge Functions)
- Région d'hébergement du projet Supabase et existence des DPA Supabase/Netlify/Brevo (BLOQUÉ : hors dépôt)
- Registre des traitements, DPO, AIPD (BLOQUÉ : documents hors dépôt, aucune trace dans docs/)
- Contenu réel servi par https://passio-app.netlify.app (BLOQUÉ : proxy 403, fait établi par l'orchestrateur)
- Décomptes de résidus par table après suppression et âge du plus ancien événement de télémétrie (BLOQUÉ : connecteur Supabase interdit à ce sous-agent — requêtes fournies dans notes)
- Suites prod user-state-horodatage / authz-critical (BLOQUÉ : comptes réels ; job « Suites production » vert au run 33861671142)
- Comportement sur WebKit/Firefox/Samsung (NON RÉALISÉ : Chromium seul)

### Affirmations des anciens rapports confrontées au code actuel

- PASSIO_PRODUCTION_READINESS.md:19 « Suppression de compte PROUVÉ… complet… couvert par suppression-compte.spec.js » → PARTIELLEMENT FAUSSE : la suite est test.skip sans PASSIO_E2E_MULTI, jamais posé en CI (deploy.yml) ; le parcours ne purge ni analytics_events, telemetry_events, comment_interactions, comment_likes, conv_reads, event_comments, event_reactions, user_safety, user_passions, video_lives, cdv_*, ni le bucket attachments (index.ts:45-75, policies.json)
- PASSIO_PRODUCTION_READINESS.md:19 « résidu (76 user_state, 54 notifications, 19 médias) = suppressions administratives » → NON VÉRIFIABLE ici ; le fait établi (84 user_state pour 5 comptes) montre que le résidu n'a pas été purgé depuis
- docs/CHECKLIST_COMMERCIALISATION.md:40 « Gate code 2125 (hash, jamais en clair, deep links protégés) » → FAUSSE : code en clair dans 19 fichiers d'un dépôt public, hash brute-forcé en 6 ms, contournement par sessionStorage (preuves/auth-rgpd/gate-bruteforce.txt, emul-auth-resultats.json)
- docs/SECURITE_CODE_ACCES.md:47 « le hash empêche seulement de retrouver le code » → FAUSSE (4 chiffres + sel public = 10 000 candidats)
- docs/CHECKLIST_COMMERCIALISATION.md:45 « RGPD : suppression de compte réelle + Edge Function déployée » → PARTIELLEMENT VRAIE (auth.users et 14 tables) mais incomplète (AUTH-05) ; déploiement non prouvé par la CI
- docs/CHECKLIST_COMMERCIALISATION.md:46 « Politique de confidentialité in-app » → TOUJOURS VRAIE (app-02:3133) mais incomplète et périmée (AUTH-03)
- docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:11 « efface les données des 12 tables via RLS » → TOUJOURS VRAIE pour 13 tables (+follows/blocks) mais 8 tables n'ont aucune policy DELETE (analytics_events, conv_reads, conversations, passion_requests, reports, telemetry_events, user_safety…)
- docs/EDGE_FUNCTION_DELETE_ACCOUNT.md:50 « les fichiers Storage (bucket attachments) ne sont pas purgés » → TOUJOURS VRAIE (index.ts ne touche que content)
- docs/CONFIRMATION_EMAIL.md ① ② (switchAuthTab d'abord, renvoi du lien) → TOUJOURS VRAIES (app-02:3255-3302, 3455-3477 ; 7/7 tests verts)
- docs/SETUP_SMTP_AUTH.md §5 « HIBP OFF, minimum 6 côté app » → TOUJOURS VRAIE (advisor + app-02:3401)
- KNOWN_RISKS.md R11 (DKIM/DMARC absents) → NON VÉRIFIABLE ici (DNS) ; toujours listé ouvert
- CLAUDE.md « Première visite : aucune demande de permission (GPS…) » → TOUJOURS VRAIE (first-run.js:35, app-07:2758-2762)
- CLAUDE.md « purgeAccountScopedData efface toute trace du compte » → FAUSSE pour six clés (AUTH-06 : passio_outbox_v1, passio_cmt_outbox_v1, passio_post_delete_outbox_v1, passio_event_comments_v1, passio_geo_cache_v1, passio_first_run_v1)
- Politique in-app §6 « l'inscription demande l'âge à l'onboarding » et index.html:311 « contrôle d'âge IA » → FAUSSES sur le chemin nominal (AUTH-02)
- telemetry.js en-tête « opt-in strict » / index.html:51 « opt-in via ?telemetry=1, sans PII » → FAUSSE en production : ON par défaut (telemetry.js:71), device_id persistant, user_id + pseudo

### Fichiers de preuve

- `preuves/auth-rgpd/playwright-suites-auth.log`
- `preuves/auth-rgpd/playwright-bilan.txt`
- `preuves/auth-rgpd/gate-bruteforce.txt`
- `preuves/auth-rgpd/emul-auth.js`
- `preuves/auth-rgpd/emul-auth-resultats.json`
- `preuves/auth-rgpd/A-gate-contourne.png`
- `preuves/auth-rgpd/emul-outbox-B.js`
- `preuves/auth-rgpd/stub-supabase.js`
- `preuves/auth-rgpd/B-outbox-rejouee-sous-identite-B.json`
- `preuves/auth-rgpd/http-8107.log`

### Notes de l'auditeur

VERDICT DU DOMAINE — Authentification : solide (confirmation d'e-mail, anti-énumération, déconnexion, isolation de l'état d'exploration : 58/58 tests verts). Juridique/RGPD : NON PRÊT pour un lancement public — six P1 (gate factice, âge jamais demandé, aucun document contractuel/légal ni acceptation, télémétrie sans consentement, effacement incomplet, rejeu de messages sous une autre identité).

DOCUMENTS JURIDIQUES — Présents : politique de confidentialité (modale app-02:3133-3150, juin 2026, incomplète), écran « À propos » (app-02:2539, sans mentions légales complètes), modale de suppression avec référence à l'art. 17. Absents : CGU/conditions d'utilisation, mentions légales LCEN (éditeur, SIREN, adresse, directeur de publication, hébergeur), bandeau/consentement traceurs, page de signalement DSA / point de contact, registre des traitements, DPO, DPA sous-traitants, information sur la géolocalisation, liste des sous-traitants, politique de conservation.

RECOMMANDATIONS — Conserver : le socle d'auth (onbDoAuth, doLogout, adopterCompteConnecte, _peutPousserEtat), l'Edge Function comme point unique de suppression (à étendre), telemetry.js (à anonymiser ou à consentir). Supprimer : le gate 2125 tel quel (ou le remplacer par verify-access), le texte « contrôle d'âge IA », la mention « carnets » de la politique, la clé morte passio_cdv_geo_v1 dans ACCOUNT_SCOPED_KEYS. Refactoriser : étape âge au premier boot de session (AUTH-02), files hors-ligne avec identité figée (AUTH-06), Edge Function lisant {error} et couvrant les 39 tables + attachments (AUTH-05). À soumettre à Benjamin : (1) passer le dépôt en privé ou retirer CLAUDE.md/docs internes/code du gate ; (2) choix télémétrie : anonymisation (exemption CNIL) vs bandeau de consentement ; (3) rédaction CGU + mentions légales + politique v2 avec un juriste, et unification de l'adresse de contact (deux adresses contradictoires aujourd'hui) ; (4) rendre le numéro de téléphone facultatif ; (5) activer HIBP + minimum 10 caractères dans le Dashboard ; (6) poser PASSIO_E2E_MULTI=1 sur le job prod pour prouver enfin delete-account ; (7) planifier purge_telemetry(90) en cron.

CAPACITÉ/COÛTS — telemetry_events croît sans purge (111 828 lignes / 54 Mo pour 5 comptes : ~10 Mo par compte actif et par mois d'usage intensif) ; user_state orphelines 10 Mo. Aucune mesure de capacité auth (non prouvée).

REQUÊTES À EXÉCUTER PAR L'ORCHESTRATEUR (lecture seule) —
1. Résidus par table : SELECT 'user_state' t, count(*) FROM user_state u WHERE NOT EXISTS (SELECT 1 FROM auth.users a WHERE a.id::text = u.user_id) UNION ALL SELECT 'notifications', count(*) FROM notifications n WHERE NOT EXISTS (SELECT 1 FROM auth.users a WHERE a.id::text = n.user_id) UNION ALL SELECT 'telemetry_events', count(*) FROM telemetry_events e WHERE e.user_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM auth.users a WHERE a.id::text = e.user_id) UNION ALL SELECT 'analytics_events', count(*) FROM analytics_events e WHERE NOT EXISTS (SELECT 1 FROM auth.users a WHERE a.id::text = e.user_id) UNION ALL SELECT 'comment_interactions', count(*) FROM comment_interactions c WHERE NOT EXISTS (SELECT 1 FROM auth.users a WHERE a.id::text = c.user_id) UNION ALL SELECT 'user_safety', count(*) FROM user_safety s WHERE NOT EXISTS (SELECT 1 FROM auth.users a WHERE a.id::text = s.user_id);
2. Ancienneté télémétrie : SELECT min(received_at), max(received_at), count(*) FROM telemetry_events;
3. Médias orphelins : SELECT bucket_id, count(*) FROM storage.objects GROUP BY 1; et SELECT count(*) FROM storage.objects WHERE bucket_id='attachments';
4. Crons : SELECT jobname, schedule, command FROM cron.job;
5. Téléphones collectés : SELECT count(*) FILTER (WHERE raw_user_meta_data ? 'phone') AS avec_tel, count(*) FROM auth.users;

FICHIERS PRÉEXISTANTS dans preuves/auth-rgpd (déposés par une exécution antérieure interrompue de ce domaine, non produits ni vérifiés par moi : attaque-auth.js, capture-*.png, e2e-auth-rgpd.log, tests-e2e-auth.txt, gate-bruteforce.js/.json, emul-auth-resultat.json, http-server-8107.log) — à ne pas confondre avec les fichiers listés dans preuves_fichiers.

HYGIÈNE — Aucun fichier suivi par git modifié (`git status --short` vide en fin de mission) ; serveur http-server du port 8107 arrêté ; aucune requête n'a atteint Supabase pendant les émulations (toutes interceptées par page.route, SDK remplacé par un stub local car cdn.jsdelivr.net est bloqué par le proxy — CONNECT 403) ; le connecteur Supabase n'a pas été utilisé.

# Matrice IRL × opérations — SHA c8cb8e99 (inspection code + policies prod déposées par supabase-isolation)

Légende : C = client (fichier:ligne), S = serveur (policy/fonction prod, preuves/supabase-isolation/policies.json), ✔ garde présente, ✖ absente.

| Opération | Point d'entrée client | Écriture serveur | Garde serveur (RLS) | Garde âge/mineur | Blocage | Verdict |
|---|---|---|---|---|---|---|
| Créer une activité | `openCreateEvent`/`submitEvent` app-07:5150/5568 | `supaPublishEvent` app-08:4109 (`author_id`=`organizer_id`=MY_UID) | INSERT `events` : `author_id = auth.uid()` — `organizer_id` NON contraint | ✖ (`isMinor` lu seulement par `irlProposerEstMineur` app-07:4960, drapeau OFF) | ✖ | fonctionne ; usurpation d'organisateur possible par REST |
| Champs obligatoires | titre ≥3, ville, date, passion (5601-5604) ; date passée refusée à la création (5608), pas à l'édition | — | aucune contrainte serveur (title/date NULLables sauf DEFAULT) | — | — | client seul |
| Géocodage | `_evPickedCoords` > `cityToLatLng` > `_geocodeAddress` (BAN puis Photon, 5639-5650, 1075) | lat/lng exacts dans `events` | lecture publique | — | — | coordonnées EXACTES publiques |
| Prix | `fmtEventPrice(ev.price)` app-07:3769 ; carte 2829 | `price` double | — | — | — | conforme (une seule fonction) |
| Modifier | `openCreateEvent(editId)` → `supaUpdateEvent` app-08:4136 (compte les lignes) | UPDATE `events` | `author_id = auth.uid() OR jsonb_exists(co_organizers, auth.uid())`, **sans WITH CHECK** → un co-organisateur peut réécrire `author_id` | ✖ | ✖ | OK pour l'auteur ; prise de contrôle possible par co-organisateur |
| Annuler (douce) | `toggleCancelEvent` 5500 → `supaCancelEvent` 4150 | UPDATE status | idem | — | — | notifie `ev.attendees` seulement (5732-5738) : « peut-être » et liste d'attente non prévenus |
| Supprimer | `deleteEventConfirm` 5522 → `supaDeleteEvent` 4159 | DELETE enfants (RLS : ses lignes seulement) puis `events` | DELETE `author_id = auth.uid()` (co-organisateur exclu) | — | — | AUCUNE notification aux inscrits ; retour serveur ignoré (toast « supprimé » même si refus) ; lignes `event_attendees` d'autrui orphelines (pas de FK) |
| Rechercher / filtrer | `_filterIrlEvents` 2395 (passion, mine/joined, dates dont `weekend` 2084, ville, distance, horaire, texte) | — | — | — | — | conforme doc 20 |
| Liste | `renderIRL` 2722, pagination 12 | `supaLoadEvents` 4178 : `order created_at desc limit 60`, SANS filtre de date | SELECT `events` = `true` (2 policies) | — | ✖ `isBlocked` jamais appliqué aux événements | dès 60 événements créés, les plus anciens créés (même futurs) disparaissent |
| Carte | `initIrlMap` 1377 via shim `window.L` de `js/map-loader.js` (MapLibre GL 4.7.1 unpkg + style `tiles.openfreemap.org/styles/liberty`) ; `L.tileLayer` = no-op (map-loader:255) ; WebGL requis (repli `.irl-map-fallback`) | — | — | — | — | Leaflet NON chargé (mention dans CHECKLIST_COMMERCIALISATION.md:12 et commentaires = obsolète) ; UI-4A3 déplace `#irlMapWrap` avant `#eventList` |
| RSVP (going/maybe/declined/waitlist) | `setEventRsvp` 3269 ; complet → waitlist (3292) ; `requireAuthentication` en mode invité | `supaSetEventRsvp` 5421 (UPDATE puis INSERT sa ligne) | INSERT/UPDATE/DELETE `event_attendees` : `user_id = auth.uid()` ; **aucune vérification de capacité, d'état de l'événement, ni de valeur `rsvp`** côté serveur | ✖ | ✖ (un compte bloqué peut s'inscrire à mon événement) | dépassement de capacité trivial par REST ou course |
| Désinscription | `setEventRsvp(id,null)` → `supaLeaveEvent` 5451 | DELETE sa ligne | ✔ | — | — | conforme ; promotion automatique côté client |
| Promotion liste d'attente | `_promoteNextWaitlisted` 3344 (auto) / `promoteWaitlisted` 5470 (organisateur) → `supaPromoteFromWaitlist` 5506 | UPDATE la ligne d'AUTRUI | UPDATE `event_attendees` = `user_id = auth.uid()` → **0 ligne** | — | — | DÉFAILLANT : la valeur de retour (false) est ignorée, notification « tu es inscrit·e ! » envoyée (3358, 5477) alors que le serveur n'a rien changé |
| Check-in GPS | `checkInEvent` 3433 : rayon 500 m, **GPS refusé/indisponible → `done()` (3463)** ; sans inscription préalable | `supaCheckInEvent` 5462 : UPDATE sa ligne (`checked_in_at`, `rsvp='going'`) | ✔ owner, aucune fenêtre horaire serveur | — | — | preuve de présence CLIENT uniquement |
| Check-in QR/code | `_eventCheckinCode` 6490 = hachage FNV public de l'id ; `submitCheckinCode` 6577 compare côté client | idem | idem | — | — | code dérivable par quiconque connaît l'id (public) ; rejeu bloqué localement seulement (`_hasCheckedIn`), serveur idempotent |
| Adresse exacte | fiche 3767-3774 : « Adresse » + lien Google Maps ; contact `tel:` 3777 | colonnes `address`, `postal_code`, `lat`, `lng`, `contact` | SELECT `events` = `true`, rôle `anon` compris (visiteur première visite : `first-run.js:516` charge les événements réels) | ✖ | ✖ | visible par TOUT LE MONDE, sans compte ni inscription |
| Participants | fiche 3742-3760 (`_faces` : prénom + avatar + ✅ pointage), carte 2836 | — | SELECT `event_attendees` = `true` (rsvp, checked_in_at, rating, feedback publics) | — | ✖ | liste nominative publique ; `feedback` libre lisible par API |
| Commentaires d'événement | `addEventComment` 4573 → `supaAddEventComment` 4345 (`author_name` libre) ; rendu `_renderCommentsList` app-04:552 (escapeHtml) | INSERT `event_comments` | INSERT `author_id = auth.uid()`, SELECT `true`, **aucun rate-limit** (seuls comment_interactions/event_reactions/reports en ont) | — | ✖ | XSS couverte ; spam possible |
| Réactions | `_setEventReaction` 3072 | `event_reactions` | owner + rate-limit 30/min | — | ✖ | conforme |
| Conversation d'événement | `_joinEventConversation` 3366 : créée par un gestionnaire (`_canManageEvent` = auteur OU co-organisateur) avec `created_by = MY_UID` (5580) | `conversations`, `conv_members` | `can_join_event_conversation` exige `c.created_by = e.author_id`, `status='active'`, rsvp going/maybe, non bloqué | — | ✔ (blocage avec l'organisateur) | si un CO-ORGANISATEUR crée la discussion, aucun participant ne peut la rejoindre (insertion `conv_members` refusée en silence, `supaJoinEventConversation` 5588 retour ignoré) |
| Signalement | `reportEvent` 5563 : `supaReport("event", id, "")` sans confirmation ni motif | INSERT `reports` `reporter_id = auth.uid()`, rate-limit 10/min | ✔ | — | — | fonctionne mais motif toujours vide ; aucune option pour signaler/bloquer l'ORGANISATEUR depuis la fiche (le nom mène au profil, où le ⋯ existe) |
| Âge / mineurs | `onbValidateAge` app-02:3178 (année seule, < 13 refusé, `isMinor` en localStorage) ; `declare_birth_year` appelé UNIQUEMENT par `irlProposalDeclareBirthYear` 5010 sous drapeau `irl_proposal_v1` OFF | `user_safety` (2 lignes en prod / 5 comptes) | `irl_interaction_allowed` : n'est consulté par AUCUNE écriture IRL (création, RSVP, check-in, conversation) | ✖ | — | un mineur (13-17) crée, rejoint, pointe, voit l'adresse et le téléphone ; sans année de naissance en base = « âge inconnu », sans effet |
| Conseils de sécurité | aucun texte (grep « sécurité / lieu public / conseil » dans app-07, ui-v4b, ui-v4a2, index.html : 0 occurrence IRL) | — | — | — | — | absents |
| Organisateur anonyme | non : nom + lien profil (fiche 3873-3877) ; `organizer_id` libre à l'INSERT | — | — | — | — | un compte peut afficher un tiers comme organisateur |
| Localisation de l'utilisateur | `requestUserLocation` 1120 (getCurrentPosition, précision brute, mémoire seulement, jamais persistée ni envoyée — verrouillé par irl-trust-safety.spec.js ⑥⑦⑧⑨) ; repli Paris | — | — | — | — | conforme |

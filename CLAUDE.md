# PASSIO — Guide pour Claude Code

Réseau social des passions. PWA vanilla JS (pas de framework, pas de bundler) + Supabase. Beta privée protégée par code d'accès.

## Architecture

- `index.html` : markup complet de l'app (landing, onboarding, 8 écrans, modals). En dev les 15 fichiers JS sont chargés séparément ; en prod `scripts/build.js` ré-assemble un monolithe dans `dist/`.
- `js/app-01` à `app-09` : logique applicative (ordre de chargement = dépendances par hoisting, NE PAS réordonner). 01=diag/seed, 02=state/utils/goTo, 03=posts/carnets, 04=commentaires/conversations rendering, 05=config/profils/reels, 06=profil principal/studio/partage, 07=IA/explore/IRL, 08=modals/tour/boot()/Supabase client, 09=PWA/emoji/pièces jointes/wrappers messagerie.
- `js/access-gate.js` : verrouillage par code (2125) — chargé en PREMIER dans <head>. Voir `docs/SECURITE_CODE_ACCES.md` pour changer le code.
- `styles.css` : 6300 lignes, thème violet (#7c3aed), variables CSS (--bg-card, --border, --muted, --accent…).
- Backend : Supabase (URL/clé anon dans app-08). Tables : profiles, posts, post_likes, post_comments, stories, events, event_attendees, conversations, conv_members, conv_messages, notifications, follows, client_errors. RLS par propriétaire (`auth.uid()::text`). Migrations dans `migrations/`.
- État local : `localStorage["passio_mvp_state_v1"]` (constante `STATE_KEY` dans app-02 — PAS `passio_state`). Contient profils, posts perso, notifs… `MY_UID` = id Supabase auth ; jeton du gate = `sessionStorage["passio_gate_v1"]`. **Les conversations** (le gros volume, vocaux base64 inclus) sont dans `localStorage["passio_conversations_v1"]` ET, depuis le 2026-06-15, dans **IndexedDB** (store durable sans limite ~5 Mo, `js/idb-store.js` : `idbConvLoad`/`idbConvSave`) : write-through à chaque `saveConversations`, hydratation+fusion sans perte au boot via `hydrateConvsFromIDB()` (tête de `boot()`). localStorage reste un cache sync toléré à échouer sur quota.

## Commandes

- Serveur local : `npm run serve` → http://localhost:8080 (code d'accès : 2125 ; http-server, plus besoin de Python)
- Tests : `npx playwright install chromium` puis `npm test` (smoke + access-gate ; le helper `tests/e2e/gate-helper.js` déverrouille le gate pour les tests)
- Build prod : `node scripts/build.js dist/index.html`
- Déploiement : `git push origin main` → GitHub Actions teste, build, minifie, déploie sur Netlify (https://passio-app.netlify.app)

## Conventions

- Vanilla JS, pas de modules ES (scripts classiques, fonctions globales).
- `$()` = querySelector (défini app-02), `$$()` = querySelectorAll. Toujours garder les guards `if (!el) return;`.
- HTML généré par template literals + `escapeHtml()` pour tout contenu utilisateur (XSS).
- Navigation : `goTo('feed'|'profiles'|'studio'|'explore'|'irl'|'wallet'|'messages'|'cdv')` — écrans = `#screen-<nom>`.
- Toasts via `toast()`, jamais `alert()`.
- Les onclick inline doivent référencer des fonctions globales EXISTANTES (l'audit du 2026-06-10 a trouvé 7 fonctions fantômes — vérifier avant d'ajouter un handler).

## État au 2026-06-11 (voir docs/RAPPORT_SESSION_2026-06-10.md pour le détail)

Fait : access gate en prod, 14 bugs corrigés, RGPD (suppression de compte réelle + politique de confidentialité), policies DELETE appliquées en prod Supabase, suite E2E gate.

## Backlog prioritaire

1. ~~Tests utilisateur multi-comptes~~ — **fait le 2026-06-12, test E2E vert de bout en bout** (`tests/e2e/multi-comptes.spec.js`, opt-in : `PASSIO_E2E_MULTI=1 npm test` ; inscription réelle × 2, conversation, texte dans les deux sens, vocal rendu dans le lecteur intégré, réception realtime ~1 s). **4 bugs critiques de prod trouvés et corrigés** : (a) RLS conv_members — migration `migration_fix_conv_members_insert.sql` appliquée en prod le 2026-06-11 ; (b) `initApp()` (emoji-misc) écrasait l'onboarding des nouveaux utilisateurs ~1,4 s après le chargement (tour lancé de force) ; (c) deadlock verrou auth supabase-js (`onAuthStateChange` → supaInit, et `onbSkipAuth` qui dépendait de la résolution de `signInAnonymously`) ; (d) `supaSubscribe()` + chargement des conversations Supabase étaient dans du code mort après un `return` dans `supaInit` → **aucun destinataire ne recevait jamais rien**.
2. ~~Réception des messages vocaux côté destinataire~~ — fait le 2026-06-11 : décodage unifié via `applyMsgContentData()` (app-04), utilisé par renderConvFpThread, supaLoadMessages, le handler realtime et l'aperçu de conversation. Reste à valider avec 2 comptes réels (point 1).
3. ~~Galerie "Pièces jointes" d'une conversation~~ — fait le 2026-06-11 : panneau `#convFilesPanel` (médias / vocaux / fichiers, état vide), `openConvFiles()`/`closeConvFiles()` (app-09).
4. ~~Edge Function Supabase `delete-account`~~ — fait le 2026-06-11 : code (`supabase/functions/delete-account/index.ts`), appel branché dans `doDeleteAccount` (app-02), **déployée en prod via le Dashboard** et testée (compte jetable supprimé, auth comprise).
5. ~~GIFs hardcodés~~ — fait le 2026-06-12 : API Tenor/Giphy via `passioFetchGifs`/`passioGifPanel` (emoji-misc.js), recherche + cache 10 min + fallback liste locale. Clé à coller dans `PASSIO_GIF_API` (1 endroit). CSP mise à jour (tenor/giphy).
6. ~~Redesign / audit visuel~~ — fait le 2026-06-12 : `scripts/capture-screens.js` (16 captures `docs/screenshots/`), UI jugée qualité commerciale ; seul fix : `.demo-ribbon` (MVP BETA) → watermark non-bloquant. Redesign approfondi écran par écran = amélioration continue.
7. ~~Accessibilité~~ — fait le 2026-06-11/12 : `user-scalable=no`/`maximum-scale` retirés, champs à 16px, `--muted` AA, touch 44px. Audit aria complet = amélioration continue.
8. ~~Perf~~ — fait : transfert prod 201 Ko brotli (<500), `loading="lazy"` partout, **pagination des conversations** (30/page + `_loadMoreConvs`, app-04) en plus du fil et des messages. Reste (humain) : Lighthouse mobile formel.

**Contrôle qualité des 16 missions** : voir `docs/CONTROLE_16_MISSIONS.md` (audit 2026-06-12, 14 ✅ / 1 ⚠️ m6 / 0 ❌, verdict « prêt à commercialiser en beta ») et `docs/CHECKLIST_COMMERCIALISATION.md`. Tests : `npm run test:all` (18 verts). Audit handlers : `npm run audit:handlers`.

## Pièges connus

- Le build exige EXACTEMENT 9 fichiers app-*.js entre les marqueurs BUILD:APP dans index.html.
- **Recherche d'un post : TOUJOURS `findPostAnywhere(id)` (app-02)** — cherche dans seed + userPosts + `supabasePosts`. Le pattern historique `seed.posts.find || userPosts.find` (2 sources) OUBLIAIT `supabasePosts` (les vrais posts réseau) → impossible d'ouvrir/commenter/réagir sur le post d'un autre compte, et les notifs de like ne partaient pas. 24 sites corrigés le 2026-06-17 (app-03/04/05/06 + emoji-misc + canaux realtime likes/comments).
- **Notifications cross-compte** : insert via `supaInsertNotif(toUserId, kind, refId, content)` (app-08, RLS INSERT `WITH CHECK (true)` → cross-user OK). Côté réception, `supaLoadNotifications()` était appelé UNIQUEMENT dans le code mort de `supaInit` (jamais chargées !) — corrigé le 2026-06-17 : chargement au boot + à l'ouverture du panneau + canal `realtime:notifications` (postgres_changes filtré `user_id===MY_UID`, **livraison realtime validée en prod** par le test 2 comptes). Fusion/dédup/badge via `mergeSupaNotifs()`, rendu liste via `_notifListHtml()`, emoji dérivé du kind via `_notifEmoji()`. Like/comment/follow notifient l'auteur/la cible.
- **Bobines (Reels) & éditeur média façon Instagram** : les bobines sont DISTINCTES du fil (flag `is_reel`). `buildReels()` (app-05) ne montre QUE les `isReel` ; `allFeedPosts()` (app-02) les EXCLUT ; une vidéo Studio classique reste au fil. Création via l'**éditeur média** `meOpen('story'|'bobine')` (app-08) façon appareil photo Instagram, en **2 phases** (classes `phase-capture`/`phase-edit` sur `#mediaEditor`) : (1) **capture caméra live** `getUserMedia` (`meStartCamera`/`meStopCamera`/`meFlipCamera`, état global `meCam`) — déclencheur `#meShutter` : tap = photo (`meCapturePhoto`, canvas + miroir si caméra avant), maintien = vidéo (`meStartRecording`/`meStopRecording`, MediaRecorder webm/mp4, 60 s max, badge `#meRecBadge`) ; galerie (`mePickMedia`) + fond coloré (story only) en alternative ; fallback `_meNoCamera` → placeholder si caméra refusée/absente. (2) **édition** : média photo/vidéo (ou fond dégradé en story) + overlays déplaçables (texte/emoji/GIF) — `meState.overlays`, drag pointer-events, `_meOverlaysData()`. `meSetMedia`/`meCycleBg` basculent en phase édition ; le ✕ (`meTopBack`) revient à la capture depuis l'édition, ferme depuis la capture. CSP/Permissions-Policy prod autorisent déjà caméra+micro (`camera=(self)`, `media-src data:`). Publication : story → `state.seed.stories` + `supaPublishStory` (upload média via `supaUploadMedia`, colonnes `media_url`/`media_type`/`overlays`) ; bobine → `state.userPosts` is_reel + `supaPublishPostWithRetry` (colonnes `is_reel`/`overlays`). Rendu overlays via `_storyOverlaysHtml()` dans le viewer story (`playCurrentStory`, couches `#storyMedia`/`#storyOverlays`) ET le viewer bobine (`renderReelHTML`, `.reel-overlays-layer`). Entrées : bulle « Ta story », type « Bobine » du Studio + bouton de l'état vide Bobines (`startBobineCreation`). Migration `migration_bobines_stories_media.sql` appliquée en prod.
- **Modération (blocage / signalement)** : tables `blocks` (blocker_id/blocked_id, RLS owner) et `reports` (insert ouvert, lecture admin only) — `migrations/migration_blocks_reports.sql`, **appliquée en prod**. Cache local `state.user.blocked` (chargé au boot via `supaLoadBlocks`, fusionné). `isBlocked(id)` (app-02) centralise le filtrage : feed (`allFeedPosts`), commentaires (`_renderCommentsList`), notifs (`mergeSupaNotifs`), messages entrants (`_handleIncomingConvMessage`) et liste des conversations (`renderMessages`). UI : `blockUser`/`unblockUser`/`reportUser`/`reportPost`/`shareUserProfile` (app-04), boutons dans `openUserProfile`, liste gérable via `openBlockedList()` (Réglages → 🚫 Comptes bloqués). Supprimé au delete-account (RGPD). `supaBlockUser`/`supaUnblockUser`/`supaReport` (app-08).
- **Monitoring d'erreurs (`client_errors`)** : `js/platform.js` (chargé dans `<head>`) capture `error`/`unhandledrejection` et insère dans Supabase. Le client `supa` est un `let` (app-08) → il ne crée PAS de `window.supa`, or le monitoring teste `window.supa` → il était TOUJOURS undefined et **rien n'a jamais été loggé** (table vide depuis le début). Corrigé le 2026-06-19 : `window.supa = supa;` après l'init (app-08), + buffer des erreurs de boot dans platform.js (vidé dès que `supa` est prêt). RLS `client_errors` INSERT = `WITH CHECK (true)`. Pour lire les erreurs : `supabase db query --linked "SELECT * FROM client_errors ORDER BY created_at DESC LIMIT 50"`.
- **Événements cross-compte** : créer (`supaPublishEvent`) et rejoindre (`supaJoinEvent`) syncent bien, MAIS `supaLoadEvents` mappait `attendees: []` → tout événement affichait « 0 inscrit » même rejoint par d'autres. Corrigé le 2026-06-18 : les participants sont chargés depuis `event_attendees` (1 requête `.in(event_id)`). Et rejoindre notifie désormais l'organisateur (`supaInsertNotif(ev.organizerId, "event_join", …)`, gardé sur `ev.fromSupabase`). `toggleJoinEvent` pousse `MY_UID` (plus `"me"`) pour ne pas doubler le compteur après rechargement.
- **XSS notifications** : le texte d'une notif est rendu en `innerHTML` (`_notifListHtml`) et contient le pseudo de l'émetteur (contrôlé par l'utilisateur) → `supaInsertNotif` échappe le pseudo via `escapeHtml()` à l'insertion (le `content` appelant est statique). NE PAS injecter de contenu utilisateur brut dans une notif. Les notifs locales (`pushNotification`) gardent leur HTML voulu (`<b>…</b>`) car non concernées.
- **Embeds PostgREST `profiles(...)` = 400 si pas de FK** : seules les tables avec une vraie FK `author_id → profiles` (ex. `posts`, d'où `profiles!author_id`) supportent l'embed. `post_comments`, `stories`, `events`, `notifications` n'ont PAS cette FK en prod → `.select("*, profiles(...)")` renvoie **400** (requête échoue → 0 résultat). Corrigé le 2026-06-17 : ces loaders font `.select("*")` puis résolvent les profils en une requête séparée via `_resolveProfilesByIds(ids)` (notifications dérive l'emoji du kind). Symptômes passés : commentaires des autres invisibles, notifs jamais chargées, 400 au boot. Le test 2 comptes (`-g notifications`) couvre ce chemin.
- `tests/e2e/access-gate.spec.js` dépend du hash dans gate-helper.js — à mettre à jour si le code d'accès change.
- **Tests multi-comptes : inscription par E-MAIL (l'anonyme est désactivé)** — l'auth anonyme Supabase est **désactivée en prod** (`anonymous_provider_disabled`, 422) : l'app exige un vrai compte. Le helper `signupAnonymous` (nom historique) de `tests/e2e/multi-comptes.spec.js` crée donc un compte **e-mail jetable** (`e2e_…@passio-e2e.test`, mot de passe fixe) via `supa.auth.signUp` — « Confirm email » étant OFF, la session est immédiate. Ces comptes restent dans `auth.users` (non supprimables sans service_role) → purge périodique : `supabase db query --linked --file` avec un DELETE sur `auth.users WHERE email LIKE '%@passio-e2e.test'` (supprimer d'abord conv_members/conv_messages/profiles qui ont une FK vers profiles/auth). Suite OPT-IN `PASSIO_E2E_MULTI=1` (5 tests : messagerie texte+vocal, notifs, événement, story+bobine, **CDV Live**) — 5/5 verts au 2026-06-24. Le test vocal envoie une **URL https** (pas un `data:` base64, retiré par `sendMessageToSupabase` pour l'hygiène DB).
- Les messages média Supabase encodent le contenu en JSON dans `content` (type gif/media/audio/doc/location) — décodage centralisé dans `applyMsgContentData()` (app-04) : vocaux (audio/webm ou "Message vocal (Xs)") → lecteur intégré, autres audios → carte téléchargement.
- Insert conv_messages : AVEC from_id d'abord (la RLS v2 exige from_id = auth.uid()), fallback sans from_id. (Inversé le 2026-06-11 — l'ancien ordre « sans d'abord » échouait systématiquement et gaspillait une requête par message.)
- `from_id` et `conv_members.user_id` ont une FK vers `profiles` : `supaUpsertProfile()` doit être passé avant (fait au boot et dans supaCreateConversation).
- La prod a une FK `conv_messages.conv_id → conversations` qui n'apparaît pas dans `migrations/supabase_tables.sql` (constaté le 2026-06-11) : la ligne `conversations` doit exister avant tout message. Le schéma du repo n'est pas la source de vérité exacte de la prod.
- Realtime : **v3 par défaut depuis le 2026-06-15** (`window.PASSIO_REALTIME_V3`, app-08) — **topic privé PAR UTILISATEUR** : chaque client s'abonne UNE fois au boot à `user:<MY_UID>` (`_subscribeUserTopic`) ; le trigger SQL `broadcast_conv_message_to_users` (migration `migration_realtime_user_topic.sql`, **appliquée en prod**) diffuse chaque message au topic perso de CHAQUE membre. Scalable (chaque client ne reçoit que ses messages) ET **sans course** (topic stable, contrairement à v2). Validé par test 2 comptes (`PASSIO_E2E_MULTI=1 PASSIO_E2E_RT=v3`). Handler de réception factorisé : `_handleIncomingConvMessage(r)` (commun v1/v2/v3). Priorité v3 > v2 > v1 dans `supaSubscribe`. Soupapes par device : `localStorage.passio_realtime_v3="0"` → v1 ; `passio_realtime_v2="1"` → v2 (expérimental, course connue) ; `passio_realtime_v3="0"` sans v2 → v1 (canal global `realtime:my_messages`, reçoit tout + filtre en JS, fiable mais pas scalable). Garde `window._supaSubscribed` contre le double abonnement (supaInit appelable 2×). Le test multi-comptes accepte `PASSIO_E2E_RT=v2|v3`.
- **SDK Supabase chargé en PARESSEUX (perf, 2026-06-23)** : le bundle `@supabase/supabase-js@2` (~120 Ko) n'est plus dans le `<head>` (il bloquait le rendu de la page verrouillée alors qu'il ne sert qu'après le code d'accès). `js/supabase-loader.js` (chargé en `<head>`, calque de `ensureLeaflet`) expose `ensureSupabase()` qui injecte le CDN à la demande. `boot()` fait `await ensureSupabase()` PUIS `_initRealSupa()` en TOUTE PREMIÈRE étape, avant le moindre `supa.*`. Conséquences : (a) au top-level, `supa` est un **stub noop** (`_buildNoopSupa()`), le vrai client n'existe qu'après `_initRealSupa()` (`window._supaReal=true`) ; (b) **ne JAMAIS référencer le global `supabase` (le SDK) au top-level** d'un `app-*.js` — il est `undefined` au parse ; passer par `supa` (déjà réassigné) ou par `ensureSupabase()`. `preconnect` vers jsdelivr + le domaine Supabase ajoutés dans `<head>`. Validé en preview : page verrouillée = 0 Ko supabase, CDN fetché en dernier post-gate, vrai client actif (URL/realtime/auth OK).
- supabase-js : ne JAMAIS faire de requête Supabase directement dans un callback `onAuthStateChange` (verrou auth interne → deadlock, la promesse du sign-in ne résout jamais) — différer avec `setTimeout(..., 0)`. Et ne pas faire dépendre l'avancement de l'UI de la résolution de `signInAnonymously()` (cf. `onbSkipAuth`).
- `supaInit` (app-08) contient un GROS bloc de code mort après `return;` (reliquat du « FIX Promise.all », variables non définies) — ne rien y ajouter, il ne s'exécute pas. Les conversations + `supaSubscribe()` en ont été extraits le 2026-06-12.
- `initApp()` (appelée par emoji-misc ~600 ms après chargement) ne doit JAMAIS tourner pour un utilisateur non onboardé — elle lançait le tour qui écrasait l'onboarding (corrigé par un garde `state.onboarded`).
- **IRL — muter l'événement CANONIQUE, pas une copie** : `allEvents()` (app-07) renvoie des copies *shallow* (`{...e}`). `toggleJoinEvent` faisait `ev.attendees = (...).filter()` sur cette copie → le compteur d'inscrits/avatars ne bougeait jamais (seul le bouton « ✓ Inscrit » via `joinedEvents`). Corrigé le 2026-06-24 : `_findCanonicalEvent(id)` retrouve l'objet dans `state.userEvents`/`state.seed.events` et on mute celui-là. `allEvents()` calcule aussi `_mine` via `organizerId/authorId === MY_UID` (un event Supabase qu'on a créé revient dans seed après reload et était sinon exclu de « Mes events »).
- **IRL — filtrage factorisé** : `_filterIrlEvents(events)` (app-07) applique les 5 filtres (passion/type/date/distance/heure) + retire le passé ; utilisé par `renderIRL` ET `updateIrlMapMarkers` (avant : ~80 lignes dupliquées qui divergeaient). Réf. distance via `_irlReferenceLoc()` (ville sélectionnée > GPS > Paris). Indicateur « X filtres · Réinitialiser » (`#irlFilterStatus` + `clearAllIrlFilters()`). Ville de référence affichée dans `#irlUserCityName` (la barre `.irl-locbar` ouvre `openIrlCitySelector()`) — `updateIrlCityTitle()` ciblait cet id qui n'existait plus dans le markup → il a été rajouté.
- **`supaLoadEvents` fusionne (ne remplace plus) `state.seed.events`** (app-08, 2026-06-24) pour garder les events démo en beta, + `renderIRL()` si l'écran IRL est actif (sinon les events Supabase n'apparaissaient qu'après re-navigation).
- **CDV — points d'entrée création sur l'écran** (`#screen-cdv`) : boutons « 📔 Nouveau carnet » (`setStudioToVlog()`) et « 🔴 Démarrer un Live » (`startCdvLive()` — modale self-contained de app-03, auparavant orpheline mais fonctionnelle). États vides distincts (recherche / filtre / vide réel) dans `renderCdvScreen`.
- **CDV Lives synchronisés Supabase (cross-compte, 2026-06-24)** : migration `migration_cdv_lives.sql` (**appliquée en prod**) = 5 tables `cdv_lives` / `cdv_live_steps` / `cdv_live_comments` / `cdv_live_reactions` / `cdv_live_followers`. RLS : live+étapes **propriété de l'auteur** (`author_id = auth.uid()::text`, privé masqué aux autres) ; commentaires/réactions/followers **ouverts à tout compte** (insert `with check user_id = auth.uid`). Fonctions app-08 : `supaPublishCdvLive` / `supaAddCdvLiveStep` / `supaAddCdvLiveComment` / `supaReactCdvLive` / `supaFollow|UnfollowCdvLive` / `supaUpdateCdvLiveStatus` / `supaLoadCdvLives` (+ `supaLoadCdvLive` un seul) / `supaRefreshCdvLives` (fusion dans `passio_cdv_lives` + re-render). Branché dans `createCdvLive`/`saveCdvLiveStep`/`addCdvLiveComment`/`reactCdvLive`/`toggleFollowCdvLive`/`endCdvLive` (app-03). Chargement au boot (bloc setTimeout de `supaInit`) + à chaque `goTo('cdv')`. **Near-realtime par polling** : `startCdvLiveRefresh` recharge le live ouvert toutes les 5 s depuis Supabase (skip si saisie d'un commentaire en cours, re-render seulement si le contenu a changé). Un Live publié localement a `authorId:"me"` puis `MY_UID` après reload → `isMyLive(l)` (app-03) couvre les deux. Plus de spectateurs/compteurs fictifs (`Math.random` retiré) : « X suivent » = vrais followers. Pas (encore) de canal realtime postgres_changes — le polling 5 s suffit pour la beta.
